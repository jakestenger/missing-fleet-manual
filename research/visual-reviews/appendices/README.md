# Appendix visual candidates

Ten candidates: nine editable SVG diagrams with PNG masters and lossless WebP assets, and one built-in imagegen Cloud City vignette. All await joint review. The subject-index illustration follows the approved Part 0 style, with blank pages and cards, a small swan, and a shallow horizontal composition. Its prompt and selected source are recorded in `prompt-history.json`.

All nine diagrams were inspected at 720 px. Grayscale checks covered flags-file replacement, role scope, and MCP allowlist derivation. Review corrected overlapping annotations and token-route labels. The GitOps brief now distinguishes API responses from the apply log generated by the client. The allowlist brief explicitly restores the full API prefix. Host-group labels use lowercase fleet/fleets.

The surrounding chapter explanations and citation ledgers informed these editorial candidates. This batch does not re-verify the entire appendix against a new release. Reference matrices, commands, syntax, and visible chapter content are unchanged. Tall figure placement and final page typography remain for joint review.

Validation passed: production website build, all 83 chapter links, em-dash and diff whitespace checks, ten complete source/asset sets, nine valid SVGs with minimum 28 px labels, terminology, current packaged briefs and unchanged visible chapter content. Nine existing nonblocking IMAGE-REDO markers remain pending.

Render native assets with `python3 research/visual-reviews/appendices/render-diagrams.py`; review `index.html` at its default 720 px width.
