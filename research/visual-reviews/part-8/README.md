# Part VIII visual candidates

Fifteen editable SVG diagrams, with PNG masters and lossless WebP candidates. All remain pending joint review; published images and approval markers are unchanged.

Every candidate was inspected at 720 px. Grayscale checks covered Windows polling/wake, incident attribution, and file-carve authentication. Refinements separated connector labels, kept APNs outside the command transport, distinguished possible load-balancer destinations from a selected node, and preserved NotNow eligibility at a later Idle check-in. The file-carve configuration comparison sits below its sequence for readable labels.

Chapter briefs now qualify the UUID-present error example and the macOS example with confirmed absent fleetd. Host-group scope uses lowercase fleet. Chapter 8.2's image comments were moved out of a shell code fence; its command text is unchanged, with only whitespace differences after removing comments.

Validation passed: production website build, 83 chapter link checks, em-dash and diff whitespace checks, fifteen complete source/asset sets, SVG XML and minimum 28 px labels, terminology, and current chapter comments in packaged briefs. The nine existing IMAGE-REDO markers remain pending and are nonblocking according to the repository check. Tall figure placement remains part of joint review.

Reproduce native assets with `python3 research/visual-reviews/part-8/render-diagrams.py`. Open `index.html` for the reading-width gallery and grayscale control.
