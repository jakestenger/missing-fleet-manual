# 2.11 Configure Windows management: citation ledger

Renumbered from 2.8 on 2026-08-30 (P6 structural moves); same chapter, new number. Split 2026-08-30: the Android Enterprise rows moved to `2.12-notes.md` with the Android half of the chapter; the review history below predates the split and covers both halves.

Verified against `fleet-v4.90.1` (commit `dd0200f062`) in `~/Source/Fleet/fleet-public` on 2026-08-25.

## Windows

| Claim | Source | Confidence |
|---|---|---|
| Fleet uses a self-generated cert/key pair for Windows management; no external issuer | `articles/windows-mdm-setup.md` Step 1 | High |
| `openssl genrsa --traditional ... 4096`; `openssl req -x509 -days 3652` | `articles/windows-mdm-setup.md` Step 1 | High |
| macOS default `openssl` is LibreSSL and rejects `--traditional` | `articles/windows-mdm-setup.md` Step 1 note | High |
| `FLEET_MDM_WINDOWS_WSTEP_IDENTITY_CERT_BYTES` / `_KEY_BYTES` | `docs/Configuration/fleet-server-configuration.md:3618,3633` | High |
| `_BYTES` suffix means contents; drop suffix for a path | `articles/windows-mdm-setup.md` Step 2 note | High |
| Server restart required | `articles/windows-mdm-setup.md` Step 2; corroborated by `server/service/orbit.go:1375` comment ("WSTEP is first parsed and cached at the start of the fleet serve process") | High |
| End user experience Automatic (recommended) or Manual | `articles/windows-mdm-setup.md` Step 3 | High |
| Windows tamper protection is disabled when MDM is turned on | `articles/windows-mdm-setup.md` Manual enrollment note | High |
| Enrollment completes in the context of a signed-in user; unattended hosts report Off with work queued; ~30s after next sign-in | `articles/windows-mdm-setup.md` Manual enrollment | High |
| Automatic enrollment is Fleet Premium | `articles/windows-mdm-setup.md` Automatic enrollment | High |
| Admin needs EMS E3; each user needs Entra P1 or E3/E5 | same | High |
| Intune Automatic Enrollment must have MDM user scope and WIP user scope set to None | same, preamble to Step 2 | High |
| Tenant ID, MDM URLs + Application ID URI, client ID must all be set | `articles/windows-mdm-setup.md` Step 2, items 4/10/13/15 | High |
| Missing tenant ID *or* missing client ID both produce "Device management could not be enabled" | same, items 4 and 15 (identical error text in both) | High |
| Entra issues v2 tokens whose audience is the client ID | same, item 15 | High |
| Fleet URL must be a verified custom domain (TXT/MX); managed cloud customers ask Fleet | same, items 5-6 | High |
| Graph permissions: delegated `Group.Read.All`, `Group.ReadWrite.All`; application `Device.Read.All`, `Device.ReadWrite.All`, `Directory.Read.All`, `Group.Read.All`, `User.Read.All`; then grant admin consent | same, items 17-20 | High |
| Entra connection enables Settings > Access work or school; Fleet stores the email as IdP username; Windows Hello + PIN prompt | same, Automatic enrollment preamble | High |
| Non-federated third-party IdP not supported | `articles/windows-mdm-setup.md` blockquote, line 11 | High |
| Automatic Windows migration requires no end user interaction | `articles/windows-mdm-setup.md` Automatic Windows MDM migration | High |
| Migration residue causes MDM "Off", `400` / `0x8018000a` in fleetd logs, locked local accounts; `fix-windows-mdm-migration.ps1`, reboot, refetch | same, Migrating from another MDM solution | High |
| RMM agents (N-able, ConnectWise, Kaseya) block enrollment and can break Windows Update | same | High |
| Autopilot re-use order: delete host in Fleet, delete Entra device object, keep the Autopilot registration, sync, reset | same, Repurposing or re-enrolling | High |
| Turn off: run `uninstall-fleetd-windows.ps1` (also removes fleetd), then toggle off | same, Turn off Windows MDM | High |
| Android's server URL is fixed once the enterprise is bound (the sibling cross-reference sentence) | `docs/Contributing/product-groups/mdm/android-mdm.md` Known issues; shared with §2.12, where the full Android rows live | High |

## The WSTEP pair as escrow key

| Claim | Source | Confidence |
|---|---|---|
| Escrowed disk encryption keys are encrypted to the WSTEP certificate | `server/service/orbit.go:1374-1382` (`microsoft_mdm.Encrypt(encryptionKey, wstepCert.Leaf)`) | High |
| Decryption path is chosen by platform: LUKS uses server private key; Windows uses WSTEP; everything else uses Apple MDM CA assets | `server/service/hosts.go:3587-3624` | High |
| The Apple path deliberately includes rolled-over CA certs so older escrows still decrypt; the Windows path has no equivalent | `server/service/hosts.go:3615-3617` (`assets.CACertsAndKeyForDecryption`) | High |
| `/api/fleet/orbit/disk_encryption_key` is gated behind `VerifyWindowsMDM()`; LUKS escrow is a separate route | `server/service/handler.go:1062-1065` | High |
| Changing the pair strands previously escrowed keys; recovery is toggling disk encryption off and on | `docs/Configuration/fleet-server-configuration.md:3632` | High |

**Documentation discrepancy, flagged in the chapter as a blockquote.** `docs/Configuration/fleet-server-configuration.md:3632` says the affected hosts are **macOS**. The code at this tag routes Windows hosts through the WSTEP decryption path and Apple hosts through the Apple MDM CA path (`server/service/hosts.go:3587-3624`), so the platform named in that sentence appears to be wrong. The chapter states the code behaviour and notes the discrepancy rather than silently contradicting Fleet's published docs. Candidate upstream doc fix.

## Licensing

| Claim | Source | Confidence |
|---|---|---|
| Windows automatic enrollment: Premium | `articles/windows-mdm-setup.md` Automatic enrollment | High |

## Deferred to other chapters

- **Windows Autopilot profile creation steps.** `articles/windows-mdm-setup.md` Windows Autopilot Steps 1-4. Enrollment experience material, so [3.3](../../manual/03-connect-devices/3.3-enroll-windows-devices.md).

---

The sections below predate the 2026-08-30 split and reviewed the combined Windows and Android chapter; the Android corrections they record now live in 2.12 and its ledger, `2.12-notes.md`.

## 2026-08-25 review pass: an inherited contradiction, and an overgeneralized heading

Independent review on `gpt-5.6-sol`.

### Corrected: the Android domain-verification paragraph contradicted itself

As published, one paragraph said Microsoft 365 organizations "verify your domain in the Google
Admin console afterwards" and then that "Workspace and Microsoft 365 organizations skip domain
verification".

**The contradiction was inherited.** `articles/android-mdm-setup.md:11` states that Google
Workspace and Microsoft organizations do not need to verify domain ownership; the Microsoft 365
procedure at `:27-28` instructs the administrator to verify it as its final two steps. The source
does not resolve itself, and no other material at the tag settles it.

Handled the way 2.5 handles the SCIM `email` discrepancy: state that the source is inconsistent,
give both readings, and recommend the action that satisfies either. Verifying a domain is
harmless, so the advice is to verify unless already verified.

**Second Fleet documentation inconsistency found in this part.** Worth reporting upstream
alongside the WSTEP platform error and the GCP "coming soon" claim.

### Corrected: a heading claimed more than its own paragraph

Heading: "An Android host has no serial number". Body, correctly: "On a personally owned Android
device enrolled with a work profile ...". `docs/REST API/rest-api.md:4091` scopes the
enterprise-specific ID to hosts with `mdm.enrollment_status` of "On (personal)". Company-owned
enrollment is a separate, Premium path and the claim was never verified for it.

Retitled to match the paragraph. Notable as a defect class: the prose was right and the heading
was wrong, so verifying claims against sources would never have caught it.

### Corrected: "Renewal: None" for the Windows key pair

Accurate as far as it goes, and misleading in a table whose purpose is to compare ongoing cost.
There is no vendor renewal, but the certificate has whatever lifetime you gave it, and the
chapter goes on to explain that replacing the pair strands every escrowed disk encryption key.
The row now says no vendor renewal and that you choose the lifetime; the framing paragraph now
points at the replacement consequence instead of describing both platforms as having "a sharper
edge later".

### Recorded, not fixed

- The review recommends reorganizing into two per-platform runbooks so an administrator can
  follow Windows or Android start to finish. The chapter is currently organized around the
  comparison, which is where its argument lives. A restructure is a larger decision than a
  correction pass; left for Jake.
- The Android agent package, signing fingerprint and batch-size settings are development
  internals the text itself says not to touch. Arguably they interrupt the setup narrative.
  Kept, because "why does Fleet install an app on Android at all" is a question an administrator
  will ask when they see it on a device.

### Licence claims re-checked 2026-08-27, and one was wrong

Part III's confirming reviews found licence claims wrong in both directions inside a single chapter, which made every licence claim in the manual worth re-checking rather than only the flagged ones. Two of this chapter's three hold; one does not.

**Holds.** Windows automatic enrollment is Premium: `windows_entra_tenant_ids` and `windows_entra_client_ids` are both rejected with `ErrMissingLicense` on Free, so the Entra connection cannot be configured at all.

**Holds.** The basic connections are free on both platforms; nothing gates the WSTEP pair or the Android enterprise binding.

**Does not hold as written.** The table said company-owned Android is "Fleet Premium, for fully managed devices". **Nothing in Fleet's Android module checks a licence** at this release, and company-owned enrollment is one query parameter, `fully_managed`, on the same enrollment-token endpoint the work-profile path uses. There is no free-tier implementation shadowing it and no `ee/` counterpart to the Android service.

That does not establish that Fleet sells it at no cost, which is a pricing question this manual cannot answer and should not pretend to. The row now says what the server does and tells the reader to confirm the commercial side against Fleet's pricing. **This is a correction to a chapter stamped `verified`**, and it is the second licence claim in this manual found wrong by looking, after 3.5's.

## Fix-loop round (2026-09-02, round3 M12 — Windows uninstall script unpinned)

"Turning Windows MDM off" named `uninstall-fleetd-windows.ps1` without a repo path or an exact
invocation. Confirmed `docs/solutions/windows/scripts/uninstall-fleetd-windows.ps1` exists at the
tag and matches the described behavior (unregisters MDM, then tears down Orbit/osqueryd/Fleet
Desktop). Added the pinned repo path on first mention, and pointed the "run it across the Windows
hosts" instruction at this book's own 5.3 batch-run mechanism (save as a Fleet script, batch-run)
rather than leaving the invocation unstated. Cross-checked against
`articles/how-to-uninstall-fleetd.md`, which documents both the local-run and Fleet-remote-run
routes for the same script.

## 4.91.0 campaign — IA2: default fleet for user-driven Windows MDM enrollments

Verified against `fleet-v4.91.0` (commit `35fc1c0244`) in `~/Source/Fleet/fleet-public` on 2026-09-08. New `## Where automatically enrolled Windows hosts land` section, plus one precedence bullet in `## Edge cases and precedence`. Changelog line: "Added a default fleet for new Windows MDM enrollments (Fleet Premium). ... assigned before the Autopilot Enrollment Status Page runs, so the default fleet's software, scripts, and configuration profiles apply during out-of-box setup."

Base legend per STYLE §27: **Stated** = source says it; **Derived** = concluded from source, reasoning given.

| Claim | Base | Source | Confidence |
|---|---|---|---|
| Config key is `mdm.windows_automatic_enrollment.default_fleet`, a fleet **name** (transport/display shape) | Stated | `server/fleet/app.go:268-271` (`WindowsAutomaticEnrollment optjson.Any` with json tag `windows_automatic_enrollment`), `:2181-2188` (`DefaultFleet string json:"default_fleet"`) | High |
| DB source of truth is a singleton row `mdm_windows_enrollment_config.default_team_id` (fleet ID), NULL when unset | Stated | `server/datastore/mysql/migrations/tables/20260803135530_AddWindowsEnrollmentDefaultFleet.go` (CREATE TABLE, singleton CHECK id=1, seed NULL row); read via `GetWindowsEnrollmentDefaultFleet` | High |
| Setting stored by ID, so rename follows automatically; config-API response re-hydrates the name from the ID | Stated | `server/service/appconfig.go:289-303` (hydrate `WindowsAutomaticEnrollment` from `GetWindowsEnrollmentDefaultFleet` when the JSON name disagrees); app.go comment "copy stored in app_config_json can be stale after a fleet rename or deletion" | High |
| Deleting the fleet clears the default (FK `ON DELETE SET NULL`; EE team-delete also clears cache) | Stated | migration FK `fk_mdm_windows_enrollment_config_default_team_id ... ON DELETE SET NULL`; `ee/server/service/teams.go:1019-1029` (clear default + `SetWindowsEnrollmentDefaultFleet(nil)`) | High |
| Setting/changing to a real fleet requires Premium; clearing (empty or "Unassigned") allowed on any tier; unchanged re-send tolerated on Free | Stated | `server/service/appconfig.go:2366-2405` `validateWindowsEnrollment` (empty/`IsUnassignedFleetName` → clear on any tier; non-premium change → `ErrMissingLicense`; `name == curName` tolerated) | High |
| Name must resolve to an existing fleet else `fleet "X" doesn't exist` | Stated | `validateWindowsEnrollment` → `TeamByName`, `IsNotFound` → `fmt.Sprintf("fleet %q doesn't exist", name)` | High |
| "Unassigned" means empty (`IsUnassignedFleetName` == lowercase "unassigned"; UI `UNASSIGNED_FLEET = ""`) | Stated | `server/fleet/teams.go:53-55`; `frontend/.../WindowsMdmPage/WindowsMdmPage.tsx:25` | High |
| GitOps key `org_settings.mdm.windows_automatic_enrollment.default_fleet`; referenced fleet may be created later in the same run; a fleet that is the default cannot be deleted in the run | Stated | `cmd/fleetctl/fleetctl/gitops.go:1309-1345` (`checkWindowsEnrollmentAssignment` reads `org_settings.mdm.windows_automatic_enrollment.default_fleet`, deferred apply), `:715-717` (delete guard "default_fleet %s cannot be deleted") | High |
| Editing the default logs `edited_windows_enrollment_default_fleet` activity | Stated | `server/fleet/activities.go:2145-2147` (`ActivityName() = "edited_windows_enrollment_default_fleet"`); emitted at `server/service/appconfig.go:1354-1359` | High |
| UI: Settings > Integrations > MDM > Windows MDM, "User driven enrollment" section, "Default fleet" dropdown (Unassigned + every fleet id>0) | Stated | `frontend/.../WindowsMdmPage/WindowsMdmPage.tsx:136-165` (dropdown, `fleetOptions`), `:216-222` ("User driven enrollment" section) | High |
| Dropdown disabled until MDM on + connected to Entra (+ not GitOps); tooltip "Fleet must be connected to Entra to set a default fleet." | Stated | `WindowsMdmPage.tsx:146` `isDisabled={!mdmOn \|\| !isConnectedToEntra \|\| gitOpsModeEnabled}`; `:99` `isConnectedToEntra = !!windows_entra_tenant_ids?.length`; tests `:59,66,70` | High |
| Whole "User driven enrollment" section is Premium-only in the UI; backend rejects otherwise | Stated | `WindowsMdmPage.tsx:216` `{isPremiumTier && (...)}`, `:54-58` comment + `...(isPremiumTier && { windows_automatic_enrollment })` | High |
| Assignment is moved iff ALL: enrollment user-driven, default set, host has no fleet, host row created at/after the enrollment (MDM-first) | Stated | `server/service/osquery_utils/queries.go:3313-3352` `maybeAssignWindowsEnrollmentDefaultFleet` (teamID!=nil, `host.TeamID==nil`, `host.CreatedAt.Before(device.CreatedAt)` → skip) | High |
| "User-driven" == enrollment carries a valid UPN (signed-in Entra user) | Stated | `queries.go:3267-3273` — `maybeAssign...` only called when `microsoft_mdm.IsValidUPN(device.MDMEnrollUserID)` | High |
| fleetd-first hosts keep the enroll-secret fleet; pre-existing hosts (incl. deliberately-Unassigned) never moved — matches macOS ABM re-enrollment | Stated | `queries.go:3313-3320` function doc ("Hosts that enrolled fleetd first keep the fleet their enroll secret chose. Pre-existing hosts are never moved, including hosts deliberately parked in Unassigned, matching macOS ABM re-enrollment behavior") | High |
| On assignment Fleet reconciles the new fleet's profiles immediately (same as a manual transfer) | Stated | `queries.go:3344-3347` `BulkSetPendingMDMHostProfiles` after `AddHostsToTeam`, comment "Same side effect as a manual transfer so the new fleet's profiles reconcile immediately" | High |
| Assignment is best-effort; on failure the host stays Unassigned and enrollment still succeeds | Stated | `queries.go:3273-3277` caller logs + `ctxerr.Handle` on error, comment "Best-effort. In the unlikely event of a failure, the host remains in Unassigned fleet." | High |
| Assigned before the Autopilot Enrollment Status Page runs, so the fleet's software/scripts/profiles apply during OOBE | Derived | Assignment fires when the MDM enrollment links to the host (`LinkWindowsHostMDMEnrollment` → `maybeAssign...`), and the migration notes Autopilot's MDM-first ordering (fleetd installed after MDM enrollment). The profile reconcile is immediate (row above). The changelog states the ESP-timing outcome explicitly; code establishes the early link + immediate reconcile that makes it true. Not separately re-timed against a live Autopilot ESP. | Med-High |
| No minimum fleetd/Orbit version stated for this feature (unlike IA1's managed local admin) | Stated (absence) | Changelog line carries no version requirement; no `orbit/` gate found for the default-fleet path (grep) | Med |

Notable: this is the Windows counterpart to 2.10's Apple "Where automatically enrolled hosts land" (per-platform default team on the ABM token). Windows has a single global default (one singleton row), gated on an Entra connection, and applies only to user-driven (UPN-bearing) enrollments. The `edited_windows_enrollment_default_fleet` activity was left in prose here (not routed to the 8.12 catalog) since IA2's placement target is 2.11 only; if a later pass catalogs it, cross-link back here.
