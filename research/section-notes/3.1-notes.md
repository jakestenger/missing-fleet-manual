# 3.1 Enrollment design and host lifecycle: citation ledger

Written 2026-08-25 against `fleet-v4.90.1` in `~/Source/Fleet/fleet-public`. First chapter written
under STYLE §27, so every row states its basis: **stated** means the source says this, **derived**
means the book concluded it, **unverified** means it could not be established at the tag.

## Stated

| Claim | Source |
|---|---|
| Dual-boot and shared-identifier VMs should use fleetd built with `--host-identifier=instance`, enrolling one host per operating system | `articles/enroll-hosts.md:207-209` |
| Switching a workstation's operating system means deleting the host first, then re-enrolling | same, `:199-201` |
| fleetd writes the enroll secret to the system keystore at launch and reads it back afterwards; Keychain on macOS, Credential Manager on Windows | same, `:431-435` |
| `--disable-keystore` on `fleetctl package` opts out | same |
| On macOS a later fleetd install must be signed by the same organization to read the secret from the Keychain | same |
| The Keychain is not used when the secret arrives via MDM profile; Keychain support on that path is documented as coming | same, note |
| Removing the profile that carries config-less settings stops fleetd working until an equivalent profile is installed, including when MDM is turned off | same, `:455-459` |
| Host identity certificates are Fleet Premium | same, `:338-340` |
| Linux only, from `.deb`, `.rpm` and `.pkg.tar.zst`, TPM 2.0 or vTPM, kernel 4.12 or later | same, `:348` |
| Certificate issued for 365 days; fleetd renews at 180 days with a new TPM key; renewal needs no enroll secret and works by proof of possession | same, `:346` |
| Cannot be combined with `--fleet-tls-client-certificate` | same, Important considerations |
| SCEP requests throttled by `osquery_enroll_cooldown` | same |
| Once a host requests a certificate the server expects all its future traffic signed, allowing mixed environments | same |
| `auth.require_http_message_signature` enforces signing server-wide, and hosts without a certificate stop communicating | same, and `:392` |
| P384 by default, requiring roughly 50 percent more CPU on the Fleet server | same, final bullet |
| `/api/fleet/orbit/ping` and Fleet Desktop requests are exempt from message signing | same, `:342` |
| Hosts without TPM 2.0 fail to enroll when the option is enabled; Fleet publishes a `/dev/tpmrm0` check | same, Important considerations |
| Duplicate identifiers overwrite by default with a logged warning | `server/datastore/mysql/hosts.go:2679-2683` |
| With a host identity certificate whose host ID does not match, enrollment is rejected | same, `:2686-2690` |

## Derived

| Conclusion | From | Reasoning |
|---|---|---|
| An enroll secret places a new host and does nothing to a host Fleet already knows | Established in 1.3 and re-checked | 1.3's verified statement that re-enrolling with a different fleet's secret leaves a host in place. Restated here because the enrollment chapter is where a reader forms the expectation |
| Retirement is stop-the-agent then delete, in that order | Deletion removes the record; a live agent with a valid secret enrolls again | Neither half is novel; the ordering consequence is the book's |
| Certificate renewal removes a long-lived shared credential from the attack path | The renewal mechanism above | Fleet states the mechanism, not this characterisation of its value |
| The P384 CPU cost is a capacity input for 2.9 | The 50 percent figure | Connecting it to sizing is the book's, not Fleet's |
| Multiple secrets per fleet make rotation possible without a flag day | Fleet supports more than one secret per fleet | The rotation procedure is a derivation, not documented as such at the tag |

## Unverified, and said so in the text

Whether a profile-delivered enroll secret survives profile removal once Keychain support arrives.
Fleet documents that support as coming; the behaviour after it lands cannot be established at this
tag. The chapter tells the reader to re-check rather than predicting it.

## Checks run

`check-links`, `check-crossrefs`, `check-absolutes`, `check-headings`, `check-activity-names`,
`unwrap`, site build. The absolutes check flagged "enrollment proves reachability and nothing
more"; rewritten, since enrollment also establishes that the host authenticated.

Whole-chapter consistency read performed per §27. No self-contradiction found; every stated count
matches the list it introduces.

---

## 2026-08-25 review pass

Independent review on `gpt-5.6-sol`, run after the chapter was written and committed.

### Corrected: the chapter generalized one enrollment path into all of them

As published, the scope line promised "the decisions common to enrolling anything" and then
described the fleetd path. Two rules did not carry:

| Rule as written | Where it fails | Source |
|---|---|---|
| The enroll secret determines fleet assignment | Automatic Apple enrollment places by the default fleet configured per platform against the Apple Business token | `articles/configuring-default-teams-for-devices-in-fleet.md:1-24`, and 2.7 of this manual |
| Retirement is stop the agent, then delete | iOS and iPadOS have no agent to stop, and a device still assigned in Apple Business returns at the next sync | same, and 2.7 |

**2.7 of this manual documents the Apple placement path correctly.** The contradiction was
between two chapters of this book, not between the book and Fleet. The §27 consistency read is
scoped to a single chapter and could not see it.

**That is a gap in the rule as written**, and worth recording: a chapter claiming to generalize
needs checking against what the book already says elsewhere, not only against itself.

The chapter now opens with the three paths and says plainly that what follows is the fleetd one.
Retirement is now framed as "deletion alone never retires a host, and what you must stop first
depends on the path", which holds for both.

### Corrected: the identity model collapsed several credentials into one

As written: "by default is an enrollment-derived node key and can optionally be a hardware-backed
certificate."

Two errors in one sentence. Fleet issues **two** node keys, an orbit node key stored in a
root-only file and an osquery node key stored in osquery's own database, each authenticating its
own component's requests (`articles/fleetd-authentication.md:29,52-60`). And the TPM certificate
is an additional signature layered onto authenticated requests rather than an alternative to a
node key.

### Corrected: rotation had no stop condition, and revocation is not limited to new hosts

As written: "remove the old one once nothing uses it."

`docs/Configuration/agent-configuration.md:66`, verbatim: "If you revoke an old enroll secret, the
`command_line_flags` won't update for hosts that enrolled to Fleet using this old enroll secret
... For these hosts, all existing features will work as expected."

So revocation silently degrades already-enrolled hosts, which keep working in every other
respect. The chapter now gives a real rotation sequence and states that Fleet does not report
which secret a host enrolled with, so the final check is behavioural rather than a query.

### Also corrected

Multiple enroll secrets are available on Fleet Free as well as Premium; Premium adds per-fleet
secrets alongside global ones (`articles/fleetd-authentication.md:22-24`). The chapter had not
said which tier supported what.

## Third round, 2026-08-27

NOT READY, seven items. The chapter is the one that defines the vocabulary the rest of Part III uses, so its universals propagate further than most.

**The config-less profile section was wrong against the implementation, and it was the confident kind of wrong.** It said a profile-delivered secret is held only by the profile, so removing the profile stops fleetd. Orbit **writes the profile's values to `secret.txt` and `fleet_url.txt`** and reads them back on a later start when no profile is present. So removing the profile from a host that has already read one does not stop it. 3.2, 8.2 and 8.4 all had this right; 3.1 was the outlier, and it is the chapter the others defer to on enrollment identity.

| The claim | What is true |
|---|---|
| "Three paths lead into Fleet", above a five-row table | The count was wrong and the table was incomplete: **Windows MDM** is its own way in, with its own discovery and enrollment endpoints, and it does not place the host |
| The path table gave no licence boundary | **ADE and account-driven enrollment are both Premium**; enrollment by link is core service and is not |
| "An enroll secret decides placement" | For a host Fleet has not seen before. **Android restores the last known fleet on re-enrollment**, and a **global** secret places a host in Unassigned legitimately, so Unassigned is not proof of the wrong secret |
| "A host appearing in the list has enrolled" | An Apple Business sync creates a pending record before the device has contacted Fleet at all |
| Deleting an ADE host: "the next sync brings it back" | It comes back **immediately**: the deletion invokes the MDM lifecycle, which restores the pending host there and then. 8.8 already said so |
| `--host-identifier=instance` gives two records | Not on Apple. Fleet matches by serial during Apple MDM enrollment, and the tagged tests call the combination incompatible and verify the records merge |
| Host identity: "every later request" is signed, renewal "at 180 days", the cooldown means "a mass rollout paces itself" | The middleware covers Orbit, osquery and certificate-request routes and excludes Orbit's ping. Renewal starts when **180 days remain**, so around day 185, with jitter. And the cooldown rejects a **repeat** request for a certificate name whose certificate is still recent, so it stops a host looping and does nothing to pace a thousand first-time hosts |

The image prompt was also drawing the node key and the hardware certificate as alternative sources of identity. They are not: the certificate signs requests the node key authenticates. Corrected in the prompt before it is rendered.

## Fourth round, 2026-08-27

NOT READY, and most of round three was confirmed resolved. Two items remained, both propagation from a neighbour: the path table still described account-driven placement as universally coming from an Apple Business token default, when this release retains a **tokenless legacy path** whose challenge names no token and therefore has no token-derived fleet; and the escrow trigger, which turned out to matter more than it looks.

## Capped at the review limit, 2026-08-27

Five rounds, every finding applied, last verdict NOT READY. Stays at `drafting` until the whole-part review.

Part III's arc matched Part I's. Rounds one and two found the organising claim of each chapter wrong; rounds three to five found propagation, the same corrected fact surviving in a table, a summary, an image prompt or a neighbour. The reviewer retracted six of its own findings across the part, every one of which I had already written into the manual on its authority, which is why `HANDOFF.md` now treats a reviewer's proposed wording as a claim rather than a citation.

