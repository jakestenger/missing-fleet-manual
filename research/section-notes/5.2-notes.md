---
section: "5.2"
---

# 5.2 Manage configuration profiles and declarative settings, citation ledger

Drafted 2026-08-27 against `fleet-v4.90.1` (dd0200f062).

## Stated

| Claim | Source at the tag |
|---|---|
| Profile delivery status is one of pending, verifying, verified, failed, or unset | `server/fleet/mdm.go`, the `MDMDeliveryStatus` constants and `IsValid` |
| Verifying means the command applied but Fleet has not independently verified it | Same file, the doc comment block above those constants, quoted almost directly |
| Verified means applied and independently confirmed, and is terminal | Same comment |
| Failed is terminal and reached after retries | Same comment, plus `MaxAppleProfileRetries = 3` and `MaxWindowsProfileRetries = 1` in `server/mdm/mdm.go` |
| An unset status means the profile needs applying or removing and is counted as pending in summaries and list filters | Same comment: "For the sake of statistics, as reported by the summary endpoints/functions or for the list hosts filter, a NULL status is equivalent to a Pending status" |
| Status can move backwards | Same comment: verifying "may transition to failed, pending, or NULL" |
| An unset status can be deleted without any MDM command when the profile becomes unneeded | Same comment |
| All three platforms share one status vocabulary | `server/datastore/mysql/schema.sql`: `host_mdm_apple_profiles`, `host_mdm_windows_profiles` and `host_mdm_android_profiles` all carry a foreign key to `mdm_delivery_status` |
| Each profile record carries an operation type of install or remove | `MDMOperationType` constants in `server/fleet/mdm.go`, and the `mdm_operation_types` foreign key on all three tables |
| Resend is permitted only from failed or verified | `checkAndResendHostMDMProfile` in `server/service/mdm.go`, which rejects pending and verifying with an explicit conflict and errors on anything else |
| Resending is recorded as an activity naming the profile and host | Same function, emitting `ActivityTypeResentConfigurationProfile` |
| Fleet refuses built-in OS update settings when a custom declaration already covers them | `CouldNotUpdateAppleOSSettingsWithCustomProfileErrorMessage` in `server/fleet/mdm.go` |
| Fleet variables are written `$FLEET_VAR_NAME` or `${FLEET_VAR_NAME}` | `FleetVarName.WithPrefix` and `WithBraces` in `server/fleet/mdm.go` |
| The host and identity variables are serial, UUID, platform, and a set of IdP-sourced values | The `FleetVarHost*` constants in the same block |
| The certificate authority variables cover NDES, DigiCert, Smallstep and custom SCEP, with renewal identifiers | The certificate authority constants and `HasCAVariables` in the same file |
| The Platform SSO registration token is expanded per host at command-fetch time and is never stored or exposed on the commands endpoint | The doc comment on `FleetVarPSSODeviceRegistrationToken` |
| Secret variable names are uppercase letters, digits and underscores, up to 255 characters | `ValidateSecretVariableName` and `secretVariableFormat` in `server/fleet/secret_variables.go` |
| A profile containing secrets includes its secrets-updated timestamp in its change fingerprint | `server/fleet/mdm.go` around line 644, describing the checksum for declarations as covering contents and the secrets updated timestamp |
| Apple declarations are supported alongside classic profiles and carry their own identifier prefix | `MDMAppleDeclarationUUIDPrefix` and `NewMDMConfigProfilePayloadFromAppleDDM` |

## Derived

**The three-step choice** (Fleet's own settings, then a declaration, then a custom profile) is
editorial. The supporting fact is the OS updates guard, which shows Fleet actively defending one
such boundary; the general ordering is my recommendation.

**"Removal is automatic."** Follows necessarily from scope being a continuously evaluated
membership test, and from the presence of a remove operation type. Stated as a consequence.

**"Uploading is not validating."** Editorial framing of the boundary between what Fleet checks and
what it cannot.

**The Apple identifier collision point** is general Apple platform behaviour rather than Fleet
behaviour, and is stated as a property of the device.

**The diagnostic order** at the end is editorial method.

## Deliberately not stated

**No profile format tutorial.** Writing a property list payload or a Windows configuration path is
the platform vendor's documentation, not Fleet's, and it would date. The chapter covers what Fleet
does with the document.

**No claim about how Fleet performs its independent verification, or on which platforms.** The
existence of the verifying-versus-verified distinction is established for all three platforms by
the shared status table. 8.6 describes the verification pass as an Apple mechanism. Whether Windows
and Android reach `verified` by the same route was not established. The chapter states what the
states mean and does not describe the mechanism. **Flag for a later round.**

**No retry timing.** The retry counts are established; how long Fleet waits between attempts was
not checked.

**No enumeration of which settings have built-in Fleet controls.** That list belongs with each
subject: encryption in 5.8, OS updates in 5.6.

## Extended 2026-09-02 (round4 RM8) — declaration user-channel section

New "Declarations: the user channel, and where it can't reach" section, verified against
`fleet-v4.90.1` (same tag as the rest of the chapter).

| Claim | Source at the tag |
|---|---|
| `PayloadScope` is a top-level declaration key, `"System"` (default) or `"User"`; System targets the device channel, User the user channel (macOS only) | `server/fleet/apple_mdm.go`: the `PayloadScope`/`PayloadScopeSystem`/`PayloadScopeUser` constants, the `Scope` field doc comment on `MDMAppleDeclaration`, `MDMAppleRawDeclaration.ScopeOrDefault` and `ValidateScope`; corroborated by `docs/Contributing/architecture/mdm/apple-declarative-device-management.md`'s DDM section, which states the same default and macOS-only restriction |
| The key is stripped from the declaration only when served to the device, so the delivered JSON stays valid Apple DDM | Same doc comment on `MDMAppleRawDeclaration.PayloadScope`; confirmed behaviorally by `TestAppleUserScopedDDMEndToEnd` in `server/service/integration_mdm_ddm_test.go`, which asserts `PayloadScope` is absent from the served content |
| iOS/iPadOS refuse a user-scoped install outright and immediately (status failed, no hold) | `resolveUserChannelDeliveries` in `server/service/apple_mdm_declarations_batched.go`, the `IsAppleMobilePlatform` branch, with `fleet.IsAppleMobilePlatform` defined as `ios`/`ipados` in `server/fleet/mdm.go` |
| A macOS host only has a user channel if it automatically enrolled; a manually enrolled Mac never gets one | Same function's `default` branch and its failure detail string, "Currently, Fleet creates the user channel for hosts that automatically enroll" |
| Grace period before failing a held user-scoped install is 2 hours from device enrollment | `HoursToWaitForUserEnrollmentAfterDeviceEnrollment = 2` in `server/mdm/apple/reconcile.go`, referenced by the same function's grace-window branch |
| Within the grace window, a pending user-scoped install is held (nil status, retried next tick) rather than failed | Same function, the grace-window `case`, setting `row.Status = nil` |
| A user-scoped REMOVE row for a host with no channel is hard-deleted rather than left pending | Same function's doc comment and its `toDelete` handling, which the comment says "mirrors how the profile reconciler cleans up undeliverable user-scoped profiles" |
| The two channels are independent connections with independent commands and status | `TestAppleUserScopedDDMEndToEnd`, which drives both channels through the simulated client and asserts each transitions independently |

**JSON example in the section** uses a placeholder `com.apple.configuration.<setting>` Type
rather than a specific Apple declaration type, since Fleet's own validation only requires the
`com.apple.configuration.` prefix (8.8 already documents the full validation order) and no
specific type name was verified as authoritative for this illustration; the point being
demonstrated is the `PayloadScope` key, not the type.

**Deliberately not stated:** what triggers Setup Assistant's user-channel enrollment step
mechanically (out of scope; 3.2 owns ADE enrollment sequencing), and Windows/Android
equivalents (neither platform has a user channel in Fleet's DDM implementation).

## Boundaries held

Disk encryption is 5.8, OS updates 5.6, scripts 5.3, software 5.4. MDM channel diagnosis stays in
8.8 and 8.9, and the SQL-level account of profile status stays in 8.6. The desired state model and
the four channels are established in 5.1 and cited rather than repeated.

## round6 M16: "Naming hosts from a template" (added 2026-09-03, verified at fleet-v4.90.0 / 7c428c6e46)

New section closes the "acknowledged but never taught" gap: the appendices already carried the
API (a.8) and the `mdm.name_template` config field (a.3), and 5.2's rotation note forward-referenced
"host display names," but no chapter taught the workflow.

| Claim in the section | Source at fleet-v4.90.0 |
|---|---|
| Premium-only | `UpdateMDMHostNameTemplate` / `ResendHostNameTemplate` both return `fleet.ErrMissingLicense` unless `license.IsPremium(ctx)` (`server/service/mdm.go:3374,3429`) |
| Apple company-owned only; BYOD/personal enrollment excluded because Apple refuses DeviceName on personal enrollment | eligibility predicate `deviceNameEligibleHostsWhere`: `h.platform IN ('darwin','ios','ipados') AND ne.enabled=1 AND ne.type='Device' AND hm.enrolled=1 AND hm.is_personal_enrollment=0` with the doc comment "Apple rejects the DeviceName setting on personal (user) enrollments" (`server/datastore/mysql/apple_device_names.go:15-46`) |
| One template per scope, fleet or Unassigned ("No team"), not per host | request carries `fleet_id` (nil = No team); No-team stored on app config, team stored on team MDM config (`server/service/mdm.go:3373-3399`); reconciler reads `appConfig.MDM.HostNameTemplate` for No-team and per-team `HostNameTemplate` (`server/service/apple_device_names.go:73-101`) |
| Set via UI / GitOps `controls.name_template` / API | REST `POST /api/_version_/fleet/host_name_template` (`handler.go:864`); GitOps `controls.name_template` string applied at `client.go:2623-2637` -> `mdm.name_template` (`server/fleet/app.go:277`) |
| Setting template to empty stops enforcing, does not rename back | empty template -> reconciler `continue` (no command); clear path removes rows (`apple_device_names.go:102-108`) |
| Variables: host identity (`HOST_HARDWARE_SERIAL`, `HOST_UUID`, `HOST_PLATFORM`) + IdP end-user (username, local part, full name, department, groups) + `$FLEET_SECRET_*`; anything else refused at save | allow-list `fleetVarsSupportedInHostNameTemplates` = identity + IdP (`server/fleet/name_template.go:23-42`); save rejects out-of-list vars (`:98-104`); secrets validated at save (`ValidateHostNameTemplateWithSecrets`, `:128-147`) |
| `HOST_PLATFORM` resolves to `macOS`/`iOS`/`iPadOS` | `hostNameTemplatePlatformDisplayNames` (`name_template.go:154-158`) |
| 63-byte device-name limit: fixed text rejected at save; per-host overflow (or missing IdP value) fails that host with a reason | `MaxResolvedHostNameBytes = 63`; save-time fixed-text floor check (`name_template.go:15-21,106-118`); reconciler `len(resolved) > 63` -> Failed "Resolved name exceeds 63 bytes.", missing IdP -> Failed with profile-style detail (`apple_device_names.go:171-184,159-168`) |
| Five-status lifecycle (Pending/Verifying/Verified/Failed + no-status), name-already-matching -> Verified with no command, synthetic "Host name" row beside profiles in host OS settings | statuses set via `SetHostDeviceNameStatus` with `MDMDeliveryPending/Verifying/Verified/Failed`; `resolved == host.ComputerName` -> Verified no command (`apple_device_names.go:185-215`); synthetic "Host name" row for eligible Apple hosts (`frontend/.../OSSettingsModal/.../OSSettingsTableConfig.tsx:243-260`) |
| Editing template / moving host between fleets re-queues; resend allowed only from failed or verified, never pending/verifying; per host | reset-on-change/transfer requeues (`BulkUpsertHostDeviceNameEnforcement`); resend blocks NULL/pending/verifying with 409 (`server/service/mdm.go:3463-3466`); resend cell enabled only for failed/verified (`OSSettingsResendCell.tsx:172`) |
| Secret change re-queues names with no re-apply (existing rotation note, unchanged) | pre-existing verified claim at 5.2 blockquote; reconciler expands `$FLEET_SECRET_*` per template (`apple_device_names.go:110-142`) |

**Appendix wiring:** A.10 gained "host display name template" (+ "name template", "device name"
cross-refs) routes to the new section. A.1's "no capability row" register grew from five outcomes
to six (host display name template is taught in 5.2 but keeps no formal CAP row, exactly like the
local sandbox); the 364-row CAP count is unchanged, so check-cap-ids stays green.

**Deliberately deferred:** a formal CAP row + A.2 platform-matrix + A.5 interface-matrix entries.
Adding a CAP-### row ripples A.1's frontmatter count, A.2's matrix + prose exclusions, and A.5's
362-row count / 1,448-cell tally / cross-counts, all gated by check-cap-ids.py, and the register's
own framing calls these outcomes ones that "wait only on a formal row." Registering it as taught-
but-unrowed is the honest, complete state without the count-coupled recount; a later dedicated
appendix pass can grant the formal row.
