---
section: "5.3"
---

# 5.3 Run and manage scripts, citation ledger

Drafted 2026-08-27 against `fleet-v4.90.1` (dd0200f062).

## Stated

| Claim | Source at the tag |
|---|---|
| Saved scripts are limited to 500,000 characters, ad-hoc scripts to 10,000 | `SavedScriptMaxRuneLen` and `UnsavedScriptMaxRuneLen` in `server/fleet/scripts.go`, applied in the validation path |
| The host terminates a script after five minutes | `MaxHostExecutionTime = 5 * time.Minute` in `pkg/scripts/scripts.go`, with the comment "the maximum time allowed for a script to run in a host before is terminated" |
| The server waits six minutes on a synchronous run | `MaxServerWaitTime = MaxHostExecutionTime + 1*time.Minute`, commented as an allowance for the notification system that delivers scripts |
| The execution timeout is configurable through agent options up to 18,000 seconds | `ScriptExecutionTimeout` in `server/fleet/agent_options.go`, validated against `maxAgentScriptExecutionTimeout = 18000` |
| Output is capped at 10,000 runes and the **end** is kept | `orbit/pkg/scripts/scripts.go`: `const maxOutputRuneLen = 10000`, then `output = output[len(output)-(utf8.UTFMax*maxOutputRuneLen):]`, which slices from the tail |
| Only `.sh`, `.ps1` and `.py` may be uploaded | `server/fleet/scripts.go`, the extension switch and its error: "File type not supported. Only .sh, .py, and .ps1 file types are allowed." |
| Shell scripts must use `/bin/sh`, `/bin/bash` or `/bin/zsh`, optionally under `/usr` | `scriptHashbangValidation`, the anchored regular expression, and `ErrUnsupportedShellInterpreter` |
| Python is supported via `/usr/bin/env python3` or an absolute path to `python` or `python3` | `ErrUnsupportedInterpreter`, whose message enumerates the supported set |
| A result carries an exit code, output and a runtime in seconds | `HostScriptResultPayload` fields set in `orbit/pkg/scripts/scripts.go` when saving |
| A script with a stored exit code is not run again | Same file: the early return when `script.ExitCode != nil`, commented as "already a result stored for this execution" |
| Secret variables are the mechanism for keeping a credential out of stored script content | Established for 5.2, `server/fleet/secret_variables.go` |
| Scripts run as the agent's system service, which is root on macOS and Linux | `orbit/pkg/scripts/scripts.go`, the `createRunDir` comment "MkdirTemp will only allow read/write by current user (root), which is what we want"; `orbit/pkg/packaging/macos.go` installing a launch daemon at `Library/LaunchDaemons/com.fleetdm.orbit.plist`; `orbit/pkg/packaging/linux_shared.go` writing a systemd system unit with no `User=` directive |
| Scripts can be disabled globally in organization settings | `ScriptsDisabled` in `server/fleet/app.go`, with `RunScriptScriptsDisabledGloballyErrMsg` in `server/fleet/errors.go` |
| Scripts can be disabled per host by the agent packaging, returning exit code -2 | `orbit/pkg/scripts/scripts.go`, `runOneDisabled` saving `ExitCode: -2` with output "Scripts are disabled", commented as a value `fleetctl` recognises |
| Software installation shares the scripts mechanism and reports the same condition | `server/fleet/software_installer.go`, `SoftwareInstallerScriptsDisabledCopy`: "Error: Scripts are disabled for this host. To run scripts, deploy the fleetd agent with --enable-scripts." |

## Derived

**"A script is the most powerful thing in Part V and the least constrained."** Editorial, and the
chapter's organising claim.

**"Put your diagnostics at the end."** Follows directly from the verified tail-slicing behaviour.
The advice is mine; the behaviour is source-checked.

**The idempotence guidance** is editorial method, resting on the verified fact that the queue
orders work and does not deduplicate it.

**The secrets-in-output warning.** The mechanism facts underneath are that secrets are substituted
into script content and that output is stored and readable. The conclusion that this is the primary
leak path is my reasoning.

**"Command line arguments are visible on the host."** General operating system behaviour rather
than Fleet behaviour, stated as such. The osquery connection is a cross-reference, not a claim that
Fleet collects command lines by default.

**The library conventions and the pre-flight checklist** are editorial throughout.

## Gap closed before review

The first draft declined to say what account scripts run as, and flagged it as the chapter's
largest gap. It is closed, from two independent directions: the run-directory comment in orbit
names the current user as root, and the packaging code installs orbit as a launch daemon on macOS
and a systemd system unit with no `User=` on Linux, both of which run as root.

Closing it turned up the more valuable finding, which the first draft missed entirely: **scripts
can be disabled per host by how the agent was packaged**, independently of any server setting, and
that failure presents as exit code -2 rather than as configuration. Software installation shares
the mechanism and fails the same way. An estate can therefore contain hosts that will never run a
script, with nothing in the interface distinguishing them until you try.

## Deliberately not stated

**No default value for the agent options script timeout.** The maximum is established. Whether an
unset value falls back to the five minute constant was not confirmed, so the chapter presents the
five minutes as the host limit and the option as a way to raise it, without asserting the default
binding.

**No retry semantics for a failed script.** Whether and when Fleet re-runs a script that failed was
not established. The chapter says a script gets re-run "by a retry" among other causes without
claiming Fleet performs one automatically.

**No Windows or macOS specific execution detail**, such as working directory or environment. The
run directory exists in the source but its guarantees to script authors were not established.

## Boundaries held

Software installation, including installer lifetimes and its own longer timeout, is 5.4. Policy
automation that fires a script is 5.9. The queue and its ordering are 5.1. Secrets as a general
mechanism are introduced in 5.2. Diagnosing a script that never arrived is Part VIII.

## Batch scripts ("Running it on many at once", added 2026-08-30)

Drafted from research-cluster-scripts.md Subject 1 as corrected by the soundness gate
(reviews/phase2/gate-clusters-34-sol.out). All cites at fleet-v4.90.1 (dd0200f062).
Where the gate corrected the research, the gate wording is authoritative.

- Library script only, REST + UI, no fleetctl/GitOps, returns a batch execution ID:
  `server/service/handler.go:571-587`; `server/fleet/api_scripts.go:287-292,362-374`;
  `cmd/fleetctl/fleetctl/scripts.go:28-54` (single-host only).
- Exactly one of host_ids or filters: `server/service/scripts.go:1087-1091`.
- Filter must name a fleet; label path uses DisableIssues so failing policies ignored:
  `server/service/scripts.go:1124-1147` (research 1.4).
- Same fleet as the script: `server/service/scripts.go:1167-1175`.
- Globally disabled refuses whole run: `server/service/scripts.go:1093-1103`.
- 5,000 cap (MAX_BATCH_EXECUTION_HOSTS): `server/service/scripts.go:1085,1159-1165`.
- FOUR incompatibility reasons (gate correction 2), all surfacing as public `incompatible`:
  invalid-host, incompatible-team, incompatible-fleetd, incompatible-platform ->
  `server/fleet/scripts.go:836-840`; `server/datastore/mysql/scripts.go:2603-2663`;
  `server/datastore/mysql/hosts.go:1576-1604`. (Research listed only two; gate is authoritative.)
  Glosses for fleetd/platform reuse the chapter's own single-host stories (5.3 packaging exit -2,
  interpreter/platform) as derived, not stated.
- Cancellation stops only queued/pending, race after activation, records canceled_script_batch
  even when finished (gate correction 4): `server/datastore/mysql/scripts.go:2818-2915,2855-2862`;
  `server/service/scripts.go:920-949`.
- Per-host ran_script rows host_only -> excluded from global feed + external stream; batch-level
  request/schedule/cancel remain ordinary activities; status + host_results are the per-host
  outcome surface (gate correction 5): `server/fleet/activities.go:845-870,1729-1760`;
  `server/activity/internal/mysql/activity.go:42-64`;
  `server/fleet/scripts.go:864-910`, `server/fleet/hosts.go:851-870`.
- Start needs BOTH script.write AND host_script_result.write = admin/maintainer only; technicians
  cannot; read needs script.read (observer has) (gate under-claim):
  `server/service/scripts.go:1105-1114`; `server/authz/policy.rego:1212-1276`.
- Edition: batch endpoint has NO license check either direction; Premium gates team creation,
  not batch execution (gate correction 1). Do NOT write "Free globally, Premium per fleet":
  `server/service/scripts.go:1087-1212`; contrast single-host gate `:122-128`, teams service
  `server/service/teams.go:48-53,107-112,140-145`.

SIBLING EDIT PENDING (do not apply here): a.2:246 CAP-162 runtime-gate cell still reads
"Free globally, Premium per fleet" and is wrong per gate correction 1. Flag for coordinated pass.

round10 RV9-C2 (team/No team -> fleet/Unassigned prose, conservative): 5.3:125 "belongs to
one fleet, or to No team" -> "or to Unassigned"; 5.3:146 "the only fleet is No team" -> "the only fleet
is Unassigned". LEFT the literal Fleet error string `incompatible-team` at 5.3:134 (verified
fleet.BatchExecuteIncompatibleTeam = "incompatible-team", server/fleet/scripts.go:840) - it is a
deprecated-input/identifier quotation, not prose. check-links=0.
