# 3.3 citation ledger: enroll Windows devices

Chapter: `manual/03-connect-devices/3.3-enroll-windows-devices.md`
Verified against: **Fleet 4.90.1**, `git tag fleet-v4.90.1`, commit `dd0200f062`, in `~/Source/Fleet/fleet-public`
Verified on: 2026-08-27
Independent review: **not yet run.** Status is `drafting` and must stay there until it has been.

Written from the outline stub on 2026-08-27. `claims.py` was run on `Windows MDM`, `Autopilot`, `WSTEP`, `msi`, `Entra`, `agent-driven`, `tamper protection`, `Access work or school`, `Windows Server`, `enroll secret`, `Fleet Desktop` and `On (manual)` before and after drafting.

## Ownership, decided before writing

Windows is the most crowded platform in this manual: three chapters already hold parts of it.

| Owned by | What |
|---|---|
| 3.1 | Enroll secrets, node keys, host identity, fleet placement, retirement |
| 2.8 | Windows MDM setup: the WSTEP pair, turning MDM on, the Entra connection and its Microsoft licensing, migration, the sign-in requirement |
| 8.9 | Diagnosis: the enrollment SOAP exchange, the three paths as failure signatures, the poll schedule, the command queue, host-side registry and certificate checks |
| 8.4 | Agent-side diagnosis |

So 3.3 owns **the direction of dependency between the two channels, the choice of path, the MSI, and how to confirm both channels arrived.** Everything else links out.

## The framing

**Superseded 2026-08-27 and rewritten in place**, because the original version of this section is the thing the first review rejected.

It said the channels "pull each other along" and that 3.2's independent-channel model would mislead on Windows. 1.2 says the channels are independent on every platform, and 1.2 is right: either can arrive without the other and a host can sit half-enrolled indefinitely. What Windows adds is **conditional automation between two independent channels**, in both directions, and each direction has conditions that can fail. "Which channel arrived first, and did the automation for the other one fire" is the diagnostic question, not the architecture.

## Stated

| Claim | Source |
|---|---|
| Fleet decides on every Orbit config check-in whether to enroll the host in Windows MDM, gated on Windows MDM being on and `enable_turn_on_windows_mdm_manually` being off | `server/service/orbit.go`, the Windows MDM notification block |
| The four eligibility conditions: Windows platform, osquery-enrolled, not a server, not enrolled in any MDM | `isEligibleForWindowsMDMEnrollment` in `server/service/microsoft_mdm.go` |
| **Corrected 2026-08-27.** The hour applies only after a **successful** registration call. `lastEnrollRun` is set only on success, so an error leaves it unchanged and the next config check-in retries, thirty seconds later on the default cadence. A Windows Server error is the one exception: `isWindowsServer` is cached and suppresses all later calls for the life of the process | `windowsMDMEnrollmentCommandFrequency = time.Hour` in `orbit/cmd/orbit/orbit.go`; the `lastEnrollRun` assignment and the `errIsWindowsServer` branch in `orbit/pkg/update/notifications.go`. The original row claimed an unconditional hourly retry and was wrong in both directions |
| The Windows Server check happens twice, on different evidence | Server side reads `mdmInfo.IsServer`; `RunWindowsMDMEnrollment` reads the installation type from the device and returns `errIsWindowsServer`, and the receiver caches the answer in a struct field for the process lifetime |
| `enable_turn_on_windows_mdm_manually` is Premium, requires Windows MDM on, and conflicts with migration | Three separate validations in `server/service/appconfig.go`, each with its own message |
| Six MSI installer properties: `FLEET_URL`, `FLEET_SECRET`, `ENABLE_SCRIPTS`, `FLEET_DESKTOP`, `END_USER_EMAIL`, `EUA_TOKEN` | `orbit/pkg/packaging/windows_templates.go`. The service's `Arguments` string substitutes them with `[PROPERTY]` syntax |
| `FLEET_SECRET` defaults to `dummy`, and the install-time script skips writing `secret.txt` when the value is empty or `dummy` | `Update-OrbitSecret` in the same file, guarded by `-not IsNullOrEmpty` and `-ne "dummy"` |
| Everything else in the service command line is build-time | Same `Arguments` string: update URL, channels, `--insecure`, `--debug`, host identifier, osquery DB, setup-experience switch are Go template values, not MSI properties |
| `--use-system-configuration` is rejected for MSI | `cmd/fleetctl/fleetctl/package.go`: "only available for pkg installers" |
| `--arch arm64` is accepted for MSI | Same file; the architecture check permits non-amd64 for Linux and Windows package types |
| Fleet enqueues the fleetd install only for enrollments whose enroll user ID is a valid UPN | `isFleetdPresentOnDevice` returns `true` unconditionally when the ID is not a UPN |
| **Corrected 2026-08-27.** A check-in counts as evidence of a working agent if it happened after the enrollment **or up to 90 seconds before it** | `!host.SeenTime.Before(enrolledDevice.CreatedAt.Add(-fleetdPresenceGracePeriod))`. The original row had the window facing the wrong way, which inverted what the grace period is for |
| The install is issued on a new-session alert, fetched by the device from Fleet's download service with a SHA-256, with `ENABLE_SCRIPTS="True"` and a global enroll secret | `enqueueInstallFleetdCommand` and `processNewSessionAlert` in `server/service/microsoft_mdm.go`; `GetEnrollSecrets(ctx, nil)`; `pkg/fleetdbase` |
| Metadata-fetch failure is a warning and the session continues | Same function: it logs and returns `nil` rather than erroring the SyncML transaction |
| The install is guarded by a fixed GUID so Windows applies it once | Source comment states exactly this |
| The service is named `Fleet osquery` | `windows_templates.go`, the uninstall helper's WMI filter |
| MDM status strings per path | `server/fleet/hosts.go` for the strings, and `installedFromDep := enrollType == WindowsMDMEnrollTypeAutomatic && isInOOBE` in `server/service/microsoft_mdm.go` for which path produces which. `mdm-bug-checklist.md` and 8.6 agree, which is consistency and not the citation |
| `enable_turn_on_windows_mdm_manually` is documented as all-fleets only | `docs/Configuration/yaml-files.md`. Stated as Fleet's documentation rather than as a validation, because the validation was not located |
| **Withdrawn as a Stated row 2026-08-27.** Tamper protection, the sign-in requirement and its thirty-second figure, and Entra licensing are **claims about Windows and Microsoft that Fleet's source does not establish**. Citing 2.8 for them cited this manual, not Fleet, which is what STYLE §27 now forbids. All three are marked in the chapter as Fleet's guidance to confirm rather than as verified behaviour, and 2.8 is corrected to match | Absence of any implementation or tagged documentation, searched at the tag |

## Derived

- **The two-directions framing** is the book's own, built from the two mechanisms above.
- **"Fleetd first, then Fleet moves the host"** for migration follows from `isEligibleForWindowsMDMMigration` requiring `IsOsqueryEnrolled`, exactly as the enrollment path does.
- **"Two separate egress paths have to work"** is an inference from the server fetching the metadata document and the device fetching the MSI. Fleet does not describe it that way.
- **The `dummy` sentinel being the first thing to check** when hosts land in the wrong fleet is judgement, not a documented failure mode.

## Not claimed

- **`EUA_TOKEN`'s full round trip.** Fleet signs a token binding the Entra UPN to the device ID and the agent sends it back, but what Fleet then does with it on the enrollment side was not traced end to end. The chapter says only that it carries end-user attribution and that Fleet sets it.
- **Minimum supported Windows version.** Not established at the tag.
- **Whether the MSI build requires WiX, and on which host platforms.** `--local-wix-dir` is restricted to Windows and macOS, and the default path was not traced. Deliberately absent rather than guessed.
- **UI click paths**, per STYLE §6.

## Outstanding

1. **No independent review.** Required before `verified`.
2. The `EUA_TOKEN` round trip is worth finishing; it is the only Windows enrollment mechanism in this chapter that is named without being explained.
3. No screenshots. Unlike 3.2 there is no irreversible choice in a modal here, so the case is weaker.

## First independent review, 2026-08-27: NOT READY. Applied.

Seven factual defects, four unmarked-unverified claims, five contradictions with neighbours. All confirmed in the source before applying.

**BitLocker enforcement was assigned to the wrong channel**, which made the chapter's opening sentence wrong. `EnforceBitLockerEncryption` is set in the **Orbit configuration response** and the agent encrypts locally; MDM enrollment is a gate on Fleet setting it, not the delivery mechanism. So it is not an MDM command, it does not appear in the Windows command queue, and a host with only one channel gets no enforcement whichever channel that is. This also resolved contradiction 1 with 1.2, which had it right.

**The chapter oversold the coupling between the channels**, and 1.2 says they are independent. Both are true in different senses and the chapter now says which: the channels **stay** independent, and what Windows adds is **conditional automation** between them in both directions. That is the durable model; "which one happened first" is now the diagnostic question rather than the architecture.

**The hourly retry throttle is narrower than it reads.** Orbit records the attempt time **only when the registration call succeeds**. A call that errors leaves the timestamp **unadvanced**, unset on a first attempt and at its previous value afterwards, so the next configuration check-in retries thirty seconds later rather than in an hour. The hour applies when registration returned success and Fleet still thinks the host is unenrolled. So a persistently failing enrollment produces a **fast** loop in the log, and the chapter had told the reader to expect an hourly one.

**The credential-free MSI advice omitted the build that makes it work.** `fleetctl package` requires `--fleet-url` and `--enroll-secret` together, and the MSI carries the `--enroll-secret-path` argument only when a build-time secret was supplied — so omitting the secret produces a package that writes `secret.txt` at install time and never reads it. Fleet's own contributor documentation builds the base artifact with `--fleet-url=dummy --enroll-secret=dummy`; `dummy` is a sentinel that suppresses writing the secret while keeping the path in place. Both commands are now in the chapter. The section previously showed a build embedding a real secret and then claimed the result had no credential in it.

**The fleetd-presence grace window was described backwards.** A check-in counts as evidence of a working agent if it happened after the enrollment **or up to 90 seconds before it**. The chapter said the check-in had to postdate the enrollment with 90 seconds of slack, which inverts what the window is for: it exists so a device re-enrolling moments after a normal check-in is not sent a redundant installer.

**Two claims about the pushed install were too broad.** A **global enroll secret has to exist** or Fleet logs a warning and builds no install command at all, which is a silent no-op and belongs in the prerequisites; with several, Fleet takes the first the datastore returns. And that secret **places only new hosts** — an existing host keeps its fleet, per 3.1, which the chapter's unqualified wording had lost.

**`dsregcmd` was presented as the MDM answer.** It is Entra evidence: joined or not, and which MDM URL Entra supplied. The device's record of being managed by Fleet is the registry enrollment whose `ProviderID` is `Fleet` with a non-zero `EnrollmentState`, which is what fleetd itself reads. And an empty `MdmUrl` has **three** causes in Fleet's own notes: a missing Entra ID P1 licence, a licence assigned but not yet propagated, or an MDM user scope of `None`. The chapter named only the first, which is the one that costs money to act on, while one of the other two costs nothing but patience. 8.9 already had both right, so this was 3.3 contradicting Part VIII.

### Marked unverified rather than asserted

- **Tamper protection.** No implementation or tagged documentation establishes it. Now stated as Fleet's guidance, to confirm on a test machine, **in 2.8 as well** — 2.8 asserted it flatly and is the chapter people will quote.
- **The sign-in requirement and the thirty-second figure.** Fleet's source establishes that the agent check-in *starts* registration, and that Orbit runs as a system service and calls the registration API without inspecting who is logged in. It establishes nothing about when Windows *completes* enrollment. The chapter now draws that boundary explicitly, which also resolves contradiction 3 between 3.3 and 2.8.
- **Entra licensing**, which Fleet's Autopilot notes describe as its current understanding rather than verified.

### Voice

Removed the phrases the review named: "worth sitting with", "the useful part", "worth repeating", "nearly always", "most Windows estates". The mechanism-led heading became "Build one base MSI and supply the rest at install time". And the false handoff to **a.7**, an empty outline, is gone, as in 3.2.

## Third round, 2026-08-27

NOT READY, five items plus one neighbour.

The checklist billed as complete was missing its controlling condition: `enable_turn_on_windows_mdm_manually`, a Premium setting explained later in the same chapter, switches the whole automatic-enrollment mechanism off. The four conditions are now four conditions under a fifth.

**The MSI introduction contradicted the property table below it.** `--enable-scripts` and `--fleet-desktop` set *defaults* on Windows; `ENABLE_SCRIPTS` and `FLEET_DESKTOP` override them at install time. So "a package built without it produces hosts you cannot run a script against later" was wrong, and wrong in the direction that makes an estate look unfixable.

Four licence gates were missing: both Entra-backed paths are Fleet Premium as well as Microsoft-licensed, since Free rejects the Entra tenant and client IDs, which leaves a Free deployment with the agent-driven path alone; Windows MDM migration is Premium; My Device's self-service is Premium while the tray and the page are not; and BitLocker enforcement is Premium.

"Three properties" had four bullets, from a round-two correction that split one bullet without updating the count.

**And this ledger was itself out of date**, still summarising two claims that round two had corrected, which is the same propagation failure the chapters keep having. A ledger that describes a superseded version of the chapter is worse than none, because it is the thing a later reader trusts.

8.9 labelled the Settings-app path "BYOD" while this chapter says Fleet records no personal-enrollment status on Windows. Reworded there.

