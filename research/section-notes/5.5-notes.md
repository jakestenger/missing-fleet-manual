---
section: "5.5"
---

# 5.5 Design setup and self-service experiences, citation ledger

Drafted 2026-08-28 against `fleet-v4.90.1` (dd0200f062), checkout verified at the tag with a clean
tree. Research at `research/section-notes/5.5-scratch-*.md`, six files. Process: outline consensus
(two rounds), research consensus (two rounds, five research sweeps), draft, one review round applied.

## Stated

| Claim | Basis at the tag |
|---|---|
| Setup runs on macOS ADE enrollment and **not** on MDM migration | macOS setup enqueueing is limited to the awaiting-configuration state and skips migration |
| The blocking paths run two concurrent lanes joined at the release decision | The release predicate joins configuration and setup-item state rather than sequencing them |
| **Linux and Android are outside that model**: Linux has only the setup-item lane and holds nothing; Android applies apps as a policy batch | Android explicitly bypasses the step machinery; Linux has no MDM channel |
| Only macOS has a setup script; elsewhere a script-only package is ordinary setup software | Setup-script rows are created for darwin only |
| **Labels do not filter setup software on any platform**, nor App Store or Play apps | The enqueue queries carry no label join and say so in a comment. **Corrected in review**: I had thought this macOS-only |
| Linux distribution compatibility still filters `.deb` and `.rpm` | The same enqueue query filters on distribution |
| Policy labels **are** honoured by the Windows and Linux gate | Policy scope is resolved after fresh label membership |
| Setup takes the first-added package when a title has variants | Same resolution rule as [5.4](../../manual/05-manage-devices/5.4-manage-software-and-applications.md) |
| End-user authentication is a separate configuration from administrator SSO, enabled per fleet | Separate stored configuration and its own callback |
| A separate IdP application is **guidance, not enforcement** | Fleet stores two configurations but does not prove they name different applications. **Corrected in review** |
| Windows and Linux authentication fails open in three cases: unreadable capability header, agent older than 1.50.0, previously Orbit-enrolled host re-enrolling | The three bypasses, each logged, none surfaced in the interface |
| The EULA is shown only during macOS ADE, with authentication on, and **acceptance is not recorded** | The button changes client state and redirects; no acceptance event or record. **Scope added in review** |
| Account type has three choices, `admin`, `standard` and `none`, and the latter two require the managed local administrator account | Validation enforces the prerequisite. **Restored in review**: the draft lost the `none` option |
| Enabling end-user authentication also locks the name fields unless overridden | The locking setting defaults from the authentication setting |
| Manual fleetd installation conflicts with macOS setup software and a setup script | Both are refused in that configuration |
| macOS release has three cases: no setup items uses the bounded worker; setup items with a capable agent use the unbounded agent poll; setup items with an older agent fall back to the worker | The worker-release flag is set when there are no setup items; capability decides the fallback. **Both qualifications added in review** |
| Manual release disables both automatic paths | The setting suppresses the release job |
| **Release-ready is weaker than successful**: failed bootstrap, errored account configuration, `Failed` and `Verifying` profiles all permit release | The release predicate accepts all four. **Renamed in review** from "terminal is not successful", because `Verifying` is not terminal in the ordinary profile lifecycle and can move backwards |
| Platform SSO is not a setup item; it arrives as a configuration profile, optionally with its extension app as setup software, and Fleet's part is timing | No setup-status row and no dispatcher branch; release waits then Apple presents the pane |
| A failed PSSO prerequisite can leave a user at Apple's sign-on pane | Documented for a supporting certificate profile or the extension app failing. **Narrowed twice in review** |
| The Setup Assistant sign-on pane needs macOS 26 | Stated in Fleet's guidance. No Fleet-side version check found on the inspected paths |
| The Windows ESP runs only when enrollment is automatic **and** the device is in OOBE; Settings enrollment is also automatic | The enrollment handler's two conditions |
| Outside OOBE, Windows takes the Linux path, so **stop-on-failure is inert there** | The cancellation cascade returns unless Windows is in a tracked ESP state |
| Pending profiles and in-flight software hold the status page; a software failure or the timeout decide the outcome | **Corrected in review**: the draft said only two things affect the hold, which confused holding with deciding |
| With stop-on-failure on, remaining work is cancelled and only reset is offered; with it off, Fleet waits and shows a continuable page with Continue anyway | The hard and soft outcomes and their copy |
| Fleet's Windows ESP timeout is **three hours**, Fleet's own choice inside Microsoft's 60 to 1440 minute range, not configurable | The constant, sent to the CSP and enforced independently |
| Fleet briefly requests a timeout below Microsoft's documented minimum to raise the failure page, admitted in code as undefined behaviour that works on tested builds | The rationale, tested builds and rejected alternative are all recorded in source |
| Linux holds nothing; closing the browser does not stop the work; no overall timeout | The agent continues regardless of the page |
| The policy gate precedence, and a **30 minute** fail-open anchored to enrollment time | The gate resolves scope after label freshness, then applies the timeout strictly greater than |
| A skipped item is recorded as **successful**; there is no skipped state | Terminal success is written with no install request |
| iOS and iPadOS setup carries profiles and App Store apps only, blocking on ADE and background otherwise, bounded by the worker unless manual release is on | **Manual-release qualification added in review** |
| Android setup is the one Android path that pushes rather than offers, with no ordering guarantee and no Fleet interface | Setup marks apps for installation in the device policy |
| Self-service entitlement is the self-service flag plus label scope; categories are navigation only, seven seeded, per fleet | The catalog query and the category table |
| Android apps are always self-service and cannot be made otherwise | Forced in four places |
| Fleet does not create the iOS web clip; the administrator deploys it | No code creates one; documentation describes the manual workflow |
| Install all is non-transactional, name-ordered, always accepted, and writes **no activity** when nothing queues | Only server logs record that case |
| The end user sees the same unredacted install output an administrator sees | Same datastore call and same output handling |
| Fleet exposes no administrator route or host-details checklist mirroring the user's setup screen | Stated as what the inspected routes and interface contain, not as a universal |
| Re-enrollment deletes previous setup results and rebuilds the contract; only retry-failed-steps preserves successes | The enqueue path deletes prior rows |
| A previously Orbit-enrolled Windows or Linux host re-enrolls without authentication, so delete it first if you need the step enforced | The third bypass, with Fleet's own guidance to delete |

## Derived

**"Setup can hold a person at a progress screen; self-service never holds anyone anywhere."** The
chapter's organising contrast. Editorial.

**"An item in setup is an item that can fail somebody's first login."** Follows from the blocking
behaviour; the advice to keep the mandatory list short is mine.

**"Two label mechanisms sit inside this one feature and behave oppositely, which is the trap."** Both
mechanisms are source-checked; naming it a trap is editorial.

**Test each enrollment path, not each operating system.** The strongest advice in the chapter, and it
is inference from the verified fact that paths differ in blocking behaviour within one platform.

**"A setup you have never seen fail is a setup whose failure behaviour you are guessing at."**
Editorial.

## Unverified, and phrased as such in the text

**No Fleet-side macOS version check for the PSSO pane.** Written as what the inspected upload and
assignment paths contain, not as a universal claim.

**No administrator setup checklist.** Written as what Fleet exposes rather than as proof none exists,
after review flagged the stronger phrasing.

**The Windows CSP behaviour on future builds.** Written as a possibility with its observable symptom,
since it depends on Windows rather than Fleet.

## Rejected, and why

**My own "no Platform SSO in setup" note.** A false negative from grepping only the setup-experience
source files. PSSO is supported and arrives through another lane. The lesson is recorded because the
failure mode generalises: zero hits is evidence about where you looked.

**"Terminal is not successful" as a heading.** Accurate about consequence, wrong about vocabulary,
because `Verifying` is not terminal in the profile lifecycle and can move backwards. Renamed to
"Release-ready is not successful", which is what the release check actually tests.

**"Labels do not filter setup software on macOS."** True but far too narrow: it holds on every
platform and for store apps too.

**"Only two things affect the Windows hold."** Confused what holds the page with what decides how it
ends. Pending profiles and in-flight software hold it; failure and timeout decide the outcome.

**The device-uninstall authorization detail.** Recorded as S2 in the private defect queue and
deliberately excluded. The manual repo is public, the behaviour is unfixed at this release, and the
passage would function as a method for removing an organisation's endpoint agent. The reviewer was
asked to argue against the omission and agreed with it, noting that omitting it does not mislead
anyone choosing what to publish, because the affected path never consults the self-service flag. The
chapter carries the reviewer's entitlement wording instead, which names a real control rather than
gesturing at a threat.

## Fleet defects found while writing this chapter

Recorded in `~/Vault/Claude Cowork/Work/General/Fleet bugs to file.md` as D21, D22, D23 and S2. Not
filed, per the standing instruction. D21 is four errors in one architecture document; D22 is the
unbounded macOS release path; D23 is a comment that misdescribes `AccountConfiguration` and caused a
research error in this very chapter; S2 is the uninstall authorization gap.
