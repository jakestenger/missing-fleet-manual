# 2.5 Identity providers, SSO, SCIM, and role sync — research notes

Renumbered from 2.2 on 2026-08-30 (P6 structural moves); same chapter, new number.

Target file: `manual/02-administer-and-deploy-fleet/2.5-identity-providers-sso-scim-and-role-sync.md`
Verified against: **tag `fleet-v4.90.1`**.
Sources: `docs/Deploy/single-sign-on-sso.md` (298 lines),
`articles/foreign-vitals-map-idp-users-to-hosts.md`.
Written 2026-08-23.

## The finding that shapes the chapter

**SCIM does two unrelated jobs in Fleet, and the stub conflated them.**

1. **Administrator deprovisioning** (`single-sign-on-sso.md`, "Automatically deprovision
   Fleet users"): when SCIM is configured, Fleet deletes a user's *Fleet account* when the
   user is deleted or deactivated in the IdP.
2. **Foreign host vitals** (`foreign-vitals-map-idp-users-to-hosts.md`): maps an *end user's*
   IdP username, groups, and department onto their *hosts*, for use as configuration-profile
   variables and label criteria. Premium.

Same protocol, same endpoint, entirely different audience and outcome. Job 1 is about who
can administer Fleet; job 2 is about what Fleet knows about a device's owner. Writing them
as one feature is the mistake the chapter has to avoid.

## Verified: SSO

- Standard is **SAML**. Documented IdPs: Okta, authentik, Google Workspace, Entra ID, plus
  any SAML IdP.
- Supports **both IdP-initiated and SP-initiated** login, and JIT provisioning.
- UI path: **Settings > Integrations > Single sign-on (SSO)**, which has two tabs:
  **Fleet users** (administrator sign-in) and **End users** (IdP authentication during
  setup experience). Distinct purposes, same page.
- Fields: **Identity provider name** (rendered on the login page), **Entity ID** (a URI,
  e.g. `fleet`, must match the IdP), **Metadata URL**, or **Metadata** (paste XML when the
  IdP offers no URL).
- ACS / callback URL: `https://<fleet_url>/api/v1/fleet/sso/callback`
- For Require IdP authentication (setup experience) the callback differs:
  `https://<fleet_url>/api/v1/fleet/mdm/sso/callback`
- Name ID format: **email address**.

## Verified: JIT provisioning

- **Fleet Premium only.**
- Enabled by **Create user and sync permissions on login**
  (`sso_settings.enable_jit_provisioning`).
- Creates the account on first login; email and full name copied from the SSO response.
- **Default role is Global Observer.**
- Fleet **re-evaluates roles on every login**, not only at creation.
- Requires the IdP to send email as Name ID, and full name as an attribute named one of:
  `name`, `displayname`, `cn`, `urn:oid:2.5.4.3`,
  `http://schemas.xmlsoap.org/ws/2005/05/identity/claims/name`.
  Falls back to the email address if none is present.

## Verified: role sync (SAML custom attributes)

- Requires `sso_settings.enable_jit_provisioning: true`.
- Attributes: `FLEET_JIT_USER_ROLE_GLOBAL`, `FLEET_JIT_USER_ROLE_FLEET_<FLEET_ID>`.
- Accepted values: `admin`, `maintainer`, `observer`, `observer_plus`, `technician`, `null`.
- **Setting both a global and a fleet role causes an error at login.** A user is global or
  fleet-scoped, never both. This is the best edge case in the chapter.
- `null`, empty, whitespace-only, or an omitted `<saml:AttributeValue>` are all ignored.
- Multi-valued attributes: **Fleet uses the last value.**
- On login: account absent and attributes present, attributes win; account absent and
  attributes absent, Global Observer; account exists and attributes present, roles updated;
  account exists and attributes absent, **no role change attempted**.

## Verified: SCIM

- Base URL: `https://<fleet_url>/api/v1/fleet/scim`. Endpoints seen: `/scim`, `/scim/`,
  `/scim/details`, `/scim/users`.
- Auth: HTTP Header, using an **API-only user with admin permissions** and access to the
  `/scim/*` endpoints.
- Unique identifier field: `userName`.
- **Required attributes: `userName`, `givenName`, `familyName`.** `department` supported
  and optional.
- **Any other attribute causes Fleet to reject the payload.** Explicit in the guide: "If you
  use attributes other than the supported attributes above, the payload will be rejected."
  Notable and easy to hit, since IdPs map extra attributes by default.
- Fleet UI: **Settings > Integrations > Identity provider (IdP)**.
- Groups arrive via the IdP's push-groups mechanism.

### One discrepancy between the two Fleet docs, unresolved
`single-sign-on-sso.md` says Fleet requires `userName`, **`email`**, `givenName`,
`familyName` for deprovisioning. `foreign-vitals-map-idp-users-to-hosts.md` lists required
as `userName`, `givenName`, `familyName` and does not mention `email`.

Not resolved at this tag. The chapter names `userName`, `givenName`, `familyName` as
required (both docs agree) and notes email is mapped in practice. **Candidate Fleet
documentation bug**; log it with the others in PROJECT_STATUS.

## Verified: 2FA

- **Email 2FA exists for break-glass accounts.** Documented purpose is an account that can
  sign in when the IdP is unavailable.
- Fleet's stated best practice: SSO for everyone else, and enforce 2FA at the IdP.
- **You cannot change the authentication method of the account you are signed in as.**
  Another admin must do it, from Settings > Users.

## Where IdP vitals get collected
Automatically when an end user authenticates during enrollment: automatic enrollment for
Apple (macOS, iOS, iPadOS) and Windows; manual enrollment for Apple, Android, Windows, and
Linux. A host's IdP username can also be set by hand on the Host details page, and Fleet
maps the remaining vitals from it.

---

## 2026-08-25 merge pass: verification of new claims

Reviewed against a Codex-authored draft. Claims added to the chapter during the merge, all
verified at `fleet-v4.90.1` in `server/fleet/sessions.go`:

| Claim | Source | Confidence |
|---|---|---|
| `FLEET_JIT_USER_ROLE_TEAM_<ID>` is still accepted alongside `FLEET_JIT_USER_ROLE_FLEET_<ID>` | `sessions.go:114-116` (both prefix constants), `:145-150` (both matched, either prefix trimmed) | High |
| Role values match exactly, with no trimming, so `" admin "` is rejected as an invalid role | `sessions.go:181-203` (`parseRole`), and the function comment states the rule explicitly | High |
| A whitespace-only or empty value is coerced to `null` and ignored, unlike a padded value | `sessions.go:191-193` (`strings.TrimSpace(value) == ""` returns the null sentinel) | High |
| Two attributes naming the same fleet are rejected as a duplicate entry | `sessions.go:96-104` (`verify`, `duplicate fleet entry`) | High |
| Both a global and a fleet role in one response is rejected | `sessions.go:92-95` (`cannot set both global and fleet roles`) | High. Already in the chapter; re-confirmed |
| Where an attribute carries multiple values, the last is used | `sessions.go:189-190` (comment and indexing) | High. Already in the chapter |

**Correction made during the merge.** The Codex draft advised against relying on a manual role
change as a durable exception for a role-synced account. That is true only when the identity
provider sends a role attribute for that person. When no attribute arrives, existing roles are
left alone, so a manual change persists. The chapter now states the condition rather than the
flat warning.

**Not adopted from the draft**, with reasons: the vocabulary table was kept, because a reader
new to Fleet needs Fleet's meaning of four generically familiar terms; "SCIM does two different
jobs" was kept over "supports two independent outcomes" as the plainer phrasing; and the draft's
"For many organizations, the practical rollout sequence is ..." was not carried over, being the
same unsourced generalization it was written to replace (STYLE §25).

Diagnostics the draft dropped and the merge retained: the email-shaped-account-name symptom of a
missing name attribute, the reason `null` exists, the constraint behind the second-administrator
step for the recovery account, and that trimming the SCIM attribute mapping is required rather
than tidying.

## Fix-loop round (2026-09-03, round5)

- **m12 — API-only SCIM token described as expiring.** The SCIM-endpoint ownership note said
  the credential had "no human attached to notice when it expires," but API-only sessions never
  expire: `server/service/sessions.go` sets `sessionDuration = 0 // make API-only tokens
  unlimited` and short-circuits the expiry check when duration is 0 (2.6 and 6.1 already state
  this). Fix: reworded to "a credential with real reach that never expires on its own, so only a
  rotation schedule you set will ever retire it," which is both accurate and the real reason it
  needs an owner and a rotation date. Verified fleet-v4.90.1.

---

## Addendum, Fleet 4.91.0: nested Entra groups in IdP vitals (IA14)

Drafted 2026-09-08 against `fleet-v4.91.0` (35fc1c024490). Four paragraphs added to `## Connect SCIM`,
after the existing "Groups arrive through your identity provider's push-groups mechanism" sentence.
Changelog line: "Added support for nested groups in Entra in IdP vitals."

### Stated: verified at the tag

| Claim | Basis |
|---|---|
| Entra provisions a nested group as a group-type member rather than flattening it | Migration `20260803182251_AddScimGroupGroup.go`, comment: "Microsoft Entra ID provisions nested groups by sending group-type members (e.g. a PATCH that adds "group-62" as a member of "group-61") rather than flattening them into user members" |
| Fleet parses a member value as either a bare numeric user id or a `group-<id>` child reference | `classifyMemberValue` usage in `ee/server/scim/groups.go:100-115`, splitting into `userIDs` and `childGroupIDs` |
| Parent-to-child edges are stored in their own table, created by that migration | `scim_group_group (parent_group_id, child_group_id)` with cascading foreign keys to `scim_groups` |
| A user's effective groups are their direct groups plus every ancestor reachable upward | `getScimUserGroups` in `server/datastore/mysql/scim.go:608-635`, a recursive CTE seeded from `scim_user_group` and walking `scim_group_group` to parents |
| Host-vitals labels expand the same way | The host-vitals label join at `server/fleet/hosts.go:521-543` replaces `scim_user_group` with a recursive derived table, with the comment: "Entra ID provisions nested groups as group-type members rather than flattening them, so without this expansion a host whose user only belongs to a child group would not match a label built on an ancestor group's display name" |
| The host's IdP groups vital is built from that group list | `server/fleet/hosts.go:1987`, `endUser.IdpGroups = append(endUser.IdpGroups, group.DisplayName)`, populated from the effective set; the field is `idp_groups` (`hosts.go:1180`) |
| A cycle terminates and duplicates are removed | `getScimUserGroups` comment: "UNION (not UNION ALL) dedupes and guarantees termination even if a cycle exists"; the same construction is used in `getTransitiveScimGroupUserIDs` and the label join |
| A membership change re-queues the profiles that use the group variable, for the transitively affected users | `triggerResendProfilesForIDPGroupChangeByUsers` called with `getTransitiveScimGroupUserIDs` on group creation (`scim.go:712-722`) and on a group rename (`scim.go:1109-1120`); a child-edge change is handled in the same reconcile path (`scim.go:1072-1125`) |
| No depth limit is imposed | Neither recursive CTE carries a depth predicate or a `cte_max_recursion_depth` override; termination comes from `UNION` deduplication |

### Derived (reasoning, not just citation)

| Conclusion | Reasoning |
|---|---|
| "Before that it did not" | The edge table is created by a migration dated 2026-08-03, after the 4.90.0 cut, and every expansion path depends on that table. With no table there is no edge to walk, so a child-only member could not have matched an ancestor. Stated as a version boundary, not re-verified by checking out 4.90 |
| "If you built flattened duplicate groups upstream to work around that, they are no longer doing anything for you" | Operational advice following from the behaviour change. Not a source claim, and written as a conditional about the reader's own directory |
| "Identity providers that flatten their groups before pushing them, which is the more common behaviour, are unaffected" | A flattened push produces direct `scim_user_group` rows only, which is the anchor of every recursive query, so the expansion adds nothing. The "more common behaviour" clause is a general statement about SCIM implementations rather than something read from Fleet's source, and is written as a parenthetical rather than as a fact about Fleet |
| "The group variable available to profiles and scripts" reads the effective set | Both read the same `IdpGroups` field. For scripts this was verified while drafting IA6 (5.3); for profiles it is the same resolver. Recorded here as a cross-feature consistency check rather than a fresh citation |

### Not edited / deferred
- The changelog says "in Entra", and the mechanism is generic SCIM group-type membership rather than
  anything Entra-specific in Fleet. The prose names Entra as the provider that sends nested groups
  this way, which is what the source comments say, without claiming Fleet special-cases it.
- 1.3's host-vitals label coverage was checked for a claim this contradicts. It describes selecting on
  vitals generally and makes no statement about direct-versus-inherited group membership, so no edit.
- 5.3's `$FLEET_VAR_HOST_END_USER_IDP_GROUPS` row needs no change: it says "the end user's groups",
  which is now the effective set and was the direct set before, and the chapter does not distinguish.
