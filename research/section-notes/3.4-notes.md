# 3.4 citation ledger: enroll Linux devices

Chapter: `manual/03-connect-devices/3.4-enroll-linux-devices.md`
Verified against: **Fleet 4.90.1**, `git tag fleet-v4.90.1`, commit `dd0200f062`, in `~/Source/Fleet/fleet-public`
Verified on: 2026-08-27
Independent review: **not yet run.** Status is `drafting` and must stay there until it has been.

Written from the outline stub on 2026-08-27. `claims.py` was run on `LUKS`, `pkg.tar.zst`, `Linux`, `TPM`, `escrow`, `systemd`, `Restart=always`, `orbit.service` and `secret.txt` before and after drafting.

## Ownership

| Owned by | What |
|---|---|
| 1.2, 2.6 | That Linux has no MDM, and what that costs |
| 3.1 | Enroll secrets, TPM-backed host identity, fleet placement, retirement |
| 8.2, 8.4 | Log surfaces and host-side diagnosis |

3.4 owns **the package choice, where fleetd's configuration lives on this platform, the escrow preconditions, and verification.** It is the shortest of the four enrollment chapters because there is one channel.

## Stated

| Claim | Source |
|---|---|
| Three Linux package types, all accepting `arm64` | `cmd/fleetctl/fleetctl/package.go` |
| The Linux packages write `/usr/lib/systemd/system/orbit.service` and `/etc/default/orbit` | `writeSystemdUnit` and `writeEnvFile` in `orbit/pkg/packaging/linux_shared.go` |
| **Narrowed 2026-08-27.** Orbit's own **startup options** are environment variables and the unit's `ExecStart` carries no arguments. Certificates, `osquery.flags` and the updater's state are files under `/opt/orbit` and are not reachable from the env file | The unit template: `ExecStart=/opt/orbit/bin/orbit/orbit` with `EnvironmentFile=/etc/default/orbit`; `writeOsqueryFlagfile` and the certificate writers in `linux_shared.go`. The original row said "every setting" |
| **`secret.txt` is not written on Linux** | `writeSecret` is called only from `writeMacOSSecret` and from `windows.go`. The Linux env template sets `ORBIT_ENROLL_SECRET` instead |
| The env file is mode `0600` | `constant.DefaultFileMode = 0o600`, used by `writeEnvFile` |
| `Restart=always`, `RestartSec=1` | The systemd unit template in `orbit/pkg/packaging/linux_shared.go`. 8.4 records the same plus `CPUQuota=20%`, which is consistency and not the citation |
| The post-install reloads systemd, enables and starts the unit, all guarded on `systemctl` existing | `postInstallTemplate` in `linux_shared.go` |
| An in-band upgrade delays the restart 60 seconds via `systemd-run`, and skips the restart entirely when `systemd-run` is absent | `postInstallSafeRestart`, keyed on `INSTALLER_PATH` being set. The fallback branch prints and exits |
| **Incomplete as written, corrected 2026-08-27.** Those three gate the *notification*. The escrow request is additionally gated on Premium, a configured server private key, disk encryption enabled for the host's scope, fleetd **1.36.0** or later, and Fleet having observed the disk as encrypted | `server/service/orbit.go` for the notification; `validateReadyForLinuxEscrow` in `ee/server/service/devices.go` and `MinOrbitLUKSVersion` in `server/fleet/service.go` for the rest; `server/service/appconfig.go` refuses to enable disk encryption without a private key |
| The supported list is Ubuntu, Zorin, Fedora, Arch, Arch ARM, Manjaro, Manjaro ARM, CachyOS | `IsLUKSSupported` in `server/fleet/hosts.go`. Fedora is matched on the OS version string with a source comment saying its platform reports as `rhel` |
| **Corrected 2026-08-27.** There are **two** escrow paths. snapd-managed TPM-backed FDE escrows a **recovery key silently**, needing no dialog and no person; everything else takes the passphrase path, which prompts through `zenity` or `kdialog`, validates against any key slot, and adds a **freshly generated** Fleet passphrase in a new slot | `Run` in `orbit/pkg/luks/luks_linux.go` detects snapd first and treats a detection error as fatal; `getEscrowKey` calls `generateRandomPassphrase`. The original row described one path and implied Fleet copies the user's key |
| TPM host identity is Linux-only, Premium, TPM 2.0, kernel 4.12+ | **Not independently sourced in this pass.** 3.1 owns the mechanism and its citations; this chapter restates its conclusion and links out, so the row is carried rather than stated. Check 3.1's ledger before relying on it here |

## Derived

- **"One channel and no second one to wait for"** as the chapter's frame follows from Linux having no MDM. It is the deliberate inverse of 3.2's and 3.3's two-channel openings.
- **"A headless server can never escrow a key"** follows from the prompt requiring Fleet Desktop and a person, plus 1.2's statement that Desktop does not run without a desktop session. Fleet does not say it in those words.
- **"A configuration-management tool is a good owner of fleetd's configuration on Linux"** follows from the configuration being a plain file the agent re-reads on restart. It is judgement, and it is consistent with 1.1's position on not displacing existing tooling.
- **The half-applied fleetd upgrade** being explained by the missing `systemd-run` is an inference from the fallback branch, not a documented failure mode.

## Errors this chapter found in other chapters

Both were found by `claims.py` and by reading the packager, not by review:

1. **8.2 and 8.4 both described `secret.txt` as the place the enroll secret lands, without platform qualification.** It does not exist on Linux. Both corrected to name `/etc/default/orbit` for that platform, and 8.2's row now says its absence on Linux means nothing at all.
2. Recorded in 8.2's own ledger: the `/var/lib/orbit` symlink claim, which is also a Linux packaging fact.

## Not claimed

- **Which distributions Fleet supports for fleetd generally.** The package types imply families; a supported-version matrix was not established at the tag and is not asserted.
- **What happens on a non-systemd distribution beyond the files being installed.** Stated as the maintainer scripts not starting anything, which is what the source shows, without speculating about whether the agent is usable.
- **Whether `--fleet-desktop` has runtime library dependencies on Linux.** Not traced.

## Outstanding

1. **No independent review.** Required before `verified`.
2. The escrow key-slot mechanics are worth a fuller treatment somewhere; this chapter states only what a reader needs at enrollment time.

## Corrected before review, 2026-08-27: the escrow prerequisites were wrong

Found by re-reading `orbit/pkg/luks/luks_linux.go` in the pattern 3.2's and 3.3's reviews had established: check the chapter's organising claim against Fleet's source rather than against what the manual already believed.

**Escrow is Orbit's work, not Fleet Desktop's.** The runner is registered directly on the Orbit client, so `--fleet-desktop` is not a prerequisite. What the interactive path needs is a **desktop dialog tool on the host**, `zenity` or `kdialog`, and `cryptsetup`. The first draft said a package built without `--fleet-desktop` could never escrow, which is the obvious guess and is wrong.

**And there are two escrow paths, not one.** A host using snapd-managed TPM-backed full-disk encryption, as on recent Ubuntu, takes a **silent recovery-key path** over the snapd socket with no dialog and no person involved. Orbit detects that first and only falls through to the passphrase path otherwise, with a source comment explaining why the detection error is fatal: a metadata read failure could mean the host is snapd-managed but unconfirmable, and prompting for a passphrase the user does not have would be worse than failing.

So the first draft's "a headless server can never escrow a key, no matter how it was packaged" was wrong in both directions. On the snapd path a headless host escrows silently; on the passphrase path Orbit fails with "No supported dialog tool found" rather than hanging, which is at least legible.

**1.2 carried the same error and is corrected with it.** It attributed the Linux passphrase prompt to Fleet Desktop and said escrow "cannot happen at all" without it. Both halves are wrong, and 1.2 is a Part I chapter that has already been reviewed once, which is a reminder that a reviewed chapter is not a verified one.

Also tightened: `--enable-scripts` is genuinely permanent on Linux, unlike macOS where Fleet's own MDM profile turns scripts on regardless ([3.2](3.2-enroll-macos-devices.md)). Worth stating as a contrast now that the macOS behaviour is documented.

## First independent review, 2026-08-27: NOT READY. Applied, plus a Fleet code defect found while applying.

Seven factual defects. Two of them had already been caught by re-reading the source before the review arrived; the review found five more, and one of its findings led to a defect in Fleet itself.

**Fleet can lock and wipe Linux hosts, and the chapter said it could not.** Fleet Premium runs both as **scripts** rather than as MDM commands, and the source says so in a comment: Windows and macOS require MDM enrollment for a wipe, Linux uses scripts. The chapter had listed lock and wipe among the things Linux does not get, which is the natural inference from "no MDM" and is wrong. It also makes `--enable-scripts` matter far more than the chapter implied, since a host with scripts disabled cannot be locked or wiped. **1.2 carried the same error twice**, in its capability table and in its Orbit-against-MDM split, and both are corrected: the split is about mechanism, not capability.

**`--enable-scripts` is not permanent on Linux.** Orbit reads `ORBIT_ENABLE_SCRIPTS`, which the Linux packager writes into `/etc/default/orbit`, so the setting can be changed with an edit and a restart using exactly the mechanism the chapter recommends for everything else. The first draft said a package built without it produced hosts you could never run a script against, **and then added a contrast with macOS asserting it**, which is the same error made twice in one paragraph.

**Escrow is not an enrollment-time decision, and the chapter listed two of its gates.** Being encrypted at provisioning time is genuinely an image decision; everything else is configuration that can be changed afterwards and any of it can leave you with no key: Premium, a configured server private key without which Fleet refuses to enable disk encryption at all, enablement for the host's scope, fleetd **1.36.0** or later, and Fleet having observed the disk as encrypted. The section is now split into what the image decides and what the configuration decides, because building the right image and still receiving nothing is the confusing outcome.

**What Fleet escrows is a key of its own.** On the passphrase path it validates the user's existing passphrase, **generates a fresh random one**, and adds that in a new key slot; the snapd path obtains a dedicated recovery key. The chapter said Fleet "takes a copy of the key", which misdescribes both what is stored and what revoking it involves.

**Encryption status and escrow are separate.** Fleet learns whether the root disk is encrypted from an ordinary osquery detail query, with no escrow, no dialog tool and no Premium licence. The chapter had told a reader who wanted *reporting* to arrange the whole escrow apparatus.

**"One channel" was the wrong model.** Linux has no MDM channel, but it still has the osquery distributed channel, the osquery logging channel and Orbit's separately authenticated API, which is what 1.2 says and what the routes show. The verification section had followed the framing into checking one thing: `systemctl status orbit` proves a supervisor is alive and nothing about live reports, ingestion, scripts or software. Now three checks, one per channel.

**Also added, from the review's omissions list:** `--fleet-certificate` for private-CA deployments, concrete install commands per package type, and a removal procedure, which 3.1 explicitly delegates here.

### A Fleet code defect, found while writing the removal procedure

Verifying the uninstall behaviour rather than citing 8.4 turned up an inverted branch in the Linux pre-removal script:

```sh
case "${1:-}" in
  1|remove|deconfigure)  systemctl disable --now orbit.service || true ;;
  0|upgrade)             ;;
esac
```

The comment directly above it states RPM's convention correctly: preun is called with `0` on uninstall and `1` on upgrade. The branches are the wrong way round for that convention. Debian is unaffected, because its prerm receives words (`remove`, `upgrade`, `deconfigure`) and never numerals, and Arch is unaffected because its script disables unconditionally.

So on an RPM host, an **uninstall** leaves `orbit.service` enabled and running while the post-removal script deletes `/opt/orbit` out from under it, and an **upgrade** stops the service and relies on the post-install script to bring it back. The chapter documents the observable behaviour and tells the reader to check `systemctl is-enabled orbit` after removing, rather than describing the intent. **Not filed**, per the standing instruction; recorded in `PROJECT_STATUS.md` as C5.

## Confirming review, 2026-08-27: NOT READY, applied

The three big first-review corrections all confirmed: Linux lock and wipe exist as scripts, escrow is Orbit's work rather than Fleet Desktop's, and `/etc/default/orbit` holds Orbit's startup options rather than "every setting". What remained is the session's recurring habit in six more places, plus one genuinely new fact.

**The new fact matters and the chapter had it wrong. `fleetd 1.36.0` is the passphrase path's floor, not the platform's.** Snapd recovery-key escrow arrives in **fleetd 1.58.0**, per orbit's own changelog. So a host on snapd-managed encryption running anything between the two passes Fleet's server-side version check and then takes no escrow path at all, which is the worst combination: eligible, silent, and no key. The chapter now names both.

**The escrow section over-fixed in the other direction.** Having been corrected from "decided at enrollment" it went to "settled when the image is built", which is also wrong: Fleet tests the disk's **currently reported** state, so a host encrypted after enrollment becomes eligible as soon as osquery reports it. And `cryptsetup` and the dialog tools were grouped as image decisions when both are package installs. The table now asks "fixable later?" instead of asserting when the decision happens, and only the unencrypted root disk answers no.

**Withdrawing Fleet's access is not one operation.** The chapter said it means removing a key slot. That is the passphrase path; on the snapd path there is **no removal at all**, because neither snapd's API nor its CLI exposes recovery-key deletion, and a key is retired by rotating it. A source comment says exactly this, and adds that it is why a failed escrow self-heals on the next attempt.

**Three exhaustive claims introduced by the last pass.** `*(nothing else)*` in the capability table; "every other platform gets lock and wipe from MDM", which is false for ChromeOS and anything else outside the enumerated switch; and "the macOS and Windows packagers write `secret.txt`", which neither always does.

**"The agent log says which happened"** was wrong about where the evidence is. The maintainer script's output is captured as software-install output rather than written to Orbit's log, so the chapter now points at `systemctl is-active orbit` on an affected host, which is faster anyway.

**The verification procedure did not test what the chapter's opening claims.** The opening says the osquery distributed, osquery logging and Orbit channels are independent and can fail separately; the procedure tested two of the three. There are four steps now, and step 3 is a scheduled report, because a host that answers live reports and sends no scheduled results has a working distributed channel and a broken logging one. That is the case the chapter existed to make and did not check.

**And the MDM `Off` claim was wrong about the database.** A Linux host has no `host_mdm` row, so `enrollment_status` is **NULL** rather than `Off`; `Off` is what the Go helper returns when a record exists and the host is not enrolled. Whatever the console renders is a display choice.
