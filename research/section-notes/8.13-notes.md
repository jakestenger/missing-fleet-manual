# 8.13 What to collect before escalating: citation ledger

Verified against `fleet-v4.90.1` (commit `dd0200f062`) in `~/Source/Fleet/fleet-public` on 2026-08-26.

The chapter's worked example, presented as the model of a well-written ticket, carried three errors. It is the passage most likely to be copied verbatim during an incident, which makes it the worst place in the chapter for them.

## Corrections made in this pass

| Claim as drafted | Corrected to | Source |
|---|---|---|
| The example named `nano_command_queue` as the per-device Apple command queue | `nano_enrollment_queue`. There is no `nano_command_queue` | `server/datastore/mysql/schema.sql` defines nine `nano_*` tables; that is not among them |
| The example cited `cron_stats` for `apple_mdm_dep_profile_assigner` as evidence about a stuck configuration profile | `mdm_apple_profile_manager`. The DEP assigner synchronises ABM devices and enrollment profiles; it has nothing to do with configuration profile reconciliation. Added `apple_mdm_apns_pusher` as the next thing to check for a queued command that is not being woken | `cmd/fleet/cron.go:1877-1889` (`newAppleMDMDEPProfileAssigner`, logged as `nanodep-syncer`), `:1940-1948` (`newAppleMDMProfileManagerSchedule`) |
| The example ruled out a failure class because "the same hosts answer live reports within seconds, so osquery distributed and Orbit API are both healthy" | A live report exercises the osquery distributed channel only. The Orbit channel is separate and was not tested. 8.7 §8.7.3 already said the live report "rides the osquery distributed channel and nothing else" | `server/service/handler.go` registers the osquery distributed and Orbit config endpoints separately |
| "Exact Fleet version now: `fleetctl --version`" | `fleetctl --version` reports the version of the local `fleetctl` binary and never contacts the server. Use `GET /api/v1/fleet/version` or the UI. The client version is now a separate, conditional field | `server/service/handler.go:316` registers the version endpoint on the authenticated-user router; `fleetctl --version` is set from the client build |
| "Six lines" above a seven-row table | Removed the count. Two rows added since (fleetd version, operational impact), so a number would have been wrong again | STYLE §26 |
| "`Failed profile: ...` is normal ... The archive is still usable" | The collector takes thirteen items and continues past any that fail, so a partial archive is indistinguishable from a complete one. `Failed <item>` means that member is absent and the omission has to be recorded in the ticket | `cmd/fleetctl/fleetctl/debug.go:285-299` lists the thirteen items; `:344-351` prints `Failed %s` and `continue`s |
| "`fleetctl debug archive` needs a DB user with global `PROCESS` and `SELECT`. Without them ... the three `db-*` sections are missing" | Two of the three. `db-locks` and `db-innodb-status` return an explicit privilege message. `db-process-list` runs `SHOW PROCESSLIST`, which succeeds without the privilege and returns only the caller's own threads, so it arrives looking like an idle server rather than like an error | `server/datastore/mysql/locks.go:129-137` and `server/datastore/mysql/mysql.go:1204-1208` carry the privilege diagnostics; `:1217-1225` is the unguarded `SHOW PROCESSLIST` |
| No mention of who may call the debug endpoints | Global administrator only | `server/service/debug_handler.go:42` requires `*v.User.GlobalRole == fleet.RoleAdmin` |
| "Read `cmdline.prof` in full. It is short." | The archive stores the command line raw, NUL-delimited, because that member comes from the generic pprof path rather than from the joining helper `fleetctl debug cmdline` uses. A viewer that stops at the first NUL shows only the binary path, so the instruction produced a review that felt complete and had read one field. Added `tr '\0' '\n'` | `cmd/fleetctl/fleetctl/debug.go:288` puts `cmdline` in the default branch (`fleet.DebugPprof`); `:113-121` is `joinCmdline`, used only by the standalone command |
| `--fleet-certificate` "must carry the full chain: client, intermediate, and root" | It is a trust anchor, not a client certificate: the flag replaces the root CA set used to verify the server. Rewritten to say what the command actually tests and what a failure implies under TLS inspection | `cmd/fleetctl/fleetctl/debug.go:456-480` assigns the flag to `cc.RootCA`; `:527` verifies the server certificate against it |
| The archive section asked for `fleetctl debug errors` without noting the archive already contains one | The archive collects `errors.json`. A separate file is worth attaching when it captures an earlier window, or when the archive's copy failed | `cmd/fleetctl/fleetctl/debug.go:288,320-326` |
| "Escalate when either of these is true, **and not before**" | Reframed as a standard rather than a gate, with three named cases for escalating earlier: outage or suspected security incident, perishable evidence, and access that requires a change request | Editorial. An absolute gate in incident material is a way to make someone sit on an outage |
| "Does it persist after a wipe?" as an intake question | Rephrased to ask whether it has already been reproduced after a wipe, with an explicit instruction not to wipe a production host to answer it. 8.11 §8.11.6 treats re-enrollment as a last resort for the same reason | Internal contradiction with 8.11 |
| "Egress allowlisting" as a heading over a list the chapter itself calls incomplete | "Network destinations to collect" | Editorial. A heading that says allowlist invites the list to be used as one |
| "Premium features silently absent otherwise"; "MDM assets are unreadable without it" | Both marked in the text as not separately verified, and reframed as leads rather than diagnoses | Neither is established by the configuration reference the chapter cites |

## Stated

| Claim | Source |
|---|---|
| The two-version split is what Fleet's bug template asks for | `.github/ISSUE_TEMPLATE/bug-report.md` |
| Host identifiers belong on a private channel, not the public tracker | `docs/Contributing/product-groups/mdm/mdm-bug-checklist.md` item 3; `.github/ISSUE_TEMPLATE/csa-task.md` |
| The device and MDM intake list is adapted from Fleet's MDM bug checklist | same checklist |
| The archive prints a sensitive-data warning on completion | `cmd/fleetctl/fleetctl/debug.go:373-382` |

## Derived

- That a partial archive reads as a complete one to the recipient, so omissions must be stated. Follows from the collector's continue-on-error behaviour; Fleet does not say it.
- That a truncated `db-process-list` is more misleading than a missing one. The behaviour of `SHOW PROCESSLIST` without `PROCESS` is MySQL's, not Fleet's, and was **not tested here**.
- The three escalate-early cases. Editorial.
- That the Orbit channel needs its own observation. Follows from 8.7's verified scope claim.

## Unverified

- The approved private customer-support route and severity terminology for this release. Fleet's public issue chooser does not answer it, and the chapter does not claim to.
- The expected Fleet status for Android Google-account-based enrollment in the intake list.
- The claim that VPP failures "expire on their own schedule". No responsible status or schedule is named, and none was located this pass.
- Whether every Premium feature fails quietly without a license key. Now marked as unverified in the text.
- The failure surface of a missing or changed `server_private_key`. Now marked as unverified in the text.

## Open, not fixed here

The review's structural recommendation was to replace the flat intake inventory with a short first pass and conditional branches, and to lead with an audience-and-destination routing table. Not done, for the same reason as 8.12: it is a chapter-shape decision that should be taken across Part VIII rather than inside a correctness sweep.

## A check that was attempted and dropped

`nano_command_queue` was in prose, so `check-table-names.py` could not see it. Extending the checker to backticked snake_case identifiers in prose produced 37 false positives and 0 true positives across the manual, because Fleet spells config keys, MDM asset names, webhook names, activity types, and columns in the same shape. Similarity scoring does not separate them: the real error scores 0.774 against its nearest table while `vpp_token`, which is correct, scores 0.947. Reverted. Recorded in CONTRIBUTING.

## Re-reviewed 2026-08-27, at current text

A second full Sol review ran against this chapter after the 2026-08-26 tag sweep, asking directly whether the chapter is finished to the `verified` standard. Its verdict, the findings, and what was applied are recorded in `reviews/2026-08-27/README.md` and in `reviews/2026-08-27/8.13-sol.out`.

**The chapter is still `drafting`, and deliberately.** That verdict describes the text as it stood *before* this round's corrections were applied, so it cannot be used to justify a `verified` stamp. A confirming review has not run.

## Confirming review, 2026-08-27: NOT READY, now fully applied

Only one of the previous round's findings had actually been applied, and the confirming pass said so plainly: "the confirmation does not close cleanly ... the problem is incomplete application". This pass closed all five.

**Checklist items 3 and 4 were swapped, and the chapter claimed its numbering followed the source.** `mdm-bug-checklist.md` item 3 is serial numbers and other identifiers, item 4 is hardware model. The chapter had them the other way round while line 155 told the reader both sides could cite the same item number, which is the worst combination: wrong and asserted to be right. Swapped, and the note now also says which rows are this manual's additions with no counterpart in the source.

**iOS and iPadOS were missing three enrollment paths.** The table carried only account-driven user enrollment, but the tagged console offers iPhones and iPads a company-owned or personal choice on an enrollment URL (`IosIpadosPanel.tsx`, radio buttons defaulting to Personal, appending `byod=true`), and the DEP sync assigns them to their own fleets alongside macOS. Four rows now, marked as derived from the code and the console rather than quoted, because Fleet's checklist covers macOS, Windows and Android and stops there. A recipient who goes looking for them in the checklist will not find them, and the chapter says so.

**Google-account Android enrollment now refuses to guess.** The source names the method and supplies no status. The row previously read "unverified", which is honest but sits inside a column headed *MDM status Fleet should show* and so still reads as a prediction. The row now points at a paragraph that says to record what Fleet actually shows, and specifically not to reason from the two Android rows above it, which disagree with each other.

**Four overclaims, all of the same shape: a true observation carried one step further than the evidence goes.**

- The class-6 shortcut said UI-disagrees-with-host is "not a failure". 8.1 says the opposite once a refetch fails to clear it, at which point detail results are not being ingested and that is a real failure. Two chapters arguing.
- The VPP row said install failures "expire on their own schedule". Failures do not expire; the **token** does, on a clock independent of the APNs certificate and the ABM token, which is the fact worth having.
- "Premium-only features are not built" is wrong in a way that costs a support round trip. Fleet Free has **two** behaviours: explicit licence errors, which `fleetctl` returns for Premium MDM operations, and silent forcing-off, which the source states in those words for the low-disk-space filter (forced to nil) and software vulnerability detail (forced to false). The silent kind is the one that reads like a bug.
- **The worked example drew a conclusion from `cron_stats` that `cron_stats` cannot support.** It suggested `apple_mdm_apns_pusher` statistics would show whether particular hosts were woken. `SendPushesToPendingDevices` catches `APNSDeliveryError`, logs it at info level and **returns nil**, so a run in which Apple rejected the push for some of the batch records as a clean success. The row is green either way. Corrected in the example and then made explicit underneath, because a reader who has just been shown a model ticket will copy its reasoning.

**Where to file was one undifferentiated menu of thirteen templates**, most of which belong to Fleet's internal product process. It now opens with an audience routing table — support channel, bug report, reliability, ask-a-question, or whatever a Fleet employee named — and the template inventory carries a *Yours to open?* column marking eight of the thirteen as Fleet's. Blank issues being disabled is now stated as the reason you cannot escape the list rather than as a trailing fact.

**The universal header block was missing the fields Fleet's own template insists on**: actual behaviour, expected behaviour, reproduction steps, and reproduction status. Added, with the point that expected behaviour is the field people skip and the one the recipient cannot infer, and that the template's reproduction status is a two-way checkbox rather than a yes or no, so "describes the workflow but not reproduced on a second instance" is a complete answer that changes who picks the ticket up.

Also: "Gather every row" now defers to 8.13.1's urgent exceptions rather than contradicting them, and "Two notes" introduces three bullets no longer.

## Second confirming review, 2026-08-27: READY WITH MINOR CHANGES, applied

Every substantive finding from both prior rounds confirmed resolved. The four remaining items were **all introduced by the audience-routing table added in the previous pass**, which is the useful lesson: adding a section is as capable of contradicting the chapter as editing one.

**The routing table could send an outage to a public tracker.** §8.13.1 says to escalate early when production is down or a security incident is suspected, and the new table's second row sent a self-hosting reader straight to a public GitHub issue with no gate. There is now a first row routing anything urgent, suspected-security or customer-identifying to the organisation's own incident path, and the bug-report row says **scrubbed and reproducible** and points at §8.13.6 before submitting.

**"The only route that can take serials" contradicted §8.13.6**, which correctly names a `fleetdm/confidential` issue and private chat as alternatives. And "the only one with an obligation attached" is a claim about support contracts that nothing in the public checkout establishes. Both absolutes removed.

**"Everything else is Fleet's internal vocabulary" argued with the table underneath it**, which marks five of the thirteen templates as yours to open. The sentence now says the inventory mixes both, which is what the ownership column already showed.

**The checklist claim was too broad.** Fleet's checklist does discuss Apple hosts generally and gives an iPad example; it is the **enrollment-method item** specifically that covers macOS, Windows and Android and stops. Narrowed to that.

## Third confirming review, 2026-08-27: READY WITH MINOR CHANGES, applied

Every finding from all three rounds resolved. One item, and it is the same shape as the two the second round found: **a sentence introduced with the routing table contradicting the table it introduces.**

The Fleet-employee row said "the rest of the table below is theirs", while that table's ownership column marks five of the thirteen templates as yours to open. The row now points at the column instead of overriding it.

Three of this chapter's four rounds ended with a defect inside the section added by the previous round, which is worth stating as a pattern rather than three separate corrections: **new material gets less scrutiny than edited material, because there is no prior claim to compare it against.** The countermeasure is the same one used for edits, applied to additions: after adding a section, read every other passage that overlaps its subject and check they still agree.

## Fourth confirming round, 2026-08-27

READY WITH MINOR CHANGES, two items, both in the licence-key row of the escalation checklist, and both applied.

**Three licence-key states behave differently and the chapter collapsed them into one.** No key starts the server as Fleet Free. An **invalid** key stops the server starting at all, which is a candidate for a deployment that will not come up. An **expired** Premium key still loads as Premium: the parser ignores expiry when it is the only validation error and marks the licence expired rather than rejecting it, so an expired key is not the explanation for a missing Premium feature. That last one is the counter-intuitive case and the reason the row was worth fixing.

The row also called an accepted-then-inert setting "the Free-tier signature". It is a possible clue, not a diagnosis; plenty of other things present that way. Reworded, and the exhaustive "two different ways" taxonomy made non-exhaustive.

## Capped at the review limit, 2026-08-27

The owner's rule: rounds until a chapter comes back clean, or five rounds, whichever is first, then leave it at `drafting` and move on. This chapter has had **6 rounds**, all findings applied, and its last verdict was **READY WITH MINOR CHANGES**.

It stays at `drafting` rather than `verified`, for the reason that has blocked every stamp in this part: **no chapter's exact current text has been reviewed**, because each kept taking corrections after its own verdict, sometimes from a neighbour's review hours later. That is a statement about bookkeeping rather than about quality.

The net is the full-part review once every chapter in the part has an initial draft. Do not start another per-chapter round here before then.

