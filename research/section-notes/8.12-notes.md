# 8.12 Audit logs: citation ledger

Verified against `fleet-v4.90.1` (commit `dd0200f062`) in `~/Source/Fleet/fleet-public` on 2026-08-26.

Drafted against `main` at approximately 4.90.0 and never tag-verified. An independent review flagged a broken command, a two-state attribution model that should be three, and a retention query that would produce a false diagnosis. Re-verifying at the tag confirmed all three and turned up a fourth: the chapter treated Fleet's audit-log reference as complete, and it is not.

## Corrections made in this pass

| Claim as drafted | Corrected to | Source |
|---|---|---|
| `fleetctl get api '<path>'`, given twice as the primary retrieval command | `fleetctl api '<path>'`. There is no `get api` subcommand, so the chapter's first actionable step failed | `cmd/fleetctl/fleetctl/api.go:248` defines the top-level `api` command; the subcommand list in `cmd/fleetctl/fleetctl/get.go` contains no `api` entry. 8.11 already had this right, so the manual contradicted itself |
| `fleet_initiated` "separates people from automation", a two-state model | Three states. `fleet_initiated = 0` with no actor is an unattributed system or device path, not a person | `server/activity/internal/mysql/new_activity.go:49-52` sets the flag only for types implementing `AutomatableActivity` where `WasFromAutomation()` returns true. 26 of 191 types implement it. `ActivityTypeMDMUnenrolled` (`server/fleet/activities.go:429-440`) implements none, and Fleet emits it for both administrator and device-initiated unenrollment |
| "`fleet_initiated: true` and no actor is a policy automation" | An automation-flagged row can carry a live `user_id`, because some automated paths attribute work to the installer's uploader. Stored `user_name` becomes the literal `Fleet`; the API then replaces it with the current user's name, so the response names a person beside an automation flag | `server/activity/internal/mysql/new_activity.go:38-52` (the automation branch overwrites `userName` but not `userID`); `types.ActivityAutomationAuthor = "Fleet"` at `server/activity/internal/types/activity.go:22`; enrichment at `server/activity/internal/service/service.go:145-180` |
| "the row shows the name the user had at the time, not the name they have now" | True of the MySQL row, false of the API response. For any non-null `actor_id` the service overwrites name, email, gravatar, and api_only from the current `users` record | `server/activity/internal/service/service.go:145-180`. The chapter's own entry-shape table said "joined `users.name`" three sections later, so this was an internal contradiction |
| `actor_id` is "Null for `fleet_initiated` rows" | Null when no user was attached and when the acting user has been deleted. Not tied to the flag | `server/activity/internal/mysql/new_activity.go:38-46` sets `userID` only when `user.ID != 0 && !user.Deleted` |
| `jq` rendered the actor as `.actor_email // "fleet"` | `//` catches null and false, and `user_email` is NOT NULL default `''`, so a Fleet-initiated row rendered blank. Replaced with explicit three-state rendering | Schema default is in the chapter's own column table; behaviour of `//` is jq's |
| Expiry monitoring query counted all rows older than the window, and told the reader a static count means the schedule is broken | Cleanup takes only rows with no `activity_host_past` link. Host-linked activities are never expired. The query now `LEFT JOIN`s and filters `IS NULL` | `server/activity/internal/mysql/activity.go:198-215`, whose comment states it outright. 1.5 already said host-tied activities are kept until the host is deleted, so this contradicted the book |
| Streaming "backfills the whole table"; backlog is a count of `streamed = 0` | Streaming reuses the global feed query, which filters `host_only = false`. Host-only rows never stream and stay `streamed = 0` permanently, so the unfiltered count never falls | `server/activity/internal/mysql/activity.go:43-61` (`WHERE a.host_only = false`); `server/activity/internal/service/service.go:190-248` calls it |
| "treat five minutes as the floor" | Five minutes is the ceiling on the wait. 2.8 says "up to five minutes" in three places, so this contradicted the book as well | `cmd/fleet/cron.go:2104` sets `interval = 5 * time.Minute` |
| "The schedule takes the usual Redis lock" | The lock is a row in the MySQL `locks` table | `cmd/fleet/cron.go:2108` passes the datastore as both lock and stats store; `server/datastore/mysql/locks.go:15-57` |
| `filesystem_enable_log_rotation` "rotates at 500 MB or 28 days" | Size rotates the active file. `filesystem_max_age` (28 days) and `filesystem_max_backups` (3) govern retention of already-rotated files, and `SIGHUP` forces a rotation | `server/config/config.go:1665-1671`; `server/logging/filesystem.go:31-60` wires them to lumberjack, where `MaxSize` triggers and `MaxAge`/`MaxBackups` retain |
| Fleet "enumerates every field of every type in its audit-log reference" and that page is "the authoritative payload reference" | The reference covers 157 of the 191 activity types the server defines. The 34 omissions include `ran_automation_webhook`, `ran_custom_mdm_command`, `added_certificate`, `reset_policy`, and the recovery-lock and conditional-access families | Counted by matching `func (a T) ActivityName() string` bodies in `server/fleet/activities.go` against the text of `docs/Contributing/reference/audit-logs.md` at the tag |
| `preserve_host_activities_on_reenrollment` "controls whether a managed host keeps its existing activities and MDM commands when it re-enrolls" | Read only in the Apple MDM re-enrollment reset, and it guards two things: the `activity_host_past` rows and the nano enrollment queue. `host_mdm_commands` and nine other host reference tables are cleared either way | `server/service/apple_mdm.go:4609` is the only read; `server/datastore/mysql/apple_mdm.go:7884-7897` lists the unconditional tables, `:7949-7953` and `:7959-7970` show what the flag guards |
| Two unfounded prevalence claims: `fleet_initiated` rows "dominate on most deployments"; version and agent version "account for most" no-change failures | Replaced the first with a query that measures the split on the reader's own deployment. Replaced the second with the reason those two are worth checking at that point, which is that neither upgrade writes an activity row | Fleet publishes no figure for either. STYLE §25 |

## Stated

| Claim | Source |
|---|---|
| Streaming batch is 500 rows, cursor-paginated on `id`, one write per row | `server/activity/internal/service/service.go:190-248` |
| `activities_streaming` interval is 5 minutes | `cmd/fleet/cron.go:2104` |
| Expiry per-run delete cap is 5,000 rows | `cmd/fleet/cron.go:1509-1532` |
| Enabling expiry also enables live report cleanup on the same window | same |
| `preserve_host_activities_on_reenrollment` defaults to `true` where `users` already had rows at migration time, `false` otherwise | `server/datastore/mysql/migrations/tables/20260522195224_AddPreserveHostActivitiesOnReenrollmentToAppConfig.go:14-26` |
| `activity_expiry_settings` field names and types | `server/fleet/app.go:1440-1449` |
| Global feed excludes `host_only` rows | `server/activity/internal/mysql/activity.go:43-61` |
| Audit streaming is Premium | Fleet builds the audit logger under a Premium license; unchanged from the original pass |

## Derived

- That the API's enrichment plus the automation-author overwrite can jointly name a person on an automation row. Each half is verified in source; the interaction is the book's, and **has not been observed on a running instance**.
- That clearing `activity_host_past` makes previously-exempt activities eligible for expiry. Follows directly from the two verified queries, not stated by Fleet.
- The three-way attribution table as an incident heuristic.
- That a host-only event will never reach a SIEM. Follows from the streamer reusing the filtered query; Fleet does not warn about it anywhere found.

## Unverified

- The corrected `fleetctl api` and `curl | jq` commands are confirmed against the command surface and the response fields in source, but have **not been run against a 4.90.1 instance**.
- The compliance claim that Fleet does not sign or hash-chain activity rows remains a negative claim from an absence of evidence. Not re-verified this pass, and still stated in the text as an absence rather than a guarantee.
- The causal claims in the symptom-to-type table were not re-tested, in particular that editing a profile redelivers to every matching host.
- Authorization for the two activity endpoints is described from the policy layer's shape, not from a live permission test.

## Open, not fixed here

Sol's structural recommendation was to lead with the incident workflow and move the schema and type catalog behind it. Not done. It is a defensible reordering and a large one, and it belongs in a pass that treats Part VIII's chapter shape as a whole rather than in a correctness sweep. Recorded so it is not lost.

## Fleet documentation gaps found

1. `docs/Contributing/reference/audit-logs.md` omits 34 of 191 activity types at 4.90.1.
2. `preserve_host_activities_on_reenrollment` appears in no published configuration reference.

## Re-reviewed 2026-08-27, at current text

A second full Sol review ran against this chapter after the 2026-08-26 tag sweep, asking directly whether the chapter is finished to the `verified` standard. Its verdict, the findings, and what was applied are recorded in `reviews/2026-08-27/README.md` and in `reviews/2026-08-27/8.12-sol.out`.

**The chapter is still `drafting`, and deliberately.** That verdict describes the text as it stood *before* this round's corrections were applied, so it cannot be used to justify a `verified` stamp. A confirming review has not run.

## Capped at the review limit, 2026-08-27

The owner's rule: rounds until a chapter comes back clean, or five rounds, whichever is first, then leave it at `drafting` and move on. This chapter has had **3 rounds**, all findings applied, and its last verdict was **READY**.

It stays at `drafting` rather than `verified`, for the reason that has blocked every stamp in this part: **no chapter's exact current text has been reviewed**, because each kept taking corrections after its own verdict, sometimes from a neighbour's review hours later. That is a statement about bookkeeping rather than about quality.

The net is the full-part review once every chapter in the part has an initial draft. Do not start another per-chapter round here before then.


## 2026-09-04 fix: audit-log reference is not exhaustive (round8 m1)

8.12:117 ("Fleet's audit-log reference lists every one with its payload") contradicted 8.12:113
("not complete ... covers 157 of [191]"). Confirmed at fleet-v4.90.0: `server/fleet/activities.go`
defines 191 activity types (191 `ActivityName()` methods), matching the "157 of 191" figure.
Rewrote 117 to "documents many of them and their payloads ... not exhaustive, so ... use the
discovery query below."

---

## Addendum, Fleet 4.91.0: `mdm_enrolled` gains host linkage (IA19)

Drafted 2026-09-08 against `fleet-v4.91.0` (35fc1c024490). Three paragraphs added to `## 8.12.2 The
table`, after the `activity_host_past` sentence and before "The rename matters if you have saved SQL".
Changelog line: "Added `host_id` and `host_serial` to the `mdm_enrolled` activity for Apple (macOS,
iOS, iPadOS) enrollments, and the activity now appears on the host's activity timeline."

### Stated: verified at the tag

| Claim | Basis |
|---|---|
| `ActivityTypeMDMEnrolled` carries `host_id` and `host_serial` | `server/fleet/activities.go:447-461`: `HostID uint json:"host_id,omitempty"`, `HostSerial *string json:"host_serial"` |
| It declares its host linkage, which is what writes the join row | `func (a ActivityTypeMDMEnrolled) HostIDs() []uint` at `:470-475`, with the comment "HostIDs links this activity to the host on the host details timeline" |
| `host_id` is omitted when zero, and the linkage is skipped in that case | The `omitempty` tag, and `HostIDs` returning nil when `a.HostID == 0`, commented "Returns nil when the host is unknown (eg the enrollment is being processed before the host record exists) so the global activity is still recorded but no activity_host_past row is inserted" |
| Apple enrollments always set it; Windows Entra automatic enrollments do not | The `HostID` field comment at `:448-452`: "It is always set for Apple enrollments and for Windows enrollments where the host is known at enrollment time; Windows Azure automatic enrollments are linked to their host later (via the serial reported on the first management session), so their enrollment activity has no host_id (see #47874)" |
| Host-linked rows are exempt from expiry | Already established in this chapter at 4.90 (its "Expiry does not delete host-linked rows, at any age" paragraph, sourced from the cleanup query's `activity_host_past` join). Re-read, unchanged at this tag |

### Derived (reasoning, not just citation)

| Conclusion | Reasoning |
|---|---|
| "The event that begins a host's life in Fleet was in the global feed and absent from the host's own timeline" | The timeline is built from `activity_host_past`; without a `HostIDs` implementation no row is written, so the activity could only appear globally. Stated as a version boundary from the changelog rather than re-verified at 4.90 |
| "A missing enrollment entry on a Windows host's timeline is expected rather than a fault" | The operational reading of the stated Windows carve-out. This is the sentence that makes the feature worth documenting, because the natural inference from "enrollment now appears on the timeline" is that it always does |
| "Linking enrollment to the host moves it from the expiring population into the permanent one" | Combines the new linkage with the chapter's existing, separately verified expiry rule. Neither source states the interaction; it follows from both, and it is the kind of second-order consequence a retention owner needs |

### Not edited / deferred
- The 8.12.4 category table's MDM lifecycle row already lists `mdm_enrolled`. No change: the row names
  types, and the linkage is a property of the type rather than a new name.
- The activity-type count sentence in 8.12.3 is stale at 4.91 (191 -> 196). It is corrected in the SE2
  commit, which is the one that adds a type to the catalog, rather than twice.

---

## Addendum, Fleet 4.91.0: the `user_mfa_requested` activity (SE2)

Drafted 2026-09-08 against `fleet-v4.91.0` (35fc1c024490). Three paragraphs added to
`## 8.12.4 Activity types: the categories`, the type added to the Authentication category row, and the
activity-type count sentence in 8.12.3 corrected for the release. Matching corrections in 1.5 and 2.6.
Changelog line: "Added a `user_mfa_requested` activity, recorded when valid credentials are submitted
for an MFA-enabled account and a verification email is sent."

### Stated: verified at the tag

| Claim | Basis |
|---|---|
| The type string is `user_mfa_requested` and it carries `email` and `public_ip` | `ActivityTypeUserMFARequested` in `server/fleet/activities.go:332-339` |
| It is written after the password validates, at the point the verification email is sent | `server/service/sessions.go:216-238`: reached only after `user.ValidatePassword(password)` succeeds and `user.MFAEnabled` is true, immediately following `svc.makeMFAEmail` |
| It has no actor | The `NewActivity(ctx, nil, ...)` call at `sessions.go:231`. A nil user means `actor_id` is null, which this chapter already explains |
| Its stated purpose is to make an otherwise unobservable event visible | The comment at `sessions.go:226-228`: "A correct password on an MFA-enabled account triggers a verification email. Record it so this event is visible in the activity feed and audit stream, since it is otherwise the only observable signal that a valid password was submitted" |
| A client that cannot handle email verification is refused before the email is sent, so no activity is written | `sessions.go:217-220`: `if !supportsEmailVerification { return nil, nil, fleet.NewAuthFailedError("client with no MFA email support") }`, which precedes both `makeMFAEmail` and the activity call |
| Failing to write the activity does not fail the sign-in | The `actErr` branch logs and continues; `err = sendingMFAEmail` is returned regardless |
| There is no completion counterpart; the completed sign-in is `user_logged_in` | No MFA-completion type exists in `activities.go` (grepped for `mfa`); the redemption path writes the ordinary login activity |
| The server defines 196 activity types at 4.91.0, up from 191 at 4.90.0, with five added and none removed | Distinct `return "..."` strings in `server/fleet/activities.go` at each tag, the same method the chapter's 4.90 figure used. Added: `user_mfa_requested`, `created_setup_experience_script`, `deleted_setup_experience_script`, `released_from_ab`, `edited_windows_enrollment_default_fleet` |
| Fleet's audit-log reference documents 165 of the 196 and omits 31 | Word-boundary match of each type name against `docs/Contributing/reference/audit-logs.md` at the tag |
| `ran_custom_mdm_command` is now documented and had to leave the chapter's list of notable omissions | Same match; it is present in the reference at 4.91 and was absent at 4.90 |

### Derived (reasoning, not just citation)

| Conclusion | Reasoning |
|---|---|
| "Read it as 'a valid password was used', not as 'somebody signed in'" | The source comment states the first half. The warning against the second reading is the manual's, and it is the point of documenting the type: the name reads like a sign-in event and is not one |
| "An unpaired request is exactly the interesting case" | Follows from there being no completion type: the only way to distinguish an abandoned challenge from a completed one is the presence of a later `user_logged_in` for that account. Offered as method, not as a Fleet feature |
| "A burst of these against one address, or one against an account whose owner was not working, is the signal it was added to give you" | Editorial, resting on the payload carrying the public IP and on the stated purpose |

### §27 corrections made to existing prose
- **1.5**: "Second-factor events are recorded almost nowhere ... after the password validates, Fleet
  sends the code and returns **without writing a login activity at all**." True at 4.90, misleading at
  4.91. Rewritten to keep the part that is still true, that a refused code writes nothing, while
  recording the new signal at the front of the gap. 1.5 frontmatter already at 4.91.0; its
  verified_source extended.
- **2.6**: "the second factor is almost invisible in the activity record". Softened to "thinly
  represented", with the 4.91 change and the remaining gap named in one sentence. 2.6 frontmatter
  bumped to 4.91.0/2026-09-08.
- **8.12.3**: the counts, from 191/157/34 to 196/165/31, with the 4.90 figures kept parenthetically so
  the change is legible rather than silently restated. `ran_custom_mdm_command` dropped from the
  examples because it is no longer omitted.

### Not edited / deferred
- 8.12.4's opening "Roughly 200 types exist" needs no change at 196.
- 8.12.7's high-value correlation table was considered for a `user_mfa_requested` row and left alone:
  that table is organised by symptom onset for configuration drift, and an authentication signal does
  not fit its column headings.

---

## Addendum, Fleet 4.91.0: setup-experience-script activities (SE3)

Drafted 2026-09-08 against `fleet-v4.91.0` (35fc1c024490). Four paragraphs added to
`## 8.12.4 Activity types: the categories`, and both type names added to the Setup experience category
row. Changelog line: "Added `created_setup_experience_script` and `deleted_setup_experience_script`
activities so that adding, replacing, or removing a setup experience script (via the API or GitOps) is
recorded in the audit log."

### Stated: verified at the tag

| Claim | Basis |
|---|---|
| The two type strings and their payload fields | `ActivityCreatedSetupExperienceScript` and `ActivityDeletedSetupExperienceScript` in `server/fleet/activities.go:1995-2013`, each carrying `FleetID *uint json:"fleet_id"`, `FleetName *string json:"fleet_name"`, `ScriptName string json:"script_name"` |
| They use the new field naming rather than the `team_id`/`renameto` compatibility pattern | The comment immediately above them at `:1993-1994`: "These activities are new, so they use the fleet_id/fleet_name field names directly rather than the team_id/team_name + renameto pattern the older team-scoped activities keep for back-compat" |
| Both are Fleet Premium | The real implementations are `ee/server/service/setup_experience.go:183-195` and `:225-237`; the free-tier `SetSetupExperienceScript` and `DeleteSetupExperienceScript` at `server/service/setup_experience.go:191-197,220-226` return `fleet.ErrMissingLicense` |
| Uploading identical content writes nothing | `SetSetupExperienceScript` returns `changed` false and the service returns before `NewActivity` (`ee/server/service/setup_experience.go:179-181`); the datastore's PUT semantics are spelled out at `server/datastore/mysql/setup_experience.go:973-992`, "existing setup script with same content -> no-op" |
| Replacing a script emits only the create | The datastore deletes the old row internally as part of the same call (`deleteSetupExperienceScript` inside `SetSetupExperienceScript`), and the service emits only `ActivityCreatedSetupExperienceScript`. No delete activity is reachable from that path |
| Deleting when no script is set writes nothing, deliberately because of GitOps | `DeleteSetupExperienceScript` loads the script first and returns nil on not-found, with the comment "Load the script first so we can skip the activity when there is nothing to delete (GitOps clears a script on every apply even when none was set) and so we can include its name" |
| The setup experience script is macOS-only and must be a `.sh` file | Same service function: "setup experience is only supported for macOS currently so we need to override the file extension check", rejecting anything but `.sh`. Not written into 8.12; recorded because it bounds what these activities can ever describe |

### Derived (reasoning, not just citation)

| Conclusion | Reasoning |
|---|---|
| "Until then the one piece of the setup contract that runs arbitrary code on a new machine could be added, replaced or removed with nothing written down" | The types are new at 4.91 (count diff against 4.90.0), and no other activity type covers this object. The characterisation of the risk is the manual's, and rests on 5.5's existing account of what a setup script is |
| "The pair to look for after a replacement is a single `created_setup_experience_script`" | The changelog says replacing is recorded, without saying as what. Reading the two paths shows it is recorded as a create only. **The placement plan had guessed create-and-delete, and that guess was wrong**; this row exists so the correction is visible |
| "The absence of an activity is not evidence the file in your repository is unchanged, only that Fleet's stored copy is" | Follows from the no-op-on-identical-content rule. Written because the two deliberate silences are exactly what an alerting rule would misread |

### Not edited / deferred
- Checked 1.5 and 5.5 for a claim that setup scripts are unaudited. 1.5's audited-by-exception callout
  is about the organisation settings document and names no script; 5.5 makes no audit claim about the
  setup script. No correction needed, unlike SE2.
- The macOS-only and `.sh`-only constraints belong to 5.5's setup-experience coverage, not to the audit
  chapter, and 5.5 already carries the setup-script material. Not duplicated here.
