---
title: "Enroll Windows devices"
chapter: "III. Connect devices"
section: "3.3"
sidebar_position: 3
verified_against: Fleet 4.90.0
verified_on: 2026-08-27
verified_source: git tag fleet-v4.90.0
further_reading:
  - https://fleetdm.com/guides/windows-mdm-setup
  - https://fleetdm.com/guides/enroll-hosts
feature_requests:
  labels: []
  match: ["Windows enrollment", "Autopilot", "Entra", "MSI", "fleetd package", "Windows MDM"]
  exclude: []
---

# Enroll Windows devices

![Explanation](../_assets/icons/explanation-light.svg) Fleet lets you bring existing Windows devices under management or prepare new ones through Microsoft Entra ID and Autopilot. The rollout connects two channels: fleetd supplies reports, scripts, software, and vitals, while Windows MDM supplies profiles and commands. Check both so a device is ready for the work you intend to perform.

Fleet can start one channel from the other when the enrollment conditions are met ([1.2](../01-foundations/1.2-how-fleet-reaches-a-device.md)):

- **fleetd enrolls, and Fleet starts MDM enrollment.** With Windows MDM turned on, automatic enrollment left on, and the host eligible, Fleet's reply to an ordinary agent check-in tells fleetd to register the device for management. Nobody delivers a profile and nobody clicks anything.
- **MDM enrolls, and Fleet pushes fleetd.** On the Microsoft Entra ID paths, where the device reaches Fleet before any agent exists, Fleet queues an install command.

Choose the path that matches your provisioning workflow, then verify that its follow-on enrollment or installation completed. The conditions below explain when Fleet starts that work and what to check if only one channel is ready.

BitLocker enforcement needs both channels and Fleet Premium. Fleet enables it in the Orbit configuration response, and the agent performs encryption locally. MDM enrollment is a prerequisite for Fleet to send that configuration. A missing agent or MDM enrollment therefore prevents enforcement, and the work does not appear as an MDM command in the queue.

<a id="the-decision-you-are-making"></a>

## Choose an enrollment approach

<!-- SCREENSHOT-REUSE: SS-2.11-01. Reuse the Windows management controls when comparing automatic and manual enrollment. The Windows-side confirmation is SS-3.3-01. -->

![Reference](../_assets/icons/reference-light.svg) Start with how the device will first reach Fleet. Existing machines can enroll through fleetd; the Microsoft-backed paths can bring MDM online during or after Windows setup.

| Path | Use it when | What the console shows |
|---|---|---|
| **Agent-driven** | Fleet's default, and the one to plan for. You can get fleetd onto the machine by any means you already have | On (manual) |
| **Autopilot / Entra** | New hardware you want provisioned at first boot, through Microsoft's out-of-box experience | On (company-owned) |
| **Settings app** | The person holding the device turns management on themselves, under **Settings > Access work or school** | On (manual) |

Those strings are what the **console** shows. The database and the API spell the Autopilot case `On (automatic)`, and the host filter spells it `automatic`; [8.6](../08-troubleshooting/8.6-server-state.md#enrolment-values-in-storage-display-and-filters) has the mapping, and it matters the first time you write a query from something you read on screen.

Fleet records automatic enrollment only when the enrollment type is automatic and it occurs during the out-of-box experience. An Entra-driven enrollment after setup displays `On (manual)`. This is why enrolling a device after it missed Autopilot at first boot does not give it the same company-owned enrollment status.

Windows has no personal-enrollment status in Fleet. Fleet records personal enrollment as false, so Windows hosts do not display `On (manual - personal)`.

**Two of the three need Microsoft Entra ID** and a connection between Entra and Fleet that [2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md) sets up. **Those two are Fleet Premium as well as Microsoft-licensed**, since Fleet Free rejects the Entra tenant and client IDs outright, so a Free deployment has the agent-driven path and nothing else. They also need Microsoft licences, per person, and Fleet's own Autopilot notes describe the end-user requirement as its **current understanding** rather than something verified end to end, so price the rollout against Microsoft's terms rather than against 2.11's summary. The agent-driven path needs none of it, which is why it is the one to start with and the one to fall back to when an Entra problem is blocking a rollout.

### What Windows MDM needs before any of this

A WSTEP certificate and key supplied to the server, and Windows MDM turned on. Both are [2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md). Until Windows MDM is on, fleetd still enrolls and everything agent-side works; there is simply no MDM channel to be on.

Fleet's guidance is that turning Windows MDM on **disables Windows tamper protection** on the host. That is a claim about Windows rather than about Fleet, and **nothing in Fleet's source at this release establishes it**, so treat it as a thing to confirm on a test machine rather than a fact to quote in a compliance answer. It is worth raising early either way, because the answer changes a security posture.

## Agent-driven enrollment, which is the default

![Explanation](../_assets/icons/explanation-light.svg) Agent-driven enrollment is a useful starting point for existing Windows devices because it needs no Entra connection. Once fleetd is installed, Fleet can request MDM registration through the agent’s normal check-ins.

With Windows MDM on **and automatic enrollment left enabled**, Fleet decides on **every Orbit configuration check-in** whether that host should be enrolled. Turning on `enable_turn_on_windows_mdm_manually`, which is the Premium setting described later in this chapter, switches this whole mechanism off, so treat it as a fifth condition sitting above the four below. When it holds, four more conditions have to hold as well:

| Condition | What it means when it does not hold |
|---|---|
| The host's platform is Windows | Nothing happens |
| The host is **enrolled with osquery** | fleetd installed but not yet enrolled is not enough. This is the ordering that matters |
| The host is **not a Windows Server** | Servers are excluded, and the check happens twice, see below |
| The host is **not enrolled in any MDM**, Fleet's or anyone else's | A host still attached to its previous manager is skipped, unless migration is turned on |

When all four hold, Fleet's reply to that check-in carries a flag and a discovery URL. Orbit calls into Windows' own management registration API, and the device runs the enrollment exchange against Fleet from there.

Orbit waits an hour between registration calls that return success while Fleet still considers the device unenrolled. Failed calls do not update that timestamp, so they can retry at the next configuration check-in, normally thirty seconds later.

Repeated enrollment errors can therefore appear at the configuration-poll cadence in agent logs. Account for that retry behavior when investigating a device that remains unenrolled.

If Orbit identifies the device as Windows Server, it stops registration attempts for the lifetime of the process. Other registration errors continue to retry.

Fleet and Orbit both exclude Windows Server. Fleet uses the installation type reported by osquery, while Orbit checks the machine locally before calling the registration API. If Fleet’s inventory has not caught up, the server may request enrollment that Orbit correctly refuses.

A new host initially has no Windows Server classification in Fleet; the osquery detail query supplies it. Allow that first query cycle when comparing the two sides. [8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md) covers the host-side checks.

### Turning the automatic behaviour off

There is a setting for estates that do not want enrollment happening unasked: `enable_turn_on_windows_mdm_manually`. Turn it on and Fleet stops sending the flag, leaving the end user to enroll through **Settings > Access work or school**. Either way the resulting MDM status is `On (manual)`; what changes is who initiates.

The setting has three constraints:

- It is **Fleet Premium**. Fleet Free rejects it with a licence error.
- It cannot be set unless Windows MDM is already on. Fleet refuses the combination.
- It is **mutually exclusive with Windows MDM migration**, and Fleet refuses both at once with a message saying to enable only one. That is consistent rather than arbitrary: migration exists to move hosts without asking anyone.

Fleet documents it as configurable only for all fleets rather than per fleet.

## Installing fleetd

![How-to](../_assets/icons/howto-light.svg) You build an MSI, then deliver it with whatever already delivers software to your Windows estate.

```sh
fleetctl package --type=msi \
  --fleet-url=https://fleet.example.com \
  --enroll-secret="<secret>" \
  --fleet-desktop \
  --enable-scripts
```

`--fleet-desktop` includes Fleet Desktop, which gives the end user a tray item and the **My Device** page. My Device works on Free, with its Details and Software tabs; **Self Service installation is Fleet Premium**, as is Fleet Desktop's summary endpoint, so on Free the tray item has less to report. Without the flag, the other components still run and the person on the machine has no Fleet-facing surface at all. `--enable-scripts` sets whether this host will run scripts by default.

On Windows, the MSI properties `FLEET_DESKTOP` and `ENABLE_SCRIPTS` can override those package defaults at installation. A package built without script support can therefore install with scripts enabled when the deployment tool supplies the property.

Two flags behave differently here than on macOS. `--arch` accepts `arm64` for MSI packages, so Windows on ARM is a supported target. And `--use-system-configuration`, which is how a Mac takes its URL and secret from a configuration profile, is **rejected for MSI**: Fleet accepts it only for `pkg`. Windows has its own answer to the same problem, below.

### Build one base MSI and supply the rest at install time

![Explanation](../_assets/icons/explanation-light.svg) MSI installer properties let you reuse one base package across deployment groups. Your software-distribution tool can supply six settings at installation, overriding their packaged values:

| Property | What it sets |
|---|---|
| `FLEET_URL` | The Fleet server the agent talks to |
| `FLEET_SECRET` | The enroll secret, written into `secret.txt` during install |
| `ENABLE_SCRIPTS` | Whether this host will run scripts |
| `FLEET_DESKTOP` | Whether Fleet Desktop runs |
| `END_USER_EMAIL` | The address recorded against the host |
| `EUA_TOKEN` | End-user attribution on the Entra paths. Fleet sets this one, not you |

To build an MSI without a usable credential, supply the literal `dummy` for both `--fleet-url` and `--enroll-secret`, as Fleet’s contributor documentation does. Omitting both flags is accepted, but it also omits the `--enroll-secret-path` argument, leaving an installer that can write `secret.txt` without configuring Orbit to read it. Omitting only one flag is rejected.

```sh
fleetctl package --type=msi --fleet-url=dummy --enroll-secret=dummy
```

`dummy` is a recognised sentinel rather than a value. It suppresses writing the secret into the artifact while keeping the command-line path in place, so the resulting MSI has no credential in it and is ready to be told one:

```powershell
msiexec /i fleet-osquery.msi /quiet FLEET_URL="https://fleet.example.com" FLEET_SECRET="<secret>" ENABLE_SCRIPTS="True"
```

That is one artifact in the software repository with no credential inside it, or one artifact serving several fleets with a different secret per deployment group. It is the same problem `--use-system-configuration` solves on macOS, solved by the platform's own installer mechanism rather than by a configuration profile.

At installation, supply the real intended values. `FLEET_SECRET` defaults to `dummy`, and the installer skips writing `secret.txt` for an empty or literal `dummy` value. An unresolved deployment variable can therefore leave the packaged secret in use, or leave no secret at all, without failing the installation. Check the deployment tool’s supplied properties if hosts appear in the wrong fleet or fail to enroll.

**Everything else is build-time.** The update URL, the TUF channels, `--insecure`, `--debug`, the host identifier, the osquery database path and the setup-experience switch are written into the service's command line when the package is built. Changing any of those means building another package.

### When Fleet installs fleetd for you

On the Entra paths the device is in MDM before any agent exists, so Fleet pushes one down. The rule for when this happens is precise, and knowing it explains why the mechanism appears to be missing on the agent-driven path:

**Fleet enqueues the fleetd install only for enrollments that carry a user principal name**, which is to say the Entra ones. Agent-driven enrollments authenticate with Orbit's node key instead, and Fleet treats those as already having an agent, which they do.

When it does fire, it fires on a **management session from a device Fleet believes has no working agent**, and that check is stricter than "is fleetd installed". Fleet also wants an agent check-in recent enough relative to the enrollment: a check-in counts as evidence of a working agent if it happened at any time after the enrollment, **or up to 90 seconds before it**. That grace window is what stops a device re-enrolling moments after a normal check-in from being sent a redundant installer. A device whose last check-in predates its enrollment by more than that gets the install enqueued again.

Four properties of that install are decided for you:

- **The MSI is fetched by the device from Fleet's download service**, and the server hands the device a SHA-256 taken from a metadata document the server fetches itself. Two separate egress paths therefore have to work: Fleet outbound to the download service, and the device outbound to wherever the MSI is served. Fleet failing to fetch the metadata is a warning in the log and not an error, and the session continues without the install.
- **Scripts are enabled.** The install command sets `ENABLE_SCRIPTS="True"` unconditionally, whatever your own packages do.
- **A global enroll secret has to exist, and Fleet uses the first of them.** With none configured, Fleet logs a warning and returns without building an install command at all, which is a silent no-op rather than an error. With several, it takes the first in the order the datastore returns them. So this is a prerequisite for the Entra paths, not a detail.
- **That secret places only new hosts.** A host Fleet has never seen lands where a global secret puts it, which is **no fleet at all**, because a global secret is one with no fleet attached; a host Fleet already knows keeps the fleet it has, because re-enrolling does not move a host between fleets ([3.1](3.1-enrollment-design-and-host-lifecycle.md)). If placement matters for new devices, install fleetd yourself with the secret you want and let the agent-driven path do the rest.

The install is guarded by a fixed identifier so Windows applies it once rather than on every session.

## Verifying an enrollment

<!-- SCREENSHOT: SS-3.3-01 | Confirming Windows work or school enrollment
     PURPOSE: Show the Windows-side view that an administrator or employee can use to confirm the management connection.
     PREPARE: Enroll a disposable Windows test device through the path described in the chapter. Use a fictional user and organization.
     CAPTURE: Windows Settings > Accounts > Access work or school, with the relevant connected organization/management entry expanded. Include the Info control or management-details view if it identifies Fleet.
     FRAME: One Windows settings crop, plus a second detail frame only if needed to identify the MDM connection. Mask tenant-specific addresses and personal details.
     EDITION: 4.91; reuse for 4.90 only when the visible controls and wording match
     PRIVACY: Use demo names and non-sensitive data; exclude tokens, passwords, keys, and personal identifiers.
     FILENAME: ss-3.3-01.png
-->

![How-to](../_assets/icons/howto-light.svg) Check both channels separately, because either can arrive without the other.

**From Fleet.** The host appears and its MDM status matches the path you used. [2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md) records Fleet's guidance that Windows completes MDM enrollment in the context of a signed-in user, so a machine sitting at a lock screen reporting MDM **Off** is not necessarily broken. **Note the boundary that guidance draws and this manual cannot:** Fleet's source establishes that the agent check-in is what *starts* registration, since Orbit runs as a system service and calls the registration API without inspecting who is logged in, and establishes nothing about when Windows *completes* it. The sign-in dependency and the thirty-second figure are Fleet's account of Windows' behaviour, not something checkable here.

**From the device**, in an elevated PowerShell:

```powershell
# Is the agent installed and running?
Get-Service -Name "Fleet osquery"

# Is the device Entra-joined, and was it handed an MDM URL?
dsregcmd /status
```

Use `dsregcmd` to check the Entra join and the MDM URL Entra supplied. To confirm management by Fleet itself, inspect the registry enrollment with `ProviderID` set to `Fleet` and a non-zero `EnrollmentState`, which is what fleetd reads. [8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md#8910-host-side-checks) covers those keys and the certificate store.

For an empty `MdmUrl` on the Autopilot path, check the three causes documented in Fleet’s notes: a missing Entra ID P1 license, a newly assigned license that has not propagated, or the Fleet application’s MDM user scope being set to `None`. Confirm which condition applies before changing licensing or enrollment configuration.

If fleetd is running with MDM off, review the expected enrollment path and its eligibility conditions; MDM may also have been disabled after enrollment. If MDM is on but the agent is absent, investigate agent delivery or later removal. For an Entra enrollment, confirm a global enroll secret exists before checking download access, because Fleet cannot construct the install command without it.

## Migrating from another MDM

Fleet can move Windows hosts off another manager with no end user interaction at all, which is the difference from Apple. **It is Fleet Premium.** [2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md) covers turning it on, and the residue from the previous manager that Fleet ships a repair script for.

The ordering is the same as the agent-driven path and for the same reason: **fleetd first, then Fleet moves the host.** A Windows host with no agent is invisible to the mechanism, because Fleet decides on the agent's own check-in.

## Who owns it afterwards

![Reference](../_assets/icons/reference-light.svg) **The WSTEP certificate and key**, which are also Fleet's escrow key for Windows disk-encryption recovery keys ([2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md)). Losing them costs more than the MDM channel.

**The Entra application**, if you use the Autopilot or Settings-app paths. Its permissions, its scope, and the licences the enrolling users hold belong to whoever administers Microsoft identity, and none of it is visible from Fleet.

**The gap between the two channels.** A count of Windows hosts with MDM on and no recent agent check-in, and the reverse, is worth putting somewhere you look at regularly rather than running it after someone complains.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting-light.svg) When enrollment does not work, the split at the top of this chapter decides where to look. MDM problems, including the enrollment exchange, the poll schedule and the command queue, are [8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md). Agent problems, including a package that installed and never enrolled, are [8.4](../08-troubleshooting/8.4-host-side-investigation.md).

## Version notes

| Change | Release |
|---|---|
| `EUA_TOKEN` installer property, carrying end-user attribution from an Entra enrollment into the agent | fleetd 1.55.0 |
| `END_USER_EMAIL` exposed as an installer property rather than a build-time argument only | fleetd 1.28.0 |
