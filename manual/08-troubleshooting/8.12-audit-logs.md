---
title: "Audit logs: who did what, when"
chapter: VIII. Troubleshooting Fleet
section: "8.12"
sidebar_position: 12
status: drafting
verified_against: Fleet 4.90.1
verified_on: 2026-08-26
verified_source: Tag-verified against fleet-v4.90.1 (dd0200f062). Attribution, actor enrichment, the streaming query and its lock, expiry's host-link exemption, filesystem rotation, and the fleetctl command surface were re-checked in the source at that tag.
further_reading:
  - https://fleetdm.com/docs/contributing/audit-logs
  - https://fleetdm.com/docs/configuration/fleet-server-configuration#external-activity-audit-logging
  - https://fleetdm.com/docs/rest-api/rest-api#list-activities
  - https://fleetdm.com/docs/configuration/yaml-files
  - https://fleetdm.com/guides/log-destinations
  - https://github.com/fleetdm/fleet/releases
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Audit logs: who did what, when

Fleet's record of server-side actions. One row per action, in MySQL, readable in the UI and over the API, optionally streamed to an external destination. Fleet calls the rows **activities**; "audit log" is the name for the same rows once they leave Fleet.

## 8.12.1 The short answers

| Question | Answer |
|---|---|
| Where do the rows live | `activity_past` in MySQL, with a host join table `activity_host_past` |
| Is it on by default | The rows are always written. **Streaming** them out is off by default (`activity_enable_audit_log` defaults to `false`) |
| Is it Premium | Reading is free. External streaming is Fleet Premium |
| Retention | Unlimited until you enable `activity_expiry_settings.activity_expiry_enabled` |
| Does it cover host-side events | Server-side action only. Host-side history lives elsewhere: [8.12.6](#8126-the-limit-server-side-action-only) |

## 8.12.2 The table

`activity_past` holds the rows themselves:

| Column | Type | Meaning |
|---|---|---|
| `id` | `int unsigned` auto-inc | Row ID. Monotonic, and the pagination cursor the API uses |
| `created_at` | `timestamp(6)` | When the action happened. Microsecond precision. Default `CURRENT_TIMESTAMP(6)` |
| `user_id` | `int unsigned` nullable | FK to `users.id`, `ON DELETE SET NULL` |
| `user_name` | `varchar(255)` nullable | Actor name **snapshotted at write time** |
| `user_email` | `varchar(255)` NOT NULL, default `''` | Actor email, also snapshotted |
| `activity_type` | `varchar(255)` NOT NULL | The type string, e.g. `edited_policy` |
| `details` | `json` nullable | Type-specific payload. Shape differs per type |
| `streamed` | `tinyint(1)` NOT NULL default `0` | Whether the audit streamer has shipped this row |
| `fleet_initiated` | `tinyint(1)` NOT NULL default `0` | `1` when the activity type reported itself as automation. `0` is not evidence a person acted |
| `host_only` | `tinyint(1)` NOT NULL default `0` | `1` hides the row from the global feed |

`activity_host_past` is `(host_id, activity_id)`, primary key on both, with `ON DELETE CASCADE` from `activity_past`. That is the many-to-many link that makes a host's own activity list possible.

**The rename matters if you have saved SQL.** These two tables were `activities` and `host_activities` until a schema migration renamed them. The index names still carry the old spelling (`activities_created_at_idx`, `idx_activities_type_created`), and Fleet's REST API reference still offers to sort by "any column in the `activities` table" (fleetdm.com/docs/rest-api/rest-api). That reference text is stale. The table is `activity_past`.

### Two columns that will surprise you

**`user_name` and `user_email` are copies, not lookups.** Deleting a Fleet user nulls `user_id` but leaves the name and email text in place. The audit trail survives user deletion, which is the point. The stored copy is the name the user had at the time.

**The API does not show you that copy when the user still exists.** For any row with a non-null `user_id`, the activities service overwrites `actor_full_name`, `actor_email`, `actor_gravatar`, and `actor_api_only` from the current `users` record before returning. So the historical name is in MySQL and the current name is in the API response, and they disagree exactly when someone has been renamed or has changed their email since. Read the table directly if the historical value is the thing you need. When the user has been deleted, `user_id` is null, no enrichment happens, and the API falls back to the stored copy, which is the case the snapshot exists for.

**`host_only = 1` rows are excluded from the global activity feed.** The query behind the global feed filters on `host_only = false`, and the flag is set at write time by the activity type itself, so certain types are written to the table, shown on the host's own page, and withheld from the global page by design. An empty result from the global endpoint is therefore not proof that nothing happened. Query the host endpoint, or the table with the host join, before you conclude the event was never recorded.

## 8.12.3 Entry shape

The API response and the streamed record are the same object:

| JSON field | Source | Notes |
|---|---|---|
| `id` | `activity_past.id` | Omitted when zero |
| `uuid` | `upcoming_activities.execution_id` | Populated for **upcoming** activities only, not past rows |
| `created_at` | `created_at` | |
| `type` | `activity_type` | |
| `actor_id` | `user_id` | Null when no user was attached, and null when the acting user has since been deleted. Not tied to `fleet_initiated` |
| `actor_full_name` | live lookup on `users.name` | Current name, not the stored copy. Falls back to `activity_past.user_name` when `actor_id` is null |
| `actor_email` | live lookup on `users.email` | Same. Stored as NOT NULL default `''`, so an absent actor is an empty string rather than a null |
| `actor_gravatar` | live lookup on `users.gravatar_url` | |
| `actor_api_only` | live lookup on `users.api_only` | Whether the actor is an API-only user |
| `fleet_initiated` | `fleet_initiated` | |
| `details` | `details` | Raw JSON, shape per type |

The `streamed` bookkeeping column stays inside Fleet and never appears in the output.

A real record:

```json
{
  "created_at": "2023-07-27T14:35:08Z",
  "id": 25,
  "actor_full_name": "Anna Chao",
  "actor_id": 3,
  "actor_gravatar": "",
  "actor_email": "",
  "type": "installed_software",
  "fleet_initiated": false,
  "details": {
    "status": "installed",
    "host_id": 1272,
    "host_display_name": "MacBook Pro",
    "policy_id": null,
    "policy_name": null,
    "install_uuid": "23c18ea1-8cd7-4af4-a1d8-f2666993a66b",
    "self_service": false,
    "software_title": "zoom.us.app",
    "software_package": "ZoomInstallerIT.pkg"
  }
}
```

The `details` object carries the useful content, and its shape is per-type. Fleet's audit-log reference, fleetdm.com/docs/contributing/audit-logs, documents the fields per type and runs to some 80 KB of tables. Use it, and do not guess a field name from a type name.

**It is not complete, so do not read an absence there as an absence in Fleet.** At 4.90.1 the server defines 191 activity types and that page covers 157 of them. The 34 it omits are not obscure ones: `ran_automation_webhook`, `ran_custom_mdm_command`, `added_certificate`, `reset_policy`, the recovery-lock password types, and the conditional-access types are all missing. Your own deployment is the authority on which type strings it emits, which is what the discovery query below is for. Treat the reference as a payload aid that may lag the server.

## 8.12.4 Activity types: the categories

Roughly 200 types exist, and Fleet's audit-log reference lists every one with its payload. For the shorter list of types your own deployment actually writes:

```sql
SELECT DISTINCT activity_type FROM activity_past ORDER BY activity_type;
```

Naming is verb-first and consistent enough to guess at: `created_`, `edited_`, `deleted_`, `added_`, `enabled_`, `disabled_`, `ran_`, `applied_spec_`, `installed_`, `uninstalled_`, `read_`, `viewed_`, `failed_`.

Categories, with representative real types:

| Category | Representative types |
|---|---|
| Users and access | `created_user`, `deleted_user`, `changed_user_global_role`, `changed_user_team_role`, `deleted_user_global_role`, `deleted_user_team_role`, `user_added_by_sso` |
| Authentication | `user_logged_in`, `user_failed_login` |
| Teams | `created_team`, `deleted_team`, `applied_spec_team`, `transferred_hosts` |
| Policies | `created_policy`, `edited_policy`, `deleted_policy`, `reset_policy`, `applied_spec_policy` |
| Queries | `created_saved_query`, `edited_saved_query`, `deleted_saved_query`, `deleted_multiple_saved_query`, `applied_spec_saved_query`, `live_query` |
| Configuration profiles | `created_`, `edited_` and `deleted_` prefixed to each of `macos_profile`, `windows_profile`, `android_profile` and `declaration_profile`, giving twelve names; plus `resent_configuration_profile` and `resent_configuration_profile_batch` |
| MDM lifecycle | `mdm_enrolled`, `mdm_unenrolled`, `fleet_enrolled`, `ran_custom_mdm_command`, `failed_enrollment_profile_renewal` |
| MDM toggles | `enabled_windows_mdm`, `disabled_windows_mdm`, `enabled_android_mdm`, `disabled_android_mdm`, `enabled_windows_mdm_migration`, `disabled_windows_mdm_migration` |
| Disk encryption and secrets | `enabled_macos_disk_encryption`, `disabled_macos_disk_encryption`, `escrowed_disk_encryption_key`, `read_host_disk_encryption_key` |
| Host lock and wipe | `locked_host`, `unlocked_host`, `wiped_host`, `failed_wipe`, `cleared_passcode`, `set_host_recovery_lock_password`, `viewed_host_recovery_lock_password`, `rotated_host_recovery_lock_password` |
| Scripts | `added_script`, `updated_script`, `edited_script`, `deleted_script`, `ran_script`, `canceled_run_script`, `ran_script_batch`, `scheduled_script_batch`, `canceled_script_batch` |
| Software | `added_software`, `edited_software`, `deleted_software`, `installed_software`, `uninstalled_software`, `canceled_install_software`, `canceled_uninstall_software`, `added_app_store_app`, `edited_app_store_app`, `deleted_app_store_app`, `installed_app_store_app`, `canceled_install_app_store_app` |
| Setup experience | `changed_macos_setup_assistant`, `deleted_macos_setup_assistant`, `added_bootstrap_package`, `deleted_bootstrap_package`, `edited_setup_experience_software`, `canceled_setup_experience`, `enabled_macos_setup_end_user_auth`, `disabled_macos_setup_end_user_auth` |
| OS updates | `edited_macos_min_version`, `edited_ios_min_version`, `edited_ipados_min_version`, `edited_windows_updates`, `enabled_macos_update_new_hosts`, `disabled_macos_update_new_hosts` |
| GitOps | `enabled_gitops_mode`, `disabled_gitops_mode`, `enabled_gitops_exception`, `disabled_gitops_exception` |
| Certificates and PKI | `added_digicert`, `edited_digicert`, `deleted_digicert`, `added_ndes_scep_proxy`, `edited_ndes_scep_proxy`, `deleted_ndes_scep_proxy`, `added_custom_scep_proxy`, `added_custom_est_proxy`, `added_smallstep`, `added_hydrant`, `added_certificate`, `installed_certificate`, `resent_certificate` |
| Automations | `enabled_activity_automations`, `edited_activity_automations`, `disabled_activity_automations`, `ran_automation_webhook`, `failed_automation_webhook`, `ran_automation_ticket`, `failed_automation_ticket`, `ran_automation_calendar_event`, `failed_automation_calendar_event`, `ran_automation_conditional_access`, `failed_automation_conditional_access` |
| Global settings | `edited_agent_options`, `edited_enroll_secrets`, `changed_org_logo`, `deleted_org_logo`, `edited_host_name_template`, `created_custom_variable`, `updated_custom_variable`, `deleted_custom_variable`, `created_custom_host_vital`, `edited_custom_host_vital`, `deleted_custom_host_vital`. **Not a complete account of settings changes**: the document is audited by exception and a change to a part with no dedicated type writes nothing ([1.5](../01-foundations/1.5-audit-and-activity.md)) |
| Hosts and labels | `deleted_host`, `created_label`, `edited_label`, `deleted_label`, `edited_host_idp_data`, `retrieved_host_my_device_url` |
| Packs (legacy) | `created_pack`, `edited_pack`, `deleted_pack`, `applied_spec_pack` |

### Attribution has three outcomes, not two

One `activity_type` covers several origins, so the type alone never answers "who did this". Fleet sets `fleet_initiated` only for the 26 activity types that implement a self-report of automation, and only when that self-report returns true. Everything else leaves the flag at its `0` default, including events no person initiated at all.

| What you see | What it means |
|---|---|
| A non-empty actor identity | Attributed to that user. Still true when the user has been deleted, in which case `actor_id` is null and only the stored name and email remain |
| `fleet_initiated: true` | The activity type reported itself as automation. Fleet writes the literal actor name `Fleet` into the stored row |
| No actor and `fleet_initiated: false` | Unattributed. A system or device path with no user behind it. Read the type and the `details` payload; do not name a person |

The third row is the one that misleads, because `fleet_initiated: false` reads like "a human did it" and is not. `mdm_unenrolled` is the clearest example: Fleet emits it both for an administrator who unenrolled a host and for a device that checked itself out or was reconciled away, and neither the flag nor the type distinguishes them.

**One more case, rarer and sharper.** An automation-flagged activity can still carry a user, because some automated paths attribute the work to the person who uploaded the installer. The stored row then has `fleet_initiated = 1`, `user_name = 'Fleet'`, and a live `user_id`. The API's enrichment sees that non-null `user_id` and replaces the name with the current user's, so the response names a person next to a flag that says automation. Believe the flag.

## 8.12.5 Reading it

### API

| Endpoint | Returns |
|---|---|
| `GET /api/v1/fleet/activities` | Global feed. `host_only = 1` rows are withheld |
| `GET /api/v1/fleet/hosts/:id/activities` | That host's past activities |
| `GET /api/v1/fleet/hosts/:id/activities/upcoming` | Queued activities for that host, still waiting to run |
| `DELETE /api/v1/fleet/hosts/:id/activities/upcoming/:activity_id` | Cancels a queued activity |
| `GET /api/latest/fleet/policies/:id/automation_activities` | Activities generated by one policy's automation |

All five are in Fleet's REST API reference at fleetdm.com/docs/rest-api/rest-api.

Query parameters on the global endpoint:

| Parameter | Use |
|---|---|
| `page`, `per_page` | Max `per_page` is 10,000, and **10,000 is also what you get when you send no pagination parameters at all.** Ask for a page size |
| `order_key`, `order_direction` | `asc` is the default direction. You almost always want `order_key=created_at&order_direction=desc` |
| `after` | Cursor. Requires `order_key`, uses that column |
| `query` | Free text over `actor_full_name` and `actor_email`. The `details` payload is out of its reach |
| `activity_type` | Exact type string |
| `start_created_at`, `end_created_at` | Time-window bounds. This is the parameter pair that makes incident correlation practical |

The window query, which is the one worth memorising:

```bash
fleetctl api \
  '/api/v1/fleet/activities?order_key=created_at&order_direction=desc&start_created_at=2026-08-19T13:00:00Z&end_created_at=2026-08-19T15:00:00Z&per_page=500'
```

Or with plain `curl`, once `FLEET_URL` and `FLEET_API_TOKEN` are set:

```sh
export FLEET_URL=https://fleet.example.com          # add your URL prefix here if you serve one
export FLEET_API_TOKEN=$(fleetctl config get token | awk '{print $NF}')
```

Render the three attribution states explicitly rather than with a `//` fallback, because `actor_email` is an empty string rather than a null when there is no actor, and `//` will not catch it:

```bash
curl -sH "Authorization: Bearer $FLEET_API_TOKEN" \
  "$FLEET_URL/api/v1/fleet/activities?order_key=created_at&order_direction=desc&start_created_at=2026-08-19T13:00:00Z&end_created_at=2026-08-19T15:00:00Z&per_page=500" \
  | jq -r '.activities[] | [.created_at, .type,
      (if .fleet_initiated then "automation"
       elif (.actor_email // "") != "" then .actor_email
       elif (.actor_full_name // "") != "" then .actor_full_name
       else "unattributed" end)] | @tsv'
```

`fleetctl` reaches activities through the top-level `fleetctl api` command, as above. There is no `get api` subcommand, and through 4.90.1 there is no dedicated activities subcommand either, so the API is the route.

**Both endpoints are authorized differently.** The global feed needs a global role. Host activity is authorized against that host's team, so a team-scoped user can read a host's history and get nothing from the global feed. Direct MySQL access is a third thing again: a self-hosted infrastructure privilege, not something every Fleet administrator has. Establish which of the three you actually hold before planning an investigation around one of them.

### UI

Fleet's home page has an **Activity** section, and each host's page has its own activity feed. Both are volatile surfaces, and both render `details` as prose rather than raw JSON, so a field you came for may be summarised away. Read the payload over the API.

### MySQL

The global feed, last two hours, actor resolved:

```sql
SELECT ap.id,
       ap.created_at,
       ap.activity_type,
       ap.fleet_initiated,
       COALESCE(ap.user_email, '(none)') AS actor,
       ap.details
FROM   activity_past ap
WHERE  ap.host_only = 0
  AND  ap.created_at >= NOW() - INTERVAL 2 HOUR
ORDER  BY ap.created_at DESC;
```

Everything touching one host, including `host_only` rows the global feed hides:

```sql
SELECT ap.created_at, ap.activity_type, ap.user_email, ap.fleet_initiated, ap.details
FROM   activity_past ap
JOIN   activity_host_past ahp ON ahp.activity_id = ap.id
WHERE  ahp.host_id = 1272
ORDER  BY ap.created_at DESC
LIMIT  200;
```

What kinds of change happened in a window, ranked. Run this first when you do not yet know what you are looking for:

```sql
SELECT activity_type, COUNT(*) AS n, MIN(created_at) AS first, MAX(created_at) AS last
FROM   activity_past
WHERE  created_at BETWEEN '2026-08-19 13:00:00' AND '2026-08-19 15:00:00'
GROUP  BY activity_type
ORDER  BY n DESC;
```

Pull one field out of `details`, here the team on transfers:

```sql
SELECT created_at, user_email,
       details->>'$.team_name' AS team_name,
       JSON_LENGTH(details->'$.host_ids') AS host_count
FROM   activity_past
WHERE  activity_type = 'transferred_hosts'
  AND  created_at >= NOW() - INTERVAL 7 DAY
ORDER  BY created_at DESC;
```

Confirm `details` field names against Fleet's audit-log reference before you rely on a JSON path. The shapes are per-type, and MySQL enforces nothing beyond valid JSON.

Backlog check on streaming, which is also a rough health signal for the streamer:

```sql
SELECT streamed, COUNT(*), MIN(created_at), MAX(created_at)
FROM   activity_past
GROUP  BY streamed;
```

Other MySQL tables worth reading during an investigation are [8.6](8.6-server-state.md).

## 8.12.6 The limit: server-side action only

**The audit log has no host-side event history.** An event on the device itself, taken outside every channel Fleet holds, leaves no row.

Be precise about the boundary, because it is not "server actions only". Every row in `activity_past` is written by the Fleet server, but plenty of them are written **in response to something a device reported**: an Apple `CheckOut` produces `mdm_unenrolled`, an enrollment produces `mdm_enrolled`, an Android event arriving over Pub/Sub can produce an unenrollment row, and a script or software result posted by Orbit produces its activity when the result comes back rather than when the work was sent. Those rows carry no user.

What is genuinely absent is anything Fleet never heard about: a locally deleted agent, a hand-removed profile, encryption turned off at the keyboard. No reconciliation pass records host state drifting away from what Fleet expects.

That boundary decides several common questions:

| You want to know | What the audit log holds | Where the answer is |
|---|---|---|
| How fleetd was removed from a device | An uninstall Fleet performed. A local `sudo` uninstall, an MDM removal outside Fleet, an imaging event, or another management tool taking the package off are all invisible to it | [8.4](8.4-host-side-investigation.md) for on-host state, [8.2](8.2-log-surfaces.md) for agent logs |
| Whether a user deleted a configuration profile locally | Fleet's own profile delivery and removal | [8.4](8.4-host-side-investigation.md), [8.8](8.8-apple-mdm-diagnostics.md) |
| Whether someone disabled FileVault on the machine | The Fleet setting change | [8.4](8.4-host-side-investigation.md) |
| Who ran a command on the host outside Fleet | `ran_script` rows for scripts Fleet dispatched | Host-side OS audit facilities, not Fleet |
| Whether the agent stopped running, and when | Nothing. There is no `agent_stopped` type | [8.4](8.4-host-side-investigation.md); host `seen_time` in [8.6](8.6-server-state.md) |

> **Do not expect the audit trail to say how an agent was removed from a device, or to name a person.** No rows for that host at all is the correct behavior and not a finding. The answer has to come from the device: package receipts, launchd state, and the local installer/removal history. Set the expectation before running the query, because "the audit log is empty for this host" reads as suspicious when it is only the tool's scope.

`mdm_unenrolled` comes closest to a host-initiated row. It records Fleet **observing** an unenrollment, so it carries the fact and the timing without naming who acted.

## 8.12.7 Diagnostic use: "what changed"

[8.1](8.1-diagnostic-method.md)'s intake asks what changed near the onset. The audit log answers exactly one slice of that: server-side configuration and action changes. It is the fastest slice to check, so check it first.

### Procedure

1. **Fix the onset time.** From the first bad result, the first failed check-in, the first alert. Get it to the minute if you can.
2. **Widen deliberately.** Query `onset - 2h` to `onset`. Two hours, not two days. Fleet's own cron and automation traffic will bury a single human change in a wide window.
3. **Aggregate before you read.** Run the `GROUP BY activity_type` query from [8.12.5](#8125-reading-it). You are looking for a type that is unusual for this deployment, not for a type with a high count.
4. **Split Fleet's automation off, then read the rest by actor.** `fleet_initiated = 1` selects the work Fleet attributed to itself, and that set is the one people forget to check; it is often the answer. The inverse does **not** give you "changes a person made": unattributed system and device events sit at the `0` default too, alongside genuine human actions. So use the flag to lift out automation, and then separate person from system by reading the actor columns rather than the flag (§8.12.4).
5. **Cross the boundary.** If the window shows nothing, the change was not made through Fleet's server. Go to [8.12.9](#8129-other-what-changed-sources).

### High-value types when correlating

| Symptom onset | Types to look for | Why |
|---|---|---|
| Broad, many hosts, all at once | `applied_spec_team`, `applied_spec_policy`, `applied_spec_saved_query`, `edited_agent_options`, `edited_enroll_secrets` | These are the GitOps apply footprint. A burst of `applied_spec_*` at one timestamp is a GitOps run |
| One team's hosts only | `applied_spec_team`, `transferred_hosts`, `edited_agent_options` | Team-scoped config, or hosts moved into a team whose config differs |
| Specific hosts, no pattern | `transferred_hosts` | Check whether the affected host IDs appear in a transfer's `details.host_ids` |
| Policy results flipped | `edited_policy`, `created_policy`, `deleted_policy`, `reset_policy` | An edited query changes pass/fail without anything on the host changing |
| Profiles reinstalling or churning | `edited_macos_profile`, `created_macos_profile`, `deleted_macos_profile`, `resent_configuration_profile_batch`, and the Windows/Android/declaration equivalents | A profile edit triggers redelivery to every matching host |
| Enrollment newly failing | `edited_enroll_secrets`, `disabled_windows_mdm`, `disabled_android_mdm`, `enabled_gitops_mode` | Rotated secrets and disabled MDM both present as enrollment failure |
| Encryption or key access questions | `enabled_macos_disk_encryption`, `disabled_macos_disk_encryption`, `read_host_disk_encryption_key` | The read type also answers the access question |
| Query load spike on hosts | `created_saved_query`, `edited_saved_query`, `live_query` | A newly scheduled expensive report, or someone running live reports at scale |
| Config changes stopped landing | `enabled_gitops_mode` | GitOps mode makes the managed interface surface read-only. It does not restrict the API, so a change arriving that way is not evidence against it |

### Two traps

**Timezone.** `created_at` is stored as a MySQL `timestamp`, so what you read back depends on the session time zone. Pin it before comparing against anything:

```sql
SET time_zone = '+00:00';
```

The API returns RFC3339 with an explicit offset, which is one reason to prefer the API when you are correlating across tools.

**The audit log is not a state record.** An `edited_policy` row proves someone saved a change. It does not prove the change reached any host, and it does not carry the before/after values unless the type's `details` payload happens to include them. For whether it landed, go to the state tables in [8.6](8.6-server-state.md) or the host itself in [8.4](8.4-host-side-investigation.md).

## 8.12.8 Streaming it out

The audit log is one of Fleet's log streams, with its own destination plugin, separate from the osquery result and status streams and from the server's own output. Which stream is which is [8.3](8.3-server-logs.md); the full destination list is [8.2](8.2-log-surfaces.md).

### Config

From Fleet's server configuration reference, fleetdm.com/docs/configuration/fleet-server-configuration:

| Setting | Env var | Default | Effect |
|---|---|---|---|
| `activity_enable_audit_log` | `FLEET_ACTIVITY_ENABLE_AUDIT_LOG` | `false` | Master switch. Nothing streams until this is `true` |
| `activity_audit_log_plugin` | `FLEET_ACTIVITY_AUDIT_LOG_PLUGIN` | `filesystem` | Destination. No effect unless the switch above is on |
| `filesystem_audit_log_file` | `FLEET_FILESYSTEM_AUDIT_LOG_FILE` | `/tmp/audit` | Path, `filesystem` plugin only |
| `filesystem_enable_log_rotation` | `FLEET_FILESYSTEM_ENABLE_LOG_ROTATION` | `false` | Rotates the active file at `filesystem_max_size`, default 500 MB. Shared with the osquery result and status files |

Size is the only thing that rotates the active file. The other two rotation settings govern what happens to files that have already been rotated: `filesystem_max_age` keeps old files for 28 days by default and `filesystem_max_backups` keeps 3 of them, whichever runs out first. Setting either to zero keeps them indefinitely. Fleet also rotates on `SIGHUP`, which is the lever when you want a clean file to start a capture against.

YAML form:

```yaml
activity:
  enable_audit_log: true
  audit_log_plugin: firehose
```

**The default path is a trap.** `filesystem` is the default plugin and `/tmp/audit` is the default file. Turn on `activity_enable_audit_log` without setting a plugin and Fleet writes your audit trail to `/tmp` on each node, unrotated, where the OS or a container restart will eventually remove it. Set `filesystem_audit_log_file` explicitly or pick a real destination.

### Destinations that carry it

`filesystem`, `firehose`, `kinesis`, `lambda`, `pubsub`, `kafkarest`, `nats`, `splunk`, `stdout`. `stdout` needs no further configuration. Each of the others has its own flag namespace, and the configuration reference gates every one of those flags on `activity_audit_log_plugin` being set to that plugin **and** `activity_enable_audit_log` being `true`.

`webhook` appears as a destination in Fleet's log destinations guide (fleetdm.com/guides/log-destinations) and is absent from the `activity_audit_log_plugin` option list. Treat audit-over-webhook as unsupported.

### Mechanism

| Property | Value |
|---|---|
| Trigger | Cron schedule `activities_streaming`, interval **5 minutes** |
| Selection | Rows with `streamed = 0` **and `host_only = 0`**, ordered by `id` ascending, cursor-paginated on `id`. Streaming reuses the global feed query, so it inherits the global feed's exclusion |
| Batch | 500 rows per fetch, looping until a fetch comes back short |
| Write granularity | **One row per write to the destination**, deliberately, so Fleet can tell which rows landed |
| Payload | The same JSON object the API returns, one object per row |
| Bookkeeping | `streamed` flips to `1` on the rows the destination accepted |
| On error | Stops at the first failed row, marks the ones before it, retries the rest on the next 5-minute tick |
| Licensing | Fleet Premium. Fleet builds the audit logger under a Premium licence and skips it otherwise |

Consequences:

- **`host_only` rows never stream.** They are filtered out of the query the streamer runs, so they stay `streamed = 0` for good. A host-scoped event that exists in Fleet and on the host's page will never reach your SIEM through the audit-log stream, and no amount of waiting changes that. [2.8](../02-administer-and-deploy-fleet/2.8-activity-audit-logs-and-log-delivery.md) owns the delivery configuration; this is the gap in what delivery covers.
- **Up to 5 minutes of lag**, which the configuration reference states directly and [2.8](../02-administer-and-deploy-fleet/2.8-activity-audit-logs-and-log-delivery.md) repeats. Five minutes is the ceiling on the wait, not a floor: a row written just before a tick goes out almost at once, and one written just after waits the full interval. Alerting built on the audit stream has to tolerate the ceiling.
- **Backlog needs the same filter the streamer uses.** Count `streamed = 0 AND host_only = 0`. Counting `streamed = 0` alone includes every `host_only` row ever written, which are permanently unstreamed by design, and produces a large number that never falls:

  ```sql
  SELECT COUNT(*) FROM activity_past WHERE streamed = 0 AND host_only = 0;
  ```

A count that grows on that filter means the destination is rejecting writes or the schedule is not running. Check the `cron_stats` row for `activities_streaming` in [8.6](8.6-server-state.md), and the server log for the `streamed events` message with its `count` attribute.
- **Enabling streaming backfills the history.** Every pre-existing global-visible row carries `streamed = 0`, so the first run after you flip the switch walks all of them in 500-row batches. On a deployment with years behind it, that is a large one-time burst arriving at a destination sized for steady state. Size for the burst, or set an expiry window first and let it shrink the table.
- **One node streams per tick.** The schedule takes the same lock every Fleet cron takes, which lives in the `locks` table in MySQL rather than in Redis. That matters when you go looking for it: a held schedule lock is a row you can read, and [8.6](8.6-server-state.md) owns the table. It also means a stuck node holds the schedule, the same failure mode as any other Fleet cron.

## 8.12.9 Other "what changed" sources

The audit log covers action taken through Fleet's server. Four other change surfaces sit outside `activity_past` entirely.

| Source | What it covers that the audit log does not | How to read it |
|---|---|---|
| GitOps repository history | The **content** of the change. The audit log records that `applied_spec_team` happened; the repo records what the YAML said before and after, who authored it, and which PR it came from | `git log`, `git diff` on the config repo. Correlate the CI run timestamp against the `applied_spec_*` timestamps |
| Fleet server version | Fleet ships roughly every three weeks. An upgrade changes behavior without producing any activity row | `GET /api/v1/fleet/version`; the startup banner in the server log ([8.3](8.3-server-logs.md)); `migration_status_tables` in MySQL for the migration floor. Compare the version you find against its release notes at github.com/fleetdm/fleet/releases |
| Agent version | fleetd and osquery version changes are per-host and are not activities | `hosts.osquery_version` and the fleetd version in host details ([8.6](8.6-server-state.md)) |
| Host OS updates | An OS minor version bump changes query results, profile behavior, and MDM behavior. Not an activity | `hosts.os_version` and `operating_systems` ([8.6](8.6-server-state.md)) |

`fleetctl debug errors` belongs to a different question. It records server faults rather than changes, and it stays silent on client 4xx errors ([8.3](8.3-server-logs.md), [8.5](8.5-fleetctl-debug.md)).

When the audit log window comes back empty, check the Fleet version and the agent version next. Neither upgrade writes an activity row, so both are invisible to the surface you just searched, which is what makes them worth checking at exactly this point. The general method is [8.1](8.1-diagnostic-method.md).

## 8.12.10 Retention and volume

| Property | Value |
|---|---|
| Default retention | **Unlimited.** Rows accumulate for the life of the deployment until expiry is enabled |
| Expiry switch | `activity_expiry_settings.activity_expiry_enabled` |
| Expiry window | `activity_expiry_settings.activity_expiry_window`, in **days** |
| Cleanup job | `cleanup_activities`, inside the `cleanups_then_aggregation` schedule |
| Per-run delete cap | **5,000 rows**, a cap Fleet sizes as adequate for deployments generating about 120,000 activities per day |

Both expiry settings are in Fleet's YAML reference, fleetdm.com/docs/configuration/yaml-files. GitOps form:

```yaml
org_settings:
  activity_expiry_settings:
    activity_expiry_enabled: true
    activity_expiry_window: 30
```

Enabling expiry also enables live report cleanup against the same window. One switch, two effects.

**Expiry does not delete host-linked rows, at any age.** The cleanup query joins `activity_host_past` and takes only rows with no link. An activity tied to a host stays until that link goes away, and deleting the host is what removes it. [1.5](../01-foundations/1.5-audit-and-activity.md) states the same thing from the design side. Since most of what a busy deployment writes is host-linked, expiry can be working perfectly and still leave the bulk of an old table in place.

**The delete cap is the second catch.** The job removes at most 5,000 eligible rows per run. Enable a 30-day window on a table holding millions and it drains at 5,000 per run rather than in one pass.

Count what cleanup can actually take, or you will diagnose a working schedule as a broken one:

```sql
SELECT COUNT(*) AS eligible
FROM   activity_past ap
LEFT   JOIN activity_host_past ahp ON ahp.activity_id = ap.id
WHERE  ahp.activity_id IS NULL
  AND  ap.created_at < NOW() - INTERVAL 30 DAY;
```

Watch that fall over days, not minutes. If it is not decreasing, the `cleanups_then_aggregation` schedule is not running. `cron_stats` is [8.6](8.6-server-state.md). A total row count that refuses to move while this number falls is expiry working as designed.

**Expiry and streaming interact badly if you get the order wrong.** Deleting a row that still has `streamed = 0` loses it permanently. If you are enabling both, enable streaming and let the backlog drain before you enable expiry.

### Volume drivers

Automated and device-driven paths write rows without a human involved: policy-triggered software installs, automation runs, and MDM lifecycle events. Fleet publishes no figure for the split, and it will vary with how much automation a deployment runs, so measure yours rather than assuming:

```sql
SELECT fleet_initiated, user_id IS NOT NULL AS has_actor, COUNT(*)
FROM   activity_past
WHERE  created_at >= NOW() - INTERVAL 7 DAY
GROUP  BY 1, 2;
```

That split is what to size the destination against, and what to compare a volume spike to.

`activity_past` carries seven secondary indexes, among them `idx_activities_type_created` on `(activity_type, created_at)`. Filter on those two columns and the query is indexed. Filter on `details` and MySQL scans the whole JSON column.

### `preserve_host_activities_on_reenrollment`

`activity_expiry_settings.preserve_host_activities_on_reenrollment` is read in one place: the **Apple MDM** re-enrollment reset. When it is `false`, that reset deletes the host's `activity_host_past` rows and deactivates its queued MDM commands. When it is `true`, both survive.

Read the scope narrowly. The reset clears a list of host reference tables either way, `host_mdm_commands` among them, so this flag is not a general "keep the host's history" switch. What it governs is the link between the host and its past activities, plus the queue.

That link is also what expiry keys on, so the two settings meet here. Deleting the `activity_host_past` rows leaves the activities themselves in place but unlinked, which makes them eligible for the expiry cleanup they were previously exempt from. A re-enrolled Apple host on a deployment with expiry enabled can therefore lose history twice over: once from the page, and later from the table.

The default depends on how the deployment came to exist. The migration sets it to `true` where the `users` table already had rows when it ran, preserving the older behavior, and `false` on an install with no users yet. Two Fleet deployments running the same version can differ here purely by age. Read the value before you conclude that a re-enrolled host has no history. Fleet's published configuration reference does not cover this setting; treat the documentation coverage as absent.

## 8.12.11 Compliance use

`activity_past` is the surface an auditor asks for when they want a record of privileged action in the device-management platform. The rows that usually matter are `read_host_disk_encryption_key`, `viewed_host_recovery_lock_password`, `read_managed_local_account`, `changed_user_global_role`, `changed_user_team_role`, `user_logged_in`, `user_failed_login`, and `wiped_host`.

Streaming to an external destination is the part that makes it hold up, because a row in Fleet's own MySQL is deletable by anyone with database access, and expiry will remove it on schedule. Fleet does not sign, hash-chain, or otherwise tamper-proof the rows.

## 8.12.12 Questions that belong to another section

| Topic | Section |
|---|---|
| The four log streams and which is which | [8.3](8.3-server-logs.md) |
| Full destination list and per-destination configuration | [8.2](8.2-log-surfaces.md) |
| Everything host-side | [8.4](8.4-host-side-investigation.md) |
| `cron_stats`, host state tables, migration status | [8.6](8.6-server-state.md) |
| `fleetctl debug errors` and the rest of `fleetctl debug` | [8.5](8.5-fleetctl-debug.md) |
| What to attach when you escalate | [8.13](8.13-escalation.md) |

## Reference and troubleshooting

![Reference](../_assets/icons/reference.svg) The configuration keys that control activity retention and streaming destinations are in [a.3](../09-appendices/a.3-configuration-model-and-precedence.md). Whether reading or streaming activities is reachable through the UI, fleetctl, the API, or GitOps is in [a.5](../09-appendices/a.5-interface-index.md).
