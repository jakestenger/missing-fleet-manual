---
title: "Advanced osquery: query design, tables, and performance"
chapter: "IV. Know your devices"
section: "4.6"
sidebar_position: 6
status: drafting
verified_against: Fleet 4.90.1
verified_on: 2026-08-27
verified_source: "drafted against fleet-v4.90.1 (dd0200f062); citation ledger at research/section-notes/4.6-notes.md"
further_reading:
  - https://fleetdm.com/tables
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Advanced osquery: query design, tables, and performance

[4.2](4.2-run-queries-and-reports.md) covers getting a result out of Fleet. This chapter is about the query itself: why osquery's tables are not database tables, what a query costs on somebody's laptop, and how to find out before the estate finds out for you.

The governing idea is that **cost is a design decision, not an optimisation you apply later**. A query that reads a whole filesystem costs that on every host that runs it, every interval, forever, and no amount of tuning afterwards recovers the time already spent.

## Why osquery tables are not database tables

![Explanation](../_assets/icons/explanation.svg) An osquery table looks like SQL and behaves differently in ways that decide what your query costs.

**Most tables are generated on demand.** There is no stored table being scanned. When you select from a table describing processes, osquery goes and enumerates processes at that moment. The cost of a query is the cost of the work the tables do, not the cost of reading rows.

**Some tables do nothing until you constrain them.** A table describing files or directory contents needs a path to work from, and without one it either returns nothing or attempts something enormous. These are the tables where a `WHERE` clause is not a filter applied to results; it is the input that makes the table work at all.

**A join can multiply that work.** Joining two generated tables can mean generating one of them repeatedly, so a join that looks cheap in SQL terms can be the most expensive thing in your query. Where a join is against a small, cheap table, this does not matter; where it is against a table that enumerates a filesystem, it matters enormously.

**Tables differ by platform and by osquery version.** A table available on macOS may not exist on Windows, and a column added in a later osquery release does not exist on hosts below it. Neither produces a partial answer: the query fails on that host, and a failed query is not a result ([4.2](4.2-run-queries-and-reports.md)).

## Design for cost first

![How-to](../_assets/icons/howto.svg) Six habits, in rough order of how much they save.

**Constrain the expensive table, not the result.** `WHERE path LIKE '/Applications/%'` on a file table is a different query from selecting everything and filtering afterwards, even though they return the same rows. The first tells the table what to generate; the second generates everything and throws most of it away.

**Aggregate on the host.** A query returning one row per host costs a row per host. A query returning four hundred rows per host reaches Fleet's per-host storage threshold at three hosts and stores nothing for any of them ([4.2](4.2-run-queries-and-reports.md)). Count, group and limit in SQL, where the work happens once, rather than in whatever reads the results.

**Select the columns you need.** Some columns are cheap to produce and some are computed per row. Selecting everything asks for all of them.

**Prefer a cheap table that answers the question over an expensive table that answers it precisely.** A great many "is this present" questions can be answered from a package or application inventory rather than by walking a filesystem.

**Scope by platform, always.** An unscoped query reaches hosts whose tables do not exist, where it errors. Those hosts then have no answer, which is worse than being excluded, because an exclusion is visible and a missing answer is not.

**Set a minimum osquery version when the query needs one.** It keeps the query away from hosts that would fail it, which converts a silent failure into a visible exclusion.

## What osquery does to a query that misbehaves

![Reference](../_assets/icons/reference.svg) osquery protects the host from your query, and the protection is invisible from Fleet unless you go looking.

**The watchdog watches the worker process** and restarts it when it exceeds its memory or CPU limits. From Fleet's side this looks like a host that stopped answering and then came back, which is easy to read as a network problem.

**A query that repeatedly triggers the watchdog gets denylisted.** osquery stops running it on that host, for a period, and the host carries on running everything else. This is the behaviour worth internalising, because of how it presents: **the query simply stops producing results from some hosts**, with no error, no failure and no Fleet-side signal that anything was refused.

**Fleet can see it, in the per-host statistics.** Each host's scheduled query statistics carry a denylisted flag alongside the performance numbers. So the diagnosis for "this report has been getting quieter" is to look at the statistics rather than at the network.

**Denylisting is per host.** A query that is too expensive for your oldest laptops and fine on everything else produces a result set that is silently biased toward newer hardware, which is a quietly serious problem for a compliance report.

## The statistics Fleet keeps, and their shape

![Reference](../_assets/icons/reference.svg) Fleet collects per-host execution statistics for scheduled queries and aggregates them per query.

Per host you get the number of executions, the average memory, system and user CPU time, the output size, when it last executed, its interval, and the denylisted flag.

Per query, Fleet aggregates CPU time as **percentiles** across hosts, at the fiftieth and ninety-fifth, along with a total execution count. The ninety-fifth percentile is the more useful of the two: a query that is fine on the median host and painful on the slowest five percent is exactly the query that will generate complaints while looking healthy on average.

**These are current values, not a series.** Per-host statistics are overwritten as new ones arrive, so there is no history to read and no trend to plot. The method is comparison, not trend: compare a query against other queries on the same hosts, and compare the ninety-fifth percentile against the fiftieth to find the queries whose cost is concentrated somewhere.

**Output size is worth watching alongside CPU.** A cheap query returning enormous results costs the host little and costs everything downstream a great deal: storage rejection at the per-host threshold, log destination volume, and network on constrained links.

## Testing a query before the estate tests it

![How-to](../_assets/icons/howto.svg) A sequence that catches the failures above before they are distributed.

1. **Run it as a live report against one host of each target platform.** Read the error column, not only the rows. A missing table or column shows here.
2. **Run it against something old and something busy.** A clean test machine is the least informative host you own. A laptop with a full disk, a server with thousands of processes, and your oldest supported hardware will tell you what the estate will.
3. **Check what it returns per host, not in total.** Multiply by your host count and compare against the per-host storage threshold.
4. **Schedule it on a small label first**, then read its statistics after a few intervals: executions, average memory, the CPU percentiles and the denylist flag.
5. **Widen the scope** only once those numbers are boring.
6. **Re-read the statistics a week later.** A query's cost changes as the estate changes, and the moment it starts being denylisted is the moment it stops being trustworthy as evidence.

## Time, platform and version

![Reference](../_assets/icons/reference.svg) Three sources of difference that produce wrong answers rather than errors.

**Time.** osquery reports times in a mixture of units and epochs depending on the table, and a query that assumes seconds where a table gives milliseconds produces a plausible number that is wrong by a factor of a thousand. Check the units on the columns you compute with, on the platform you are targeting.

**Platform.** The same conceptual question maps to different tables per platform, and the honest way to handle that is usually several platform-scoped queries rather than one query with branching logic. Separate queries fail visibly on the wrong platform; a clever universal query fails quietly.

**Version.** A column added in a later osquery release does not exist below it, and the difference between "no rows" and "no such column" is the difference between an answer and a failure. Where a query needs a recent table or column, say so with the minimum version rather than discovering it as missing hosts.

## Building a library rather than a pile

![How-to](../_assets/icons/howto.svg) The queries that survive are the ones somebody else can pick up.

**Name for the question, not the mechanism.** "Hosts without disk encryption" is findable; "FileVault check v2" is not.

**Write the description as though the reader is not you.** What it answers, what a row means, and what it does not cover. The last of those is the part people leave out and the part that matters when a query is reused in a context it was not written for.

**Keep the platform scope and the minimum version on the query rather than in your head.** They are the two things a person reusing the query has no way to reconstruct.

**Manage them as code.** A query library in GitOps is reviewable, diffable, and recoverable, and it stops the collection from becoming a pile of near-duplicates that nobody dares delete ([6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md)).

**Prune deliberately.** A scheduled query nobody reads still costs every host that runs it, every interval. The cheapest performance work available in most deployments is deleting queries whose answers nobody has looked at in a year.

<!-- IMAGE-TODO: assets/4.6-query-cost.webp
     WHY IT HELPS: the chapter's core claim is that cost is decided at design time and that
     the failure mode is silent. A picture of where a query's cost lands, and where the
     protection kicks in, holds that better than prose.
     PROMPT: Landscape 16:9 flat vector diagram, a single left-to-right flow in four stages
     with thin #192147 arrows between them. Stage boxes, banded with alternating #F9FAFC and
     #E8F1F6: "1. Query design: which tables, what constrains them"; "2. On the host: tables
     generate rows on demand"; "3. osquery watchdog: memory and CPU limits"; "4. Fleet:
     statistics and stored results". Make stage 2 the solid anchor, #192147 fill with #F9FAFC
     text, because that is where the cost is actually paid.
     Below stage 3, a branch arrow in #D66C7B leading down to a chip reading "Denylisted on
     this host: the query silently stops returning results". Below stage 4, a branch arrow in
     #FAA669 to a chip reading "Per-host threshold exceeded: nothing stored for that host".
     Both chips share a small #515774 caption: "neither appears as an error in Fleet".
     Above stage 1, a short #515774 line: "everything downstream is decided here".
     Caption: "Cost is designed in. The protections are silent."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text. -->

<!-- SCREENSHOT: A scheduled query's per-host statistics, showing executions, average memory,
     the CPU figures and the denylisted flag together. This is the surface the chapter tells
     the reader to go to and the one they are least likely to have found. Capture a query
     with at least one denylisted host so the flag is visible in context rather than as an
     empty column. Crop to the statistics table. Highlight the denylisted column.
     Placeholder hostnames, no serial numbers. -->
