---
title: "Extend Fleet telemetry"
chapter: "IV. Know your devices"
section: "4.7"
sidebar_position: 7
verified_against: Fleet 4.90.0
verified_on: 2026-08-27
verified_source: "drafted against fleet-v4.90.0 (7c428c6e46); citation ledger at research/section-notes/4.7-notes.md"
further_reading:
  - https://fleetdm.com/docs/configuration/agent-configuration
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Extend Fleet telemetry

Everything so far in this part reads what Fleet already collects. This chapter is about the question you reach after that: **osquery does not have a table for the thing I need to know.**

There are several answers, and they differ enormously in cost, risk and how much of your life they consume. The mistake this chapter exists to prevent is reaching for the heaviest one first. Writing an osquery extension is a real engineering project with a maintenance tail, and most questions that feel like they need one do not.

## The hierarchy, lightest first

![Explanation](../_assets/icons/explanation.svg) Work down this list and stop at the first thing that answers your question.

| | Mechanism | Use when |
|---|---|---|
| 1 | **A saved or scheduled query** | The data exists in a table and you want to see it ([4.2](4.2-run-queries-and-reports.md)) |
| 2 | **A policy or a label** | The answer is a yes-or-no you want to act on or target by ([4.3](4.3-use-policies-for-compliance.md), [1.3](../01-foundations/1.3-hosts-fleets-labels.md)) |
| 3 | **A custom host vital** | The data is not on the device at all. You know it and want it on the host record ([4.1](4.1-understand-hosts-vitals-and-inventory.md)) |
| 4 | **`features.additional_queries`** | The data exists in a table and you want it **on the host record**, refreshed with the host's other details |
| 5 | **`auto_table_construction`** | The data is in a **SQLite file on disk** with no table for it. Makes a real table, no code |
| 6 | **`features.detail_query_overrides`** | A built-in detail query is wrong or harmful for your estate. An escape hatch, not an option |
| 7 | **An osquery extension** | The data genuinely is not reachable any other way |

The first three are covered elsewhere. This chapter is about four through seven.

**The question that separates a report from a host field** is whether you want an answer *about* a host or a field *on* a host. A scheduled report gives you rows you go and read. `additional_queries` puts the result on the host record itself, where it refreshes with everything else and travels with the host.

## `additional_queries`: adding fields to the host record

![Reference](../_assets/icons/reference.svg) The mechanism for putting a collected value on the host record rather than in a report.

**What it is.** A JSON object of name-to-SQL pairs in the server's feature settings. Fleet appends each one to the set of detail queries a host is asked, prefixed internally as `fleet_additional_query_<name>`, and stores the returned rows on the host as `additional` data under the name you gave.

**When it runs.** With the host's other detail queries, on the detail update interval, not on a schedule you set. That is the point of the mechanism: the field ages with the rest of the host record rather than on its own clock ([4.1](4.1-understand-hosts-vitals-and-inventory.md)).

Four properties that will surprise you.

**It is not platform-gated.** Built-in detail queries carry platform conditions. An additional query is registered with a discovery condition that is **always true**, so it is asked of every host regardless of platform. Where the table does not exist it simply fails, nothing warns you, and the failure is per host ([8.4](../08-troubleshooting/8.4-host-side-investigation.md)). If your query is platform-specific, guard it inside the SQL rather than expecting Fleet to scope it.

**It is skipped when Fleet is in a hurry.** When Fleet is refetching only critical detail queries for a host, additional queries are not included, so the field is not refreshed in that window.

**Its scope is per-fleet, but only through one path.** These are feature settings, and they can be set globally or per fleet. The per-fleet route is GitOps `spec/fleets` only: the fleets PATCH endpoint accepts a `features` object but acts on a **narrow subset** of it, and `additional_queries` is not in that subset. So a per-fleet additional query set through the API endpoint is accepted and does nothing. The same is true of `detail_query_overrides` ([6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md)).

**The name is the contract.** It becomes the key on the host record, so anything reading that key breaks if you rename it.

**Use it for** a licence key, an asset tag, a locally meaningful registry value or configuration file field, a site or building identifier that lives on the machine. Things you want to see next to a host, rather than things you want to count across the estate.

**Do not use it for anything expensive.** It now runs on every host on the detail interval, indefinitely. The design discipline in [4.6](4.6-advanced-osquery-queries-and-tables.md) applies with more force here than to a scheduled query you could simply delete.

## `auto_table_construction`: a real table, without writing code

![Reference](../_assets/icons/reference.svg) The step almost everybody skips, because it does not look like it belongs between configuration and programming. It does, and it answers a surprisingly common question.

**The problem it solves.** An application on your devices keeps its state in a SQLite database on disk. The data you need is sitting there in a file, and no osquery table exposes it.

**What it does.** Automatic table construction is an osquery feature Fleet exposes in agent options. You give it a table name and four things: the **path** to the SQLite file, a **query** to run against it, the **columns** to expose, and a **platform**. osquery then presents that as a genuine table, which you can query, join, schedule, or use in a policy exactly like any built-in.

**Why it sits where it does in the hierarchy.** It creates a new table with no code, no build, no signing and no distribution. Every reason an extension is expensive is absent. If your data is in a SQLite file, this is the answer and an extension is over-engineering.

**Its limits are the shape of its inputs.** The source has to be SQLite, and the path has to be predictable enough to write down. A path containing a user directory means one entry per user layout you support, and a file that only exists after the application has run means the table is empty rather than missing on hosts that have not run it.

**It is per platform**, by the `platform` field on each definition, so unlike `additional_queries` it does not need guarding inside the SQL.

## `detail_query_overrides`: an escape hatch with sharp edges

![Troubleshooting](../_assets/icons/troubleshooting.svg) This one replaces or removes Fleet's **own** detail queries. Fleet's contributor documentation describes it as a debugging tool and warns that it can break host data ingestion. That warning is accurate, and the mechanism behind it is worth understanding before you use it.

**How it behaves,** for each name-to-SQL pair you supply:

- Give it SQL, and Fleet **replaces the query text** for that built-in detail query.
- Give it null or an empty string, and Fleet **deletes that detail query entirely**. The host stops being asked, and whatever it populated stops being updated.
- Name something that is not a real detail query, and Fleet logs an error and **carries on applying the rest**. There is no rejection, so a typo silently does nothing while its neighbours take effect.

**Why it breaks ingestion.** Replacing the SQL does not replace the code that reads the result. Fleet keeps the original ingestion function, which expects specific column names. An override returning a perfectly sensible result set with different columns will be collected and then fail to ingest, and the host field it feeds goes stale while the configuration looks correct.

**So the rule is:** an override must return exactly the columns the original returned. Read the built-in query first and match its shape, not its intent.

**Legitimate uses are narrow:** suppressing a detail query that is pathological on your hardware, or working around a known defect on a specific platform. If you are reaching for it to *add* something, you wanted `additional_queries`.

**Record why.** An override is invisible in the interface and changes what every host reports. Six months on, an unexplained override is indistinguishable from a mistake, so it belongs in GitOps with a comment ([6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md)).

## Extensions: when the data really is not there

![Explanation](../_assets/icons/explanation.svg) An osquery extension is a separate program that registers new tables with osquery. Fleet has a managed path for distributing them and a raw path for when you are on your own.

**The managed path** is the `extensions` key in agent options: a map of extension name to a small object naming a **platform**, a **TUF update channel**, and optionally **labels**. Fleet's updater delivers the binary on that channel and orbit loads it.

Three things to know before planning around it:

- **Label scoping is Fleet Premium.** Naming labels without a licence fails the request outright. That is a genuine refusal, unlike the silent parameter dropping described in [4.5](4.5-monitor-fleet-wide-state.md).
- **A named label must already exist** on that fleet, or the request is rejected. Create the label first.
- **The platform tokens are not the query platform tokens.** An extension's platform is `macos`, where a query's platform is `darwin`. Orbit also accepts `windows`, `linux`, `linux-arm64` and `windows-arm64`, so the architecture-specific builds are addressable. The struct's own documentation comment names only three of these, so read the token list as what orbit resolves rather than what the comment says. A token orbit cannot resolve is an error, but a **valid token for the wrong platform** is simply an extension that never lands anywhere.
- **The filename is inferred, not configured.** Orbit expects `<extension name>.ext`, with `.exe` appended on Windows, under `bin/extensions/<name>/<platform>/<channel>/` in its root directory. The extension name in your configuration is therefore also a filename contract with whatever publishes to your update server.

**Orbit owns the autoload file, which rules out the obvious workaround.** osquery loads extensions listed in an `extensions.load` file, and orbit passes `--extensions_autoload` pointing at one in its root directory. It is tempting to place your own extension there and skip the update server entirely. Do not: **orbit writes that file itself** from the managed extensions configuration, filtering to the host's own platform and architecture. When Fleet sends no extensions configuration at all, orbit **truncates the file to empty and restarts osquery** specifically to unload whatever was there. A hand-placed entry survives only until the next reconciliation.

So on a host running fleetd there is no supported side door. Either the extension goes through the managed path, or you are running osquery outside orbit's management and Fleet's agent options are not what is loading it.

**What you are taking on.** An extension is code running alongside osquery on every machine you deploy it to. It needs building per platform and architecture, distributing through an update server, versioning, and maintaining against osquery's extension interface. It is subject to the same watchdog and denylisting as anything else ([4.6](4.6-advanced-osquery-queries-and-tables.md)).

**Before you write one,** confirm three things: that no existing table answers the question on that platform, that the question is worth per-host code in your estate for years, and that somebody will own it when its author leaves. The third is the one that gets skipped.

## Other collection you can turn on without writing code

![Reference](../_assets/icons/reference.svg) Two osquery subsystems sit in agent options and produce telemetry without a new table.

**File integrity monitoring** takes sets of paths to watch, path sets defined by query, and exclusions. It produces events rather than snapshot rows, so it belongs in a log destination rather than in a report ([2.8](../02-administer-and-deploy-fleet/2.8-activity-audit-logs-and-log-delivery.md)).

**YARA** takes named signature sets and the paths to scan with them, with the same event-shaped output.

**Decorators** add columns to results rather than adding tables. They come in two kinds in agent options: queries run `always`, and queries run on an `interval`. Their value is provenance: a decorator that stamps every result with an identifier you care about makes results self-describing once they leave Fleet for a log destination.

**Event subscribers** can be enabled or disabled individually. This is the control for turning off an event source that is expensive on your estate without disabling event collection entirely.

Both are worth knowing about specifically because they answer a class of question, "tell me when this file changes", that people otherwise try to answer by scheduling an expensive filesystem query every fifteen minutes.

## Choosing, in practice

![How-to](../_assets/icons/howto.svg) A sequence for a question you cannot currently answer.

1. **Search the table schema for the platform**, not for the concept. The table may exist under a name you would not have guessed.
2. **Ask whether a nearby table answers a slightly different question well enough.** An approximate answer that costs nothing often beats an exact one that costs a watchdog restart ([4.6](4.6-advanced-osquery-queries-and-tables.md)).
3. **Decide whether you want a row or a field.** Rows are a report. Fields on the host record are `additional_queries`.
4. **If a built-in is actively wrong for you**, that is the only case for an override, and match the original's columns exactly.
5. **If nothing has the data**, check whether an existing open-source extension already provides it before writing one.
6. **If you write one**, treat it as software: version it, scope it with labels, roll it out to a small population first, and watch the query statistics on the hosts that have it.

**Whatever you choose, write down why.** Every mechanism in this chapter except the first two is invisible from the interface. Whoever inherits your deployment will find an unexplained additional query, an override, or an extension on a channel, with no way to reconstruct the reasoning.

![Five ways to extend osquery, drawn as a ladder of increasing width and weight: a saved or scheduled query, a policy or label, features.additional_queries, features.detail_query_overrides, and an osquery extension. Cost, risk and maintenance rise going down; how often you should need it falls. Notes flag that additional_queries is a field on the host record not a row in a report, that detail_query_overrides can break host data ingestion, and that an extension is code you own, on every host, indefinitely.](assets/4.7-extension-hierarchy.webp)

<!-- IMAGE-OK: assets/4.7-extension-hierarchy.webp
     NOTE: accepted 2026-08-30. Sol diagram recipe (HANDOFF), rendered as a vertical ladder.
     PROMPT below.
     WHY IT HELPS: the chapter's whole argument is an ordering, and the failure it exists to
     prevent is people jumping to the bottom of it. A picture of the five options as a descending
     staircase, with cost rising as you go down, makes the argument before the prose does.
     PROMPT: Landscape 16:9 flat vector diagram. Five steps descending from upper left to lower
     right, drawn as stacked rectangles of increasing width, each labelled in #192147:
     "1. Saved or scheduled query"; "2. Policy or label"; "3. features.additional_queries";
     "4. features.detail_query_overrides"; "5. osquery extension".
     Fill steps 1 and 2 with #F9FAFC, step 3 with a light #D3E8F3, step 4 with a roughly 20
     percent tint of #FAA669, step 5 solid #192147 with #F9FAFC text, so visual weight increases
     with depth.
     Down the left edge, a thin vertical #8B8FA2 arrow pointing down, labelled along its length
     "cost, risk and maintenance". Down the right edge, a matching arrow pointing up, labelled
     "how often you should need it".
     To the right of step 3, a small #515774 note: "a field on the host record, not a row in a
     report". To the right of step 4, a #D66C7B chip reading "can break host data ingestion". To
     the right of step 5, a #515774 note: "code you own, on every host, indefinitely".
     Caption: "Stop at the first step that answers the question."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text. -->

<!-- SCREENSHOT: The extensions block of a fleet's agent options in the interface, showing at
     least two extensions with different platform tokens and channels, because the chapter's
     managed-path section is entirely about that configuration shape and readers are unlikely to
     have seen one. Crop to the agent options editor. Placeholder extension names and channel
     names. No host identifiers.
     NOTE: an earlier brief here asked for additional_queries results on a host's Vitals card.
     That was withdrawn: `additional` is stored as a JSON blob on the host record and this
     manual has not established which interface surface renders it, so the brief would have sent
     the screenshotter looking for something that may not exist. See the ledger. -->
