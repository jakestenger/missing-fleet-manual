---
title: "Self-hosting architecture and capacity"
chapter: "II. Administer and deploy Fleet"
section: "2.2"
sidebar_position: 2
verified_against: Fleet 4.90.0
verified_on: 2026-08-25
verified_source: "was verified; demoted to drafting 2026-08-27 after taking factual corrections from the Part I and Part III reviews. The stamp meant reviewed-at-this-text and the text changed. Needs a review round before it goes back. Restructured 2026-08-30: renumbered from 2.9 (P6 structural moves); claims inherited with their ledger rows."
further_reading:
  - https://fleetdm.com/docs/deploy/reference-architectures
  - https://fleetdm.com/docs/configuration/fleet-server-configuration
feature_requests:
  labels: [":product", "#g-orchestration"]
  match: ["scaling", "capacity", "high availability", "read replica", "healthz", "metrics"]
  exclude: []
---

# Self-hosting architecture and capacity

Sizing a Fleet deployment is mostly sizing MySQL. The application tier grows by adding identical instances rather than larger ones, and the constraint most likely to stop you sits between the two.

## Purpose and scope

![Explanation](../_assets/icons/explanation.svg) This chapter is about how large to make a self-hosted deployment, how to make it survive a failure, and how to confirm it is healthy. [1.6](../01-foundations/1.6-the-fleet-server.md) explains why Fleet's state is split the way it is, and that model is assumed here rather than repeated. Running it on a specific cloud is [2.3](2.3-deploy-on-aws-or-gcp.md), containers and virtual machines are [2.4](2.4-deploy-with-containers-or-virtual-machines.md), and handing the result to whoever operates it is [7.7](../07-operate-fleet/7.7-production-readiness-checklist-and-handoff.md), at the end of Part VII.

The one-time initialization that every one of those deployment paths ends on, creating the first administrator on a server that has none, lives here too, under [Complete first-run setup](#complete-first-run-setup). It sits in this chapter so the deploy chapters can point at one place rather than each repeating it, and so the step that turns a running-but-empty server into one you can sign in to is not left implicit.

It teaches the self-hosting model and the reference paths well enough to run Fleet. It does not walk through every possible topology, so where a specific build belongs with Fleet's own reference architectures rather than here, it points you there.

None of this applies to managed cloud hosting, where these are Fleet's decisions rather than yours. [2.1](2.1-administration-model-and-deployment-choices.md) covers which path you are on.

## What a deployment requires

![Reference](../_assets/icons/reference.svg) Fleet needs MySQL, Redis, and a TLS certificate. Object storage is a fourth requirement, though not from the first day, for reasons covered below. The version numbers below are worth looking up when you're provisioning, not memorizing; this chapter's closing note explains why.

**MySQL 8.0.44 is the floor.** Fleet is tested against 8.0.44, 8.4.8 and 9.5.0, and **9.6.0 is currently incompatible**. On Fleet a newer MySQL is not automatically a safer one, so an automatic minor-version upgrade on a managed database service can take a working deployment down. Check the version in both directions before you let anything upgrade itself. MariaDB and other drop-in replacements are unsupported.

**Redis 6.2 is the floor.** A release in the 4.86 line added a Redis-backed cache for host lookups on the osquery and orbit authentication paths and states that requirement in its own release notes, so a deployment on an older Redis is below the floor for the agent-facing path rather than merely untested. Fleet is actively tested against 6.2 and 7, and its own reference architectures specify Redis 7.x at every size.

**The TLS certificate has a requirement that catches people**, because it is enforced by the agent rather than by Fleet. The CNAME or a subject alternate name on the certificate must match the hostname that osquery uses to connect. A wildcard SAN covers a subdomain, so `*.example.com` serves `fleet.example.com`. If you use a self-signed certificate, the full chain has to reach the agent, which on the host side is the `--tls_server_certs` flag rather than anything Fleet controls.

**If Fleet terminates TLS itself, prefer an ECDSA certificate over RSA.** This is not a general security preference. RSA certificates have been associated with a specific performance problem in Fleet, arising from Go's standard library TLS implementation, and the cost lands on the Fleet process rather than on the proxy. If a load balancer terminates TLS instead, the question does not arise.

## Complete first-run setup

![How-to](../_assets/icons/howto.svg) A freshly migrated server has no organization and no user who can sign in yet. **Setup is a one-time step: the route that creates the first administrator stops existing once one has been created**, on every deployment path in this part.

Loading the server's URL in a browser shows the setup screen, which asks for a name, email, password and organization name, then signs you in directly. The command-line equivalent is the only `fleetctl` command that runs before you have credentials, which is why it takes those same values as flags rather than reading them from a saved context:

```
fleetctl config set --address https://fleet.example.com
fleetctl setup --email admin@example.com --name "Admin" --org-name "Example Org"
```

`config set --address` points the client's active context at the server. Leave `--password` off and `setup` prompts for it twice, interactively, so it never lands in shell history or a process listing; pass it as a flag only in a throwaway environment.

**`setup` creates the administrator and writes their API token into the context you just pointed at the server, in the same call.** `fleetctl get config` immediately afterward is enough to confirm both worked, since a missing or stale token makes that call fail.

Run `setup` again against the same server and it fails on purpose: the client reports `Fleet has already been setup` rather than creating a second administrator. That is Fleet's server refusing the route because it no longer exists, not a client-side guard, so the setup screen fails the same way.

This is what [2.3](2.3-deploy-on-aws-or-gcp.md), [2.4](2.4-deploy-with-containers-or-virtual-machines.md), and every other first deployment in this part means by "create the first administrator through the setup screen."

## What grows with your estate, and what does not

![Explanation](../_assets/icons/explanation.svg) Fleet publishes sized configurations for **its AWS reference architecture**, from five thousand hosts up to three hundred thousand. These are starting points to measure from, not capacity guarantees, and they describe one deployment on one cloud. Pick the row matching your enrolled host count, then revise it against your own reporting volume, API traffic and availability target.

Three of the eight published breakpoints, with the per-instance connection limit included because the next section turns on it:

| Up to | Fleet instances | Per instance | `max_open_conns` | Redis | MySQL |
|---|---|---|---|---|---|
| 5,000 hosts | 6 | 1 CPU, 4GB | 10 | 3 nodes, `cache.t4g.small` | 2 nodes, `db.t4g.medium` |
| 50,000 hosts | 15 | 1 CPU, 4GB | 20 | 3 nodes, `cache.r6g.large` | 3 nodes, `db.r6g.xlarge` |
| 300,000 hosts | 70 | 1 CPU, 4GB | 20 | 3 nodes, `cache.r6g.xlarge` | 3 nodes, `db.r6g.8xlarge` |

Read across the range rather than picking a row out of it, because the shape of the change carries more than any single row. Three things hold steady across a sixtyfold increase in hosts, and each is a design decision showing through.

**The per-instance allocation does not change.** Across every AWS row it stays at one CPU and four gigabytes, from the smallest deployment to the largest, and the count rises from six to seventy instead. The GCP rows hold their allocation constant too, at one CPU and one gibibyte, so the pattern is not an artefact of one cloud. This is the disposable server from [1.6](../01-foundations/1.6-the-fleet-server.md) expressed as a purchase order: because no instance holds state the others need, capacity in the application tier comes from adding instances rather than enlarging them.

**Redis stays at three nodes throughout** and climbs only two instance classes across the whole range. Redis holds work in progress, and work in progress does not accumulate the way records do. Sizing Redis by host count overshoots badly. The thing that actually stresses Redis is result volume from a single wide report, which is [1.6](../01-foundations/1.6-the-fleet-server.md)'s output buffer limit and is not a capacity question at all.

**MySQL absorbs nearly all of the growth.** It is the only component moving substantially in both node count and instance size. When a deployment is slow for capacity reasons rather than configuration ones, MySQL is where to look first.

The practical consequence is that the sizing conversation you need to have with whoever owns databases is a much longer conversation than the one you need to have with whoever owns compute.

![Fleet's AWS reference architecture, from 5,000 to 300,000 hosts, on one shared host-count axis. The Fleet instances layer scales out, from six same-size squares to about seventy. Redis stays three nodes throughout but each node is larger. MySQL grows in both node count and size, drawn as one small block becoming a much larger one.](assets/2.2-what-scales.webp)

<!-- IMAGE-OK: assets/2.2-what-scales.webp
     NOTE: accepted 2026-08-30. Sol diagram recipe (HANDOFF); working prompt at
     reviews/phase2/imgtest3/scale-v2-prompt.txt. Brief de-AI'd (dropped the 'sixty times the
     hosts' slogan) and corrected: MySQL is drawn 2 blocks -> 3 larger blocks to match the
     reference table (2 to 3 nodes, and up in size), so the visual carries both count and size.
     PROMPT: Landscape 16:9. Three horizontal tracks stacked vertically, sharing one x-axis
     labelled at the left "5,000 hosts" and at the right "300,000 hosts". Title the whole
     figure "Fleet's AWS reference architecture" so it is not read as a universal rule. Top
     track, "Fleet instances": six small identical squares at the left becoming seventy of the
     same size at the right, drawn as a dense row rather than counted out, labelled "same size,
     six to seventy". Middle track, "Redis": three stacked circles at the left and three
     stacked circles two classes larger at the right, labelled "three nodes throughout".
     Bottom track, "MySQL": a small filled block at the left growing into a much larger filled
     block at the right, labelled "grows in both node count and size". Draw the MySQL track at the heaviest stroke weight and give its right-hand block
     a solid fill so it anchors the composition. Use one dot colour per track, matching the
     store colours used elsewhere in the book: Fleet instances neutral, Redis #FAA669, MySQL
     #5CABDF. Caption: "Sixty times the hosts. Each layer changes differently."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted. Five large panels at full
     saturation looks like a children's infographic, not a technical manual. And do not
     colour a panel that has no content in it; colour has to carry meaning, and an empty
     coloured box is just noise.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart. Take only as many as
     there are categories; do not spread all six across four items to look lively. If nothing
     in the picture needs distinguishing, use none of them and let the structure set carry it.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text.
 -->

## Database connections are a shared budget

![Explanation](../_assets/icons/explanation.svg) One number does not appear in the sizing tables as a resource, and it is the one most likely to stop a deployment that was scaling comfortably.

Each Fleet instance opens its own pool of connections to MySQL. **The default limit is fifty per instance.** Every one of Fleet's own sized configurations sets it to ten or twenty instead, at every scale including the largest.

The number the database sees is instances multiplied by the per-instance limit, so scaling the application tier out scales connection demand with it. Twenty-five instances left on the default would ask for as many as twelve hundred and fifty connections. The same twenty-five at Fleet's recommended twenty ask for five hundred. Treat that product as a worst-case ceiling rather than a count of connections actually held open. The symptom when you exceed it is connections being refused, which presents as a database fault and is arithmetic.

Fleet's own documented rule is to divide: take the connections your database will accept, divide by the maximum number of Fleet instances you will ever run at once, and leave a margin. Include the margin deliberately, because autoscaling and rolling deployments both mean the real instance count briefly exceeds the steady-state one.

The settings are `mysql.max_open_conns`, `mysql.max_idle_conns`, which defaults to the same fifty and should not exceed the open limit, and `mysql.conn_max_lifetime`, which defaults to unlimited so Fleet reuses a connection indefinitely unless something else closes it.

**A read replica has its own pool.** `mysql_read_replica.max_open_conns` is configured separately and inherits nothing from the primary, so a deployment with replicas has a second budget to calculate rather than a shared one.

**Connections are not the only ceiling.** Fleet uses prepared statements, and MySQL's `max_prepared_stmt_count` has to be at least `max_open_conns` multiplied by the maximum number of Fleet servers, multiplied by four. On a default MySQL this is reached well before anyone thinks to look at it, and the same scaling-out decision drives both numbers.

## Async host processing moves check-in writes off the writer

![Explanation](../_assets/icons/explanation.svg) The connection budget above is one way to protect the single writer. Async host processing is another, and it works by taking a class of writes off the synchronous check-in path altogether. It is off by default, and it is a configuration choice rather than a licensed feature, so it is available and identical on Fleet Free and Premium.

Four kinds of per-host update normally happen inline, as part of each host's check-in: label membership, policy membership, the host's last-seen timestamp, and scheduled query statistics. Under a check-in storm, when many hosts enrol or report at once, every one of those writes lands on the MySQL writer at the same moment. Turning async processing on changes the path. Fleet buffers these updates in Redis, and a collector drains them to MySQL in batches on an interval. The writer sees fewer, larger writes in place of many small synchronous ones, and that is what relieves it under load.

The cost is Redis memory to hold the buffer, and a lag before an update reaches MySQL, normally about one collection interval plus jitter, so membership and last-seen are briefly stale. A skipped collection lock, a failed or timed-out collection, or a backlog can stretch that lag beyond a single interval.

The defaults describe the batching, and they are not uniform, which is worth knowing before you reason about the load the collector produces. Thirty seconds is its nominal default interval, spread by up to ten percent of jitter so collectors do not all fire at once, and it holds its collection lock for up to a minute. Inserts and deletes flush two thousand rows at a time, updates one thousand, and the Redis side pops one thousand entries and scans one thousand keys per pass. The toggle is `osquery_enable_async_host_processing`, `false` until you set it.

**Test this against a non-production deployment before you enable it**, for two reasons. Fleet publishes no figures for how much it helps or costs, so the only way to know whether it earns its place on your estate is to measure the writer and Redis before and after ([7.4](../07-operate-fleet/7.4-observe-progress-and-service-health.md), [7.5](../07-operate-fleet/7.5-maintain-capacity-and-availability.md)). And it interacts with automations that watch the same state it now updates late: with the failing-policies webhook also enabled, some transitions between passing and failing can be missed. [8.14](../08-troubleshooting/8.14-degradation.md) covers that interaction from the degradation side.

## Where Fleet sits in the network

![How-to](../_assets/icons/howto.svg) Fleet listens on all interfaces on port 8080 by default and serves TLS itself unless you tell it otherwise. In most production deployments a load balancer terminates TLS and Fleet sits behind it, which is also what lets you run the many identical instances the sizing tables assume.

**Outbound traffic matters more than people expect.** Vulnerability processing and device management both need Fleet to reach the internet. Fleet honours the conventional `HTTP_PROXY`, `HTTPS_PROXY` and `NO_PROXY` environment variables, and there is one thing to be clear about: `NO_PROXY` is an exclusion list, not a fallback. Hosts named there connect directly. Nothing named there gets a second attempt through the proxy if the direct connection fails.

Putting Fleet behind a proxy also changes what it can see about your devices. Fleet works out a device's public IP from request headers, checking `True-Client-IP` first, then `X-Real-IP`, then the first value in `X-Forwarded-For`, and falling back to the connection's remote address. **Behind an ingress proxy that sets none of those headers, that fallback is the proxy's own address**, so every host appears to share one public IP. If public IP is something you rely on, confirm your proxy sets one of the three headers rather than assuming it does.

## Built-in and commonly configured egress destinations

![Reference](../_assets/icons/reference.svg) The proxy settings above govern how outbound traffic leaves; this is what that traffic is. Some of it is built in and happens on a default deployment, some appears only once you turn a feature on, and a few classes are defined entirely by addresses you supply. What follows is the built-in set together with the destinations that MDM, Fleet-maintained apps, webhooks and integrations add once you enable them. It is deliberately not the whole list: certificate-authority integrations (SCEP, EST, DigiCert and OIDC endpoints) and any software-download URLs you register are reached at addresses only you know, so they cannot be enumerated here.

The rows differ in kind. Most of what Fleet reaches out for is a download, pulling data and putting nothing of substance about your estate in the request, and a smaller set carries real data outward. Fleet's own device geolocation is a local lookup against a bundled database and never leaves the server, so it is not in the map.

| Destination | What sets it off | What leaves Fleet | Turning it off |
|---|---|---|---|
| `github.com`, `api.github.com`, `epss.cyentia.com`, `www.cisa.gov` | The vulnerability job, hourly by default, when software inventory is on and a database path is set | Download only. No host or inventory data is in the request | `vulnerabilities.disable_data_sync=true` with the feeds supplied another way ([4.4](../04-know-your-devices/4.4-understand-software-and-vulnerabilities.md)). Runs on every edition, and the job itself can be switched off on a node ([8.6](../08-troubleshooting/8.6-server-state.md)) |
| `fleetdm.com`, at `receive-usage-analytics` | Usage analytics, hourly, on by default | The usage payload described below | `enable_analytics=false` on Free. On Premium it keeps sending unless the licence permits disabling telemetry ([a.3](../09-appendices/a.3-configuration-model-and-precedence.md)) |
| `fleetdm.com`, at `get-human-interpretation-from-osquery-sql` | Someone uses Autofill to draft a policy's description and resolution from its SQL | `{"sql": <the policy SQL>}`, and nothing else | `ai_features_disabled=true`. All editions |
| `maintained-apps.fleetdm.com`, falling back to `raw.githubusercontent.com` | The Fleet-maintained apps catalogue sync, at startup and hourly on every Premium server, whether or not an app is selected | Download only | No per-use switch: it runs on every Premium server. Premium |
| The vendor's own installer URL | Adding or installing a Fleet-maintained app | Download only, from the vendor | Do not add maintained-app installers. Premium |
| `gdmf.apple.com` | Reading or enforcing Apple OS-version data | Download only | Tied to Apple MDM being configured. Apple MDM |
| `login.microsoftonline.com` | Validating a Windows automatic-enrollment token | Download only, of the token signing keys | Do not use Windows automatic enrollment. Windows MDM |
| `api.push.apple.com` and its development hosts | Sending an MDM command, to wake a device | The push notification: the device push token and the topic. No command body, the device then checks in | Do not configure Apple MDM. Apple MDM |
| `fleetdm.com` (APNs certificate signing) | One-time APNs certificate setup | The unsigned certificate request, which carries your organization name and the administrator email embedded in it | Use the manual APNs certificate path. Apple MDM |
| `mdmenrollment.apple.com` | The Apple Business Manager device-sync job | Your server token, to authenticate, and it pulls back the assigned-device list | Do not configure ABM. Apple ADE |
| `vpp.itunes.apple.com` | Assigning apps bought through Volume Purchasing | Your VPP server token and the app-to-user assignment | Do not configure VPP. Apple VPP |
| `fleetdm.com`, at `/api/vpp/v1/metadata` and `/auth` | Looking up VPP app metadata | To `/auth`, your Fleet server URL and your Fleet licence key; the metadata request carries adam IDs and a region | Supply a bearer token to reach Apple's `api.ent.apple.com` directly instead. Apple VPP |
| `fleetdm.com/api/android/` | Android management operations, an hourly poll, and enterprise binding | Ongoing calls carry the licence key. Creating the enterprise sends the licence key, the Fleet Pub/Sub callback URL, the enterprise token, the signup URL name, the enabled notification types, and the Fleet server URL | Bring your own Google project to reach `androidmanagement.googleapis.com` and Cloud Pub/Sub directly. Android MDM |
| The `destination_url` you set on each webhook | Activity, failing-policy, host-status, vulnerability, and macOS migration events | The event payload, including host and policy or vulnerability detail | Leave the destination empty. The per-webhook contracts are [6.5](../06-automate-fleet/6.5-integrations-webhooks-and-external-workflows.md)'s. Editions vary by event |
| Jira, Zendesk, Google Calendar, SMTP, and your identity provider | The matching ticketing, calendar, mail, or sign-in event | The ticket, calendar, email, or sign-in payload | Do not configure the integration. Editions vary |

**GitHub downloads can be redirected.** A GitHub asset download may follow a redirect that Fleet does not control, so the host that finally serves a downloaded file is not always one of the fixed names above. Treat the GitHub rows as the addresses Fleet starts from rather than a guarantee of where every byte arrives from.

**What the usage payload contains.** Each hourly send carries an anonymous identifier and the Fleet version, the licence tier, and the organization (the licensed organization name on Premium, and the literal value `unknown` on Free); scale counts (enrolled hosts, users, teams, policies, queries, and software and CVE totals); feature flags (including whether AI features and telemetry are enabled); per-version host breakdowns by operating system and agent version; and the server's stored errors, serialized, which include an `error_message` and `error_additional_info` whose contents depend on what failed. It does not carry hostnames, host identifiers, IP addresses, or query results. Why a Premium deployment cannot simply switch this off is [a.3](../09-appendices/a.3-configuration-model-and-precedence.md)'s subject.

**A few senders are not the Fleet server.** The ChromeOS agent contacts `ipapi.co` when a live query reads its geolocation table, and that request tells `ipapi.co` the device's public IP; the agent's own update server is covered with the agent ([3.8](../03-connect-devices/3.8-manage-fleetd-orbit-and-updates.md)); and `fleet-mcp` is a separate binary rather than part of the server. They sit here so the boundary of the map is clear: it covers the Fleet server and the destinations you configure on it, not every component in the wider Fleet system.

## Shared object storage is required before you scale software distribution

![Explanation](../_assets/icons/explanation.svg) Fleet starts without a bucket configured and does not refuse to run. That is what makes this a capacity decision rather than a prerequisite, and it is why it tends to be discovered late.

What breaks at the second instance depends on where each artifact falls back to, and the two destinations behave very differently.

| Artifact | Without object storage | What that means with several instances |
|---|---|---|
| File carves | MySQL | Stays shared and keeps working. Grows the database |
| Apple bootstrap packages | MySQL | Stays shared and keeps working. Grows the database |
| Software installers | Local filesystem | Breaks. Not shared, and the default location is a temporary directory |
| Software title icons | Local filesystem | Breaks. Same reason |

The database-backed fallbacks survive horizontal scaling because every instance reaches the same database. The filesystem-backed ones do not: an installer uploaded through one instance is invisible to every other instance, and to whichever instance the load balancer hands the download to. Fleet logs that it is using a local store and that this is not suitable for production, once for installers and once for icons.

So the rule is conditional rather than universal. If you deploy software or use Apple setup experience assets, configure object storage before the second instance. If you use neither, the fallbacks stay shared and the decision becomes one about database growth instead. Since every sized configuration above the smallest specifies at least six instances, most deployments built to those figures need it. A single-instance deployment is a different case and can be a legitimate small production one, as [2.4](2.4-deploy-with-containers-or-virtual-machines.md) covers.

## Availability, and the constraint that shapes it

![Explanation](../_assets/icons/explanation.svg) Fleet supports exactly one writable database. Read replicas are supported and help some access patterns, particularly API and command-line traffic. Multi-writer and group replication setups are not supported.

**The Fleet instances must reach a writer in the same datacenter.** Cross-region replication is available for failover, but writes have to be local. This one constraint decides the shape of most availability designs: you are building a single-region deployment with a failover story, not an active-active one across regions, and no amount of load balancer configuration changes that.

The rest is conventional. Put a load balancer in front of the Fleet instances, which also lets it terminate TLS and hand the Fleet process back that compute. Run Redis with the three nodes the reference configurations specify. Use a database service that supports automated backups and managed failover, and turn both on.

**The agents absorb more than most people expect.** When Fleet is unreachable, osquery keeps running its existing configuration and buffers results locally until the server comes back, bounded by the agent's own buffer setting. A Fleet outage is not a data-loss event within that buffer. Fleet's guidance treats short planned downtime as ordinary for this reason, which is the argument to make to whoever approves maintenance windows.

## Upgrades take the service down on purpose

![How-to](../_assets/icons/howto.svg) Database migrations are meant to run with the server offline. The sequence is to stop Fleet, run `fleet prepare db`, then start the new version.

Fleet's own guidance puts the window at seconds to minutes, even on large installations. Back the database up first, every time. Agents buffer through it, so the cost is a gap in freshness rather than a hole in your history.

Leaving old instances serving traffic against a partly migrated schema is not a supported configuration. Fleet's instruction is that all instances are shut down for the migration, so a rolling upgrade is not a supported alternative to the outage.

## Verification and ownership

![How-to](../_assets/icons/howto.svg) Fleet exposes a health check at `/healthz`, and it is the right target for a load balancer. It returns 200 when the server is running and its connections to MySQL and Redis are healthy, and 500 when they are not. Individual dependencies can be checked with `/healthz?check=mysql` or `/healthz?check=redis`, which is how you tell the two apart quickly during an incident.

**Know what that check does not cover.** It tests exactly two dependencies. Object storage is not among them, so a deployment returning 200 can still be failing to deliver software installers. The response body carries no detail either; the reason a check failed is written to the server logs, not returned to the caller.

**Metrics are switched off until you configure them.** Fleet serves Prometheus-compatible metrics at `/metrics`, but that endpoint does not exist unless you set a basic auth username and password for it, or explicitly disable the auth requirement. With neither set, Fleet logs that the metrics endpoint is disabled at startup and serves nothing there. Anyone who points Prometheus at a fresh deployment and gets nothing back should read the startup log before debugging the scrape configuration.

Worth alerting on, from Fleet's own recommendations: host enrollment departing from its expected level, latency rising on HTTP endpoints, and error rates rising on them. Enrollment count is the one that catches problems the other two miss, because a deployment can be healthy by every infrastructure measure while hosts quietly stop checking in.

Ownership here divides along existing team lines, with one exception that falls between them.

| What | Owner | What they verify |
|---|---|---|
| The database, its version pinning, backups and failover | Whoever runs databases | That the version is inside Fleet's tested set, and that a restore has been performed |
| The TLS certificate and its renewal | Whoever runs TLS | That the name still matches what agents connect to |
| Connection limit arithmetic | Assign this deliberately | Instances multiplied by the per-instance limit, against what the database accepts |

The third row has no natural owner, which is why it needs an assigned one. It is the only number in the deployment that changes meaning whenever either of the other two teams acts alone.

## Shared database capacity and credential handling

![Reference](../_assets/icons/reference.svg) A few behaviours here are easy to get wrong in ways that are hard to attribute afterwards.

Running several Fleet deployments against one database server needs the server resourced for all of them together. Fleet's guidance is explicit that this requires more provisioning and offers no formula for it.

Pass credentials to Fleet through environment variables or a configuration file rather than command line arguments. Anything on the command line is visible to process listing, and it is also captured in the debug archive that Fleet asks for when you report a performance problem, which means it has to be scrubbed by hand before the archive leaves your organization.

A single virtual private server can carry hundreds and possibly thousands of hosts. The sized configurations start at five thousand for a reason, and reaching for the smallest of them at a few hundred hosts buys availability rather than capacity.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting.svg) For deciding whether a slow or failing deployment is a capacity problem at all, [8.1](../08-troubleshooting/8.1-diagnostic-method.md) is the place to start, and [8.6](../08-troubleshooting/8.6-server-state.md) covers reading server state directly.

Generating and interpreting a debug archive is [8.5](../08-troubleshooting/8.5-fleetctl-debug.md), including how to aim it at one instance behind a load balancer rather than at whichever one the balancer picks.

Configuration settings named here are documented in Fleet's own configuration reference; [a.3](../09-appendices/a.3-configuration-model-and-precedence.md) holds this manual's account of how server settings relate to what is set in the interface.

## Version notes

![Reference](../_assets/icons/reference.svg) Verified against Fleet 4.90.0. The MySQL version window is the most perishable fact in this chapter: 8.0.44 as the floor, 8.0.44, 8.4.8 and 9.5.0 tested, 9.6.0 incompatible. Confirm it against the release you are deploying rather than trusting this list, because it moves independently of everything else here.

The sized configurations are Fleet's published reference architectures and describe one opinionated deployment on one cloud. Treat the pattern across them as durable and the specific instance classes as a starting point to measure from.

The connection limit default of fifty has been stable for a long time, but it is a default rather than a recommendation, and the gap between it and Fleet's own sized configurations is deliberate rather than an inconsistency to resolve.
