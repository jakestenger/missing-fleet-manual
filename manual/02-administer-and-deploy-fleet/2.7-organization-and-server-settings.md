---
title: "Organization and server settings"
chapter: "II. Administer and deploy Fleet"
section: "2.7"
sidebar_position: 7
verified_against: Fleet 4.90.0
verified_on: 2026-09-05
verified_source: "verified against fleet-v4.90.0 (7c428c6e46) and carried through review rounds 9-12; citation ledger at research/section-notes/. Renumbered from 2.4 during the Part 6 structural moves (2026-08-30); claims inherited with their ledger rows."
further_reading:
  - https://fleetdm.com/docs/configuration/fleet-server-configuration
  - https://fleetdm.com/docs/configuration/yaml-files
feature_requests:
  labels: [":product"]
  match: ["server_url", "org settings", "host expiry", "activity expiry"]
  exclude: []
---

# Organization and server settings

![Explanation](../_assets/icons/explanation-light.svg) Fleet’s settings let you make the service recognizable to employees, connect it to your organization’s systems, and decide how long to keep device history. Some changes belong to Fleet administrators; others belong to the people running the infrastructure. Knowing where a setting lives helps you make the change once and verify it in the right place.

## Purpose and scope

Use this chapter when choosing Fleet’s address, setting organization details, planning retention, or coordinating changes to the server process. It explains the decisions that benefit from early planning and the ownership needed to keep them consistent.

For individual keys, [a.3 Configuration sources, scopes, and precedence](../09-appendices/a.3-configuration-model-and-precedence.md) catalogs all 320 registered settings with their defaults and usage strings. This chapter provides the operating context for those values. Read Fleet’s configuration reference alongside the known documentation and registration differences in [8.14](../08-troubleshooting/8.14-degradation.md) when checking a default or whether a key takes effect.

## Two kinds of configuration

![Explanation](../_assets/icons/explanation-light.svg) **Organization settings** control Fleet’s name, contact information, retention, enabled features, and integrations. They live in Fleet’s application configuration and can be managed through the supported UI and API controls or reviewed as code. Four status values are server-owned: Apple management enablement, Apple Business Manager enablement and its terms state, and Android enablement. Fleet restores those values if you attempt to write them directly ([a.3](../09-appendices/a.3-configuration-model-and-precedence.md)).

**Server settings** are the ones the process needs in order to run: database and Redis connections, TLS, logging, proxying, process behaviour. They are supplied to the Fleet server as command-line flags, environment variables or a configuration file, in that order of precedence, they are owned by whoever owns the infrastructure, and they change when the deployment changes rather than when policy does.

The practical test for which is which: **if changing it requires restarting Fleet, it is a server setting.** If Fleet can apply it while running, it is an organization setting.

Manage organization settings through GitOps wherever you can, so that changes are reviewed, attributable, and reversible. Keep secrets out of source control. Details are in [6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md).


<!-- IMAGE-TODO: assets/2.7-configuration-storage-planes.webp
     QUESTION: Which settings are stored in Fleet, and which start the server process?
     PROMPT: DIAGRAM: Two parallel input-to-consumer paths. Organization settings: UI / API / GitOps
     to Stored organization configuration to Running Fleet, labelled Apply while running. Server
     settings: Flags > Environment > Config file, a precedence ladder, to Fleet process startup,
     labelled Restart to change. Example chips may say Branding / retention and MySQL / TLS
     respectively. A small server-owned badge beside stored organization configuration notes Some
     enablement fields are restored by Fleet. Never draw a universal precedence chain that places
     GitOps above process flags.
     TERMINOLOGY: Fleet is the company, product, or server. Host groups are lowercase
     fleet/fleets, including headings and labels. Do not call these groups teams. Preserve
     exact code/API identifiers. These instructions are not text to render.
     DESIGN: Flat vector technical diagram. Fleet is software for managing computers; draw no
     vehicles. Use Inter labels and Roboto Mono identifiers. At 1400 px source width use 48 px
     titles, 36 px body labels, and at least 28 px secondary text; scale proportionally. Check at
     720 px reading width and intended print size. Use a 32 px spacing grid, at least 24 px node
     padding, and consistent corner radii. Center short node names; left-align multiline
     explanations. Never shrink text to fit. Use #F9FAFC background, #192147 headings and primary
     connectors, #515774 text, #8B8FA2 secondary connectors, #C5C7D1 borders, and #D3E8F3 or #E8F1F6
     quiet fills. Use #5CABDF and #C98DEF for named categories, #3AEFC4 for labelled positive
     outcomes, #D66C7B for labelled failures, and #FAA669 for labelled cautions. Tint large panels
     to 20 to 25 percent; full strength is for small marks. Keep text navy or slate, or off-white on
     a navy anchor. Never rely on colour alone. Use one arrowhead shape, consistent stroke weights,
     box-edge termination, and labelled branches and return paths. Keep connectors clear of text. No
     gradients, shadows, decorative icons, logo, watermark, em-dashes, or slogan footer. Render only
     the specified reader-facing labels. Choose orientation to fit the relationship, not a default
     poster. Keep captions outside the artwork. If labels crowd, split the figure before shrinking
     them. Keep editable SVG when the production method supports it.
     NOTE: Proposed 2026-09-08; editorial brief, not technical re-verification. Replace the two
     opening definition paragraphs with a concise comparison and this storage/consumer diagram.
     Retain the exact four server-owned exceptions and secret-handling advice. Keep current prose
     and this TODO until the actual image is reviewed. Then check alt text against the artwork and
     retain an accessible summary plus all required technical qualifications.
     CANDIDATE: ../../research/visual-reviews/part-2/assets/2.7-configuration-storage-planes.webp
     Rendered and inspected for the overnight batch; awaiting final joint review.
-->

<!-- IMAGE PENDING. Install reviewed artwork, then activate the image line below.
![Stored organization settings apply while Fleet runs; process settings resolve from flags, environment, and configuration file at startup.](assets/2.7-configuration-storage-planes.webp)
-->

## The server URL is the one to get right first

![Explanation](../_assets/icons/explanation-light.svg) Choose a stable server URL before enrollment. It becomes part of the connection between Fleet, devices, administrators, and external management services, so changing it later requires coordination and can interrupt management.

`server_url` is the base address that people, automation, and devices all use to reach Fleet. It is baked into what fleetd receives, what fleetctl talks to, what your identity provider redirects to, and what platform management services call back on.

**If the server URL changes after Apple devices have MDM turned on, end users have to turn MDM off and back on to restore MDM features.** That is a per-device, human-performed action across every Mac, iPhone, and iPad you manage. There is no administrative shortcut for it.

**On Android the URL cannot change at all.** The Pub/Sub subscription that carries device events is created against one address and Fleet has no way to move it, so a URL change breaks Android management rather than degrading it. [2.12](2.12-bind-android-enterprise.md) covers what that commits you to.

So plan the DNS name, the TLS certificate, the reverse proxy, and any URL path before enrolling a single device, and choose a name you will still be happy with after a reorganization or an acquisition. A vendor-neutral hostname you control outlasts one named after a team or a project.

Two related settings exist for the cases where one URL is not enough:

- **`sso_server_url`** covers the case where administrators sign in at a different address from the one devices use. When it is set, SSO works only from that URL.
- **`server_url_prefix`** serves Fleet under a path such as `/apps/fleet`, with no trailing slash. Note the naming here: this one is organization configuration, while the process-level `server.url_prefix` is a different setting in a different plane, and the two are easy to confuse when searching. Changing it also changes the URLs given to osquery through its flagfile, the configuration Fleet serves to agents, the prefix fleetctl uses, and the redirect URL registered with your identity provider. It is a coordinated change across four places rather than one setting.

## Organization name, logo, and contact URL

![Reference](../_assets/icons/reference-light.svg) `org_info` holds your organization's name and logo. Fleet accepts a light-mode and a dark-mode logo, each as either a path or a URL, and you set one or the other for each rather than both. Square logos work best at 256 by 256 pixels; a wordmark at 516 by 256.

Set **`contact_url`** to your internal help desk or support destination. Fleet includes it in end-user error messages, giving employees a direct path to the people who can help. Its default is Fleet’s own contact page. The organization logo also appears on the My device page, helping employees recognize that the service belongs to their organization.

These settings apply organization-wide only; there is no per-fleet version.

<!-- SCREENSHOT: SS-2.7-01 | Organization branding and support details
     PURPOSE: Show where administrators make Fleet recognizable and direct employees to internal support.
     PREPARE: Prepare a fictional organization name, demo logo, and example help-desk URL.
     CAPTURE: Settings > Organization settings > Organization info, showing the name, logo controls, and contact URL.
     FRAME: One panel crop with all relevant labels; use a neutral demo logo and a non-sensitive support address.
     EDITION: 4.91; reuse for 4.90 only when the visible controls and wording match
     PRIVACY: Use demo names and non-sensitive data; exclude tokens, passwords, keys, and personal identifiers.
     FILENAME: ss-2.7-01.png
-->

## Retention: how long Fleet keeps hosts and history

![Reference](../_assets/icons/reference-light.svg) Host and activity expiry help you balance useful history with database growth. Both are off by default; choose windows that fit your device lifecycle and investigation needs.

**Host expiry** removes a host record when it has not communicated with Fleet for a set number of days. `host_expiry_enabled` turns it on, `host_expiry_window` sets the number of days, and the window must be greater than zero once enabled. A fleet can set its own window, which is useful when a lab of loaner machines should age out faster than a fleet of assigned laptops. **What a fleet cannot do is opt out.** Turning expiry off at fleet level does not exempt its hosts, it puts them back under the organization-wide window, so once expiry is enabled globally every host is subject to some window and the only fleet-level choice is which one.

**Activity expiry** cleans up activity records, and the live report data associated with them, older than a set window. `activity_expiry_enabled` and `activity_expiry_window` control it. **It only ever removes activities with no host link left**, so it trims the general history and cannot orphan a device's story however short the window.

A third setting sits alongside it: **`preserve_host_activities_on_reenrollment`**, plural, keeps a host's activities across a wipe and re-enrollment, which is what you want when a machine is redeployed and you still care what happened on it. It is currently supported for company-owned Apple devices enrolled through Automated Device Enrollment.

Check the effective value of `preserve_host_activities_on_reenrollment`. Its introducing migration enabled it if any user account already existed and disabled it if none did, so upgraded and fresh deployments can differ on the same Fleet version. When it is off, re-enrollment unlinks the host’s activities; records with no remaining host link can then expire under the activity window ([1.5](../01-foundations/1.5-audit-and-activity.md)).

Decide these deliberately rather than leaving the defaults. Keeping everything forever is a choice with a storage cost; keeping nothing is a choice with an investigative cost.

### One exception to how GitOps works

Omission has different effects across GitOps settings. Four blocks are replaced outright, and some individual fields receive defaults; other values remain as configured when absent from YAML. Review the field-level rules in [6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md) when deciding whether the repository fully describes your deployment.

Omitting **`host_expiry_settings`** preserves its current value.

If you want host expiry disabled, set it to off explicitly. A repository that omits the key can still manage a deployment where expiry is enabled.

## GitOps mode: making the interface read-only

<!-- SCREENSHOT: SS-2.7-02 | Recognizing GitOps-managed settings
     PURPOSE: Explain why a managed setting cannot be edited in the interface and where to make the change.
     PREPARE: Use a Premium demo instance with GitOps mode enabled and a public example repository URL configured.
     CAPTURE: Open one managed settings section with disabled controls and show the tooltip or message linking to the owning repository.
     FRAME: One crop showing the setting, its read-only state, and the repository message together. Capture the actual tooltip if needed; do not add explanatory UI overlays.
     EDITION: 4.91; reuse for 4.90 only when the visible controls and wording match
     PRIVACY: Use demo names and non-sensitive data; exclude tokens, passwords, keys, and personal identifiers.
     FILENAME: ss-2.7-02.png
-->

![How-to](../_assets/icons/howto-light.svg) GitOps mode helps administrators follow the repository’s change process. It makes managed sections of the interface read-only and points people to the repository, reducing the chance that a quick UI edit will be overwritten by the next apply.

Enable it with `gitops_mode_enabled` and provide a valid `http://` or `https://` `repository_url`. GitOps mode requires Fleet Premium.

Both are organization-wide only; there is no per-fleet version.

GitOps mode changes the interface while API access continues to follow each user’s role. Users who can write through the API retain that ability. Use roles and scope to control who can make changes, as described in [2.6](2.6-user-accounts-roles-and-service-identities.md).

**Labels are treated separately, and how depends on a setting.** Fleet has a change-management exception for labels, and whether omitting the `labels` key from your YAML deletes unlisted labels depends on whether it is on. **Its default differs between a fresh 4.90.0 installation and one upgraded to it**, so two deployments on the same version can behave oppositely on identical YAML. Read the effective value rather than assuming it ([6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md)).

**A second exception to the reset rule lives here.** As with host expiry above, **omitting the `gitops` section from your YAML preserves whatever is already configured** rather than resetting it. So GitOps mode cannot be switched off by deleting the key; it has to be set to false explicitly.


## Verification and ownership

![How-to](../_assets/icons/howto-light.svg) Changing a setting is a small act with occasionally large reach, so give it the same handling as any other change: record the previous value, the intended outcome, who it affects, and how to put it back. Apply it in a non-production environment first where you have one.

Then verify along two paths, because a setting can look correct in the UI and still not have reached devices:

- **The administrator path.** Confirm the value is what you set. **Do not treat a missing activity entry as a failed change**: the feed is audited by exception, each entry is written by a guard on its own block, and a change to a part with no dedicated type writes nothing at all ([a.3](../09-appendices/a.3-configuration-model-and-precedence.md)). Where an entry does exist, check the actor on it.
- **The device path.** Confirm a representative host behaves as intended. This matters most for anything that changes what agents receive.

Ownership splits along the same line as the settings themselves. The global administrator group owns organization settings and the review cycle for them. Whoever owns the infrastructure owns server settings, TLS, and the restart that a server-setting change implies.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting-light.svg)

- Every configuration source and scope, which one owns a value, and the rules and exceptions that decide the effective value: [a.3 Configuration sources, scopes, and precedence](../09-appendices/a.3-configuration-model-and-precedence.md). Per-key defaults stay in Fleet's own reference, read with [8.14](../08-troubleshooting/8.14-degradation.md)'s caveat that the reference and the server are known to disagree, both about defaults and about which keys exist
- Which interface can change which setting: [a.5 Action-to-interface index](../09-appendices/a.5-interface-index.md)
- When a setting appears correct but is not taking effect: [8.6 Inspecting server state](../08-troubleshooting/8.6-server-state.md)
- When the Fleet service itself is misbehaving after a server-setting change: [8.3 Fleet server logs](../08-troubleshooting/8.3-server-logs.md)

## Version notes

![Reference](../_assets/icons/reference-light.svg) Verified against Fleet 4.90.0. `preserve_host_activities_on_reenrollment` applies to company-owned Apple hosts enrolled through Automated Device Enrollment at this release.
