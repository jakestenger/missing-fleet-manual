---
title: "Deploy with containers or virtual machines"
chapter: "II. Administer and deploy Fleet"
section: "2.4"
sidebar_position: 4
status: drafting
verified_against: Fleet 4.90.1
verified_on: 2026-08-25
verified_source: "was verified; demoted to drafting 2026-08-27 after taking factual corrections from the Part I and Part III reviews. The stamp meant reviewed-at-this-text and the text changed. Needs a review round before it goes back. Restructured 2026-08-30: renumbered from 2.11 (P6 structural moves); claims inherited with their ledger rows."
reviewed_by: codex gpt-5.6-sol
reviewed_on: 2026-08-25
further_reading:
  - https://fleetdm.com/guides/deploy-fleet-on-kubernetes
  - https://fleetdm.com/guides/deploy-fleet-on-docker-compose
  - https://fleetdm.com/guides/deploy-fleet-on-proxmox
feature_requests:
  labels: [":product", "#g-orchestration"]
  match: ["docker", "kubernetes", "helm", "systemd", "proxmox", "valkey"]
  exclude: []
---

# Deploy with containers or virtual machines

Every packaging runs the same binary against the same three dependencies. What changes between them is who runs the database migration on upgrade, and whether that happens because you arranged it or because the packaging already did.

## Purpose and scope

![Explanation](../_assets/icons/explanation.svg) This chapter covers running Fleet without a cloud provider's managed services underneath it: Docker Compose, Kubernetes, ordinary virtual machines, and Proxmox. How large to make any of it is [2.2](2.2-self-hosting-architecture-and-capacity.md). The two clouds Fleet publishes reference architectures for are [2.3](2.3-deploy-on-aws-or-gcp.md). Handing the finished thing to whoever operates it is [7.7](../07-operate-fleet/7.7-production-readiness-checklist-and-handoff.md).

These paths overlap with the previous chapter more than the titles suggest. Kubernetes on a managed cluster is a container deployment that happens to be on a cloud, and the object storage and database advice from [2.3](2.3-deploy-on-aws-or-gcp.md) applies unchanged.

What this chapter gives you is the bootstrap sequence and the decisions inside it, not a copy-paste production deployment. It walks the packagings, where the migration runs, how TLS is arranged, and what a first launch needs, so you can reason about and operate any of them.

It does not hand you a finished runnable stack, because the parts that make one production are yours to supply: the server private key, the TLS certificate and where it terminates, persistent and backed-up storage, a load balancer and DNS, and the secrets to hold all of it. Fleet's published Compose and Helm files are starting points you adapt rather than production drop-ins. [2.2](2.2-self-hosting-architecture-and-capacity.md) sizes that infrastructure, and [7.7](../07-operate-fleet/7.7-production-readiness-checklist-and-handoff.md) is the readiness checklist for handing the result over.

## What the packaging choice changes

![Explanation](../_assets/icons/explanation.svg) Very little, which is the useful part. It is the same binary in every case, needing the same MySQL, the same Redis, and the same TLS certificate described in [2.2](2.2-self-hosting-architecture-and-capacity.md).

Fleet's published container image is small and deliberately plain: an Alpine base pinned by digest, the Fleet binary, and a command that runs `fleet serve`. It creates its own unprivileged user and runs as that user rather than as root.

That last detail is behind a first-run problem worth knowing before it happens rather than after. Docker creates volumes owned by root, and Fleet is not root, so a fresh deployment can fail to write to its own log directory. Fleet's Compose deployment handles this with a small initialization container that fixes ownership and exits. **That container is supposed to exit.** It shows up as `Exited (0)` in a container listing, which reads as a failure to anyone who has not seen it before and is in fact the successful case.

The image's own command is `fleet serve`, which does not migrate. Whether your deployment migrates on start therefore depends on whether something overrides that command, and the published packagings differ on exactly this point. It is covered under upgrades below.

## Fleet will substitute one of its datastores and not the other

![Explanation](../_assets/icons/explanation.svg) Fleet is emphatic that MySQL variants are unsupported. On Kubernetes it recommends a Redis variant. Both positions are correct, and the reason they differ is the most useful thing in this chapter.

**Valkey is the recommended cache for Kubernetes deployments.** It is a Linux Foundation-governed, Redis-compatible project, Fleet's Helm chart ships an optional Valkey sub-chart, and Fleet's guide points at Valkey's own chart for anything more serious. Nothing about this is grudging tolerance.

**MariaDB and other MySQL drop-in replacements remain unsupported**, as [2.2](2.2-self-hosting-architecture-and-capacity.md) records, to the point that known problems with them may not be fixed.

The asymmetry follows directly from what each store holds. [1.6](../01-foundations/1.6-the-fleet-server.md) draws the line: MySQL is the system of record, and Fleet depends on its exact behaviour under concurrency, migrations and schema features. Redis holds work in progress, reached through a small and stable command surface, and if its data goes, most of what was held costs a retry, though not all of it: pending failing-policy deliveries and the recorded error history are not reconstructed from anywhere ([7.2](../07-operate-fleet/7.2-back-up-and-restore-service-state.md)). A store whose loss costs work in flight rather than recorded facts is a store you can swap. A store whose subtle behaviour differences would corrupt your history is not.

## Docker Compose

![How-to](../_assets/icons/howto.svg) Fleet publishes a Compose deployment, and it is the shortest path to a working server on hardware you control.

It brings up three services and one initialization container: MySQL for the durable state, Redis for the work in progress, Fleet itself, and the permissions helper described above. The published file maps host port 1337 to Fleet, so that is what devices connect to and what your firewall needs to allow inbound. If you put a reverse proxy in front to terminate TLS, that mapping becomes internal and the port devices actually use is the proxy's.

**Generate the server private key properly and keep it.** Fleet refuses to start if the key is present but shorter than 32 bytes, and the check runs after any secrets manager lookup, so a short value fetched from a secret store is caught the same way. This is the key that encrypts session tokens and, as [7.2](../07-operate-fleet/7.2-back-up-and-restore-service-state.md) covers, escrowed Linux disk encryption data. Losing it is not recoverable by regenerating it.

For TLS you either give Fleet a certificate and key directly or terminate TLS at a reverse proxy in front of it and turn Fleet's own TLS off. **The two arrangements need opposite settings**, so a half-configured one produces certificate errors on first start: Fleet either has a certificate it is not meant to serve, or is meant to serve one it does not have. Check which of the two you intended before reading the error.

This deployment is a genuinely reasonable small production deployment, and Fleet is direct about what to change as it grows: managed MySQL and Redis, several Fleet containers behind a load balancer, certificates from a real authority, pinned image versions, backups, and log forwarding. Everything on that list is [2.2](2.2-self-hosting-architecture-and-capacity.md) arriving one item at a time.

## Proxmox is a virtual machine with Docker on it

![Explanation](../_assets/icons/explanation.svg) Fleet publishes a Proxmox guide, and the honest summary of it is that there is nothing Proxmox-specific about Fleet. The guide creates a virtual machine, installs Ubuntu, installs Docker, and then runs the Compose deployment above.

That is worth saying plainly rather than treating Proxmox as its own deployment target, because it means anything true of the Compose deployment is true here, and any other hypervisor works the same way.

What the guide does provide, and what is hard to find elsewhere, is a floor. **Four gigabytes of memory, two virtual CPUs and twenty gigabytes of disk** run a complete Fleet deployment including its database and cache. Set against the five thousand host configurations in [2.2](2.2-self-hosting-architecture-and-capacity.md), that gives you both ends of the range.

One requirement is easy to skip and expensive to discover later: **you need a domain name or a local DNS entry pointing at the machine.** A bare IP address will not do, because the certificate has to match the hostname the agents use, and device management enrollment depends on that name.

![Four deployment packagings shown as a 2x2 grid: Docker Compose on one host; Proxmox running a VM with Docker Compose, marked same as the panel to its left; virtual machines under systemd; and Kubernetes via Helm or Terraform, marked plus a migration job. Every panel contains the identical three marks Fleet, MySQL, and Redis. A band beneath reads: the same binary and the same three dependencies in every case.](assets/2.4-deployment-shapes.webp)

<!-- IMAGE-OK: assets/2.4-deployment-shapes.webp
     NOTE: accepted 2026-08-30. Sol diagram recipe (HANDOFF). Laid out as a 2x2 grid rather
     than a single row so the panels stay legible in the web column; sameness still reads first.
     PROMPT: Landscape 16:9. Four labelled panels in a row, each containing the same three
     small stacked marks labelled "Fleet", "MySQL", "Redis", drawn identically in every panel
     so the repetition is obvious. Panel titles left to right: "Docker Compose, one host",
     "Proxmox, a VM running Docker Compose", "Virtual machines, systemd", "Kubernetes, Helm or
     Terraform". Under the Proxmox panel add a small label "same as the panel to its left".
     Under the Kubernetes panel add a small label "plus a migration job". Across the bottom, a
     single full width band reading "the same binary and the same three dependencies in every
     case". Keep all four panels visually identical apart from their frames, so the eye reads
     sameness first. Use a light tint for the panel frames and reserve full strength for the
     bottom band. Caption: "Four packagings, one deployment."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted. Five large panels at full
     saturation looks like a children's infographic, not a technical manual. And do not
     colour a panel that has no content in it; colour has to carry meaning, and an empty
     coloured box is just noise.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart. Take only as many as
     there are categories; do not spread all six across four items to look lively. If nothing
     in the picture needs distinguishing, use none of them and let the structure set carry it.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text.
 -->

## Virtual machines and systemd

![How-to](../_assets/icons/howto.svg) Running the binary directly on a machine is still supported and still sometimes right, particularly where containers would add a layer nobody on the team wants to operate.

Fleet publishes a sample systemd unit. Run it as a dedicated unprivileged user rather than root, exactly as you would any web server, and raise the open file limit, since Fleet holds a great many connections at once and the default on many distributions is too low. Logs go to the journal, so `journalctl` is where you follow a failing start. Changing the unit needs a daemon reload before a restart, or systemd keeps running the old definition and the change appears to have done nothing.

**Do not copy the credential handling from the sample.** Fleet's example passes the database password as a command line flag, which is convenient in a sample and wrong in a deployment. Command line arguments are visible to anyone who can list processes on the machine, and they are also captured in the debug archive Fleet asks for when you report a performance problem, which means they leave your organization unless somebody remembers to strip them. Put the credentials in the environment or a configuration file.

## Kubernetes

![How-to](../_assets/icons/howto.svg) There are two supported routes, a Helm chart and a Terraform module, and they deploy the same thing. Pick whichever matches how the rest of your cluster is managed.

The chart can bring up MySQL and Valkey as sub-charts, which is the fastest way to a working cluster deployment. **Those sub-charts are for development and testing.** Fleet says so directly, and the reasoning is the same as everywhere else in this part: running a highly available database inside Kubernetes is a real undertaking, and a managed database service is almost always the better trade. For anything you intend to keep, point Fleet at a database and cache that live outside the cluster.

Credentials go in Kubernetes secrets in the deployment's namespace, and the addresses Fleet needs are ordinary in-cluster service names.

## Rolling updates are the wrong strategy for Fleet

![Explanation](../_assets/icons/explanation.svg) This is the one place where the packaging genuinely changes what you have to do, and it catches people precisely because the orchestrator is trying to help.

[2.2](2.2-self-hosting-architecture-and-capacity.md) establishes that migrations run with the server offline. On Kubernetes that requirement collides with the default behaviour of a deployment, which is to keep old pods serving while new ones start. Fleet needs no active connections to the database when migrations run, and a rolling update guarantees the opposite.

**Scale the deployment to zero replicas first**, let the pods terminate, then upgrade. Fleet's Helm chart runs migrations as a job wired into the install and upgrade lifecycle, so the migration itself is handled, but the chart cannot stop your running pods for you. That step is yours.

**Compose is the opposite case, and it is worth knowing which one you are in.** Fleet's published Compose file overrides the image command to run `fleet prepare db --no-prompt` before `fleet serve`, so pulling a new image and bringing the service up migrates on start with no separate step. Fleet's guide says so directly. The single-container shape is what makes that safe: there are no other instances to be serving traffic against a half-migrated schema.

The Terraform path replaces the deployment explicitly as part of the apply, which achieves the same stop before the migration runs.

There is a subtlety in the chart worth knowing if you use the built-in database sub-chart: the migration job is only wired in as a pre-install and pre-upgrade hook when you are pointing at an external database. With the sub-chart enabled the ordering differs, because the database it would migrate is being created by the same release.

## Moving vulnerability processing off the serving instances

![Explanation](../_assets/icons/explanation.svg) Fleet's Helm chart offers something the other deployment paths do not package for you, and it is a genuine architectural option rather than a convenience.

Vulnerability processing is periodic, heavy, and unrelated to serving agent traffic. The chart can run it as its own scheduled job, on its own resource allocation, instead of inside the instances that are answering hosts. Turning that on does two things at once, and both are necessary: it creates the scheduled job, and it sets the server instances to stop running the work themselves. Doing only the first would leave the work happening twice.

The setting that suppresses the in-server schedule exists on every deployment, not just Kubernetes, and is documented for exactly this case: use it when something outside Fleet is scheduling that job. If you are running Fleet on plain machines and vulnerability processing is competing with agent traffic, that is the lever, and you would build the external schedule yourself.

## Verification and ownership

![How-to](../_assets/icons/howto.svg) On Kubernetes, confirm the deployment and pods are running and then reach the URL you configured. On Compose, check the containers and remember that the initialization container should have exited cleanly rather than be running. Reaching the URL for the first time on any of these paths lands on the setup screen; [2.2](2.2-self-hosting-architecture-and-capacity.md#complete-first-run-setup) covers creating the first administrator there or with `fleetctl setup`.

Neither check is sufficient, and the reason is in [2.2](2.2-self-hosting-architecture-and-capacity.md): the health endpoint covers MySQL and Redis and nothing else. Upload a software installer and download it to a host. On these deployments that test matters more than on a cloud reference architecture, because object storage is the piece most likely to have been left unconfigured, and its fallback to local disk is invisible until a second instance appears.

Ownership on these paths tends to be concentrated rather than divided, and the risk is the opposite of the cloud case. One person builds it, it works, and nothing is written down. The specific things worth recording before you move on are where the server private key lives, what the certificate is and when it expires, whether object storage is configured or falling back, and what the upgrade sequence is on this particular packaging, since it differs meaningfully between Compose, systemd and Kubernetes. That list is the input to [7.7](../07-operate-fleet/7.7-production-readiness-checklist-and-handoff.md).

## Precedence and edge cases

![Reference](../_assets/icons/reference.svg) A few behaviours here are easy to meet without recognising them.

An initialization container in the exited state is a success, not a failure, and there is no warning anywhere that says so.

The Compose deployment listens on 1337 while Fleet's own default is 8080, so guidance written for one will name a port the other does not use.

Pin the image version. An unpinned image tag means an unplanned upgrade, and an unplanned Fleet upgrade means unplanned database migrations against a server that was not stopped for them.

The migration step exists in every packaging. What differs is whether it is arranged for you, as in Fleet's Compose file and Helm chart, or left to you, as with a plain binary under systemd. Find out which before your first upgrade rather than during it.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting.svg) For separating an infrastructure problem from a Fleet problem, [8.1](../08-troubleshooting/8.1-diagnostic-method.md) is the starting point. Reading Fleet's own logs, wherever your packaging puts them, is [8.3](../08-troubleshooting/8.3-server-logs.md).

The configuration settings named here, and how they relate to what is set through the interface, are in [a.3](../09-appendices/a.3-configuration-model-and-precedence.md).

## Version notes

![Reference](../_assets/icons/reference.svg) Verified against Fleet 4.90.1. Helm chart values, image contents and the published Compose file all move independently of Fleet releases, so treat the structure as durable and the specific keys as things to confirm against the version you deploy.

Valkey being the recommended cache for Kubernetes is Fleet's guidance at this release, and the guidance is what may move. The underlying reason, that Redis holds work in flight while MySQL holds the record, is a property of the design rather than of a release.

The minimum machine sizing quoted here comes from Fleet's Proxmox guide and describes a small deployment on one virtual machine, not a lower bound on production.
