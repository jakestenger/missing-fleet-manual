---
title: "Manage configuration profiles and declarative settings"
chapter: "V. Manage devices"
section: "5.2"
sidebar_position: 2
verified_against: Fleet 4.90.0
verified_on: 2026-09-03
verified_source: "drafted against fleet-v4.90.0 (7c428c6e46); citation ledger at research/section-notes/5.2-notes.md. Extended 2026-09-02 (round4 RM8) with a new 'Declarations: the user channel, and where it can't reach' section covering PayloadScope System/User syntax, iOS/iPadOS refusal, the automatic-enrollment prerequisite for a macOS user channel, the two-hour grace period before failure, and undeliverable-removal handling; findings and citations recorded in the same notes file. Extended 2026-09-03 (round6 M16) with a 'Naming hosts from a template' section teaching the Premium host display name template: Apple-company-owned-only eligibility (BYOD/personal enrollment excluded), fleet/Unassigned scope, host+IdP+secret variables, the 63-byte device-name limit enforced at save and per host, the five-status enforcement/resend lifecycle, and REST + GitOps (controls.name_template) surfaces; citations in the notes file. Extended 2026-09-03 (round6 M17) with a 'Declaration assets' section teaching the Premium Apple DDM asset workflow: the com.apple.asset.* JSON shape and Reference/DataURL payload, the validation rules (type prefix, valid data URL, no secret in the identifier, no authentication key), the per-fleet name/identifier uniqueness, the delete-while-referenced refusal, GitOps controls.macos_settings.assets applied before declarations, and the licence asymmetry (interactive endpoints return a licence error on Free while a GitOps run silently makes no asset call); citations in the notes file."
further_reading:
  - https://fleetdm.com/docs/rest-api/rest-api
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Manage configuration profiles and declarative settings

Profiles are the clearest case of the desired state model in [5.1](5.1-plan-target-and-govern-device-changes.md). You do not run a profile. You declare that hosts in a scope have it, and Fleet works continuously to make that true.

That single fact explains almost everything in this chapter: why there is no "it ran" moment, why removing a host from a scope removes the profile from the device, why a status can go backwards, and why the most useful status word in the product means something narrower than it sounds.

## Choosing where a setting lives

![How-to](../_assets/icons/howto.svg) The same setting can often be delivered three ways, and the right choice is usually the highest one on this list that can express what you need.

**Fleet's own settings first.** Where Fleet has a built-in control for something, use it. Disk encryption is the clearest example ([5.8](5.8-enforce-disk-encryption-and-manage-recovery-credentials.md)), as are operating system updates ([5.6](5.6-control-operating-system-updates.md)). Fleet understands what these express, and guards them against conflicting custom profiles.

**One exception to the visibility part of that, because it is easy to assume otherwise.** The profiles Fleet generates for **OS-update enforcement are hidden, on both Apple and Windows**: they do not appear in profile listings, in host details, or in the aggregate profile summary. So built-in update enforcement gives you *less* delivery evidence than a custom profile would, not more. What you get instead is the activity record of the setting changing, and OS-version inventory afterwards ([5.6](5.6-control-operating-system-updates.md) explains what to check). Disk encryption does not behave this way.

**Then a declaration, on Apple platforms.** Apple's declarative device management lets the device hold the desired state and report its own status, rather than waiting to be told. Fleet supports Apple declarations alongside classic profiles.

**Then a classic profile.** The general-purpose mechanism, and the only one for many settings.

**Why the order matters:** each step down loses Fleet's understanding of what you did. A custom profile that enforces something Fleet has a built-in control for is a setting Fleet cannot report on, cannot verify, and may conflict with. Fleet actively guards one of these cases: it refuses to apply built-in OS update settings when a custom declaration already covers them, and tells you to remove the custom one first.

## Authoring, by platform

![Reference](../_assets/icons/reference.svg) The three platforms take different formats, and the differences are not cosmetic.

**Apple** takes property list profiles, and declarations as JSON. A profile carries an identifier that must be unique on the device; two profiles claiming the same identifier are one profile as far as the device is concerned, and the second replaces the first.

**Windows** takes configuration in the syntax its management protocol uses, addressing settings by path. There is no equivalent of the Apple profile identifier, so replacement is not implicit.

**Android** works through the device's policy rather than through documents you upload. Configuration is a property of the policy Fleet maintains for the device, which is why Android behaves like desired state even more completely than the other two.

**Uploading is not validating.** Fleet checks that what you supply is well formed and that it does not collide with something it manages itself. It cannot tell you that a setting is correct for your fleet, that a key is spelled the way the vendor expects, or that the payload will do what you intended on a version of the OS you have not tested. The first ring in your rollout is where that gets caught ([5.1](5.1-plan-target-and-govern-device-changes.md)).

## Declarations: the user channel, and where it can't reach

![Explanation](../_assets/icons/explanation.svg) A declaration can target a connection a classic profile cannot: the user channel, a separate link that exists only once a signed-in user has enrolled it, and only on macOS. Choosing it is one JSON key, but that key routes the declaration somewhere with its own existence conditions, and getting those wrong produces a specific, informative failure rather than a silent one.

**The choice is a top-level key in the declaration itself, `PayloadScope`, set to `"System"` or `"User"`.** Leave it out and Fleet defaults to `"System"`, the device channel: the same one every classic profile uses, and where a declaration lands if you never think about scope at all. Two declarations differing only in that key are delivered on entirely independent connections, each with its own acknowledgment and its own status:

```json
{
  "Type": "com.apple.configuration.<setting>",
  "Identifier": "com.example.device-scoped",
  "Payload": { "<key>": "<value>" }
}
```

```json
{
  "Type": "com.apple.configuration.<setting>",
  "PayloadScope": "User",
  "Identifier": "com.example.user-scoped",
  "Payload": { "<key>": "<value>" }
}
```

**iOS and iPadOS refuse a user-scoped declaration outright, immediately, because those platforms have no user channel to route it to.** This is not a delay: the host's record shows the install as failed as soon as reconciliation looks at it, with a message naming the reason. If a declaration needs to reach a setting on those platforms, it has to go on the device channel or not at all.

**On macOS, the user channel exists only for a host that automatically enrolled**, and Fleet is explicit about this in the failure it produces otherwise: a manually enrolled Mac never gets one, so a user-scoped declaration aimed at it fails for the same reason iOS and iPadOS do, just for a different cause. This is a second, independent reason a user-scoped declaration can be unreachable, beyond platform.

**A newly enrolled automated Mac gets a grace period, not an immediate failure.** For the first two hours after device enrollment, a user-scoped declaration with no user channel yet is held rather than failed, and reconciliation keeps checking on its normal schedule so the declaration delivers as soon as the channel appears, typically once someone signs in and Setup Assistant's account step completes. Only after that window, with still no channel, does Fleet give up and mark it failed. A user-scoped declaration that fails on a brand-new Mac within minutes of enrollment is not evidence of a problem; check again once the grace period has actually elapsed.

**Removing a user-scoped declaration behaves differently too, when there is no channel to remove it from.** Fleet cannot enqueue a removal on a connection that does not exist, so instead of leaving a permanent pending row, it drops the record outright, the same way it handles other undeliverable user-scoped content ([8.8](../08-troubleshooting/8.8-apple-mdm-diagnostics.md) covers the general device/user-channel routing mechanics this builds on).

## Declaration assets

![How-to](../_assets/icons/howto.svg) A declaration that has to carry content too large or too binary to sit inline references an *asset* instead: a separate object Fleet stores once and serves to the device when a declaration asks for it. It is an Apple DDM mechanism, it is Premium, and it needs MDM turned on. Fleet keeps the asset apart from the declaration that points at it, so it has its own upload, its own uniqueness rules and its own deletion behaviour.

**An asset is a small JSON object in its own right, not the file it points to.** Its type begins `com.apple.asset.`, it carries an identifier a declaration will reference, and its payload holds a reference to where the real content lives rather than the content itself:

```json
{
  "Type": "com.apple.asset.<type>",
  "Identifier": "com.example.my-asset",
  "Payload": {
    "Reference": {
      "ContentType": "<mime type>",
      "DataURL": "https://<host>/<path>",
      "Hash-SHA-256": "<hex digest>",
      "Size": 12345
    }
  }
}
```

**Fleet validates the asset before it stores it, and the checks catch real mistakes.** The type must begin `com.apple.asset.` and the reference must carry a non-empty, well-formed data URL, or the upload is refused. The identifier may not contain a secret variable, because an identifier is not a place a secret belongs. And the payload may not carry an authentication key at all: Fleet forces MDM authentication when it serves the asset, so an authentication block in the payload is rejected rather than honoured. The whole asset is capped at roughly a megabyte of content.

**The identifier and the name are each unique within a fleet.** Upload a second asset whose identifier or name is already in use on that fleet and Fleet refuses it, naming which one collided. A declaration then references the asset by its identifier, and the two are delivered together.

**Deletion is blocked while a declaration still points at the asset.** Fleet refuses to delete an asset a configuration profile references and tells you to remove the profile first, so you cannot strand a declaration pointing at content that is no longer there.

**Premium is enforced differently on the two routes, and the difference is a trap.** Through the interface or the API, an asset call on Fleet Free fails outright with a licence error, which is loud and obvious. Through GitOps the assets are a list of file paths under `controls.macos_settings.assets`, and Fleet applies them before the declarations in the same run so a declaration can reference an asset uploaded alongside it. But on Fleet Free, or with MDM off, that same run makes no asset call at all and reports no error: the declarations still apply, and the assets they name are silently never uploaded. Confirm a GitOps run actually applied its assets rather than assuming a clean run did. The interface and API surfaces, including the upload, the per-asset download and the delete, are in [a.8](../09-appendices/a.8-api-action-and-endpoint-reference.md); [8.8](../08-troubleshooting/8.8-apple-mdm-diagnostics.md#888-ddm-declarations) covers the `declaration/asset/...` requests a device makes once an asset is in place.

## Scoping, and what happens at the edges

![Explanation](../_assets/icons/explanation.svg) A profile is attached to a fleet, and optionally narrowed by labels.

**The scope is a membership test, evaluated continuously.** This is the part worth internalising, because it means **removal is automatic**. A host that leaves the scope has the profile removed from it, without anyone asking for a removal. That is the desired state model working correctly, and it surprises people the first time a host moves between fleets and quietly loses configuration.

**Moving a host between fleets is therefore a configuration change**, not just a reporting change. The host loses what the old fleet declared and gains what the new one declares, and both halves happen as MDM work on the device.

**Label scoping inherits the label's behaviour.** Scope to a dynamic label and membership recomputes as hosts change, so a host can install or remove a profile because its own state changed and nobody touched Fleet ([1.3](../01-foundations/1.3-hosts-fleets-labels.md)).

**Nothing is instant.** Fleet reconciles on a schedule, not on the keystroke. A change to scope or content becomes work to be done, and that work then waits for the device's channel.

## Variables and secrets

![How-to](../_assets/icons/howto.svg) A profile is a static document, which is a problem when the value differs per host or must not be stored in plain text. Fleet has two distinct answers, and confusing them is a security mistake.

**Fleet variables are per-host substitutions**, written `$FLEET_VAR_NAME` or `${FLEET_VAR_NAME}`, resolved when the profile is sent to a particular device. They fall into two families.

*Host and identity variables* fill in something Fleet already knows about the host or its user: the hardware serial, the host UUID, the platform, and a set of values sourced from your identity provider including the username, the local part of the username, the full name, the department and group membership. These are how one profile serves an estate.

*Certificate authority variables* are the more consequential family. They carry challenges, proxy URLs and renewal identifiers for certificate issuance, including NDES, DigiCert, Smallstep and custom certificate authorities. A profile using them is not merely templated, it is participating in certificate enrollment, and it should be treated as infrastructure. **The named authority must already be connected**, or the variable has nothing to resolve against and the profile does not issue: [2.13](../02-administer-and-deploy-fleet/2.13-connect-certificate-authorities.md) connects each authority type and explains which of them deliver through profiles on which platforms. Do that first.

**Secrets are different.** A secret variable is a named value you store in Fleet and reference from a profile, so the profile's content in Fleet does not contain the sensitive value. Names are uppercase letters, digits and underscores.

**The distinction that matters:** a Fleet variable is a substitution Fleet performs; a secret is a value Fleet deliberately keeps out of the document. Putting a credential in a Fleet variable rather than a secret defeats the point, because the profile then contains it.

**One variable is designed never to be stored at all.** The Platform SSO device registration token resolves to a per-host signed token at the moment the device fetches the command, specifically so that it is not stored and does not appear when commands are listed. That design is worth knowing about as the pattern to expect from anything genuinely sensitive. This variable only authenticates a device's registration; the identity provider configuration, the password relay and what the token maps to on the Mac's local account are covered in [5.5](5.5-design-setup-and-self-service-experiences.md#provision-and-sync-the-local-account-with-platform-sso).

> ### Rotating a secret does not redeliver the profiles that use it
>
> A profile that references secrets carries a second timestamp beside its checksum, recording when its secrets were last updated, and reconciliation does compare it. **That timestamp is stamped when the profile is applied, from the secrets as they stood at that moment.** Changing a secret's value updates the secret and moves nothing on the profile.
>
> So the loop does not close on its own. **Rotate a credential and every device keeps the old one** until something re-applies the profiles that reference it, which means a GitOps run or a re-upload. The checksum will not have changed either, because it covers the document you authored, placeholders and all, rather than the resolved values.
>
> **Re-apply the profiles as part of the rotation**, and treat the rotation as unfinished until you have. What Fleet does re-queue on a secret change is host display names that reference it, which is a much narrower thing and is easy to mistake for the profiles moving.

## Naming hosts from a template

![How-to](../_assets/icons/howto.svg) The display name a host reports can itself be set from a template, resolved per host from the host and identity variables the previous section describes. It is a Premium feature, and it reaches only the Apple devices Fleet manages as company owned. A personally enrolled (BYOD) Mac, iPhone or iPad is left alone, because Apple refuses a device-name command on a personal enrollment.

**You set one template per scope, on a fleet or on Unassigned, never per host.** The template is a line of text with variables in it; Fleet resolves it against each host in the scope and renames the device to the result. Set it in the interface under the fleet's OS settings, through GitOps as `controls.name_template`, or through the API ([a.5](../09-appendices/a.5-interface-index.md) and [a.8](../09-appendices/a.8-api-action-and-endpoint-reference.md) carry the exact surfaces). Setting the template back to empty stops Fleet enforcing a name; it does not rename anything back to what it was before.

**The variables are the host and identity family, plus secrets.** A template may use the host-identity variables (`$FLEET_VAR_HOST_HARDWARE_SERIAL`, `$FLEET_VAR_HOST_UUID`, and `$FLEET_VAR_HOST_PLATFORM`, which resolves to `macOS`, `iOS` or `iPadOS`) and the identity-provider end-user variables (the username, its local part, the full name, the department and group membership), the same set the section above describes for profiles. A secret variable works too, for the rare name that has to embed a stored value. Any other Fleet variable is refused when you save. It is not the certificate-authority family: those belong to certificate issuance, not to a name.

**Apple caps a device name at 63 bytes, and that limit is enforced in two places.** Fleet rejects a template outright when its fixed text alone, everything in it that is not a variable, already exceeds 63 bytes, because no host could ever resolve it to a usable name. The rest is per host: if a particular host's resolved name comes out over 63 bytes, or the template asks for an identity-provider value that host does not have, that one host's name fails with a stated reason rather than renaming to something wrong. A template can be valid as a whole while individual hosts fail on their own data.

**Enforcement is desired state, and it reports with the five statuses from the next section.** When a host is in scope Fleet enqueues the rename as work on the device channel, and the host gains a synthetic "Host name" entry beside its profiles that moves through pending, verifying and verified exactly as [Reading status honestly](#reading-status-honestly) below describes; a host already carrying the resolved name goes straight to verified with no command sent. Editing the template, or moving a host between fleets, re-queues the name the same way it re-queues a profile. **Resending obeys the profile rule too:** you can resend a host name that is failed or verified, never one that is pending or verifying, and the resend is per host and recorded ([1.5](../01-foundations/1.5-audit-and-activity.md)). The one thing that re-queues names with no re-apply is a secret change, the narrow case the rotation note above already flagged.

## Reading status honestly

![Reference](../_assets/icons/reference.svg) Fleet reports a per-host, per-profile status. There are four named states and a fifth unnamed one, and the differences are not decorative.

**The same five states are used for all three platforms.** Apple, Windows and Android profile records all draw their status from one shared set, so the vocabulary below is worth learning once and applies everywhere. What differs between platforms is how Fleet reaches a given state, not what the state is called.

| Status | What it means |
|---|---|
| **Pending** | Fleet is processing this host and the command may be enqueued |
| **Verifying** | The command was applied successfully, and Fleet has **not** independently confirmed it |
| **Verified** | The strongest state, not a terminal one. On macOS Fleet has independently confirmed the profile against the device's own inventory, and a later report that finds it missing or out of date moves it back into delivery or to Failed. On iOS and iPadOS it means the device acknowledged the install, which is the strongest signal those platforms give. **On ordinary Windows profiles it means only that the device reported the write succeeded**, see below; a Fleet-proxied SCEP profile is the exception and is verified for real ([8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md)) |
| **Failed** | The automatic retry budget is spent, so Fleet will not re-deliver on its own. Not terminal either: on macOS a failed profile that a later report shows present and current moves to Verified by itself |
| *No status yet* | The profile needs to be applied or removed and reconciliation has not reached it |

**"Verifying" is the state people over-read.** It means the device accepted the command. It does not mean Fleet has checked the setting is actually in force. On macOS only **Verified** means that, and the gap between them is where a profile that was accepted and then overridden, reverted or ignored will sit.

> **On ordinary Windows profiles, Verified is a weaker claim than the word suggests.** Fleet marks a custom Windows profile verified when the device reports that the write succeeded. **It does not read the value back.** So a Windows profile can sit at Verified while the setting it carried was later overridden by another policy source, and Fleet would not know. Treat Windows Verified as "the device accepted and applied the write", not as "Fleet has confirmed the current state". **The exception is a profile using a Fleet-proxied SCEP certificate** (custom SCEP proxy, NDES or Smallstep): those report Verified only once Fleet has observed the issued certificate on the host, so for those profiles Verified really does mean confirmed; see [8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md) for the failure/timeout behavior around it.

**The unnamed state is counted as Pending.** A profile with no status yet is reported as pending in summaries and filters, so the pending count is really "pending plus not yet looked at". This matters when you are watching a rollout and expecting the pending count to fall monotonically.

**Status can move backwards, and that is not a bug.** Verifying can go back to pending, or to no status at all, when circumstances change, and on macOS Verified and Failed move too when a later inventory report disagrees with the recorded state. A number that goes the wrong way during a rollout is usually reconciliation reacting to something, not a failure.

**Failed means the retry budget is spent, not that the case is closed.** Fleet retries a failed Apple profile install up to three times and a Windows one once before marking it failed, so a profile reading failed has already been through its retries; resending it is a deliberate act, not a first attempt. It is also not a permanent verdict: on macOS, a failed profile that a later inventory report finds present and current moves to Verified with no operator action.

**Each profile also carries an operation type**, install or remove, and it is the piece people forget when reading status. A profile sitting in pending with a **remove** operation is not failing to install. It is waiting to be taken off.

## Changing, resending and removing

![How-to](../_assets/icons/howto.svg) Four operations, with different consequences.

**Editing the content** makes it a new desired state and reconciliation delivers it. You do not need to remove the old version first.

**Resending is narrower than it looks.** On Apple and Windows you can resend a classic profile that is **failed** or **verified**; Apple declarations and Android profiles have no per-host resend at all. You cannot resend one that is pending or verifying, and Fleet refuses with an explicit message rather than quietly doing nothing. This is a sensible restriction that catches people out: the profile you most want to poke is usually the one stuck in verifying, and that is precisely the one you cannot resend. Wait for it to reach Verified or Failed.

**Removing the profile from the fleet removes it from the devices.** This is not the same as deleting a file; it enqueues removal work per host, with its own status.

**Narrowing the scope has the same effect for the hosts that fall outside it**, which is the more common accidental removal. Edit a label used by a profile and you have changed configuration on every host whose membership changed.

**Resending is per host and is recorded.** It appears in the activity record with the profile name and the host, so it is auditable, and it is worth using deliberately rather than as a reflex ([1.5](../01-foundations/1.5-audit-and-activity.md)).

## When a profile is not on a device

![Troubleshooting](../_assets/icons/troubleshooting.svg) The order to check things, before escalating to Part VIII.

1. **Is the host actually in scope?** Fleet membership and every label the profile uses. This is the most common answer.
2. **Is the host MDM-enrolled**, and on the platform the profile targets ([3.2](../03-connect-devices/3.2-enroll-macos-devices.md))?
3. **What is the status, and what is the operation type?** A remove in progress looks like a failure to install if you do not read the second column.
4. **Has reconciliation run, and has the device checked in since?** Desired state is not instant, and the wait is the channel's rather than Fleet's ([5.1](5.1-plan-target-and-govern-device-changes.md)).
5. **If the status is verifying and staying there**, the device accepted it and Fleet cannot confirm it. Ask what on the device might be overriding it, including another profile claiming the same identifier.
6. **If failed**, it has exhausted its retries. The reason is the useful part, and resending without a hypothesis just consumes another attempt.

Beyond that, profile delivery problems are MDM channel problems ([8.8](../08-troubleshooting/8.8-apple-mdm-diagnostics.md), [8.9](../08-troubleshooting/8.9-windows-mdm-diagnostics.md)).

![A state diagram of the five profile-delivery statuses. The forward path runs No status yet to Pending to Verifying to Verified, with Verifying also able to reach Failed once delivery runs out of retries. Reconciliation edges move states backward, and on macOS Failed can heal to Verified. A strip beneath shows that a profile can only be resent from Failed or Verified, not from Pending or Verifying.](assets/5.2-profile-status.webp)

<!-- IMAGE-OK: assets/5.2-profile-status.webp
     NOTE: accepted 2026-08-30 (state-machine v3), for now, with one polish item: the three grey
     backward arcs still cluster a little on the left. Generated with the Sol diagram recipe in
     HANDOFF; the working prompt is reviews/phase2/imgtest2/sm-v3-prompt.txt. This supersedes the
     original brief, which was de-AI'd and cut during review: the delivered diagram uses a plain
     caption ("Profile delivery status: five states, and the moves between them, including
     backward"), folds each state's meaning inside its box, shows the backward transitions as
     separated curved arcs with a bordered Note callout, and drops the resend strip (a separate
     idea, left to the prose). One graphic, one job. -->

<!-- SCREENSHOT: A host's configuration profile list showing several profiles in different
     states, ideally including at least one Verifying and one Failed alongside Verified ones,
     with the operation type visible. The chapter's argument is that these words mean different
     things and that operation type changes how you read the row, so a real mixed list carries it
     better than prose. Crop to the profile table. Highlight a Verifying row. Placeholder profile
     names and hostnames, no serial numbers, no organization identifiers. -->

## Reference and troubleshooting

![Reference](../_assets/icons/reference.svg) Which profile formats and settings each platform accepts, and the licence each requires, are in [a.2](../09-appendices/a.2-platform-capability-matrix.md). Whether an operation is reachable through the UI, fleetctl, the API, or GitOps is in [a.5](../09-appendices/a.5-interface-index.md); the underlying endpoints are in [a.8](../09-appendices/a.8-api-action-and-endpoint-reference.md).

For the disk-encryption and OS-update profiles Fleet manages on your behalf rather than as raw content, see [5.8](5.8-enforce-disk-encryption-and-manage-recovery-credentials.md) and [5.6](5.6-control-operating-system-updates.md).
