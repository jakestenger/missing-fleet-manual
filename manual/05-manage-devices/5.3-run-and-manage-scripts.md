---
title: "Run and manage scripts"
chapter: "V. Manage devices"
section: "5.3"
sidebar_position: 3
status: drafting
verified_against: Fleet 4.90.1
verified_on: 2026-08-30
verified_source: "drafted against fleet-v4.90.1 (dd0200f062); citation ledger at research/section-notes/5.3-notes.md"
further_reading:
  - https://fleetdm.com/docs/rest-api/rest-api
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Run and manage scripts

A script is the most powerful thing in Part V and the least constrained. A profile can only express what its format allows. **A script runs with full administrative privilege on every device you point it at**, and Fleet will not stop you.

It is also, in the vocabulary of [5.1](5.1-plan-target-and-govern-device-changes.md), a **discrete activity**: it queues per host, it runs once, it ends in a terminal state, and it does not reconcile. Nothing brings a device back if the script leaves it somewhere unintended.

So this chapter leads with the constraints, because they are the shape of what you can safely write.

## What a script runs as, and when it will not run at all

![Explanation](../_assets/icons/explanation.svg) Two facts to establish before anything else, because both change what you can write and one of them makes scripts fail on hosts that look healthy.

**Scripts run with full administrative privilege.** Fleet's agent is installed as a system service: a launch daemon on macOS, a system service unit on Linux, and a service on Windows. Scripts run as that service, which means root on macOS and Linux. There is no lower-privilege mode and no per-script privilege setting.

The consequences are worth stating plainly. You do not need to elevate, and nothing in the script needs to ask for permission. You also cannot rely on Fleet to contain a mistake, because there is nothing above the privilege level you already have. And a script that touches a user's files is not running as that user, so paths and permissions will not behave the way they do in a terminal.

**Scripts can be switched off, at two levels, and both look like failure rather than configuration.**

- **Globally**, in organization settings. Fleet refuses the run and says scripts are disabled in organization settings.
- **Per host, by how the agent was packaged.** Scripts are enabled on a host only if fleetd was built with them enabled. A host packaged without that flag rejects the script on the device and returns **exit code -2** with the output "Scripts are disabled".

That second one is the trap. It is a property of the installer a host was enrolled with, not of any setting you can see in the interface, so an estate can contain hosts that will never run a script alongside hosts that always will, with nothing distinguishing them until you try. **Software installation uses the same mechanism**, so those hosts also cannot install software through Fleet, and the failure message says exactly that ([5.4](5.4-manage-software-and-applications.md)).

If a script fails instantly on a subset of hosts with no meaningful output, check this before anything else.

## The limits, and what each one implies

![Reference](../_assets/icons/reference.svg) Six hard numbers. They are not arbitrary, and each one changes how you write.

| Limit | Value | What it implies |
|---|---|---|
| Saved script length | 500,000 characters | Effectively no constraint on a reasonable script |
| Ad-hoc script length | 10,000 characters | Roughly 250 lines. Meant for one-off work |
| Runtime on the host | **5 minutes**, then terminated | The single most important number here |
| Server wait for a synchronous run | 6 minutes | The host limit plus a minute for delivery |
| Output kept | 10,000 characters | And it keeps the **end**, not the beginning |
| Configurable execution timeout | Up to 18,000 seconds | Raises the host limit, through agent options |

**Five minutes is the number to design around.** A script exceeding it is terminated, not paused, and a terminated script has done whatever it did up to that point. There is no rollback and no completion. If your work might take longer, either it belongs in a package with an installer that owns its own lifetime ([5.4](5.4-manage-software-and-applications.md)), or the script should start something and exit rather than wait for it.

**The timeout is raisable, and raising it is a real decision.** Agent options accept a script execution timeout up to eighteen thousand seconds, which is five hours. That applies to every script for hosts under those options, not to the one you are worried about. A five-hour ceiling means a runaway script blocks that host's queue for five hours ([5.1](5.1-plan-target-and-govern-device-changes.md)).

**Output truncation keeps the tail.** When output exceeds the limit, Fleet keeps the last ten thousand characters and discards the earlier ones. This is the opposite of what most people assume, and it has a direct consequence: **put your diagnostics at the end.** A script that echoes its plan at the start and its result at the end will keep the result. A script that produces a wall of progress output will keep only the last part of the wall, which is usually what you wanted anyway.

## Platforms and interpreters

![Reference](../_assets/icons/reference.svg) Fleet accepts three file types and is specific about what may run them.

**`.sh`, `.ps1` and `.py`.** Anything else is refused at upload.

**Shell scripts must declare a supported interpreter**: `/bin/sh`, `/bin/bash` or `/bin/zsh`, with or without a `/usr` prefix. A shell script with a different shebang is refused, and the error names the supported set.

**Python is supported** through `/usr/bin/env python3` or an absolute path to `python` or `python3`. That is a real dependency on the host having Python, which Fleet does not install for you.

**PowerShell is the Windows path**, by file extension rather than by shebang.

**The interpreter is a targeting decision as much as a technical one.** A `.ps1` script has no meaning on macOS, and Fleet's platform scoping is what keeps you from finding that out at scale.

## Writing a script that can run twice

![How-to](../_assets/icons/howto.svg) The habit [5.1](5.1-plan-target-and-govern-device-changes.md) named, made concrete. Scripts get re-run: by a retry, by a policy automation firing again, by a colleague who did not know you had already done it.

**Check before you act.** Ask whether the state you want already exists, and exit successfully if it does. A script that says "if the file is absent, write it" is safe to run a hundred times; one that says "write the file" may be too, and one that says "append the line" definitely is not.

**Make the end state the goal, not the action.** "Ensure this setting is X" survives repetition. "Toggle this setting" does not.

**Exit codes are your interface.** Zero means success. Anything else means failure, and Fleet records it. Use a non-zero exit deliberately for "I could not do this", and do not exit non-zero for "there was nothing to do", which is a success.

**Say what you did.** A script whose entire output is silence on success is a script nobody can audit later. One line stating what state it found and what it changed costs nothing and is the difference between a readable history and a list of green ticks.

**Fail early and loudly on assumptions.** If the script needs a directory, a binary or a privilege level, check for it at the top and exit non-zero with a clear message rather than failing obscurely halfway.

## Secrets and per-host values

![How-to](../_assets/icons/howto.svg) The same two mechanisms as profiles, with the same distinction and higher stakes ([5.2](5.2-manage-configuration-profiles-and-declarative-settings.md)).

**Secret variables** keep a credential out of the script's stored content. The script references the name; Fleet supplies the value when the script is sent.

**The leak is not in the script, it is in the output.** A secret substituted into a script is a value that script can print, and script output is stored in Fleet and visible to anyone who can read the host's activity. A script that runs a command with a credential as an argument, and echoes its command line while doing so, has written that credential into Fleet's records permanently.

**So the rule is:** never echo a command line containing a secret, never run with shell tracing enabled, and redirect the output of anything that might report its own arguments. Treat script output as published, because within your deployment it is.

**Command line arguments are visible on the host** while the process runs, to anything that can list processes, which includes osquery ([4.6](../04-know-your-devices/4.6-advanced-osquery-queries-and-tables.md)). A credential passed as an argument can therefore end up in your own telemetry. Pass secrets through the environment or standard input where the tool allows it.

## Running one, and reading what came back

![How-to](../_assets/icons/howto.svg) Two ways to run, and they differ in more than convenience.

**Asynchronously** is the normal path. The script joins the host's queue and you read the result when it arrives. This is the only sensible option for more than a handful of hosts.

**Synchronously** waits for the result, up to six minutes, and is for the case where you are watching. The extra minute over the host's own limit exists to cover delivery, so a synchronous run that times out at the server has not necessarily failed on the device.

**A result has three parts** and all three matter: the exit code, the output, and the runtime. An exit code of zero with empty output and a runtime of zero seconds usually means the script did not do what you think.

**Exit code -2 is not your script's exit code.** It is Fleet's, and it means scripts are disabled on that host. Any negative code is worth reading as Fleet telling you something rather than as your script's own result.

**A pending script is not a failed script.** It is waiting for the host, or waiting behind other queued work on that host. The queue is per host and serialised, so a script queued behind a long software install starts when that install finishes ([5.1](5.1-plan-target-and-govern-device-changes.md)). A queue that is not draining at all, on any host, is a server-state question ([8.6](../08-troubleshooting/8.6-server-state.md#864-upcoming_activities-the-unified-per-host-queue)).

**Results are per execution, not per script.** Each run is its own record with its own identity, which is what makes the history readable when a script has run on a host many times.

## Running it on many at once

![How-to](../_assets/icons/howto.svg) A batch run sends one saved script to many hosts in a single request. It is the same discrete activity as a single run, multiplied: each host queues its own copy, runs once, and ends in its own terminal state. What changes is how you target, what can turn a host away before the script ever queues, and where you read the outcome back.

A batch runs a saved library script, never ad-hoc content: you pick one saved script and a set of hosts. It is exposed through the REST API and the UI, with no `fleetctl` batch command and no GitOps path, because a batch is imperative work rather than declared state ([a.5](../09-appendices/a.5-interface-index.md) carries the interface grid). Fleet hands back a batch execution ID, and that identifier is how you read status and results afterwards.

You target either by an explicit list of host IDs or by a filter, and exactly one of the two: sending both is refused.

A filter has to name a fleet, so batch targeting will not sweep every team at once, and every target has to sit on the same fleet as the script anyway. A saved script belongs to one fleet, or to No team, and a batch only touches hosts in that fleet; an explicit list that reaches across fleets is refused as a whole rather than run in part, which is the ownership boundary a single run respects too.

Host health plays no part either way: failing policies are not a targeting criterion, so a host is never selected or dropped for being unhealthy, though the all-hosts branch does skip loading each host's issue counts as a minor query-cost optimisation.

**Two conditions refuse the whole run before it starts.** Scripts disabled globally turn it away with the same organization-settings message a single run gives ([What a script runs as](#what-a-script-runs-as-and-when-it-will-not-run-at-all)). And a batch targets at most **5,000 hosts**; a larger selection is rejected outright, not trimmed to the first five thousand.

**A targeted host can still be turned away as incompatible, for one of four reasons**, and all four surface under the single status `incompatible`:

- **`invalid-host`**: the host ID does not resolve to a host Fleet can target.
- **`incompatible-team`**: the host is on a different fleet from the script.
- **`incompatible-fleetd`**: the host's agent was packaged without scripts, the same condition that returns exit code -2 on a single run.
- **`incompatible-platform`**: the script's interpreter has no meaning on the host's operating system, a PowerShell script aimed at macOS being the usual case.

An incompatible host never queues the script, so it shows in the counts but produces no run.

**Cancellation stops only what has not run yet.** A cancel request reaches the pending work: on a started batch it pulls each host's not-yet-activated copy from the queue, and on a batch scheduled for later it marks the whole thing cancelled before it begins. A script that has already run on a host cannot be recalled, so cancellation after activation is a race against the device fetching the work ([6.1](../06-automate-fleet/6.1-automation-design-and-change-control.md)). Cancel always records a `canceled_script_batch` activity with the targeted and cancelled counts, and it records that activity even when the batch had already finished and there was nothing left to stop.

**Read status and per-host outcomes from the batch's own endpoints, not from the log stream.** `GET /api/v1/fleet/scripts/batch/{batch_execution_id}` returns the batch's status, scheduled, started, or finished, with counts of how many hosts ran, are pending, errored, were cancelled, or were incompatible. `GET /api/v1/fleet/scripts/batch/{batch_execution_id}/host_results` returns one row per host with that host's own status. These are the surface that tells you what happened, because the per-host `ran_script` records a batch produces are host-only: they land on each host's own timeline but stay out of the global activity feed and the external log stream ([1.5](../01-foundations/1.5-audit-and-activity.md)). The batch's request, schedule, and cancellation are ordinary activities and do reach those surfaces; the per-host results do not, so the status and host-results endpoints are where you confirm each machine's outcome.

**Only an admin or a maintainer can start a batch.** Starting one needs both `script · write` and `host_script_result · write`, and their intersection is the global and fleet admin and maintainer roles. A technician can run and read an individual script but cannot start a batch this way. Reading a batch's status or host results needs only `script · read`, which an observer has ([a.4](../09-appendices/a.4-roles-and-permissions-matrix.md)).

**The batch run performs no licence check.** Premium is what creates and manages the fleets a team-scoped batch targets, not what runs the batch across one. On Free, where the only fleet is No team, a batch runs across those hosts without a licence.

## Building a library worth having

![How-to](../_assets/icons/howto.svg) Scripts accumulate faster than any other artefact in Part V, and they age worse.

**Name for the outcome.** "Reset printing subsystem" tells the next person what it does. "fix-printers-v3-FINAL" tells them about your afternoon.

**Put the contract in a comment at the top**: what it assumes about the host, what it changes, what it does not handle, and how to undo it. This is the same discipline as a query description ([4.6](../04-know-your-devices/4.6-advanced-osquery-queries-and-tables.md)) and it matters more here, because a script changes things.

**One script, one job.** A script that does three loosely related things fails partway through and leaves the host in a state you cannot name.

**Manage them as code.** Scripts in GitOps get review before they run on an estate, which is the single most valuable control available for a mechanism with no other guardrails ([6.2](../06-automate-fleet/6.2-manage-fleet-with-gitops.md)).

**Delete aggressively.** An unused script is a loaded weapon that somebody will eventually pick up believing it still works.

## Before you run it on everything

![Troubleshooting](../_assets/icons/troubleshooting.svg) A checklist, because the cost of getting this wrong is unlike the rest of Part V.

1. **Confirm scripts are enabled** on the hosts you are targeting, globally and in their agent packaging.
2. **Read it as though somebody else wrote it**, and ask what it does on a host in a state you did not anticipate: no network, disk full, the file already present, a user logged in.
3. **Run it on your own device**, then on the awkward hosts rather than the clean ones.
4. **Run it twice on the same host** and confirm the second run is uneventful.
5. **Check the output is legible**, and that the part you need survives truncation.
6. **Confirm it finishes well inside five minutes**, on the slowest hardware you support.
7. **Know how you would undo it**, and write that script before you need it.
8. **Then a ring**, and only then the estate ([5.1](5.1-plan-target-and-govern-device-changes.md)).

![Two script limits shown as bars. Runtime: the host terminates the script at 5 minutes, the server stops waiting on a synchronous run at 6 minutes, and work already done is not undone; the host limit is raisable to 5 hours in agent options, for every script on those hosts. Output: of a script's full output, roughly the first four fifths is discarded and only the last 10,000 characters are kept, so put your diagnostics at the end.](assets/5.3-script-limits.webp)

<!-- IMAGE-OK: assets/5.3-script-limits.webp
     NOTE: accepted 2026-08-30. Sol diagram recipe (HANDOFF), generated from the prompt below.
     WHY IT HELPS: the chapter argues that the limits are the shape of what you can safely write,
     and the two that actually change behaviour are the five minute runtime and the fact that
     output truncation keeps the tail. Both are quantities on a timeline, which a diagram carries
     and a table only lists.
     PROMPT: Landscape 16:9 flat vector diagram, two stacked panels sharing a left margin.
     TOP PANEL, headed "Runtime": a horizontal bar representing elapsed time, tinted #E8F1F6,
     with a solid #192147 marker at "5 minutes" labelled "host terminates the script" and a
     lighter #8B8FA2 marker just after it at "6 minutes" labelled "server stops waiting on a
     synchronous run". To the right of the 5 minute marker, shade the remaining bar with a 20
     percent tint of #D66C7B and label it "work already done is not undone". Beneath, a small
     #515774 note: "raisable to 5 hours in agent options, for every script on those hosts".
     BOTTOM PANEL, headed "Output": a long horizontal bar representing a script's full output,
     with the leftmost four fifths drawn in #C5C7D1 and struck through, labelled "discarded", and
     the rightmost fifth solid #192147 with #F9FAFC text reading "kept: the last 10,000
     characters". An arrow from the kept region points to a chip in a 20 percent tint of #3AEFC4
     reading "put your diagnostics at the end".
     Caption: "Two limits that change how you write, not just what Fleet stores."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text. -->

<!-- SCREENSHOT: A completed script run on a host showing the exit code, the output, and the
     runtime together, since the chapter's point is that a result has three parts and all three
     carry meaning. Ideally capture a non-zero exit code, because a failed run is the one readers
     need to interpret and success shows them nothing. Crop to the result panel. Placeholder
     hostnames and script names, and no real command output, credentials or paths from a live
     estate. -->
