---
title: "Enforce disk encryption and manage recovery credentials"
chapter: "V. Manage devices"
section: "5.8"
sidebar_position: 8
status: outline
verified_against: Fleet 4.90.1
verified_on: 2026-08-28
verified_source: release-review-required
---

# Enforce disk encryption and manage recovery credentials

<!--
Structured placeholder. Replace with release-verified guidance for Fleet 4.90.1.

OUTLINE AGREED 2026-08-28 with the independent reviewer. NINE `##` sections. The six-heading stub
was judged too compressed for an expert chapter.

THE ORGANISING CLAIM, corrected from mine. I proposed "three unrelated technologies behind one
control", carrying the shape of 5.6 and 5.7 across, and I asked whether I was pattern matching
rather than reading the product. Partly yes. The reviewer's version:

  Fleet exposes one fleet-scoped desired-state control and a shared status and retrieval surface
  over three platform-specific mechanisms.

The EXECUTION PIPELINES barely generalise. But licensing, scoping, secret storage, archives,
permissions, activity logging and some status vocabulary DO. Do not write this as three separate
products; write it as one control with three mechanisms underneath.

ESCROW AND ENFORCEMENT, my hypothesis, confirmed with a product-level qualification:
  - A disk can be encrypted while Fleet holds no usable current credential.
  - Fleet can create and escrow a new Fleet-managed credential for a disk it did not encrypt.
  - **They are NOT separate administrator toggles** in the built-in macOS and Windows control.
  - **On Linux the same setting is effectively escrow-only**, because Fleet cannot turn LUKS on.
    Linux rejects escrow until the disk is already encrypted.

THE DISCLOSURE LINE, ruled explicitly because this chapter's whole subject is credentials that
decrypt disks. The line is "operationally specific but procedurally restrained".

INCLUDE: what each credential unlocks; which roles can reveal it; that retrieval is per host and
activity-logged; that Recovery Lock disclosure starts a ONE HOUR rotation timer; whether Fleet is
returning a current or an ARCHIVED credential; which server-side key material must survive; how to
move the value through the organisation's approved secret channel; and how to tell Fleet's
verification apart from a real controlled recovery test.

EXCLUDE: API paths, curl, SQL, table names, archive-restoration statements; bulk export techniques;
sample credential values; anything about extracting encrypted blobs or using Fleet's private keys;
and a keystroke-by-keystroke recovery-screen recipe.

A sufficient public statement, given by the reviewer: authorised users who can read the host can
reveal an available recovery credential from Host details; Fleet records the successful read; and an
archived credential may be offered when the current one is unavailable. Recovery Lock additionally
needs the warning that viewing schedules rotation.

BOUNDARIES. 5.2 owns profile syntax and lifecycle. 2.7 and 2.8 own MDM setup. 4.3 and 4.6 own
compliance policies and encryption-detection queries. 5.7 owns lock, unlock PINs and wipe, and
already says the unlock PIN is not disk-recovery material. Part VI and the appendices own GitOps and
API mechanics. Part VIII owns registry investigation, platform tooling, Escrow Buddy logs, stalled
commands, database archive restoration and private-key handling. 7.3 and 7.6 own backup and
certificate rotation; this chapter identifies the dependency and the consequence only.
-->

## Separate desired enforcement, observed encryption, escrow, and recovery proof

<!--
Four different claims that get collapsed into "encrypted". This is the chapter's spine and the
section every later one refers back to.
-->

## Check licensing, prerequisites, compatibility, and competing configuration

<!--
- The Premium boundary, fleet scoping, and what competing configuration Fleet refuses.
- Per-platform prerequisites and compatibility.
-->

## Enforce FileVault and escrow a personal recovery key

<!--
Ordered observable pipeline. Include the already-encrypted path, which behaves differently from the
Fleet-encrypted one.
-->

## Enforce BitLocker and escrow a numerical-password protector

<!--
Ordered observable pipeline. Name the protector type precisely; it is not "the BitLocker key".
-->

## Escrow recovery material from an already-encrypted LUKS host

<!--
- Fleet CANNOT enable LUKS. The setting is escrow-only here, and Fleet rejects escrow until the disk
  is already encrypted. Say that plainly rather than implying parity with the other two.
- Preserve the observable distinction for TPM-backed Ubuntu, silent escrow and a separate
  Fleet-owned recovery key, but omit the implementation plumbing.
-->

## Read compound status without overclaiming recovery

<!--
The status is compound: enforcement, observed encryption, escrow, and whether the credential Fleet
holds is current or archived. A single word in the interface does not carry all four.
-->

## Reveal, validate, and repair disk-recovery credentials safely

<!--
Written to the disclosure line above. "Repair" must be explicitly PLATFORM-SPECIFIC rather than
promising a universal rotate action.
- Who can reveal, that it is per host, and that Fleet records the successful read.
- Current against archived credentials.
- Fleet's verification is NOT a controlled recovery test; say how to tell them apart.
- Moving the value through an approved secret channel.
-->

## Survive re-enrollment, scope changes, disablement, certificate rollover, and archives

<!--
- What happens to escrowed material across each event.
- Which server-side key material must survive, stated as a dependency and a consequence rather than
  as a backup procedure (7.3, 7.6 own those).
-->

## Manage Recovery Lock as a separate Apple credential

<!--
A different credential from the FileVault key, and it behaves differently.
- Eligibility, separate control and status, reveal permissions.
- DISCLOSURE-TRIGGERED AUTO-ROTATION: viewing it schedules rotation within an hour. That is the
  single most surprising fact in this chapter and belongs where a reader meets it.
- Explicit rotation, and removal.
-->
