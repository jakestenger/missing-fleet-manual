---
title: "Back up and restore service state"
chapter: "VII. Operate Fleet"
section: "7.2"
sidebar_position: 2
verified_against: Fleet 4.90.0
verified_on: 2026-08-28
verified_source: "drafted against fleet-v4.90.0 (7c428c6e46); citation ledger at research/section-notes/7.2-notes.md, which records the evidence class of every consequential claim. Restructured 2026-08-30: renumbered from 7.3 and the retire section split to 7.8-retire-a-fleet-deployment (P6 structural moves); claims inherited with their ledger rows. Working research is held outside this repository, see research/section-notes/README.md"
further_reading:
  - https://fleetdm.com/docs/deploy/reference-architectures
feature_requests:
  labels: []
  match: []
  exclude: []
---

# Back up and restore service state

**Fleet ships no backup tooling, no restore verification and no point-in-time recovery.** Everything in this chapter is either your database vendor's, your storage vendor's, or your own procedure. What Fleet supplies is the map: where the state is, what depends on what, and which parts are useless without something you might not have thought to keep.

Two things make Fleet different from an ordinary web application here, and both are below. **Some of its most valuable state is encrypted with a key that does not live in the database.** And **a restored Fleet does not sit quietly**: it will act, on real devices and real external services, unless you stop it.

## Define the recovery objective and failure scope

![How-to](../_assets/icons/howto.svg) Decide what you are protecting against before deciding how.

Three questions, and they are yours rather than Fleet's:

1. **Which failures are covered?** Accidental deletion, a corrupted database, a lost region, a compromised deployment. They need different answers.
2. **How much time may pass** between the failure and Fleet serving again?
3. **How much data may be lost?**

Fleet publishes no recovery-time or data-loss target, and neither does this manual. **What you can take from here is what each answer costs**, and the constraint that shapes all of them: the parts of Fleet's state that cannot be recovered from a database backup alone.

## Inventory the complete recovery set

![Reference](../_assets/icons/reference.svg) Four places, and only two of them are obvious.

| Where | What is there | Notes |
|---|---|---|
| **MySQL** | Nearly everything: configuration, hosts, policies, activity, escrowed credentials, **and the Apple certificate authority material, encrypted** | The system of record |
| **File or object storage** | Installer bytes, icons, bootstrap packages | Referenced from MySQL. **Where this lives is a deployment choice**, so resolve it for yours rather than assuming a bucket |
| **Redis** | Live query results, counters, pending notification sets, and other transient coordination state | See below |
| **Outside all of them** | **The server private key, the Windows enrolment identity and its key, your certificates and your server configuration** | The part people forget, and the part that makes the rest usable |

**Resolve that table against your own deployment before using it.** Where installers live, whether a replica exists, and what your configuration holds are all local, and a recovery set assembled from a generic list will be missing something.

### The server private key, and what it does not cover

Fleet encrypts a set of stored assets with a server private key that **lives in your configuration, not in the database**. Restore MySQL without it and those assets are ciphertext.

> **The three disk-encryption escrow populations do not share one chain, and this is the most important paragraph in the chapter.**
>
> - **Linux** recovery material is encrypted under the server private key directly.
> - **macOS** FileVault escrow goes through the Apple certificate authority key. **That material is in the database**, encrypted, and the server private key is what protects it. Two hops, both inside the backup, with one key outside it.
> - **Windows** BitLocker **does not use the server private key at all.** It depends on the Windows enrolment identity certificate and its key, which live in your server configuration rather than in the database.
>
> **So a database restore plus the correct server private key recovers Linux and macOS, and still loses BitLocker** unless you separately preserved that enrolment material. [5.8](../05-manage-devices/5.8-enforce-disk-encryption-and-manage-recovery-credentials.md) explains what those credentials are worth; this is where you make sure you can still read them.

**Back up the configuration as material, not as documentation.** The server private key, **the Windows enrolment identity and its key**, your other certificates, the secret-store references, and the infrastructure definitions that create the rest. The Apple certificate authority material does not belong on that list, because it is already in the database.

![Linux, macOS, and Windows escrow chains depend on a MySQL backup plus different external recovery material.](assets/7.2-three-escrow-chains.webp)

<!-- IMAGE-TODO: assets/7.2-three-escrow-chains.webp
     PROMPT WRITTEN 2026-08-28 by the independent reviewer, against the chapter as corrected
     after its review round. The first brief placed the Apple certificate authority material
     outside the database, which is wrong and which the reviewer flagged as likely to be
     reproduced by any brief that did not say so explicitly. Do not hand-edit this prompt.
     PROMPT: Landscape 16:9 flat-vector dependency diagram titled "Three escrow chains, two external
     recovery dependencies". Use three equal vertical columns labelled "Linux LUKS", "macOS FileVault",
     and "Windows BitLocker".

     Draw one large boundary across the lower two-thirds labelled "MySQL backup". Inside it place
     exactly three stored-escrow boxes, one per platform. In the macOS column, also place a separate
     box labelled "Apple CA material, encrypted". Make it unmistakable that this Apple CA box is
     inside the MySQL backup.

     Above the database boundary, draw a narrow band labelled "Configuration material backed up
     separately". It contains exactly two items: "Fleet server private key" spanning the Linux and
     macOS columns, and "Windows enrolment identity certificate and key" above Windows only.

     Draw the dependency arrows: Linux escrow up to the server private key directly. macOS escrow up
     to the Apple CA material box, and from that box up to the server private key, so the two hops are
     visible and the first hop stays inside the database boundary. Windows escrow up to the Windows
     enrolment identity, with NO connection of any kind to the server private key.

     Add a caption: "A database restore plus the server private key recovers two of the three."

     ACCURACY: the Apple CA material must be inside the database boundary. The Windows chain must have
     no link to the server private key. Do not draw a single shared root. Do not show any key value,
     storage location, table name or retrieval path; this is a dependency shape, not a map.

     PALETTE: Fleet Black #192147 for the strongest marks and the boundary label; #515774 for ordinary
     text; #8B8FA2 and #C5C7D1 for secondary structure; #E2E4EA and #E8F1F6 for grouping; #F9FAFC
     background. Use #5CABDF for the server private key and its dependency arrows, and #D66C7B for the
     Windows enrolment identity and its arrow, so the reader can see at a glance that one column is
     served by different material. Keep text in the neutral set. No gradients, shadows, 3D, logos,
     watermark, vehicles, ships or aircraft. Generous whitespace, legible at half-page width. Never use
     an em dash in rendered text. NOTE: "Fleet" here is software for managing computers.
-->

### The failure modes are inverted

Worth knowing before an incident rather than during one:

- **A wrong key** fails at the point something encrypted is touched. That may be startup, or it may be later, when a feature that needs it is used.
- **A missing key** is worse, because Fleet may start cleanly. Depending on what else is configured it can come up, serve, and **persist a disabled Apple MDM status into the database.** A key-less binary pointed at a production database can therefore write a disabling change into production. Confirming whether a restore left such a state behind means reading the tables directly ([8.6](../08-troubleshooting/8.6-server-state.md)).
- **The health endpoint does not test the key or whether anything encrypted is readable.** It may stay green throughout.

**Never point a binary at a production database without the production key.** That is the whole instruction, and it is easy to violate while testing a restore.

## Account for the Fleet MCP server, if you run one

![Reference](../_assets/icons/reference.svg) Not part of the recovery set above, and does not need database or storage backup of its own.

The server stores no Fleet data of its own ([6.6](../06-automate-fleet/6.6-connect-fleet-to-an-ai-assistant.md)), so a MySQL or object-storage restore has nothing of its to lose. What still has to survive independently:

- **The source tag to rebuild from.** `cmd/fleet-mcp` carries no release tag of its own; it tracks the Fleet repository, so a rebuild after a restore uses the same tag as the Fleet server you are restoring, not whatever happened to be checked out when you last built it.
- **Its deployment definition.** The systemd unit or equivalent and the TLS-terminating proxy configuration in front of an SSE deployment are your infrastructure rather than Fleet's; back them up as material, the same instruction this chapter gives the server private key and certificates above.
- **`FLEET_API_KEY`**, the API-only user's token, is recovered the way any API-only user's token is: from wherever you captured it when it was created, not from the database, because Fleet never displays a token again after creation ([2.6](../02-administer-and-deploy-fleet/2.6-user-accounts-roles-and-service-identities.md)).
- **`MCP_AUTH_TOKEN`** is a value you generated yourself; Fleet has no record of it at all. The server requires it at startup on every transport, but it is a client credential only under SSE, where it is the bearer the client sends. Losing it costs a restart, not data: generate a new one and set it in the server's environment; under SSE also update the client's `Authorization` header, while a stdio client passes the new value through its own launch configuration instead.

## Protect MySQL and object storage coherently

![Troubleshooting](../_assets/icons/troubleshooting.svg) Two stores, two mechanisms, two moments.

Use your database vendor's procedures. Fleet has no opinion and provides no tooling.

**The gap that matters is between the two stores.** They are snapshotted separately, so any restore has a window where one is newer than the other, in one of two directions:

- **A database row whose bytes are missing.** An installer Fleet believes it has and cannot deliver.
- **An object with no row.** Storage Fleet does not know about.

**Fleet detects and repairs neither.** Document the consistency gap your procedure produces, and order the two captures deliberately rather than by accident.

> **One direction is worse than the other.** A cleanup job removes stale unreferenced installer, icon and bootstrap objects. Restore a **database older than your storage** and objects the older database does not reference become candidates for deletion. Whether that is recoverable depends entirely on whether your object store keeps versions. **Check that before you need it.**

## Plan Redis loss and restart

![Explanation](../_assets/icons/explanation.svg) Decide deliberately, because both answers are defensible.

Redis holds live query results as messages rather than records, plus counters and pending notification sets. Almost all of it is reconstructible or expendable.

**What losing it costs**: live query results in flight are gone, pending notification sets are gone so the deliveries they represented never happen ([6.5](../06-automate-fleet/6.5-integrations-webhooks-and-external-workflows.md)), and rate-limit and error counters reset. **Scheduler locks are not part of that cost**: that lock lives in MySQL, in the `locks` table, not in Redis, so scheduled jobs keep their bookkeeping across a Redis loss ([7.3](7.3-upgrade-fleet-and-fleetd.md)).

**Neither answer is wrong.** Persisting Redis makes recovery quieter; starting empty is faster, and what it costs is work in flight rather than any system of record. **Choose from those consequences rather than from a general preference for durability**, and write down which you chose so a person recovering at three in the morning does not have to work it out.

## Restore into an isolated environment

![Troubleshooting](../_assets/icons/troubleshooting.svg) This is the section that will save somebody's week.

**A restored Fleet is not passive.** There is no read-only mode, no global switch that stops scheduled work, and nothing that tells Fleet it is a copy.

Individual schedules have their own gates and timings, so not everything starts at once. But **enough starts**, and it acts on the real world:

> A restored instance can, without any device involvement, do all of the following **where the relevant feature is configured, its credentials restored with it, and its triggering state present in the backup**:
>
> - **Drain its restored job queue** into real tickets in your real ticketing system.
> - **Reconcile Apple enrollment assignments** against Apple, changing what production devices are assigned to.
> - **Create and delete calendar events** on real people's real calendars.
> - **Send webhooks** to real destinations.
>
> None of that requires a device to check in. It requires Fleet to start with those things in place, which is exactly what a faithful restore gives it.

**The only control that covers all of it is network isolation.** Configuration will not do it, because the settings live in the database you just restored.

**So: deny egress before you start the restored instance.** Not after, not once you have checked something. Before.

That is operational practice rather than a Fleet feature, and it needs an explicit list. Block outbound access to the device push services, the enterprise mobility and business-manager services, your identity provider, your ticketing system, your calendar provider, and every configured webhook destination. **Allow only what the restored instance genuinely needs**, which is normally its own database and storage and nothing else, and keep it that way for the life of the copy.

## Verify semantic recovery

![How-to](../_assets/icons/howto.svg) Proving a restore is good, rather than proving it is present.

In order:

1. **The version and migration state** are what you expect ([7.3](7.3-upgrade-fleet-and-fleetd.md)).
2. **Sign in**, which proves the database and the session machinery.
3. **Representative records exist**: a fleet, some hosts, a policy, a configuration profile.
4. **An object can be retrieved**, which tests the storage half and the reference between them.
5. **Something encrypted can be decrypted.**

That last one is the point of the exercise, and it needs care.

> **Do not prove decryptability by counting what Fleet says is decryptable.** The job that verifies stored disk-encryption credentials does not re-test rows already marked one way or the other, so a restored database keeps whatever the flags said before. **Those counts are inherited, not measured.**
>
> Prove it by **retrieving one credential per platform** in the isolated environment and confirming you got a value. One from each of the three, because they use three different key chains and one working does not imply another.
>
> **Be precise about what that proves.** It establishes that the service can still decrypt a stored value on that chain. It does not establish that every credential in the population is readable, and it certainly does not establish that any of them would unlock a disk, which is [5.8](../05-manage-devices/5.8-enforce-disk-encryption-and-manage-recovery-credentials.md)'s fourth claim and needs a real machine.
>
> **Confirm you received a value; you do not need to display it or use it.**

## Set retention and deletion order

![Reference](../_assets/icons/reference.svg) What Fleet expires, and the larger set it does not.

Fleet has retention settings, and their coverage is narrower than their names suggest.

> **Activity expiry does not remove host-linked activities.** Every policy-automation activity is host-linked by construction, so a thirty-day window leaves them in place indefinitely ([5.9](../05-manage-devices/5.9-automate-remediation-with-policies.md)). They become eligible only once the host is deleted and the link goes with it.

**Deleting a host is three different operations at once**: some rows are removed, some are marked deleted and stay, and some are left orphaned for a later sweep. If a deletion has to be complete for a legal or contractual reason, verify it rather than assuming it.

**Coordinate the layers.** Fleet's expiry, your database's growth, your object store's lifecycle rules, and whatever your external log destination does are four independent policies. The strictest one does not govern; each one governs its own copy.

## Rehearse disaster recovery

![How-to](../_assets/icons/howto.svg) The only way any of this becomes true.

**Measure, do not assume.** Time an actual restore and record what was lost.

**Those two numbers are your measured recovery capability**, and they are a different thing from the objectives you declared in [7.1](7.1-define-the-service-operating-model.md). If the measurement is worse than the declaration, the declaration is aspirational and should be changed or invested in, rather than quietly kept.

A rehearsal worth running covers: the database and the object store together, **from a real backup rather than a copy of production**; the private key and certificates supplied separately, to prove you have them; **egress denied throughout**; the five verification steps above, including one credential retrieval per platform; and an honest note of everything you had to look up, because that is the runbook you did not have.

**Topology failure is [7.5](7.5-maintain-capacity-and-availability.md)'s**: writer promotion, traffic, regional recovery. This chapter's rehearsal answers a narrower question, which is whether the state you kept is the state you needed.

## Reference and troubleshooting

![Reference](../_assets/icons/reference.svg) The configuration keys that control storage backends, retention, and credentials are in [a.3](../09-appendices/a.3-configuration-model-and-precedence.md). Rotating the private key and certificates this chapter's rehearsal proves you can retrieve is covered in [7.6](7.6-maintain-credentials-certificates-and-access.md).

## Version notes

![Reference](../_assets/icons/reference.svg) Verified against Fleet 4.90.0. Fleet ships no backup tooling, so nothing in this chapter versions as a feature; what a release can change is the shape of the state you protect. The escrow chains, the cleanup job that makes an older database dangerous to newer storage, and the verification job that inherits rather than re-tests were all checked at this release.
