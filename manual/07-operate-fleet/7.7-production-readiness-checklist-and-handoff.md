---
title: "Production readiness checklist and handoff"
chapter: "VII. Operate Fleet"
section: "7.7"
sidebar_position: 7
verified_against: Fleet 4.90.0
verified_on: 2026-08-25
verified_source: "was verified; demoted to drafting 2026-08-27 by the owner's decision that nothing carries the stamp until every part is drafted and every chapter has had at least one review round. Its source check and review history stand; see the section ledger. Restructured 2026-08-30: moved from 2.12 to 7.7 and retitled (P6 structural moves); the backup, upgrade, certificate-rotation and monitoring sections cut to their readiness gates with cross-references to 7.2, 7.3, 7.6 and 7.4, which own the procedures; claims inherited with their ledger rows."
further_reading:
  - https://fleetdm.com/docs/deploy/upgrading-fleet
  - https://fleetdm.com/docs/get-started/faq
feature_requests:
  labels: [":product", "#g-orchestration"]
  match: ["backup", "restore", "upgrade", "migration", "runbook", "handoff"]
  exclude: []
---

# Production readiness checklist and handoff

Everything Fleet documents about running Fleet is now behind you. The part nobody documents is the part that gets handed over.

## Purpose and scope

![Explanation](../_assets/icons/explanation.svg) A production handoff transfers standing obligations, not just a running server. This chapter covers what is being handed over, the obligations that have no published procedure, and what has to be true before a deployment carries real hosts.

It assumes Part II and the six chapters before this one.

## What is being handed over

![Explanation](../_assets/icons/explanation.svg) The handover is a set of standing obligations, most of which are invisible on the day everything works.

There are three data stores with different consequences on loss, and more than one of them can lose things that nothing reconstructs.

- Credentials expire on other organizations' schedules, several of them in consoles that are not Fleet.
- A certificate's rotation can disconnect every agent if it is done in the wrong order.
- The documented upgrade procedure stops the service, though not every deployment runs it that way.
- Periodic jobs can stop without anything reporting an error.
- If the deployment runs a Fleet MCP server for an AI assistant, that is another standing service. It has two credentials of its own: the Fleet API token it holds, and, for the SSE transport, the shared client bearer its clients present. It has its own liveness probe, which lies by omission, and its own rotation and retirement steps ([6.6](../06-automate-fleet/6.6-connect-fleet-to-an-ai-assistant.md), [7.4](7.4-observe-progress-and-service-health.md), [7.6](7.6-maintain-credentials-certificates-and-access.md)). An inheriting operator won't know any of it exists unless someone tells them.

An operator who inherits the URL and the admin password has inherited none of that, and meets each item for the first time when it fails. Writing them down is the handover.

![The Fleet URL and admin login are a small part of the larger operational system that must be handed over.](assets/7.7-what-is-handed-over.webp)

<!-- IMAGE-TODO: assets/7.7-what-is-handed-over.webp
     QUESTION: Which ongoing responsibilities accompany the Fleet URL and admin login?
     PROMPT: DIAGRAM: Title: Hand over the ongoing operating responsibilities. One small navy box Fleet
     URL + admin login points to a much larger bracketed group Standing obligations. The size contrast
     makes the point. Inside the larger group, five independent rows use one small blue dot each:
     Three stores, More than one can lose unrecoverable data; Expiring credentials, Renewal schedules
     belong to other organizations; Certificate rotation, Order preserves agent connectivity;
     Upgrades, The documented procedure stops service; Periodic jobs, Can stop without reporting an
     error. Use left-aligned labels and enough room for the explanations. These are selected operating
     obligations, not a sequence. Avoid a slogan footer, decorative icons, or a product UI appearance.
     TERMINOLOGY: Fleet is the company, product, or server. Host groups are lowercase
     fleet/fleets, including headings and labels. Do not call these groups teams. Preserve
     exact code/API identifiers. These instructions are not text to render.
     DESIGN: Flat vector technical diagram. Fleet is software for managing computers; draw no
     vehicles. Use Inter labels and Roboto Mono identifiers. At 1400 px source width use 48 px
     titles, 36 px body labels, and at least 28 px secondary text; scale proportionally. Check at
     720 px reading width and intended print size. Use a 32 px spacing grid, at least 24 px node
     padding, and consistent corner radii. Center short node names; left-align multiline
     explanations. Never shrink text to fit. Use #F9FAFC background, #192147 headings and primary
     connectors, #515774 text, #8B8FA2 secondary connectors, #C5C7D1 borders, and #D3E8F3 or #E8F1F6
     quiet fills. Use #5CABDF and #C98DEF for named categories, #3AEFC4 for labelled positive
     outcomes, #D66C7B for labelled failures, and #FAA669 for labelled cautions. Tint large panels
     to 20 to 25 percent; full strength is for small marks. Keep text navy or slate, or off-white on
     a navy anchor. Never rely on colour alone. Use one arrowhead shape, consistent stroke weights,
     box-edge termination, and labelled branches and return paths. Keep connectors clear of text. No
     gradients, shadows, decorative icons, logo, watermark, em-dashes, or slogan footer. Render only
     the specified reader-facing labels. Choose orientation to fit the relationship, not a default
     poster. Keep captions outside the artwork. If labels crowd, split the figure before shrinking
     them. Keep editable SVG when the production method supports it.
     NOTE: Layout and labels refined 2026-09-08 for readable collection artwork, against
     the current chapter. This editorial adaptation is not a new release verification.
     Preserve published artwork and prose until the final joint review. Do not mark IMAGE-OK yet.
     CANDIDATE: ../../research/visual-reviews/part-7/assets/7.7-what-is-handed-over.webp
     Rendered and inspected for the overnight batch; awaiting final joint review.
-->

## The go-live decision

![How-to](../_assets/icons/howto.svg) A deployment is ready to carry real hosts when a specific set of things are true and someone is willing to say so.

Treat the matrix as the gate and everything below it as the operational handoff that stands behind it. Each row is a claim someone has to be willing to sign, with the chapter or section that establishes it.

| Area | What must be true before the deployment carries real hosts | Where it is established |
|---|---|---|
| Infrastructure | The database is backed up on a schedule | [7.2](7.2-back-up-and-restore-service-state.md) |
| Infrastructure | A restore has actually been performed at least once, and the recovery time is a measured number rather than an estimate | [7.2](7.2-back-up-and-restore-service-state.md) |
| Infrastructure | The account Fleet uses can perform schema changes | [The database user needs more permission](#the-database-user-needs-more-permission-than-a-careful-dba-will-grant), below |
| Infrastructure | Object storage is configured rather than falling back, and an installer has been uploaded and downloaded end to end | [2.2](../02-administer-and-deploy-fleet/2.2-self-hosting-architecture-and-capacity.md) |
| Infrastructure | The server private key is stored somewhere the organization will still have it after the person who generated it leaves | [7.2](7.2-back-up-and-restore-service-state.md), [7.6](7.6-maintain-credentials-certificates-and-access.md) |
| Operational | The health endpoint is what the load balancer targets | [7.4](7.4-observe-progress-and-service-health.md) |
| Operational | Metrics are actually being collected, which is a real question given they are off until enabled | [7.4](7.4-observe-progress-and-service-health.md) |
| Operational | Someone is alerted when enrollment counts move | [7.4](7.4-observe-progress-and-service-health.md) |
| Operational | The upgrade procedure has been performed once, on this deployment, in a maintenance window, rather than read about | [7.3](7.3-upgrade-fleet-and-fleetd.md) |
| Ownership | Every credential in the deployment has a named owner and a named console | [Ownership that reaches outside your organization](#ownership-that-reaches-outside-your-organization), below |
| Ownership | Every external console has someone in your organization who can reach an administrator of it | [Ownership that reaches outside your organization](#ownership-that-reaches-outside-your-organization), below |
| Ownership | The runbook exists in your own words, with your own hostnames in it | [A runbook for the failures that are not obviously Fleet](#a-runbook-for-the-failures-that-are-not-obviously-fleet), below |

A pilot turns these from claims into observations, which is why [2.1](../02-administer-and-deploy-fleet/2.1-administration-model-and-deployment-choices.md) puts one before the rollout. The go-live decision is mostly reviewing what the pilot already told you.


<!-- IMAGE-TODO: assets/7.7-handoff-evidence-gate.webp
     QUESTION: What turns a readiness claim into an accepted operational handoff?
     PROMPT: DIAGRAM: Three tangible evidence artifacts, Restore rehearsal, Upgrade rehearsal, and
     Dependency/credential ownership record, feed a review table represented as a simple document
     outline. Each artifact carries an Owner and Recorded result tab. The reviewer checks Evidence
     complete? and branches to Resolve gap or Accept handoff. A live dependency link remains from
     the accepted handoff to Named primary + backup, showing responsibility continues. Do not render
     every checklist row as a miniature poster or invent product-enforced approval gates; this is
     the organization's process.
     TERMINOLOGY: Fleet is the company, product, or server. Host groups are lowercase
     fleet/fleets, including headings and labels. Do not call these groups teams. Preserve
     exact code/API identifiers. These instructions are not text to render.
     DESIGN: Flat vector technical diagram. Fleet is software for managing computers; draw no
     vehicles. Use Inter labels and Roboto Mono identifiers. At 1400 px source width use 48 px
     titles, 36 px body labels, and at least 28 px secondary text; scale proportionally. Check at
     720 px reading width and intended print size. Use a 32 px spacing grid, at least 24 px node
     padding, and consistent corner radii. Center short node names; left-align multiline
     explanations. Never shrink text to fit. Use #F9FAFC background, #192147 headings and primary
     connectors, #515774 text, #8B8FA2 secondary connectors, #C5C7D1 borders, and #D3E8F3 or #E8F1F6
     quiet fills. Use #5CABDF and #C98DEF for named categories, #3AEFC4 for labelled positive
     outcomes, #D66C7B for labelled failures, and #FAA669 for labelled cautions. Tint large panels
     to 20 to 25 percent; full strength is for small marks. Keep text navy or slate, or off-white on
     a navy anchor. Never rely on colour alone. Use one arrowhead shape, consistent stroke weights,
     box-edge termination, and labelled branches and return paths. Keep connectors clear of text. No
     gradients, shadows, decorative icons, logo, watermark, em-dashes, or slogan footer. Render only
     the specified reader-facing labels. Choose orientation to fit the relationship, not a default
     poster. Keep captions outside the artwork. If labels crowd, split the figure before shrinking
     them. Keep editable SVG when the production method supports it.
     NOTE: Proposed 2026-09-08; editorial brief, not technical re-verification. Shorten the
     introductory readiness prose. Retain the complete fillable readiness matrix and all standing
     obligations; the diagram explains evidence flow rather than replacing the checklist. Keep
     current prose and this TODO until the actual image is reviewed. Then check alt text against the
     artwork and retain an accessible summary plus all required technical qualifications.
     CANDIDATE: ../../research/visual-reviews/part-7/assets/7.7-handoff-evidence-gate.webp
     Rendered and inspected for the overnight batch; awaiting final joint review.
-->

<!-- IMAGE PENDING. Install reviewed artwork, then activate the image line below.
![Named owners bring rehearsal and dependency evidence to the readiness review; an accepted handoff retains ongoing responsibility.](assets/7.7-handoff-evidence-gate.webp)
-->

## Backup and restore have no Fleet procedure

![Explanation](../_assets/icons/explanation.svg) This is the largest gap an operator inherits: Fleet publishes no backup procedure and no restore procedure. Where the subject comes up, Fleet advises backing up the database and links to MySQL's own documentation, or recommends turning on a cloud provider's managed backups.

That is a reasonable position for Fleet to take, since the mechanics belong to whichever database you chose. It is not a reasonable position for you to inherit unexamined, because it means the single most consequential operational task in the deployment has no written procedure unless somebody writes one. The written procedure is now [7.2](7.2-back-up-and-restore-service-state.md); what the handover adds is the deployment's own copy, with its hostnames and its measured restore time.

Four things complicate that procedure, and all four come from earlier chapters.

**MySQL's loss is the catastrophic one, and not the only unrecoverable one.** [1.6](../01-foundations/1.6-the-fleet-server.md) establishes what each store holds and what "lost" means for each, including the difference between a store being unavailable and its data being gone. Redis carries pending failing-policy deliveries and the recorded error history, and nothing reconstructs either ([7.2](../07-operate-fleet/7.2-back-up-and-restore-service-state.md)). Object storage can hold completed file-carve evidence that nobody can re-upload, alongside installers and icons that anybody can. Losing MySQL costs your history, your configuration, and your enrolled estate. Whether object storage needs backup therefore depends on whether carve evidence must survive, not on how cheap installers are to replace.

**The three stores cannot be captured at one instant.** A restore therefore reconstructs a state that never quite existed. That matters less than it sounds, because no supported procedure backs up Redis in any case, but it does mean the restore procedure is a database restore plus a reconciliation, not a rollback.

**The server private key is not in the database and is not optional.** A perfect database backup restored alongside a lost private key does not give you your disk encryption keys back. [7.2](7.2-back-up-and-restore-service-state.md) covers which escrow chains the key protects and which it does not. It belongs in whatever holds your organization's other irreplaceable secrets, and it belongs in the handover document by name.

**A backup you have not restored is a hypothesis.** The restore is the part that fails, and [7.2](7.2-back-up-and-restore-service-state.md) has the rehearsal that catches it. The handover records that a restore has been performed against this deployment and how long it took, because that number is your actual recovery time regardless of what the backup schedule promises.

## Upgrades

![How-to](../_assets/icons/howto.svg) Fleet's documented upgrade procedure stops every instance, though its own Helm chart does not. Whether your deployment takes an outage is therefore yours to choose and test. [7.3](7.3-upgrade-fleet-and-fleetd.md) has the procedure, from the recovery point through the migration to proving the service resumed. [a.6](../09-appendices/a.6-glossary-and-release-compatibility.md) records the semantic versioning commitment it plans against and the three exceptions that can break a minor release. Three things belong in the handover itself, because a successor plans upgrade paths and maintenance windows from them.

**Within the current major version you can skip releases freely.** Going from an old 4.x directly to the latest 4.x is supported and expected. Crossing a major boundary is done in stages: upgrade to the last release of the old major, migrate, test, then move on, so that a problem can be attributed to a specific step.

Budget the window honestly. Fleet describes migration time as seconds to minutes even on large installations, and separately describes a typical upgrade as five to ten minutes of downtime. Both are true and they measure different things: the first is the migration, the second is the whole procedure including replacing the binary or image and starting back up. Plan the maintenance window against the larger number.

Agents buffer through the outage and send what they collected once the server returns, so for a short window the cost is delay rather than loss. The buffer is finite: osquery holds results until delivery succeeds or the buffer's limit is passed, at which point the oldest are discarded, and the default is a million log entries. A maintenance window is delay; an outage long enough to fill the buffer on a busy estate is a hole in the record. Tell whoever approves the window which one they are approving.

## The database user needs more permission than a careful DBA will grant

![Troubleshooting](../_assets/icons/troubleshooting.svg) Migrations are schema changes, so the account Fleet uses to run them must be able to create, alter and drop tables, and to create temporary tables.

It collides with a sensible instinct. Asked to provision a database user for an application, a careful administrator grants read and write on the data and nothing else. The deployment then works until the first upgrade, when migrations fail on permissions during a maintenance window with the service already stopped.

Establish this before the handover rather than during an incident, and note it beside the restore procedure, since restoring into a fresh database has the same requirement.

## Rotating the certificate can disconnect every agent

![Troubleshooting](../_assets/icons/troubleshooting.svg) Certificate rotation is routine work that a platform team will do without consulting anyone, and on Fleet it carries a failure mode that belongs in the handover explicitly: if the agents were deployed with an explicit certificate chain, the new chain has to reach them before the server starts presenting the new certificate. Get the order wrong and the agents fail verification and stop reporting, all at once, with the server itself healthy.

The rotation order that avoids this, and the hostname check that belongs beside it, are in [7.6](7.6-maintain-credentials-certificates-and-access.md). The handover names who rotates certificates for this deployment, and that they have read the constraint before the renewal date rather than after it.

## What to monitor, and who answers

![How-to](../_assets/icons/howto.svg) [7.4](7.4-observe-progress-and-service-health.md) covers what to watch, how to collect it, and how to prove the alerts fire. The handoff question is narrower: what should wake someone up, and which person answers when it does.

**Enrollment count is the signal that catches what the others miss.** Fleet's own suggested alerts are a change in host enrollment levels, rising endpoint latency and rising endpoint error rates. Insist on the first, because a deployment can be healthy by every infrastructure measure while hosts stop checking in, and nothing else in the stack will tell you.

Name a person for each of these, not a team. Otherwise the database is watched by whoever watches databases, the certificate is watched by whoever watches certificates, and the enrollment count is watched by nobody, because it does not belong to either. If a Fleet MCP server is part of the deployment, name the same person for it that you named for Fleet itself ([7.1](7.1-define-the-service-operating-model.md)). It has no team of its own, and its liveness probe answering is not the same as it working ([7.4](7.4-observe-progress-and-service-health.md)).

## A runbook for the failures that are not obviously Fleet

![Reference](../_assets/icons/reference.svg) These failures have a common shape: the symptom appears in Fleet and the cause is somewhere else.

| What you see | Where to look first |
|---|---|
| Slow after a database change | Connection limits. The per-instance limit multiplied by instance count, against what the database accepts, from [2.2](../02-administer-and-deploy-fleet/2.2-self-hosting-architecture-and-capacity.md) |
| An unknown column error after an upgrade | Migrations did not run. The binary was replaced and the database was not prepared |
| Too many open files | The process file descriptor limit, which on plain machines is set in the service definition, from [2.4](../02-administer-and-deploy-fleet/2.4-deploy-with-containers-or-virtual-machines.md) |
| Agents stop verifying the server | The certificate chain or the hostname on the certificate, above |
| Software installers fail while health checks pass | Object storage. It is not covered by the health endpoint, from [2.2](../02-administer-and-deploy-fleet/2.2-self-hosting-architecture-and-capacity.md) |
| Hosts appear and disappear, or share an identity | Duplicate enrollment, usually a cloned machine image carrying an enrolled agent. The identifier settings are in [3.1](../03-connect-devices/3.1-enrollment-design-and-host-lifecycle.md) |
| Windows hosts report management off | They are waiting for a user to sign in, from [2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md) |

## Ownership that reaches outside your organization

![Explanation](../_assets/icons/explanation.svg) Several of the credentials in Part II live in consoles that are not Fleet, administered by people who may not know Fleet depends on them. That boundary is easy to leave undrawn, and either side of it can end a capability without touching Fleet at all.

The Apple push certificate expires annually and is renewed by hand, and [2.10](../02-administer-and-deploy-fleet/2.10-apple-mdm-configuration.md) covers why it must be created under an account the organization controls rather than a person's. The Entra application registration and its permissions belong to whoever administers Microsoft identity. The Android enterprise belongs to whoever administers your Google organization. As [2.12](../02-administer-and-deploy-fleet/2.12-bind-android-enterprise.md) records, deleting it from the Google console ends management for every enrolled Android device, with no confirmation step in Fleet, and on personally owned devices Google removes the work profile too.

For each one, write down which console it lives in, who administers that console, and what breaks when it lapses. The renewal dates matter less than the names, because a date in a document nobody owns is not a control.

## Precedence and edge cases

![Reference](../_assets/icons/reference.svg) A few things about handover are true of Fleet specifically rather than of software generally.

MySQL replication is supported and recommended for production, but it does not substitute for backups. A replica faithfully reproduces a deletion.

[2.1](../02-administer-and-deploy-fleet/2.1-administration-model-and-deployment-choices.md) names the decisions that are hard to reverse, and two of them belong in the handover document because a successor will not guess them. Fleet will accept a change to the server URL while Google keeps delivering to the old one ([2.12](../02-administer-and-deploy-fleet/2.12-bind-android-enterprise.md)), so editing it silently breaks Android management. The Windows key pair is also the escrow key for disk encryption ([2.11](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md)).

An upgrade is never only a binary or image replacement, on any packaging. If your tooling does not run migrations as a step, it will meet them as a failure.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting.svg) For working a problem rather than recognising one, [8.1](../08-troubleshooting/8.1-diagnostic-method.md) is the method, [8.3](../08-troubleshooting/8.3-server-logs.md) covers the server's own logs, and [8.13](../08-troubleshooting/8.13-escalation.md) covers what to gather before escalating.

[a.8](../09-appendices/a.8-api-action-and-endpoint-reference.md) lists which endpoints have to be reachable from where, a question a security review will ask during handover. Command line operations are in [a.7](../09-appendices/a.7-fleetctl-command-reference.md).

## Version notes

![Reference](../_assets/icons/reference.svg) Verified against Fleet 4.90.0. Fleet publishes no backup or restore procedure at this release. That is a documentation gap rather than a product limitation, and it may close. The obligations it leaves with you stay either way.

The upgrade downtime figures come from Fleet's own guidance and describe two different things, the migration and the whole procedure. Measure your own on the first upgrade and use that number afterwards.
