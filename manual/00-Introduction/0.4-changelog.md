---
title: "What changed, by release"
chapter: 0. Introduction
section: "0.4"
sidebar_position: 4
slug: /Introduction/0.2-changelog
verified_against: Fleet 4.91.0
verified_on: 2026-09-08
verified_source: "not applicable: this chapter indexes the manual's own release changes, not Fleet behaviour. Corrected 2026-09-08 (overnight campaign step 7, round-2 review findings 5, 7 and 10), verified at fleet-v4.91.0 (35fc1c0244): the iOS/iPadOS vitals row said all 29 are withheld from a personal (BYOD) enrollment, but `byodDeviceInformationQueryKeys` in `server/mdm/apple/commander.go` is the same nine keys 4.90.0 asked every host for, so 26 are withheld and `supplemental_os_version_extra`, `wifi_mac` and `is_mdm_lost_mode_enabled` still arrive; the GCS setting is registered as `s3.software_installers_signed_url` at `server/config/config.go:1768` and the underscore spelling is its flag form, not the config key; and the Firefox / Firefox ESR change is a clearer conflict error (`CHANGELOG.md:43`), not a corrected match, so it is described separately from Docker, Git and Steam"
further_reading:
  - https://github.com/fleetdm/fleet/releases/tag/fleet-v4.91.0
---

# What changed, by release

![Reference](../_assets/icons/reference-light.svg) Each edition follows a Fleet minor release line, such as 4.90 or 4.91. Point releases stay within that edition: affected chapters carry inline notes, collected here with links to the relevant passages. Releases appear newest first.

When you are planning an upgrade, use this changelog to find the changes that affect your devices and workflows. Each entry points to the explanation in context, so you can review the relevant setup, behavior, or operating procedure before introducing the new release.

This edition covers **Fleet 4.91**, checked against 4.91.0. The 4.90 edition remains available in the version dropdown.

<!-- IMAGE-OK: assets/0.2-release-maintenance-vignette.webp
     QUESTION: Can the release ledger have a quiet visual opening without inventing a feature story?
     PROMPT: ILLUSTRATION: A shallow chapter-opening vignette in Fleet's Cloud City vocabulary: one
     small floating island holding a tidy, human-scale maintenance bench, a closed field notebook,
     and a carefully aligned replacement glass panel. A small swan on still water provides one
     organic focal point. Use generous empty space and a restrained pale-blue/navy palette. The
     scene should suggest upkeep and careful revision, with no charts, screens, product logos,
     version numbers, dates, or text. No devices connected by arrows and no architectural meaning
     assigned to scenery. Compose as a low horizontal strip, not a full-page hero.
     TERMINOLOGY: Fleet is the product, server, or company. Host groups are lowercase
     fleet/fleets, even in titles and node labels. Do not call those groups teams. Preserve
     exact code/API identifiers; do not render these terminology instructions as image text.
     DESIGN: Fleet is software for managing computers; draw no vehicles. Use the established Cloud
     City illustration treatment consistently: #F9FAFC background, #192147 navy, #515774 slate, and
     #D3E8F3 / #E8F1F6 pale-blue surfaces. Keep large areas quiet and the composition balanced at
     720 px reading width. Use a shallow horizontal crop. No photorealistic elements mixed with flat
     artwork, product logo, watermark, rendered text, em-dashes, or technical claims. Do not imitate
     a diagram.
     NOTE: Reviewed and installed 2026-09-09 in the live 4.91 and frozen 4.90 editions.
     Checked against the chapter and its version-specific evidence during the editorial pass.
     The surrounding text retains technical qualifications and accessible explanation.
     SOURCE: research/visual-reviews/part-0-1/assets/0.2-release-maintenance-vignette.png
-->
![A floating island with a maintenance bench, notebook, glass panel, and a swan beside still water.](assets/0.2-release-maintenance-vignette.webp)

## Fleet 4.91.0

The feature table links to the relevant chapters. The following fixes and improvements summarise changes to behaviour already covered in the manual.

### New features

| Area | Feature | Where in the manual |
|---|---|---|
| Windows MDM | A managed local admin account (`_fleetadmin`), created and password-escrowed by fleetd. Premium; needs fleetd 1.60.0+. | [5.5 Managed local admin](../05-manage-devices/5.5-design-setup-and-self-service-experiences.md#the-windows-managed-local-admin-account) |
| Windows MDM | A default fleet for user-driven (Autopilot / Entra) enrollments, assigned before the enrollment status page runs. Premium. | [2.11 Where enrolled Windows hosts land](../02-administer-and-deploy-fleet/2.11-configure-windows-management.md#where-automatically-enrolled-windows-hosts-land) |
| OS updates | Keep macOS, iOS, and iPadOS on the latest version with `minimum_version: latest` and `deadline_days`. Premium. | [5.6 Enforce the latest version](../05-manage-devices/5.6-control-operating-system-updates.md#enforce-the-latest-version-with-a-rolling-deadline) |
| Automations | A per-fleet activity webhook that fires on any activity for the fleet's hosts. Premium. | [6.5 The activity webhook](../06-automate-fleet/6.5-integrations-webhooks-and-external-workflows.md#the-activity-webhook-global-or-per-fleet) |
| Policies | A "patch when closed" patch-policy option: patch only when the app is not running. Premium. | [5.9 Patch when closed](../05-manage-devices/5.9-automate-remediation-with-policies.md#patch-a-fleet-maintained-app-only-when-it-is-closed) |
| Scripts | Built-in `$FLEET_VAR_*` variables in install, post-install, uninstall, and ad-hoc scripts, resolved per host. Premium. | [5.3 Secrets and per-host values](../05-manage-devices/5.3-run-and-manage-scripts.md#secrets-and-per-host-values) |
| Configuration profiles | `$FLEET_HOST_VITAL_<id>` custom-vital variables in Android profiles and managed app config, re-delivered when a host's value changes. | [5.2 Variables and secrets](../05-manage-devices/5.2-manage-configuration-profiles-and-declarative-settings.md#variables-and-secrets) |
| Host naming | `$FLEET_HOST_VITAL_<id>` in host-name templates, resolved per host. Premium. | [5.2 Naming hosts from a template](../05-manage-devices/5.2-manage-configuration-profiles-and-declarative-settings.md#naming-hosts-from-a-template) |
| Software inventory | Adobe Creative Cloud plugins (CEP/UXP) detected on macOS and Windows, listed as "Plugin (Adobe)". | [4.4 Adobe Creative Cloud plugins](../04-know-your-devices/4.4-understand-software-and-vulnerabilities.md#adobe-creative-cloud-plugins) |
| Software inventory | Software inventory can be turned on or off per fleet via `PATCH .../fleets/{id}`. Premium. | [4.4 Turning software inventory on and off](../04-know-your-devices/4.4-understand-software-and-vulnerabilities.md#turning-software-inventory-on-and-what-off-looks-like) |
| Device vitals | 29 new iOS/iPadOS vitals (battery, cellular, attestation, cloud backup, and more). Personally owned (BYOD) devices omit 26 of them; `supplemental_os_version_extra`, `wifi_mac` and `is_mdm_lost_mode_enabled` still arrive. | [4.1 What an iPhone or iPad reports](../04-know-your-devices/4.1-understand-hosts-vitals-and-inventory.md#what-an-iphone-or-ipad-reports-and-what-a-personally-owned-one-does-not) |
| Device vitals | Human-readable Apple hardware model ("MacBook Pro (16-inch, 2021)") instead of the raw identifier. | [4.1 Identity, OS, and network](../04-know-your-devices/4.1-understand-hosts-vitals-and-inventory.md#identity-operating-system-and-network) |
| Hosts page | A sortable "Added to Fleet" column, showing the last enrollment. | [4.1 Added to Fleet dates](../04-know-your-devices/4.1-understand-hosts-vitals-and-inventory.md#added-to-fleet-dates-the-last-enrollment-not-the-first) |
| Compliance | macOS CIS benchmark policies updated to the latest releases (Sonoma, Sequoia, Tahoe). Premium. | [4.3 The CIS benchmark policy library](../04-know-your-devices/4.3-use-policies-for-compliance.md#the-cis-benchmark-policy-library) |
| Vulnerabilities | Adobe plugins excluded from vulnerability scanning, since no CVE source maps them. | [4.4 Software Fleet never scans](../04-know-your-devices/4.4-understand-software-and-vulnerabilities.md#a-sixth-emptiness-software-fleet-never-scans) |
| Apple Business | Release a device from Apple Business Manager inside Fleet. Premium. | [2.10 Releasing a device from Apple Business](../02-administer-and-deploy-fleet/2.10-apple-mdm-configuration.md#releasing-a-device-from-apple-business) |
| Deployment | `s3.software_installers_signed_url` serves installer and bootstrap downloads via GCS presigned URLs. | [2.3 Object storage on GCP](../02-administer-and-deploy-fleet/2.3-deploy-on-aws-or-gcp.md#object-storage-on-gcp-works-despite-what-the-architecture-page-says) |
| Identity | Nested Entra groups resolved in IdP vitals. | [2.5 Connect SCIM](../02-administer-and-deploy-fleet/2.5-identity-providers-sso-scim-and-role-sync.md#connect-scim) |
| Labels | `linux` as a label platform, targeting any Linux distribution. | [1.3 Restricting a label to a platform](../01-foundations/1.3-hosts-fleets-labels.md#a-dynamic-label-can-be-restricted-to-a-platform) |
| Enrollment | `--bypass-end-user-auth` on `fleetctl package`, to skip the end-user auth prompt on Linux and Windows. Needs fleetd 1.60.0+. | [5.5 End-user authentication](../05-manage-devices/5.5-design-setup-and-self-service-experiences.md#end-user-authentication) |
| Security | Orbit enrollment now decides end-user authentication from server policy (`mdm.allow_orbit_end_user_auth_bypass`), not the client-advertised capability. | [5.5 End-user authentication](../05-manage-devices/5.5-design-setup-and-self-service-experiences.md#end-user-authentication) |
| Audit | `host_id` and `host_serial` on the `mdm_enrolled` activity, now shown on the host's timeline. | [8.12 The activity table](../08-troubleshooting/8.12-audit-logs.md#8122-the-table) |
| Audit | A new `user_mfa_requested` activity, recorded when an MFA verification email is sent. | [8.12 Activity types](../08-troubleshooting/8.12-audit-logs.md#8124-activity-types-the-categories) |
| Audit | New `created_setup_experience_script` and `deleted_setup_experience_script` activities. | [8.12 Activity types](../08-troubleshooting/8.12-audit-logs.md#8124-activity-types-the-categories) |
| Apple Business | `token_invalid` and `dep_device_error` on the ABM token and DEP-assignment responses, to explain a failed device lookup. | [8.8 Why Apple is not answering](../08-troubleshooting/8.8-apple-mdm-diagnostics.md#why-apple-is-not-answering-from-fleet-491) |

### Fixes and improvements

Fleet 4.91.0 also shipped around a hundred bug fixes and refinements. The following groups highlight changes relevant to the workflows in this manual:

- **Configuration profiles.** A dynamic-label profile now keeps a host's current state until the label is evaluated, so adding an exclude-any or include-all label no longer strips profiles from hosts that have not yet run the label's query. Windows SCEP profiles fail with a clear message when the certificate-authority challenge contains characters Windows rejects, instead of reporting "Verified" with no certificate installed. The Windows profile-summary endpoint no longer times out on large fleets.
- **Software and installs.** A VPP install a device acknowledged but never verified now fails after 24 hours and releases the host's activity queue (configurable with `FLEET_SERVER_VPP_INSTALL_REAP_TIMEOUT`), instead of blocking every later install and script. "Exclude any" on a host-vitals label no longer hides and blocks software for every host rather than the label's members. Several Fleet-maintained-app matches were corrected (Docker Desktop, Windows Git, and Steam), and `.sh` and `.py` packages saved with Windows line endings now install on Linux. Adding both Mozilla Firefox and Firefox ESR to one fleet now says they share a bundle identifier and only one can be added, instead of a generic conflict error; which of the two matches did not change.
- **Apple MDM.** Profile reconciliation handles more `NotNow` cases; re-enrolling a device with a different type (Manual to ADE) now updates the enrollment type; a DDM profile no longer becomes undeletable when the allowed declaration set changes; and a disk-encryption key is no longer stored or escrow-logged for a host that is not in Fleet's MDM.
- **Windows MDM.** fleet-level BitLocker PIN enforcement now reaches Windows hosts when Apple MDM is not configured, and SCEP-proxy challenge validation and LocURI handling were tightened.
- **Android.** The Pub/Sub handler deduplicates and tolerates out-of-order notifications, the device-reconciliation and directory-sync crons are bounded against runaway pagination, and stuck Lock, Wipe, and Clear-passcode commands are re-checked with Google once a day.
- **Security and sessions.** MFA token redemption is atomic, password-reset tokens are single-use and case-sensitive, an SSO-only invitation can no longer be redeemed with a password, and several endpoints (software titles, "My device" policies, host transfer, query packs) were tightened so they no longer leak data across fleets or accept non-existent IDs.
- **Identity.** Google Workspace directory syncs are bounded, a SCIM authorization failure no longer overwrites the admin-visible status, and a deactivated SCIM user whose identifiers change in the same request is now deprovisioned.
- **Interface and reporting.** The OS-versions and self-service endpoints validate their inputs, the fleets dropdown gained search and an "Add fleet" action, "Program (Windows)" is renamed "Application (Windows)", and a range of accessibility, dark-mode, and empty-state issues were fixed.

The complete itemised list is in Fleet's 4.91.0 release notes (see further reading below).

## Fleet 4.90.2

Fleet 4.90.2 was a bug-fix point release on the 4.90 line. One fix touches behaviour the manual teaches.

| Area | What 4.90.2 changed | Where in the manual |
|---|---|---|
| Policies · queue | Fleet-initiated activities (policy-automation installs and scripts, and iOS/iPadOS scheduled app updates) are released to hosts at a configurable rate rather than all at once, and user-initiated activities take precedence over queued Fleet-initiated work on the same host. | [5.9 Queueing and duplicates](../05-manage-devices/5.9-automate-remediation-with-policies.md#queueing-and-duplicates) |

## Fleet 4.90.1

Fleet 4.90.1 contained fixes and no new features. This table lists the fixes that affect behaviour covered in the manual, with links to the notes in context.

| Area | What 4.90.1 changed | Where in the manual |
|---|---|---|
| Apple MDM · labels | Re-enrolling an Apple host through ADE could clear its built-in label memberships and not restore them, so label-scoped profiles, software, and OS updates stopped reaching a host that had done nothing wrong. 4.90.1 restores the memberships during the re-enrollment reset and backfills hosts already affected. | [3.2 The ADE sequence, in order](../03-connect-devices/3.2-enroll-macos-devices.md#the-ade-sequence-in-order) · [8.3 Error strings worth recognising](../08-troubleshooting/8.3-server-logs.md#836-error-strings-worth-recognising) |
| Apple MDM · labels | A host re-enrolling through DEP could receive a profile scoped by a dynamic *exclusion* label before it had reported any label results, slipping past the guard that withholds label-scoped items until a host has reported. 4.90.1 closes that gap. | [1.3 Why exclude targeting behaves differently from include](../01-foundations/1.3-hosts-fleets-labels.md#why-exclude-targeting-behaves-differently-from-include) |
| Apple MDM · profiles | Above a certain host count, a profile-delivery page was judged full after duplicate commands were removed rather than before, so a subset of Macs never received a profile. 4.90.1 tracks fullness before the dedupe. | [8.8 Profile status values](../08-troubleshooting/8.8-apple-mdm-diagnostics.md#887-profile-status-values) |
| Software | The hourly auto-update job could keep an app's previous install script, which names the old installer file, so the install failed; and a new build that reused the same shortened version did not swap in the new file. 4.90.1 fixes both. | [5.4 Automatic updates to the software library](../05-manage-devices/5.4-manage-software-and-applications.md#automatic-updates-to-the-software-library) |
| Policies | A policy automation could act on an App Store app added for a platform other than the host's, instead of skipping it. 4.90.1 skips it. | [5.9 Validate policy, action, scope, and transport compatibility](../05-manage-devices/5.9-automate-remediation-with-policies.md#validate-policy-action-scope-and-transport-compatibility) |
| Policies · software | When a host's upcoming-activity queue was not draining, App Store install suppression did not hold, so automatic updates and policy automations could queue a duplicate install. 4.90.1 skips an app that already has an install waiting. | [5.9 Queueing and duplicates](../05-manage-devices/5.9-automate-remediation-with-policies.md#queueing-and-duplicates) |
| Windows MDM | A removed Windows profile stayed listed under Host details → OS settings when another profile on the same host still enforced every setting it had covered, so nothing was ever sent to clear the row. 4.90.1 clears it. | [8.9 Version notes](../08-troubleshooting/8.9-windows-mdm-diagnostics.md#version-notes) |
| Operations | One 4.90.0 migration could run for hours on deployments with many host certificates. 4.90.1 fixes that specific migration, though not the general fact that migration time cannot be predicted. | [7.3 There is no duration model](../07-operate-fleet/7.3-upgrade-fleet-and-fleetd.md#there-is-no-duration-model) |

<!-- NEXT RELEASE: when the manual is re-cut for the next Fleet minor (4.91), add its section
     ABOVE this one, so the most recent release stays at the top. Seed it the way this 4.90.1
     section was seeded, by grepping the manual for that point release's inline flags, e.g.
     `grep -rn "4\.91\.1" manual/`, then linking each flagged passage. See STYLE.md,
     "Point releases are footnotes, not versions". -->
