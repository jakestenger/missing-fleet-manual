---
title: "Manage fleetd, Orbit, and updates"
chapter: "III. Connect devices"
section: "3.8"
sidebar_position: 8
verified_against: Fleet 4.90.0
verified_on: 2026-08-30
verified_source: "partial: claims checked at git tag fleet-v4.90.0; independent review not yet run. Restructured 2026-08-30: renumbered from 3.7 (P6 structural moves); same chapter, new number."
further_reading:
  - https://fleetdm.com/guides/enroll-hosts
  - https://fleetdm.com/docs/configuration/agent-configuration
feature_requests:
  labels: [":product", "#g-orchestration"]
  match: ["fleetd", "orbit", "update channel", "TUF", "osqueryd"]
  exclude: []
---

# Manage fleetd, Orbit, and updates

![Explanation](../_assets/icons/explanation-light.svg) fleetd’s update system helps keep the agent current without repeatedly distributing installers. Administrators can let components follow a stable release track, pin a version, or test a new build with a pilot group. Fleet Premium manages those channel choices centrally; every tier can configure them on individual hosts.

## Purpose and scope

![Explanation](../_assets/icons/explanation-light.svg) Use this chapter to plan agent updates, understand when a change reaches devices, and verify the versions actually running. The enrollment chapters beginning with [3.1](3.1-enrollment-design-and-host-lifecycle.md) cover installing fleetd for the first time.

[1.2](../01-foundations/1.2-how-fleet-reaches-a-device.md) establishes the channels fleetd talks over. This chapter is about the agent itself rather than its traffic.

## What fleetd contains

![Reference](../_assets/icons/reference-light.svg) fleetd contains several components with separate versions and responsibilities. Understanding the bundle helps you choose update channels and investigate a component that has fallen behind.

| Component | What it does |
|---|---|
| **osquery** | Answers questions about the machine |
| **Orbit** | Manages osquery's version and configuration, and performs host-side work such as installing software and running scripts |
| **Fleet Desktop** | The end-user-facing surface. **Optional**, included by `fleetctl package --fleet-desktop` |

ChromeOS uses the separate Fleetd for Chrome extension, updated by Chrome from Fleet’s published manifest. Orbit, osquery, and Desktop channels, timers, retry rules, and agent options do not apply to it. Use [3.7](3.7-enroll-chromeos-devices.md) for that lifecycle.

**Fleet Desktop is an optional component of the bundle**, which Fleet's own architecture documentation describes exactly that way, and `fleetctl package --fleet-desktop` puts it in the package. It is the end-user-facing piece and it is a packaging choice rather than a given, so a deployment can have fleetd everywhere and Fleet Desktop nowhere.

Orbit manages the bundle: it selects and fetches osquery, manages the optional Desktop component, and replaces its own binary when its channel publishes an update.

## How a version gets chosen

![Explanation](../_assets/icons/explanation-light.svg) Orbit, osquery and Fleet Desktop each follow an update channel, and a channel is a standing instruction rather than a pinned version.

A channel is a path in the update repository, and Fleet publishes two kinds. It is not a closed set: Fleet accepts an arbitrary channel name in agent options, and an organisation running its own repository can publish whatever tags it likes, which `fleetctl updates add` will do for it on macOS and Linux, the platforms that command supports; it declares the Fleet EE licence in its own help and requires an EE subscription, rather than carrying a Premium validation of the kind `update_channels` has. What follows is the conventional set Fleet itself publishes.

Three prefix forms, each narrower than the last:

1. `4` tracks any `4.x.x`.
2. `4.6` tracks any `4.6.x`.
3. `4.6.0` pins that exact version.

And two named tracks, `stable` and `edge`, which are not version prefixes at all.

For osquery, `edge` carries new versions for beta testing. Fleet tests them and shares results with the osquery technical steering committee; once the committee declares a version stable, Fleet promotes it to `stable`.

The `stable` channel moves as Fleet promotes releases. Version prefixes also move within their range: `4` follows `4.x.x`, and `4.6` follows `4.6.x`. Use the full three-part version when you need an exact pin.

Channels are set with `--orbit-channel`, `--desktop-channel` and `--osqueryd-channel` when the installer is built.

## Changing channels after deployment

![How-to](../_assets/icons/howto-light.svg) **Changing a channel centrally requires Fleet Premium.** The `update_channels` block in agent options sets them for `orbit`, `osqueryd` and `desktop` across an estate, and Fleet Free rejects the block with a licence error. What Free still has is the per-host route: Orbit reads `--orbit-channel`, `--osqueryd-channel` and `--desktop-channel`, and the matching `ORBIT_*_CHANNEL` environment variables, so a channel can be changed on a host by editing its service configuration and restarting. That is a per-host operation, which is exactly what Premium buys you out of.

```yaml
agent_options:
  update_channels:
    orbit: stable
    osqueryd: '5.10.2'
    desktop: edge
```

**Remote channel configuration needs fleetd 1.20.0 or later.** Older agents ignore the block entirely and stay on whatever they were packaged with, which looks identical to a change that did not apply. And Fleet documents a hazard when crossing that boundary downwards: a packaged channel and a server channel that disagree across it can put Orbit into a restart loop. An estate old enough to be affected by the 1.38.1 stepping stone below is old enough to be affected by this.

That block sits directly under `agent_options` rather than under `config`, unlike the osquery options in [1.2](../01-foundations/1.2-how-fleet-reaches-a-device.md). The rule behind the difference is that Fleet passes `config` through to osquery, and reads everything else itself.

**Agent options win over what the installer was built with, including downwards.** Fleet's documentation is explicit that an agent generated at one version will move to the version the channel specifies even when that means downgrading. A package built at Orbit 1.53.1 enrolling into a deployment whose `update_channels.orbit` says 1.50.0 will end up on 1.50.0.

Use agent options to stage a rollout. Point a pilot fleet at the version or track you want to evaluate, keep the wider population on its current version, and expand after the pilot meets your checks.

Include the complete required agent options for a pilot fleet. Fleet-scoped options replace global options rather than merging with them, so a block containing only `update_channels` can omit other settings those hosts need. Writing agent options requires an administrator or GitOps role ([1.4](../01-foundations/1.4-identity-and-roles.md)).

## How often fleetd checks

![Explanation](../_assets/icons/explanation-light.svg) A change to the configured channel reaches a host through its Fleet configuration poll. New content published on an unchanged channel is discovered through the separate update timer. Allow for the relevant interval when checking rollout progress.

Orbit normally polls Fleet for configuration about every thirty seconds, so a channel change can reach it before the next scheduled update check. From fleetd 1.58.0, failed configuration cycles introduce exponential backoff with jitter, up to five minutes; one successful cycle restores the thirty-second interval. Any cycle error can trigger backoff, including failure to apply a configuration already received. Allow for that delay when changing channels while hosts or the server are unhealthy.

A new `update_channels` value is written to disk and triggers an Orbit restart, and **the restart performs an update check before it starts anything else**: Orbit does that early check first, deliberately, so that a bug in another subsystem cannot interfere with fetching an update. If the update is to Orbit itself, it exits and comes back on the new binary. So the gap between saving a channel change and the host acting on it is **one configuration poll plus the time to download and restart**, and the poll is the part that varies.

**Finding new content on a channel you are already following is the slower loop.** After that startup check, the update runner waits: the next check is one `update-interval`, which defaults to fifteen minutes, plus a random amount so agents do not all reach the update server together. The randomisation is capped at the smaller of the interval and ten minutes, so at the default it is up to ten and a deployment that shortened the interval gets proportionally less. On a large estate that spread is the difference between a rolling update and a stampede.

**Both of those depend on updates being enabled at all.** An agent packaged with updates disabled performs no startup check and runs no update loop, so neither clock applies to it and a channel change reaches it and does nothing.

After three consecutive failures, Orbit waits 24 hours before retrying the same target. The retry count and cooldown are keyed by artifact hash. Publishing a corrected build creates a new target, so it can be fetched without waiting for the previous artifact’s cooldown.

Each update pass checks targets in order: Orbit, osquery, then Desktop when installed. A lookup or update failure ends that pass immediately. Although the cooldown belongs to an individual artifact, a failing earlier target can prevent later components from being checked. Investigate Orbit first when the whole bundle is behind.

Retry counters live in memory, so restarting Orbit clears the cooldown. This can help retry an update on a single stuck host and explains why a reboot may change the result without a new configuration.

![How an update reaches one host, shown as two separate clocks. The main update line runs from fleetd starting and checking immediately, through a first interval plus up to 10 minutes of jitter, then every 15 minutes. A separate configuration-poll track above runs at 30 seconds when healthy, backing off to 5 minutes after repeated configuration-cycle errors; when a channel change arrives on it, Orbit restarts and checks immediately, cutting in on the main line rather than waiting. A branch below shows three failures on one artifact triggering a 24-hour hold for that artifact only, with a note that a newly published build is a different artifact and is not held.](assets/3.8-update-cadence.webp)

<!-- IMAGE-REDO: assets/3.8-update-cadence.webp
     WHY: Equal visual spacing on clocks with different cadences suggests a shared time scale; the cooldown panel overloads the main idea.
     PROMPT: Explain the two ways an update check starts. Title: "fleetd update checks".
     Scope subtitle: "Updates enabled; default update interval". Use two compact horizontal tracks.
     Upper track, "Configuration changes": "Configuration poll" connects to "Channel change received",
     then "Orbit restarts", then a distinct event node "Immediate update check".
     Under the poll node: "30 seconds when healthy; backs off to 5 minutes after repeated errors".
     Lower track, "Existing channel": "Startup check" connects through a labelled waiting interval
     "15 minutes + up to 10 minutes of initial jitter" to "Next check", then "Every 15 minutes".
     Use an explicit note "Schematic: the tracks use different time scales". Do not align poll ticks
     with update ticks or put 30 seconds and 15 minutes on equal-length measured intervals.
     Show the immediate-check event above an interval on the lower track, with a labelled connector
     "Does not wait for the scheduled check". This is an interrupting event, not a completed update.
     Keep the download cooldown, per-target failures, and retry details out of this picture; they
     remain in the surrounding prose. Do not imply checks guarantee downloads or successful installation.
     TERMINOLOGY: Fleet is the company, product, or server. Host groups are lowercase
     fleet/fleets, including headings and labels. Do not call these groups teams. Preserve
     exact code/API identifiers. These instructions are not text to render.
     DESIGN: Flat vector technical diagram. Fleet is software for managing computers; draw no vehicles.
     Use Inter for labels and Roboto Mono for literal identifiers. On a 1400 px wide canvas use
     48 px titles, 36 px body labels, and at least 28 px secondary text; scale proportionally.
     Check the raster at 720 px wide and at its intended print size. Never shrink text to fit.
     Use a 32 px spacing grid, at least 24 px internal padding, and consistent corner radii.
     Left-align multiline explanations; center short node names. Keep a clear reading direction.
     Use #F9FAFC background, #192147 headings and primary connectors, #515774 body text,
     #8B8FA2 secondary connectors, #C5C7D1 borders, and #D3E8F3 or #E8F1F6 quiet fills.
     Use #5CABDF and #C98DEF only to distinguish named categories; #3AEFC4 for a labelled
     positive outcome, #D66C7B for a labelled failure, and #FAA669 for a labelled caution.
     Use accents at 20 to 25 percent tint for panels; full strength only for small marks.
     Keep text navy or slate, with off-white text on a navy anchor. Do not rely on color alone.
     Use one arrowhead shape and consistent stroke weights. Put connectors behind nodes,
     terminate at box edges, and keep labels clear of lines. Label branches and return paths.
     No gradients, shadows, decorative icons, logo, watermark, em-dashes, or slogan footer.
     Render only the specified reader-facing labels. Instructions and review notes stay out of the image.
     NOTE: Brief revised 2026-09-08 following visual review. Editorial draft grounded in the
     current chapter and its existing ledger; this edit is not a new release verification.
     Before rendering, check every arrow, timing, scope qualifier, and selected transition against
     the chapter. Resolve any conflict before accepting the artwork. Preserve the current asset
     until its replacement is reviewed. Update alt text to describe the replacement when installed.
     Retain editable SVG when available. Change to IMAGE-OK only after rendered-image review.
     CANDIDATE: ../../research/visual-reviews/part-3/assets/3.8-update-cadence.webp
     Rendered and inspected for the overnight batch; awaiting final joint review.
-->

## The update server moved, and old agents cannot cross the gap

![Troubleshooting](../_assets/icons/troubleshooting-light.svg) Fleet moved its update server from `tuf.fleetctl.com` to `updates.fleetdm.com`, and the current default in the agent is the new address.

**Orbit versions below 1.38.1 cannot upgrade directly to a current release.** 1.38.1 itself is the exception and the way across.

Use **1.38.1** as the intermediate release, as Fleet’s configuration reference directs. Both 1.38.0 and 1.38.1 contain migration code, but 1.38.0 also had a rollback path to the old update server. Older agents can remain pointed at an address that no longer serves their files, producing this log message:

```
update failed error="lookup failed: lookup orbit: tuf: file not found
```

The route across is to upgrade to 1.38.1 first, wait for agents to reach it, then upgrade to the version you actually want. An estate carrying agents old enough to be affected needs two rollouts rather than one, and a channel pointed straight at a modern version will leave those hosts stranded rather than failing loudly.


<!-- IMAGE-TODO: assets/3.8-orbit-update-bridge.webp
     QUESTION: Why do older Orbit agents need an intermediate rollout?
     PROMPT: DIAGRAM: Three steps across a visible compatibility gap: Orbit below 1.38.1 at
     tuf.fleetctl.com, Bridge release 1.38.1, Target release at updates.fleetdm.com. The first and
     second rollout arrows are separately numbered; each ends at a Verify arrival checkpoint. A thin
     crossed direct arrow from the old version to the target is labelled Cannot upgrade directly. A
     small note may state Migration code also existed in 1.38.0; use documented bridge 1.38.1. Avoid
     a time axis or invented elapsed rollout durations.
     TERMINOLOGY: Fleet is the company, product, or server. Host groups are lowercase
     fleet/fleets, including headings and labels. Do not call these groups teams. Preserve
     exact code/API identifiers. These instructions are not text to render.
     DESIGN: Flat vector technical diagram. Fleet is software for managing computers; draw no
     vehicles. Use Inter labels and Roboto Mono identifiers. At 1400 px source width use 48 px
     titles, 36 px body labels, and at least 28 px secondary text; scale proportionally. Check at
     720 px reading width and intended print size. Use a 32 px spacing grid, at least 24 px node
     padding, and consistent corner radii. Center short node names; left-align multiline
     explanations. Never shrink text to fit. Use #F9FAFC background, #192147 headings and primary
     connectors, #515774 text, #8B8FA2 secondary connectors, #C5C7D1 borders, and #D3E8F3 or #E8F1F6
     quiet fills. Use #5CABDF and #C98DEF for named categories, #3AEFC4 for labelled positive
     outcomes, #D66C7B for labelled failures, and #FAA669 for labelled cautions. Tint large panels
     to 20 to 25 percent; full strength is for small marks. Keep text navy or slate, or off-white on
     a navy anchor. Never rely on colour alone. Use one arrowhead shape, consistent stroke weights,
     box-edge termination, and labelled branches and return paths. Keep connectors clear of text. No
     gradients, shadows, decorative icons, logo, watermark, em-dashes, or slogan footer. Render only
     the specified reader-facing labels. Choose orientation to fit the relationship, not a default
     poster. Keep captions outside the artwork. If labels crowd, split the figure before shrinking
     them. Keep editable SVG when the production method supports it.
     NOTE: Proposed 2026-09-08; editorial brief, not technical re-verification. Replace the repeated
     bridge explanation with a short two-stage procedure and the figure. Retain the exact error
     string and why 1.38.1 is the supported stepping stone. Keep current prose and this TODO until
     the actual image is reviewed. Then check alt text against the artwork and retain an accessible
     summary plus all required technical qualifications.
     CANDIDATE: ../../research/visual-reviews/part-3/assets/3.8-orbit-update-bridge.webp
     Rendered and inspected for the overnight batch; awaiting final joint review.
-->

<!-- IMAGE PENDING. Install reviewed artwork, then activate the image line below.
![Older Orbit agents need a verified rollout through 1.38.1 before advancing to a current target release.](assets/3.8-orbit-update-bridge.webp)
-->

## Running your own update repository

![How-to](../_assets/icons/howto-light.svg) An organisation that cannot reach `updates.fleetdm.com`, that ships its own osquery extensions, or that wants to pin in-house builds can run its own update repository and point fleetd at it instead. `fleetctl updates` builds and maintains that repository. Every subcommand works on a local directory named by `--path`, and none of them contacts a Fleet server, so this is a signing-and-publishing workflow on a build host rather than anything your Fleet instance mediates.

It is a macOS and Linux command; on Windows the whole family is a single entry that refuses and tells you to use a Linux environment. And its help text declares the Fleet EE licence, while nothing in the command checks a licence at runtime, because with no Fleet server in the loop there is nothing to check one against. Read that line as a subscription statement, which is how [a.2](../09-appendices/a.2-platform-capability-matrix.md) records it.

For the first publication, use `init`, `roots`, `add`, and `timestamp` in that order. Use `rotate` later to replace signing keys in an existing repository.

`fleetctl updates init` creates the repository and the four signing keys it needs, one each for the root, targets, snapshot and timestamp roles. Each key is encrypted with its own passphrase, taken from a `FLEET_<ROLE>_PASSPHRASE` environment variable or an interactive prompt. It refuses to run when a repository or a `keys` directory already exists, so it never overwrites key material you already have.

`fleetctl updates roots` prints the repository's root metadata. That is the trust anchor a host pins so it will accept everything else the repository serves, so capture its output: you hand it to the agents you build below.

`fleetctl updates add` publishes one artifact. It takes `--target`, `--name`, `--platform` and `--version`, and any number of repeatable `--tag` values, copies the file into the repository under the layout fleetd expects, and signs the new metadata. The tags are the channel names hosts follow, so this is where your own `stable`, `edge` or a pinned version comes from, the same channels described in [How a version gets chosen](#how-a-version-gets-chosen) above.

`fleetctl updates timestamp` re-signs the repository's timestamp metadata and nothing else. It exists because that metadata expires 14 days after it is generated, and an expired timestamp makes clients reject the whole repository as stale even when every artifact in it is current. Treat the 14 days as a hard maximum rather than a safe interval: schedule the re-timestamp well inside it, so a failed or delayed job still leaves room before hosts start refusing metadata. This is the one piece of routine upkeep a live repository needs.

`fleetctl updates rotate <role>` replaces the signing key for one role, for a compromised key or a scheduled retirement. It generates a new key, re-signs the root, regenerates the dependent metadata and commits. The rotation commits reliably; the cleanup that removes its backups afterwards is the one place to watch, because a cleanup failure prints a warning and still exits zero while retired private-key material may be left on disk. [a.7](../09-appendices/a.7-fleetctl-command-reference.md#the-exit-zero-register) records this as Z27: when you see that warning, check the named backup directory by hand rather than trusting the exit code. It is maintenance you run against an existing repository, not a first-time step.

The following template uses one repository directory throughout. Supply your built artifact, repository URL, and TLS certificate where indicated. `init`, `roots`, and `timestamp` can run as shown; `add` and the later packaging command need those environment-specific inputs.

```sh
export FLEET_ROOT_PASSPHRASE='<choose a passphrase for the root key>'
export FLEET_TARGETS_PASSPHRASE='<choose a passphrase for the targets key>'
export FLEET_SNAPSHOT_PASSPHRASE='<choose a passphrase for the snapshot key>'
export FLEET_TIMESTAMP_PASSPHRASE='<choose a passphrase for the timestamp key>'

fleetctl updates init --path ./repo

fleetctl updates roots --path ./repo > ./repo-root-metadata.json
```

`add` publishes a file you already have; it does not build or fetch one for you. `orbit` and `osqueryd` version independently of the Fleet server, so there is no single tag that hands you "1.35.0": get the artifact first, from wherever your own release process keeps it (a build from `fleetdm/fleet`'s `orbit/cmd/orbit` at the orbit version you're publishing, or a copy extracted from an existing fleetd install), then confirm it's actually there before publishing it:

```sh
test -f ./orbit-1.35.0-macos || { echo "no artifact at that path yet, get one before running add"; exit 1; }

fleetctl updates add --path ./repo \
  --target ./orbit-1.35.0-macos --name orbit --platform macos \
  --version 1.35.0 --tag stable
```

Skip the four exports and `init` prompts for each passphrase interactively instead; either way, the same four variables unlock the corresponding key again for every later `add`, `timestamp`, or `rotate` against this repository. Repeat `add` once per component and platform you publish, orbit, osqueryd, and desktop across macos, linux, and windows, each with its own built `--target` file and real `--version`; the `--tag` values are the channels hosts follow, so tag a build `stable` (or `edge`, or a pinned version string) to put it on that channel. Then finish the publish with the one command that has to run again on a schedule:

```sh
fleetctl updates timestamp --path ./repo
```

After publishing the repository, serve it over HTTPS and configure new agent packages to trust and use it.

Serve the directory over TLS. What `fleetctl updates` writes is a static tree of metadata and target files, so any web server or object store that serves those files over HTTPS will do, and the address you serve it at is what agents aim their update traffic at.

Then bootstrap agents against it when you build their installers with `fleetctl package`:

- `--update-url` sets the address the agent fetches updates from, in place of the default `updates.fleetdm.com`.
- `--update-roots` carries the root metadata you printed with `fleetctl updates roots`, so the agent trusts your repository's keys and no others.
- `--update-tls-certificate` supplies a certificate to trust for that connection, for the case where your server presents one the agent would not otherwise chain to a public root. The `updates-ca.pem` in the example is that server's own certificate chain, which you export from the HTTPS server you set up above; no earlier step produces it, and nothing in `fleetctl` generates it. Drop the flag when your server already presents a publicly trusted certificate.

```sh
export FLEET_ENROLL_SECRET='<your Fleet enroll secret>'

fleetctl package --type=pkg \
  --fleet-url=https://fleet.example.com \
  --enroll-secret="$FLEET_ENROLL_SECRET" \
  --update-url=https://updates.example.internal \
  --update-roots="$(fleetctl updates roots --path ./repo)" \
  --update-tls-certificate=./updates-ca.pem
```

Agents built with these flags use your repository from first launch. Channel selection and both update timers continue to work as described above.

![A private update repository pipeline. On the build host, fleetctl updates runs four steps: init to create the repository and four keys; add to publish an artifact on a tag; timestamp to re-sign every 14 days as a hard maximum, refresh earlier; and rotate to replace a key. A thick arrow leads to a web server or object store served over HTTPS, and another to the fleetd host, which points at it with --update-url, --update-roots, and --update-tls-certificate. A key labelled root, targets, snapshot, timestamp loops to the --update-roots flag, root metadata pinned as trust anchor.](assets/3.8-private-update-repo.webp)

<!-- IMAGE-OK: assets/3.8-private-update-repo.webp
     NOTE: accepted 2026-08-30. Sol diagram recipe (HANDOFF), generated from the prompt below.
     PROMPT: Landscape 16:9 flat vector pipeline, left to right, titled "Publish once, then
     hosts consume". Left group, a panel labelled "Build host (fleetctl updates)" holding a
     vertical stack of four small chips in order: "init: repository and four keys", "add:
     publish an artifact on a tag", "timestamp: re-sign every 14 days", "rotate: replace a
     key". A key icon sits beside the panel labelled "root, targets, snapshot, timestamp".
     A thick arrow leads right to a single box "Web server or object store, served over HTTPS".
     From that box a thick arrow leads right to a panel "fleetd host" with three small chips
     "--update-url", "--update-roots", "--update-tls-certificate". A thin curved arrow loops
     from the "roots" key icon on the left directly to the "--update-roots" chip on the right,
     labelled "root metadata pinned as trust anchor". Draw the "timestamp: re-sign every 14
     days" chip with a small clock and the note "hard maximum, refresh earlier".
     Caption: "The repository is a static signed tree. Hosts point their update traffic at it."
     PALETTE. Two sets, doing different jobs.
     STRUCTURE, the Fleet Black ramp and neutrals: #192147 for the most important marks,
     #515774 for ordinary labels, #8B8FA2 and #C5C7D1 for secondary structure, #E2E4EA for
     grouping bands, #F9FAFC background, #D3E8F3 and #E8F1F6 for quiet fills. All text stays
     in this set; do not tint type.
     THE FLEET DOTS, the six accent colours taken from Fleet's own logo mark: #5CABDF sky
     blue, #3AEFC4 mint, #63C740 green, #C98DEF lavender, #FAA669 apricot, #D66C7B rose.
     Fleet Green #009A7D sits alongside them. These are soft and pastel, so they work as
     fills with #192147 text on top.
     STRENGTH. Use a dot colour at full strength only for small marks: an arrow, a chip, a
     single filled icon, a rule. Over any large area, use it at roughly a 20 to 25 percent
     tint, so a panel reads as tinted rather than painted. Do not colour a panel that has no
     content in it.
     HOW TO USE THE DOTS. One colour per category, used consistently within the image, to
     fill or stroke the things a reader genuinely has to tell apart. Take only as many as
     there are categories.
     GIVE IT WEIGHT. At least one element should be a solid filled shape rather than an
     outline, so the composition has an anchor. Vary stroke weight so the primary flow is
     visibly heavier than the scaffolding around it.
     Never invent a colour outside these two sets. Flat vector, generous whitespace, no
     gradients, no drop shadows, no 3D or photorealistic icons, no logo, no watermark.
     Legible at half page width.
     NOTE: "Fleet" here is a software product for managing computers. Never draw vehicles,
     cars, vans, trucks, ships or aircraft. Never use an em-dash in rendered text. -->

The full contract for each subcommand, its flags, and the Windows behaviour is in [a.7](../09-appendices/a.7-fleetctl-command-reference.md). Reading the agent's own update logs when a self-hosted repository misbehaves, including a timestamp that has expired, is [8.4](../08-troubleshooting/8.4-host-side-investigation.md).

## Verification and ownership

<!-- SCREENSHOT-REUSE: SS-1.2-01. Reuse the host agent-version capture to show verification after a channel change. Keep update_channels YAML and repository commands as selectable text. -->

![How-to](../_assets/icons/howto-light.svg) Check the osquery and Orbit versions in Host details after an update. These describe the running host, which may have moved to a different version since its installer was built.

Worth confirming after any channel change:

- A **pilot host has actually moved**, rather than the change having been saved and not yet reached anything. Allow one configuration poll, thirty seconds ordinarily and up to five minutes after repeated configuration-cycle errors, plus the download and restart. New content on an unchanged channel is the slower case, and there the first interval plus its jitter is what to allow before concluding anything.
- The **spread across the estate closes** over the following hours. A long tail of hosts on the old version has several possible causes worth separating in the agent logs rather than assuming: an unreachable update server, repeated failure on one artifact, agents below 1.38.1, or updates disabled at packaging time.
- **Fleet Desktop is where you expect it**, since it is an optional component, included in the package only when it was built with `--fleet-desktop`, and can be absent without anything appearing wrong.

Ownership sits with whoever owns the agent rather than whoever owns the Fleet server. The two questions to settle are who decides when a channel moves, and who watches the version spread afterwards. On the free tier there is a third: who changes a channel at all, since without agent options it means either rebuilding installers or setting the channel on the host itself, through the flags or environment variables Orbit reads at startup.

## Precedence and edge cases

![Reference](../_assets/icons/reference-light.svg) These precedence rules determine whether a saved channel change affects an installed agent.

Agent options override the installer, so a package built with a specific channel does not keep that channel if the deployment says otherwise.

A channel can move an agent backwards. Downgrading is a supported outcome of setting a lower version, not an error.

Omitting a component, removing the entire block, and naming a missing channel have different effects:

- **A component omitted from `update_channels`** is not left alone. It is treated as `stable`, so a block naming only `orbit` moves osquery and Fleet Desktop to `stable` as well.
- **Removing the `update_channels` block entirely** is a no-op rather than a reset. Orbit treats an absent block as the server not using the feature and leaves each host on the channel it last recorded, so this is how you stop managing channels centrally and **not** how you undo a pin. To undo a pin, set the channel you want.
- **Naming a channel that does not exist** produces host-side errors and stops that component updating until the channel exists or the value is corrected. A typo therefore freezes a component quietly rather than failing loudly in Fleet.

Orbit ignores the Desktop channel on hosts where Desktop is disabled, including when deciding whether to restart. A Desktop-only channel change therefore affects only hosts using that component.

You can disable updates when packaging or through Orbit’s startup `--disable-updates` flag or `ORBIT_DISABLE_UPDATES` environment variable. While disabled, Orbit performs neither startup nor scheduled update checks. Central agent options cannot re-enable them; change the host’s startup configuration to restore updating.

## Reference and troubleshooting

![Troubleshooting](../_assets/icons/troubleshooting-light.svg) Reading the agent's own logs, which is where update failures appear, is [8.4](../08-troubleshooting/8.4-host-side-investigation.md). Deciding whether a symptom belongs to the agent or to something else is [8.1](../08-troubleshooting/8.1-diagnostic-method.md).

Packaging flags are listed by `fleetctl package --help`; [a.7](../09-appendices/a.7-fleetctl-command-reference.md) carries each command's contract rather than a second copy of the flag list. How agent options relate to what is set centrally elsewhere is [a.3](../09-appendices/a.3-configuration-model-and-precedence.md), which also carries what the server can and cannot change on an installed agent.

## Version notes

![Reference](../_assets/icons/reference-light.svg) Verified against Fleet 4.90.0. The default update interval, the jitter, and the three-retry cooldown are agent behaviour and move with fleetd rather than with the Fleet server, so an old agent may not match this description.

The 1.38.1 stepping stone is a consequence of a one-time server migration. It matters only for estates carrying agents from before that change, and it will matter less as those age out.
