---
title: "Enroll macOS devices"
chapter: "III. Connect devices"
section: "3.2"
sidebar_position: 2
status: drafting
verified_against: Fleet 4.90.1
verified_on: 2026-08-27
verified_source: git tag fleet-v4.90.1
further_reading:
  - https://fleetdm.com/guides/enroll-hosts
  - https://fleetdm.com/guides/apple-mdm-setup
feature_requests:
  labels: []
  match: ["macOS enrollment", "ADE", "ABM", "BYOD", "fleetd package"]
  exclude: []
---

# Enroll macOS devices

![Explanation](../_assets/icons/explanation.svg) A Mac arrives in Fleet over two channels. **fleetd** brings queries, scripts, software and vitals. **MDM enrollment** brings profiles, commands, lock and wipe. Disk encryption needs both and is the clearest case for the split: MDM delivers the FileVault configuration, and the agent is what moves the recovery key into escrow ([1.2](../01-foundations/1.2-how-fleet-reaches-a-device.md)). A Mac can have either channel without the other, and both states are normal for a while.

The thing to get right before anything else is **who delivers the agent**, because the obvious answer is wrong:

> **Fleet installs fleetd itself on every macOS MDM enrollment, manual as well as automated.** Both paths queue a post-enrollment task, and on macOS that task sends an `InstallEnterpriseApplication` command carrying Fleet's own agent package. You do not have to deliver a package to a Mac you are enrolling in MDM.

That changes what this chapter is about. The decision is not "which path delivers the agent" but **"is this Mac being MDM-enrolled at all, and did the one command that installs the agent land?"**

[3.1](3.1-enrollment-design-and-host-lifecycle.md) covers what enrollment establishes in general: the host record, the fleet assignment, and the credentials. This chapter covers the macOS-specific part.

## The decision you are making

**How does MDM get turned on?** Decided by how the Mac was bought and how it reaches you.

| Path | Use it when | What the console shows |
|---|---|---|
| **Automated Device Enrollment (ADE)** | The Mac is in Apple Business and company-owned. Enrollment happens during Setup Assistant. **Fleet Premium**, because Apple Business tokens are | On (company-owned) |
| **Enrollment link, company-owned** | The Mac is not in Apple Business, or it is already deployed | On (manual) |
| **Enrollment link, personal (BYOD)** | The Mac belongs to the person using it | On (manual - personal) |

Those strings are what the **console** displays. The database and the API spell the first one `On (automatic)`, and the host filter spells it `automatic`; [8.6](../08-troubleshooting/8.6-server-state.md#one-status-three-vocabularies) has the mapping, and it matters the first time you write a query from something you read on screen.

**On all three, Fleet delivers the agent.** Building a package yourself is for the cases in the next section, not for these.

### What personal enrollment actually changes

This is a capability boundary rather than a label. Fleet builds a personal enrollment profile with a **reduced set of MDM access rights**: it takes the full set and removes device lock and device erase. A company-owned profile carries all thirteen rights, a personal profile carries eleven.

So on a personally-enrolled Mac, Fleet cannot lock the device and cannot wipe it, and that is enforced by the profile the device is holding rather than by a Fleet-side permission you could change afterwards. Treat the choice as one you will have to re-enroll to undo. Fleet exposes no way to change a delivered profile's access rights, and Apple's rules on narrowing rights in place are its own; nothing in Fleet's source establishes a supported path from one to the other.

The mechanism explains the status string. Fleet appends a `byod` parameter to the MDM server URL inside the personal profile. The MDM stack surfaces URL parameters to the check-in handler, which reads it during `Authenticate` and records it on the host record, where it becomes `host_mdm.is_personal_enrollment`. That is also why the distinction survives on the host rather than living only in whatever you clicked.

### What each path needs first

| Path | Prerequisites |
|---|---|
| ADE | Apple Business connected with an ABM token uploaded, an APNs certificate, and the device assigned to Fleet as its MDM server in Apple Business. [2.7](../02-administer-and-deploy-fleet/2.7-apple-mdm-configuration.md) covers all three |
| Either link | An APNs certificate. No Apple Business involvement |
| Anything | A Fleet URL the Mac can reach over TLS |

**Who can get the link matters more than it looks.** The enrollment link carries an enroll secret, and reading an enroll secret needs global or fleet-scoped admin or maintainer. Technicians, observers, Observer Plus and GitOps users cannot read one. If your help desk is expected to enroll machines, that is a role decision to take before the rollout rather than during it ([1.4](../01-foundations/1.4-identity-and-roles.md)).

## Enrolling by link, which is the ordinary manual path

![How-to](../_assets/icons/howto.svg) With Apple MDM configured, Fleet's **Add hosts** modal stops offering a package command for macOS and offers an **enrollment link** instead, with a company-owned or personal choice beside it. That link is the artifact: send it to the person holding the Mac.

The link is your Fleet URL's `/enroll` path carrying an enroll secret, plus `byod=true` when you picked personal. Underneath it is Apple's over-the-air enrollment, the same mechanism [3.5](3.5-enroll-ios-and-ipados-devices.md) describes for iPhones, and one consequence is worth carrying over: **the enroll secret in the link is what places the host in a fleet.** One link per fleet, and the link is a credential.

**This path is not licence-gated, and it is the one that makes Fleet Free usable for Macs.** The over-the-air enrollment service carries no licence check. What *is* Premium is the separate endpoint that downloads an unsigned manual enrollment profile, downloading the default Setup Assistant profile, and **Apple Business itself**: Fleet Free refuses to upload, list, renew or count ABM tokens, so ADE is out of reach on Free. Those are different artifacts from the link, and conflating them is how a Free deployment concludes it cannot enrol Macs at all.

**If end-user authentication is turned on** for the destination fleet, `/enroll` sends the person to your identity provider before it hands over anything, and the identity that comes back is attached to the host. Decide and test that before sending links, because it changes what the end user sees and what you get ([2.2](../02-administer-and-deploy-fleet/2.2-identity-providers-sso-scim-and-role-sync.md)).

**Two prompts, not one.** macOS asks once to download the profile and once to install it in System Settings. A profile downloaded and never installed is the most common "it did not work" on this path.

## The ADE sequence, in order

Worth reading once even if you never drive it by hand, because an ADE support question is usually about which of these did not happen.

1. The Mac is assigned to Fleet as its MDM server in Apple Business. Fleet learns about it on its next sync.
2. Fleet assigns the device an enrollment profile, which is what makes Setup Assistant offer, or require, enrollment.
3. The device reaches Setup Assistant and retrieves that profile from Apple.
4. It enrolls.
5. Fleet runs its post-enrollment work: the fleetd install, a bootstrap package if you have one, and the setup experience.

Steps 2 and 3 each leave a visible timestamp, and the gap between them is where ADE problems live. **Read the second one carefully: Fleet's own tooltip defines the pushed time as the last time Apple reported the host *retrieved* its assigned profile.** So an assignment time with no push time says the device has not collected the profile, not that Fleet failed to send anything. [8.8](../08-troubleshooting/8.8-apple-mdm-diagnostics.md#886-assignment-time-but-no-push-time) covers it.

**Setup Assistant behaviour is configured, not inherited.** Whether enrollment is mandatory, which panes are skipped, and whether the MDM profile can be removed are set by the profile Fleet sends Apple. [2.7](../02-administer-and-deploy-fleet/2.7-apple-mdm-configuration.md) owns that.

**Fleet placement on this path does not come from an enroll secret.** An ADE device is placed by the fleet configured against the ABM token for its platform, or by a move made before it enrolls ([3.1](3.1-enrollment-design-and-host-lifecycle.md), [2.7](../02-administer-and-deploy-fleet/2.7-apple-mdm-configuration.md)).

## When you do build a package

![How-to](../_assets/icons/howto.svg) Three cases, and none of them is "a manually enrolled Mac".

- **Macs you are not putting in MDM at all.** An agent-only host is a perfectly good state if queries, scripts and software were what you wanted.
- **ADE, with the agent install deliberately turned off.** `macos_setup.manual_agent_install` tells Fleet not to send the fleetd command on the ADE path, for deployments that would rather deliver the agent themselves. **There is no equivalent switch on the manual path**, which always installs.
- **Getting the agent on before MDM**, or into a fleet the MDM path would not have chosen.

```sh
fleetctl package --type=pkg \
  --fleet-url=https://fleet.example.com \
  --enroll-secret="$FLEET_ENROLL_SECRET" \
  --fleet-desktop \
  --enable-scripts
```

The two optional flags are worth deciding rather than copying. `--fleet-desktop` includes Fleet Desktop, which is what gives the end user a menu bar item, the **My Device** page and self-service software; without it the other components still run and the person on the Mac has no Fleet-facing surface at all.

`--enable-scripts` is **not** the permanent decision it is on other platforms. A Mac started without the flag polls Fleet's own fleetd configuration profile and turns scripts on from its `EnableScripts` value, which Fleet always sets to true in the profile it generates. So on an MDM-enrolled Mac carrying that profile, scripts end up enabled either way; on a Mac without it, the flag is the only answer. Check for the profile before rebuilding a package over this.

### Taking the URL and secret from a configuration profile

`--use-system-configuration` builds a package carrying no secret and no Fleet URL, reading both from an Apple configuration profile.

```sh
fleetctl package --type=pkg --use-system-configuration
```

Reach for it when you do not want the credential sitting inside an installer in a software repository. **It does not impose an installation order.** Orbit loops, logging at debug that the values are not there yet and retrying every thirty seconds, so a package installed before its profile waits rather than failing. If such a host is not enrolling, deliver the profile; do not reinstall the agent.

Once the values arrive, Orbit writes them into the Orbit root as `secret.txt` and `fleet_url.txt`. **So the absence of `secret.txt` describes the window before the profile lands, not the steady state**, and a running host built this way has one like any other.

## Verifying an enrollment

![How-to](../_assets/icons/howto.svg) Check both channels, because either can be the one that failed.

**From Fleet.** The host appears and its MDM status matches the path you used. A status of `Pending` means Apple Business has told Fleet about a device that has not enrolled yet, which is the expected state between steps 1 and 4 above.

**From the Mac**, with a shell or a script:

```sh
# Is MDM on, and to whom?
sudo profiles status -type enrollment

# Did fleetd install and enroll?
sudo ls -la /opt/orbit
sudo launchctl print system/com.fleetdm.orbit
```

`profiles status` answers the MDM half: whether the device came through Automated Device Enrollment, whether MDM enrollment is on and user-approved, and which server owns it. The presence of `secret-orbit-node-key.txt` in Orbit's root answers the fleetd half, and it is the file to look for, not `secret.txt`, which Orbit moves into the Keychain and deletes on first launch ([3.1](3.1-enrollment-design-and-host-lifecycle.md)). [8.4](../08-troubleshooting/8.4-host-side-investigation.md#844-macos-mdm-truth-from-the-host) reads both in detail.

**The two answers disagreeing is information, not a fault.** MDM on with no Orbit root is the half-enrolled state below. An Orbit root with no MDM is an ordinary agent-only host.

### The half-enrolled state is not an ADE problem

Because Fleet delivers the agent by MDM command on **both** enrollment paths, and because that command is issued once with nothing retrying it, a Mac can complete enrollment perfectly and end up with **MDM on and no agent** whichever way it enrolled. The shape is the same and so is the recovery: install the agent, and leave the enrollment alone.

Audit for it after any bulk enrollment rather than assuming both channels arrived together. [8.1](../08-troubleshooting/8.1-diagnostic-method.md#the-half-enrolled-state) describes the shape and [8.4](../08-troubleshooting/8.4-host-side-investigation.md#845-the-half-enrolled-state-mdm-without-an-agent) covers detection and recovery.

## Who owns it afterwards

**The APNs certificate**, which Apple issues for a year and which takes every Apple device's MDM channel with it when it lapses. It is renewed with the Apple Account that created it, and Fleet's guidance is that losing that account means re-enrolling the estate. Those are Apple's rules rather than Fleet's, and the manual states them because they decide the renewal calendar; [2.7](../02-administer-and-deploy-fleet/2.7-apple-mdm-configuration.md) owns it.

**The gap between the two channels.** A count of Macs with MDM on and no recent osquery check-in is worth putting somewhere you look at regularly, rather than running it after someone complains.

## Reference and troubleshooting

The enrollment endpoints are in [a.8](../09-appendices/a.8-api-action-and-endpoint-reference.md). A Mac enrolling by link has to reach `/enroll` and Fleet's over-the-air enrollment profile endpoint as well as the MDM protocol endpoints, which matters if a reverse proxy is deciding what is exposed.

When enrollment does not work, the split at the top of this chapter decides where to look. MDM problems, including APNs, the command queue and ADE sync, are [8.8](../08-troubleshooting/8.8-apple-mdm-diagnostics.md). fleetd problems, including an agent that never arrived, are [8.4](../08-troubleshooting/8.4-host-side-investigation.md).

## Version notes

| Change | Release |
|---|---|
| Enrollment profile URL added to the macOS tab of the Add hosts modal, with a company-owned or personal choice for MDM users | 4.90.0 |
| Turn off MDM enabled for offline macOS hosts, with the unenroll command queued until the device returns | 4.90.0 |
